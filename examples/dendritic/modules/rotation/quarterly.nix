# One interval, declared once, in a file of its own. Every entry naming
# `quarterly` moves together when this number does, and no entry is edited.
{
  flake.safix.rotation.quarterly.every = "90d";
}
