# Tasks: extend-bridge-fields

Citations are as read while designing this change, on 2026-09-16; re-read the named lines before editing, since line numbers drift.
No real secret name, user name, recipient or database enters this repository; fixtures use `alice`, `bob` and `carol`, synthetic `age1` strings, and a database under the fixture's own scratch directory, matching the existing integration fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
The decisions this change rests on are `design.md`'s F1 through F10; each group names the ones it realizes.
Three sibling changes — `add-pass-bridge`, `add-bitwarden-bridge`, `add-onepassword-bridge` — consume `modules/flake/safix/fields.nix`, `modules/flake/safix/reserved.nix` and `crates/safix-core/src/endpoint.rs` and must not edit them; `rewrite-readme-and-examples` owns `README.md` and `examples/`, so neither is touched here.

## 1. The reserved mapping words, in one place (F8)

- [ ] 1.1 Create `modules/flake/safix/reserved.nix` taking no arguments and evaluating to `{ ids = [ "clan" "keepassxc" "pass" "bitwarden" "1password" "all" ]; }`, with a module doc comment stating: that these are the words `sync` and `audit` read as a target keyword rather than a mapping name; that the list is complete before three of the targets answer to it, and why (F8 — three changes each appending one word would move the same equality check three times); that `op` is deliberately absent because one spelling per target is the rule and `1password` is it; and that `crates/safix-core/src/bridge.rs`'s `RESERVED_MAPPING_WORDS` carries the same words in the same order, held equal by `checks.safix-reserved-words`
- [ ] 1.2 Replace the inline literal in `modules/flake/safix/bridge.nix:421-430`'s `reservedId` with `(import ./reserved.nix).ids`, leaving the sentence it produces byte for byte as it is — the list is what was duplicated, the sentence names this module's own option path and stays here (F8)
- [ ] 1.3 Replace the inline literal in `modules/flake/safix/keepassxc.nix:218-227`'s `reservedId` the same way, again leaving its sentence unedited
- [ ] 1.4 Widen `crates/safix-core/src/bridge.rs:89`'s `pub const RESERVED_MAPPING_WORDS: [&str; 3] = ["clan", "keepassxc", "all"]` to `[&str; 6] = ["clan", "keepassxc", "pass", "bitwarden", "1password", "all"]`, and extend its doc comment (`bridge.rs:84-88`) to name `modules/flake/safix/reserved.nix` as the one declaration of the list and `checks.safix-reserved-words` as what holds the two equal
- [ ] 1.5 Add a unit test in `crates/safix-core/src/bridge.rs`'s `mod tests` named `the_reserved_words_are_the_six_the_declaration_carries` asserting the array equals the six literals in order — a literal-against-literal assertion, so the nix half is what the check in task 13.1 compares against and this holds only that the Rust half was not reordered
- [ ] 1.6 Severity drill: reordering two words in `reserved.nix` without reordering the Rust array turns `checks.safix-reserved-words` (task 13.1) red while every Rust test stays green, which is the evidence the equality is held across the boundary rather than inside one side
- [ ] 1.7 Verify: `cargo test -p safix-core --lib bridge::tests` green, and `nix eval .#safix.lib 2>/dev/null` is not the check — the nix half is verified by task 13.1's build

## 2. The shared field submodule and the capability table (F1, F6)

- [ ] 2.1 Create `modules/flake/safix/fields.nix` as `{ lib }:` with a module doc comment stating: that a field is metadata carried beside the value on the far side of a mapping and never on `safixSide` (F2, with the reason — a safix entry is a file, a key inside it and an audience); that a literal is not a secret because the declaration is evaluated into a world-readable store and an `{ entry = …; }` reference is one; and that each target refuses what it cannot carry in its own `violationsOf`, reading the table this file exports
- [ ] 2.2 In `fields.nix`, define `fieldValue = lib.types.either lib.types.str (lib.types.submodule { options.entry = lib.mkOption { type = lib.types.str; description = "…"; }; })`, whose description states that the named entry belongs to the mapping's own person and is decrypted when the mapping is converged, never at evaluation
- [ ] 2.3 In `fields.nix`, define `fields = lib.types.submodule { options = { username = nullOr fieldValue, default null; url = same; notes = same; tags = listOf fieldValue, default [ ]; }; }`, each option carrying an `example` and a description naming what the field is for and that a target which cannot carry it refuses the declaration
- [ ] 2.4 In `fields.nix`, define `fieldNames = [ "username" "url" "notes" "tags" ]` — the fixed order every report and every diff uses — and `channels`, an attribute set keyed by target name carrying one of `"argv"`, `"stdin"` or `"unsupported"` per field: `keepassxc = { username = "argv"; url = "argv"; notes = "argv"; tags = "unsupported"; }`, `pass`/`bitwarden`/`onepassword` = all `"stdin"` except `bitwarden.tags = "unsupported"`, and `clan = { username = "unsupported"; url = "unsupported"; notes = "unsupported"; tags = "unsupported"; }`
- [ ] 2.5 Give `channels` a comment recording where each row was verified: `keepassxc-cli add/edit` take `-u/--username`, `--url` and `--notes` in argv and have no tags flag and no custom-attribute write, read off `keepassxc-cli add --help` on 2026-09-16; the three stdin rows are the sibling changes' own evidence and are declared here so that the table is one artifact rather than four
- [ ] 2.6 Export `{ inherit fieldValue fields fieldNames channels; }` from `fields.nix` and nothing else — no message builder, for F8's reason: a refusal's wording belongs beside the option path it names
- [ ] 2.7 Add `fields = import ./fields.nix { inherit lib; };` to `modules/flake/safix/default.nix`'s lib imports beside `bridge` and `keepassxc` (`default.nix:19-20`), and confirm nothing new needs to reach `lib.bridge`/`lib.keepassxc` (`default.nix:325-345`) because `fields` is a type and a table rather than a resolved value
- [ ] 2.8 Severity drill: changing `channels.keepassxc.tags` from `"unsupported"` to `"argv"` turns the `tagsRefusedMessages` field of `modules/flake/checks/keepassxc.nix` (task 13.3) empty, which is the evidence the refusal is driven by the table rather than by a hard-coded field name
- [ ] 2.9 Verify: `nix build .#checks.x86_64-linux.safix-keepassxc` green after group 3, and the drill in 2.8 observed

## 3. keepassxc's nix half: migrate `username`, refuse what it cannot carry (F6, F7)

- [ ] 3.1 Delete `modules/flake/safix/keepassxc.nix:74-87`'s `username` option in full, including its description — the wall this change moves, quoted in the proposal
- [ ] 3.2 Add `fields = lib.mkOption { type = fields.fields; default = { }; description = … }` to `kdbxSide` (`keepassxc.nix:57-89`), whose description names the four fields, states that `tags` is refused for this target and why, states that an `{ entry = …; }` source is refused for this target and why (its only channel is an argument vector and a referenced value is a secret), and carries the migration sentence naming the option it replaces
- [ ] 3.3 Add `fields = import ./fields.nix { inherit lib; };` to `keepassxc.nix`'s `let` beside `bridge` (`keepassxc.nix:23-24`)
- [ ] 3.4 Update `kdbx`'s own description (`keepassxc.nix:126-136`) from "a path under the declared group, and optionally a username" to name the path and the fields, keeping its "Evaluation verifies neither" paragraph unedited
- [ ] 3.5 Add `fieldUnsupported` to `keepassxc.nix`'s `violationsOf` (`keepassxc.nix:151-232`): for each declared mapping and each name in `fields.fieldNames` whose channel in `fields.channels.keepassxc` is `"unsupported"` and which the mapping declares a value for (non-null, or a non-empty list for `tags`), the sentence `flake.safix.keepassxc.mappings.<id> declares the field '<field>', and keepassxc cannot carry it: the store's own command has no way to write it, so accepting the declaration would mean writing less than it says`
- [ ] 3.6 Add `fieldSourceInArgv` to the same function: for each declared mapping and each field whose channel is `"argv"` and whose declared value is an attribute set (the `{ entry = …; }` form, told apart with `lib.isAttrs`), the sentence `flake.safix.keepassxc.mappings.<id> sources the field '<field>' from the entry '<entry>', and keepassxc carries that field in an argument vector, where a secret value may not travel`
- [ ] 3.7 Judge both new rules over `declared` rather than over `sound`, matching `reservedName`'s treatment (`keepassxc.nix:205-211`) and its stated reason, so a mapping with two faults hears about both; append them to the returned list at `keepassxc.nix:232` in the order `… ++ reservedName ++ reservedId ++ fieldUnsupported ++ fieldSourceInArgv`
- [ ] 3.8 Add `capabilities = fields.channels.keepassxc;` to `keepassxc.nix`'s export list (`keepassxc.nix:234-245`), so `modules/flake/checks/keepassxc.nix` can assert the table this module refuses on is the shared one
- [ ] 3.9 Replace `username = "alice@example.com";` in the `mappings` example at `modules/flake/safix/options.nix:596-603` with a `fields = { username = "alice@example.com"; url = "https://grafana.example.com"; };` block — an option's own `example`, which stops evaluating the moment the option is gone, and therefore this change's rather than the documentation change's
- [ ] 3.10 Confirm `modules/flake/safix/bridge.nix` gains no field surface at all, and state that in the `clanSide` description (`bridge.nix:45-101`): a clan var is a file's bytes and clan's command offers no field beside it, so this target declares none — the `bridge-transfer` delta's "A clan mapping has no field surface" requirement
- [ ] 3.11 Severity drill: judging `fieldUnsupported` over `sound` instead of `declared` empties the field half of a two-fault fixture in `modules/flake/checks/keepassxc.nix`, which is the same drill `reservedName` already carries and the evidence the ordering claim is live
- [ ] 3.12 Verify: `nix build .#checks.x86_64-linux.safix-keepassxc .#checks.x86_64-linux.safix-keepassxc-drill` green, drills in 2.8 and 3.11 observed

## 4. The Rust model: `Fields`, `FieldValue`, and `KdbxSide` (F1, F7)

- [ ] 4.1 Add `FieldValue` to `crates/safix-core/src/model.rs` beside `KdbxSide` (`model.rs:900-910`) as `#[derive(Debug, Clone, Deserialize)] #[serde(untagged)] pub enum FieldValue { Literal(String), Entry { entry: String } }`, with a doc comment stating that a literal came from a world-readable declaration and an `Entry` names a secret to be read at run time
- [ ] 4.2 Add `Fields` as `#[derive(Debug, Clone, Default, Deserialize)] #[serde(deny_unknown_fields)] pub struct Fields { pub username: Option<FieldValue>, pub url: Option<FieldValue>, pub notes: Option<FieldValue>, #[serde(default)] pub tags: Vec<FieldValue> }`, with a doc comment naming `modules/flake/safix/fields.nix` as the other half of the coupling `model.rs:1-15` describes
- [ ] 4.3 Add `Fields::declares(&self) -> bool` (any scalar is `Some` or `tags` is non-empty) and `Fields::named(&self) -> Vec<(&'static str, &FieldValue)>` returning the declared fields in `fieldNames` order, with `tags` yielding one pair per element — the order every report and every diff uses
- [ ] 4.4 Replace `KdbxSide::username: Option<String>` (`model.rs:908-909`) with `pub fields: Fields`, keeping `path` and `#[serde(deny_unknown_fields)]`, and update the struct's doc comment (`model.rs:900-901`) from "what to call its user" to "and what it carries beside the value"
- [ ] 4.5 Fix the fixture JSON in `crates/safix-core/src/sync.rs`'s `mod tests` (`sync.rs:665-681`), replacing `"kdbx": { "path": "alice/grafana", "username": null }` with `"kdbx": { "path": "alice/grafana", "fields": {} }`
- [ ] 4.6 Add a unit test in `model.rs`'s `mod tests` named `a_field_is_a_literal_or_a_named_entry_and_nothing_else` deserialising `{"username": "alice@example", "url": {"entry": "grafana-url"}, "notes": null, "tags": []}` and asserting the two shapes land in the two variants, plus a second assertion that `{"username": {"name": "x"}}` fails to deserialise
- [ ] 4.7 Add a unit test named `an_unknown_field_name_is_refused_by_the_model` asserting `{"path": "a/b", "fields": {"pin": "1234"}}` fails to deserialise as a `KdbxSide`, which is the coupling `model.rs:1-15` states and the mechanism that makes task 3.1's deletion a clean cutover
- [ ] 4.8 Severity drill: adding `#[serde(default)]` to `Fields`'s scalar options changes nothing observable and must be rejected on review rather than by a test; instead drill the real claim by deleting `#[serde(deny_unknown_fields)]` from `Fields` and observing 4.7 turn red
- [ ] 4.9 Verify: `cargo test -p safix-core --lib model::tests` green, drill in 4.8 observed

## 5. The endpoint contract: one record, one capability table, one judge (F5, F9)

- [ ] 5.1 Create `crates/safix-core/src/endpoint.rs` with a module doc comment stating: what a far side is asked (unlock, read, write, list, capabilities) and nothing else; that every refusal only one far side can raise stays outside the trait, listing the six from the `bridge-sync` delta with the file and function each lives in; and that this module exists to delete `sync::two_way` and `bridge_sync::judge` rather than to anticipate a target
- [ ] 5.2 Add `mod endpoint;` to `crates/safix-core/src/lib.rs` in alphabetical position, `pub` at the same visibility `sync` and `store` have
- [ ] 5.3 Add `pub enum Channel { Argv, Stdin, Unsupported }` and `pub enum FieldName { Username, Url, Notes, Tags }` with `FieldName::as_str` returning `"username"`, `"url"`, `"notes"`, `"tags"` and `FieldName::ALL` in that order — the same order `fields.nix`'s `fieldNames` carries
- [ ] 5.4 Add `pub struct Capabilities { pub username: Channel, pub url: Channel, pub notes: Channel, pub tags: Channel, pub multiline: bool }` with `Capabilities::channel(&self, FieldName) -> Channel`, and a doc comment stating that `multiline` is where a value-shape refusal comes from, so `Error::ValueSpansLines` is a declared property of a transport rather than a special case
- [ ] 5.5 Add `pub enum Field { Literal(String), Resolved(Secret) }` and `Field::equals(&self, other: &Field) -> bool`, comparing by materialising a `Literal` through `Secret::read_from` and never by extracting bytes from a `Resolved` — the direction is one-way by construction, which is F1's whole point
- [ ] 5.6 Add `pub struct ResolvedFields { pub username: Option<Field>, pub url: Option<Field>, pub notes: Option<Field>, pub tags: Vec<Field> }` with `Default`, `ResolvedFields::declares`, `ResolvedFields::wanted(&self) -> Vec<FieldName>` (the declared scalar fields plus `Tags` when non-empty) and `ResolvedFields::diff(&self, held: &ResolvedFields) -> Vec<&'static str>` returning the names of the declared fields the held side does not match, in `FieldName::ALL` order
- [ ] 5.7 Give `diff` this exact semantics and state it in the doc comment: a field the declaration does not name is never compared and never reported, because safix declares nothing about it and an entry's own username is the operator's until a declaration claims it; `tags` compares as an ordered list and a length difference is a difference
- [ ] 5.8 Add `pub struct Record { pub value: Option<Secret>, pub fields: ResolvedFields }` and `pub enum Existing { Absent, Present }`
- [ ] 5.9 Add `pub enum Verdict { Unchanged, Conflict, PushValue { remember: bool }, PullValue { remember: bool } }` and `pub fn judge(ours: Option<&Secret>, theirs: Option<&Secret>, remembered: Option<&Secret>, agrees: &dyn Fn(&Secret, &Secret) -> bool) -> Verdict`, whose body is `sync.rs:459-492`'s five arms verbatim with the `Decision` constructors replaced by `Verdict` ones, and whose doc comment states why `agrees` is a parameter (F5 — the two targets' format tags are deliberately distinct, `bridge.rs:633-639`)
- [ ] 5.10 Add `pub trait Endpoint { fn unlock(&mut self) -> Result<()>; fn read(&self, id: &str, declared: &ResolvedFields) -> Result<Option<Record>>; fn write(&mut self, id: &str, record: &Record, existing: Existing) -> Result<()>; fn list(&self) -> Result<Vec<String>>; fn capabilities(&self) -> Capabilities; }`, with `read`'s doc comment stating that `declared` is what decides which attributes are asked for and whether a second invocation happens at all (F3)
- [ ] 5.11 Add `pub fn resolve_fields(workspace: &Workspace, target: &'static str, user: &str, declared: &Fields, capabilities: &Capabilities) -> Result<ResolvedFields>`: per declared field, `Channel::Unsupported` raises `Error::FieldUnsupported { target, field }`; a `FieldValue::Entry` on a `Channel::Argv` field raises `Error::FieldSourceInArgv { target, field }`; a `FieldValue::Literal` becomes `Field::Literal`; a `FieldValue::Entry` on a `Channel::Stdin` field is read through `bridge::held_by_safix(workspace, target, user, &entry)` and becomes `Field::Resolved`, with an absent entry raising `Error::UnknownName`
- [ ] 5.12 Give `resolve_fields` a doc comment recording that the `Stdin` arm has no production caller in this change, that its first one is `add-pass-bridge`, and that shipping the refusal without the resolution would make the refusal a statement about nothing (F9)
- [ ] 5.13 Add unit tests in `endpoint.rs`: `judge_is_the_decision_sync_and_the_bridge_both_reached` over the five arms with a stub `agrees`; `a_field_the_declaration_does_not_name_is_never_compared`; `an_ordered_tag_list_differs_by_order`; `a_literal_and_a_read_value_compare_without_leaving_the_secret_type`; `an_unsupported_field_is_refused_naming_the_target_and_the_field`; `an_entry_source_on_an_argv_channel_is_refused`; and `an_entry_source_on_a_stdin_channel_resolves` over a fixture `Capabilities` whose four channels are all `Stdin`
- [ ] 5.14 Delete `crates/safix-core/src/sync.rs:451-493`'s `two_way` and `crates/safix-core/src/bridge.rs:794-835`'s `judge`, rather than leaving either as a wrapper; `sync.rs:512-515`'s `agrees` and `bridge.rs:864-873`'s `agrees` stay where they are and become the closures passed in
- [ ] 5.15 Severity drill: changing one arm of `endpoint::judge` — making `(Some, Some)` with no memory a `PushValue` instead of a `Conflict` — must turn 5.13's judge test red *and* turn `crates/safix/tests/sync_path.rs`'s two-way conflict claim and the bridge two-way conflict claim red, which is the evidence both targets now reach the same function
- [ ] 5.16 Severity drill: reordering `FieldName::ALL` turns `an_ordered_tag_list_differs_by_order`'s sibling — a `diff` test asserting the returned names' order — red, which is the evidence the report's field order is fixed rather than incidental
- [ ] 5.17 Verify: `cargo test -p safix-core --lib endpoint::` green, drills in 5.15 and 5.16 observed

## 6. The two refusals: variants, prose, codes, samples, snapshots (F6)

- [ ] 6.1 Add `Error::FieldUnsupported { target: &'static str, field: &'static str }` to `crates/safix-core/src/error/mod.rs` beside the store variants (`mod.rs:1219-1258`), as `#[error("{}", prose::field_unsupported(target, field))]`
- [ ] 6.2 Add `Error::FieldSourceInArgv { target: &'static str, field: &'static str, entry: String }` the same way, carrying the entry name because the refusal names which declaration to edit
- [ ] 6.3 Add `prose::field_unsupported(target, field)` to `crates/safix-core/src/error/prose.rs` beside `value_spans_lines` (`prose.rs:750-758`): the target cannot carry this field, the store's own command has no way to write it, nothing was written, and the remedy is to remove the field from the mapping — wrapped at the same column the neighbouring sentences use
- [ ] 6.4 Add `prose::field_source_in_argv(target, field, entry)`: this field is carried in an argument vector on this target, the named entry's value is a secret, a secret value travels standard input or a pipe, nothing was written, and the two remedies are a literal in the declaration or a target whose channel for that field is a pipe
- [ ] 6.5 Add `FieldUnsupported => "safix::field_unsupported"` and `FieldSourceInArgv => "safix::field_source_in_argv"` to the table in `crates/safix-core/src/error/code.rs:92-210`, positioned after `ValueSpansLines` so the table's order follows the variants'
- [ ] 6.6 Add the two sample arms to `crates/safix/src/reporter.rs`'s match (`reporter.rs:251-675`, beside `Code::ValueSpansLines` at `:580`): `FieldUnsupported { target: "keepassxc", field: "tags" }` and `FieldSourceInArgv { target: "keepassxc", field: "notes", entry: "grafana-note".into() }`
- [ ] 6.7 Accept four snapshots under `crates/safix/src/snapshots/`: `safix__reporter__tests__graphical-field_unsupported.snap`, `…plain-field_unsupported.snap`, `…graphical-field_source_in_argv.snap`, `…plain-field_source_in_argv.snap`
- [ ] 6.8 Severity drill: deleting either table entry from `code.rs` fails the build rather than a test, because `reporter.rs`'s match over `Code` is exhaustive — perform it once and record that the compiler is the check here, which is why no test asserts the mapping
- [ ] 6.9 Severity drill: changing one word of `prose::field_unsupported` turns exactly the two `field_unsupported` snapshots red and leaves the two `field_source_in_argv` ones green
- [ ] 6.10 Verify: `cargo test -p safix --bin safix -- reporter::` green, drills in 6.8 and 6.9 observed

## 7. The keepassxc transport: the fields read, the fields write, the capabilities (F3, F6)

- [ ] 7.1 Add `pub fn fields_arguments(database: &Path, entry: &str, wanted: &[FieldName], yubikey: Option<&Yubikey>, key_file: Option<&str>) -> Vec<String>` to `crates/safix-core/src/store.rs` beside `read_arguments` (`store.rs:426-449`), producing `show --quiet <composite key args> --attributes <Attr> …  <database> <entry>` with `UserName`, `URL` and `Notes` as the attribute spellings, in `FieldName::ALL` order, and **neither** `--show-protected` **nor** `Password`
- [ ] 7.2 Give `fields_arguments` a doc comment stating that the absence of `--show-protected` and of `Password` is what makes it impossible for a value to return on this pipe, and that it is a second invocation rather than a widening of `read_arguments` for the reason `store.rs:426-431` records
- [ ] 7.3 Add `Database::read_fields(&self, entry: &str, declared: &ResolvedFields) -> Result<ResolvedFields>`: return `ResolvedFields::default()` when `declared.wanted()` is empty; otherwise spawn `fields_arguments`, write the database password on standard input the way `read` does (`store.rs:215-222`), read standard output, split it on newlines into one `Field::Resolved(Secret)` per requested attribute in the order they were asked for, and raise `Error::StoreCommandFailed` on a non-zero status exactly as `read` does
- [ ] 7.4 Make `read_fields` tolerate a shorter answer than it asked for by treating a missing trailing line as an absent field rather than an error, and state in the doc comment that a field the entry does not carry prints an empty line, so an empty line is an absent field and a diff against a declared value is therefore a divergence
- [ ] 7.5 Change `Database::write`'s signature (`store.rs:255`) from `username: Option<&str>` to `fields: &ResolvedFields`, and change its `ValueSpansLines` guard (`store.rs:256-260`) to read `if !self.capabilities().multiline && value.spans_lines()`, leaving the refusal, its data and its sentence unedited
- [ ] 7.6 Change `write_arguments`'s signature (`store.rs:458-479`) from `username: Option<&str>` to `fields: &ResolvedFields`, appending `--username <v>`, `--url <v>` and `--notes <v>` for each declared `Field::Literal` in `FieldName::ALL` order, before the database and the entry; a `Field::Resolved` is unreachable here and must be handled by `unreachable!` carrying the reason (`resolve_fields` refused it, F6) rather than by silently skipping it
- [ ] 7.7 Add `Database::capabilities(&self) -> Capabilities` returning `username`/`url`/`notes` as `Channel::Argv`, `tags` as `Channel::Unsupported` and `multiline: false`, with a doc comment naming `modules/flake/safix/fields.nix`'s `channels.keepassxc` as the other half and `keepassxc-cli add --help` as where it was verified
- [ ] 7.8 Implement `Endpoint for Database`: `unlock` is a no-op returning `Ok(())` with a doc comment stating that this transport's unlock happens in `Database::open` because the password is read once before the listing that proves the database opened (`store.rs:132-160`), and that moving it here would mean an endpoint that exists before it can be read from; `read` performs `read` then `read_fields`; `write` takes `Existing` from its caller rather than recomputing `holds`; `list` is `under(&group)`; `capabilities` is 7.7
- [ ] 7.9 Update the three `Database::write` callers passing `None` today — `sync.rs:619` and `sync.rs:644` (the companion writes) — to pass `&ResolvedFields::default()`, and state in `remembered_after`'s doc comment that a companion carries no field because it is safix's own bookkeeping rather than a mirrored entry
- [ ] 7.10 Update `store.rs`'s unit tests: `a_username_reaches_argv_and_its_absence_leaves_the_field_alone` (`store.rs:651-682`) becomes `the_declared_literal_fields_reach_argv_and_an_undeclared_one_leaves_the_field_alone`, asserting `--username`, `--url` and `--notes` appear for declared literals, that none appears for an empty `ResolvedFields`, and that the composite-key arguments still ride alongside; `no_argument_vector_can_carry_a_value` (`store.rs:543`) gains a `ResolvedFields` carrying all three literals and still asserts no value among the arguments
- [ ] 7.11 Add a `store.rs` unit test named `the_fields_read_asks_for_the_named_attributes_and_never_the_password` asserting `fields_arguments`' output equals a literal argument vector and contains neither `--show-protected` nor `Password`
- [ ] 7.12 Add a `store.rs` unit test named `a_mapping_declaring_no_field_issues_no_fields_read` asserting `read_fields` over a default `ResolvedFields` returns without spawning, against a `Database` whose `program` is `safix-no-such-store-command` — the same absent-command fixture `a_read_of_an_entry_the_listing_does_not_carry_is_absence_rather_than_a_refusal` (`store.rs:736`) uses
- [ ] 7.13 Severity drill: adding `--show-protected` to `fields_arguments` turns 7.11 red on that one assertion while `the_read_asks_for_the_protected_password_attribute_and_nothing_else` (`store.rs:685`) stays green, which is the evidence the two invocations are held separately
- [ ] 7.14 Severity drill: making `read_fields` spawn unconditionally turns 7.12 red with the absent-command refusal, which is the evidence the no-declared-field path issues nothing rather than issuing and discarding
- [ ] 7.15 Verify: `cargo test -p safix-core --lib store::` green, drills in 7.13 and 7.14 observed

## 8. `sync`: the widened decision and the two new outcomes (F3, F10)

- [ ] 8.1 Add `Outcome::FieldsUpdated(Vec<&'static str>)` and `Outcome::FieldsDiverged(Vec<&'static str>)` to `crates/safix-core/src/sync.rs:75-94`, each with a doc comment: the first is "the value already agreed and the declared fields were written", the second is "the declared fields differ and this mode does not write them"
- [ ] 8.2 Extend `Outcome::as_str` (`sync.rs:99-108`) with `"fields updated"` and `"fields diverged"`, and `Outcome::is_failure` (`sync.rs:116-118`) so that `FieldsDiverged` fails the run and `FieldsUpdated` does not — F10's reason, stated in the doc comment: a declared field that is not there is a declaration that is not true
- [ ] 8.3 Add `fields_updated` and `fields_diverged` to `Tally` (`sync.rs:183-197`) and to `Report::tally`'s `count` calls (`sync.rs:162-179`), and extend `every_outcome_has_a_word_and_only_the_ones_needing_an_operator_fail_the_run` (`sync.rs:720-738`) with the two new rows
- [ ] 8.4 Widen `Decision::Push` (`sync.rs:215-224`) to `Push { value: Secret, remember: bool, resolved: ResolvedFields, drifted: Vec<&'static str> }` and add no second push arm: a push that repairs a value and a push that repairs a field take the same arm, because they are the same single database write and a second arm would be a second write path to the same `add`/`edit`. `drifted` empty means the value is what moved; `drifted` non-empty with an agreeing value is what `push` turns into `Outcome::FieldsUpdated`
- [ ] 8.5 Rewrite `decide` (`sync.rs:378-443`) to resolve the mapping's declared fields once through `endpoint::resolve_fields(workspace, "keepassxc", &mapping.safix.user, &mapping.kdbx.fields, &database.capabilities())`, raising `Outcome::Refused(reason)` on either field refusal, then read the far side through `database.read(&entry)` and `database.read_fields(&entry, &declared)`
- [ ] 8.6 In `decide`, replace the `Mode::TwoWay` arm's `two_way(..)` call with `endpoint::judge(ours.as_ref(), theirs.as_ref(), recorded(database, &entry).as_ref(), &|remembered, value| agrees(remembered, value))` and map the `Verdict` onto the existing `Decision` arms
- [ ] 8.7 In `decide`, after the per-mode value decision settles as `Unchanged`, compute `declared.diff(&held_fields)` and: under `Mode::SafixToKeepassxc` and `Mode::TwoWay`, and under `Mode::Backup` where the entry is absent, upgrade to `Decision::Push { value: ours, remember: <as the mode says>, resolved: declared, drifted: <the diff> }`; under `Mode::KeepassxcToSafix`, and under `Mode::Backup` where the entry exists, settle as `Outcome::FieldsDiverged(diff)`; leave `Unchanged` when the diff is empty
- [ ] 8.8 State the precedence in `decide`'s doc comment and hold it in the code: a value divergence is decided first and a field divergence never displaces it, so a mapping whose value and fields both differ is `Updated` or `Conflict` and never `FieldsDiverged` (F10)
- [ ] 8.9 Change `push` (`sync.rs:543-566`) to take the resolved fields and the diff, call `database.write(&entry, value, &fields)`, and return `Outcome::FieldsUpdated(diff)` when the value was already in agreement and the diff is non-empty, `Outcome::Updated` otherwise; its `log` line gains no field name, because the log names endpoints
- [ ] 8.10 Confirm `pull` (`sync.rs:570-624`) is unedited except for the companion write's new `&ResolvedFields::default()` argument, and state in its doc comment that a pull writes only the value because a field is a declaration and safix's side has nowhere to hold one (F2)
- [ ] 8.11 Add a `sync.rs` unit test named `a_field_drift_does_not_displace_a_value_divergence` over a fixture `ResolvedFields` and two differing values, asserting the decision is the value's
- [ ] 8.12 Severity drill: dropping the `Mode::Backup`-with-an-existing-entry case from 8.7 — letting it push fields — turns the integration claim in 11.6 red, which is the evidence `backup` never overwrites a field either (F4)
- [ ] 8.13 Severity drill: making `FieldsDiverged` return `false` from `is_failure` turns the integration claim in 11.7 red on the exit status while every other sync test stays green
- [ ] 8.14 Verify: `cargo test -p safix-core --lib sync::` green, drills in 8.12 and 8.13 observed

## 9. `audit`: the fourth outcome (F10)

- [ ] 9.1 Add `KeepassxcOutcome::FieldsDiverged(Vec<&'static str>)` to `crates/safix-core/src/audit.rs:146-155`, with a doc comment stating that the value agrees and the named fields do not, and that the names are `&'static str` so no run-time string — and therefore no field content — can reach the report
- [ ] 9.2 Extend `KeepassxcOutcome::as_str` (`audit.rs:157-167`) with `"fields diverged"`, and `KeepassxcReport::is_clean` (`audit.rs:199-210`) so that only `Agreeing` is clean — which it already is, since it matches on `Agreeing`; confirm that by reading it rather than editing it, and record the confirmation in the task
- [ ] 9.3 Rewrite `compare_keepassxc` (`audit.rs:437-462`) to resolve the declared fields through `endpoint::resolve_fields` (an `Err` becoming `Unjudgeable(reason)`), read both sides and, when the values agree, return `FieldsDiverged(diff)` for a non-empty diff and `Agreeing` otherwise; a value difference stays `Diverged` whatever the fields say
- [ ] 9.4 Extend `compare_keepassxc`'s doc comment: its agreement-only, not-per-mode disposition is unchanged (`audit.rs:432-436`), and a field divergence is reported without pre-empting which side `sync`'s mode would resolve it toward, which is the same reason the value comparison gives
- [ ] 9.5 Add an `audit.rs` unit test named `a_value_divergence_is_reported_as_one_even_when_the_fields_also_differ` over fixture readings, asserting `Diverged`
- [ ] 9.6 Severity drill: returning `FieldsDiverged` before checking the values turns 9.5 red, which is the evidence the precedence is held rather than incidental
- [ ] 9.7 Verify: `cargo test -p safix-core --lib audit::` green, drill in 9.6 observed

## 10. Rendering the new words (F10)

- [ ] 10.1 Extend `sync_flow` (`crates/safix/src/render.rs:993-1001`) so `Outcome::FieldsUpdated` renders the same `safix -> kdbx` flow `Updated` does and `Outcome::FieldsDiverged` renders no arrow, matching `Conflict`'s treatment
- [ ] 10.2 Extend `push_sync_detail` (`render.rs:1003-1074`) with a paragraph per new outcome: `FieldsUpdated` names the fields written and states the value was already in agreement; `FieldsDiverged` names the fields, states nothing was written, and gives the remedy per mode — `safix sync keepassxc <mapping>` under a pushing mode, and "the declaration is the author; edit the declaration or the mode" under `keepassxc-to-safix`, and "`backup` never overwrites an existing entry" under `backup`
- [ ] 10.3 Extend `push_keepassxc_audit` (`render.rs:740-806`) with the `FieldsDiverged` arm, naming the fields and the same remedy split, and confirm the existing `Diverged` arm's sentence is unedited
- [ ] 10.4 Hold, in every new rendering, that a field's content never appears: the only interpolation is a `&'static str` name, which is what makes the property structural rather than reviewed
- [ ] 10.5 Extend `sync`'s closing tally line (`render.rs:949-991`) with the two new counts, printed only when non-zero, matching how the existing counts are printed
- [ ] 10.6 Severity drill: interpolating a field's value into `push_sync_detail` does not compile, because `FieldsUpdated` and `FieldsDiverged` carry `&'static str` names and no value at all — perform the attempt once and record the compiler's refusal as the check
- [ ] 10.7 Verify: `cargo test -p safix --bin safix -- render::` green, drill in 10.6 observed

## 11. The harness, the stub, and the integration claims

- [ ] 11.1 Change `Fixture::seed_sync_mapping`'s fifth parameter (`crates/safix/tests/harness/mod.rs:971-994`) from `username: Option<&str>` to `fields: serde_json::Value`, emitting `"kdbx": { "path": path, "fields": fields }`, and update its doc comment's "the shape `modules/flake/safix/default.nix` projects" sentence to name the fields record
- [ ] 11.2 Add `Fixture::store_fields(&self, entry: &str) -> BTreeMap<String, String>` and `Fixture::store_field(&self, entry: &str, field: &str) -> String` to the harness beside `store_username` (`harness/mod.rs:1075-1085`), reading the stub's spool; delete `store_username` and migrate its two callers (`crates/safix/tests/sync_path.rs:151-155`)
- [ ] 11.3 Extend `crates/safix/tests/support/card-stubs.rs`'s `keepassxc` role: record `--username`, `--url` and `--notes` per entry in the `kdbx-entries` spool as a tab-separated record rather than the single trailing username field (`card-stubs.rs:549-572`), and answer `show --attributes <Attr>` by printing the requested attributes one per line in the order asked, an empty line for one the entry does not carry (`card-stubs.rs:478`)
- [ ] 11.4 Extend the stub's argv guard so a `--username`, `--url` or `--notes` whose value equals the entry's password refuses — the stub's own version of "no value in argv" — beside the existing `no value in argv` discipline `positionals`/`following_flag` implement (`card-stubs.rs:490-500`)
- [ ] 11.5 Migrate every `seed_sync_mapping` call to the new fifth argument: `crates/safix/tests/sync_path.rs:50-75`, `:253`, `:310`, `crates/safix/tests/audit.rs:512`, `:555`, `:581`, `:616`, `crates/safix/tests/vault_reads.rs:114` — `json!({})` where the old argument was `None`, and `json!({"username": "alice@example.com"})` where it was `Some("alice@example.com")`
- [ ] 11.6 Add `a_backup_mapping_never_overwrites_a_field_either` to `crates/safix/tests/sync_path.rs`: a `backup` mapping whose declared `url` differs from an existing entry's, asserting the entry's fields are unchanged, the exit status non-zero, and `fields diverged` in the report naming `url`
- [ ] 11.7 Add `a_field_drift_on_an_agreeing_entry_is_repaired_under_a_pushing_mode`: a `safix-to-keepassxc` mapping whose value agrees and whose declared `notes` does not, asserting exactly one `edit` invocation in `store_invocations`, the entry's value unchanged, its notes now the declared one, and `fields updated` naming `notes`
- [ ] 11.8 Add `a_declared_tag_is_refused_at_evaluation_rather_than_dropped`: declare `fields = { tags = [ "work" ]; }` on a keepassxc mapping through the real option tree and assert the evaluation message names the target and the field — the drill the assignment names, held in `modules/flake/checks/keepassxc.nix` (task 13.3) for the message and here for the run's refusal code `safix::field_unsupported` when the same record is handed to the runtime through `--entry`
- [ ] 11.9 Add `a_field_read_out_of_another_entry_is_refused_for_this_target`: a mapping declaring `fields.notes = { entry = "grafana-note"; }`, asserting `safix::field_source_in_argv`, that the refusal names the field and the entry, and that `store_invocations` is empty for that mapping
- [ ] 11.10 Add `no_field_reaches_the_report_only_its_name_does` to `crates/safix/tests/audit.rs`: an entry whose `url` differs from the declaration by a distinctive literal, asserting the literal appears in neither stream and the word `url` appears in the report
- [ ] 11.11 Add `a_mapping_declaring_no_field_issues_no_second_read` asserting `store_invocations` for such a mapping contains exactly one `show` per mapping — the property task 7.12 holds at the unit level, held here against the real argument vectors
- [ ] 11.12 Severity drill: making the stub answer `show --attributes` with the password turns 11.10 red, which is the evidence the report is checked against the value's bytes rather than against a field name alone
- [ ] 11.13 Severity drill: deleting the fields half of task 11.3's spool record turns 11.7 red on the notes assertion while every value assertion stays green
- [ ] 11.14 Verify: `cargo test -p safix --test sync_path --test audit --test vault_reads -- --test-threads 1` green, drills in 11.12 and 11.13 observed

## 12. `bridge_sync` reaches the shared judge (F5)

- [ ] 12.1 Rewrite `bridge_sync::decide` (`crates/safix-core/src/bridge.rs:775-792`) to call `endpoint::judge(safix_value.as_ref(), clan_value.as_ref(), remembered.as_ref(), &|remembered, value| agrees(remembered, value))` and map the `Verdict` onto its own `Decision`, leaving its "read a companion only when both sides differ" preamble (`bridge.rs:785-790`) and its `Outcome::Refused`-on-read-failure disposition unedited
- [ ] 12.2 Replace `bridge.rs:611-626`'s module doc reference to "`decide`/`judge` mirror `crate::sync::two_way` (`sync.rs:451-493`) exactly" with a sentence naming `crate::endpoint::judge` as the one both now reach, and delete the claim that they mirror each other
- [ ] 12.3 Confirm, and state in `bridge_sync`'s module doc, that the five per-target behaviours stay outside the shared contract: `generator_stale` on both the one-way and two-way push paths, `Addressing`'s shared-placement discovery, no commit on clan's side, `AbsentAtSource` as an ordinary absence, and the separate companion commit ordering
- [ ] 12.4 Confirm `bridge.rs`'s `selected` and `sync.rs`'s `selected` stay duplicated, and record F5's reason in a comment on each: their refusal variants name different declared lists and `bridge::selected` additionally refuses `MappingWrongDirection`, so unifying them costs more apparatus than the lines it saves
- [ ] 12.5 Severity drill: task 5.15's judge perturbation is this group's drill as well — record which bridge two-way test it reddens, by name
- [ ] 12.6 Verify: `cargo test -p safix-core --lib bridge::` green, and `cargo test -p safix --test bridge_sync -- --test-threads 1` green

## 13. Nix checks

- [ ] 13.1 Create `modules/flake/checks/reserved-words.nix` adding `checks.safix-reserved-words`, a `pkgs.runCommand` that writes `(import ../safix/reserved.nix).ids` as one word per line, extracts the six words from the `RESERVED_MAPPING_WORDS` literal in `../../crates/safix-core/src/bridge.rs` with `sed`, and `diff -u`s the two, failing with a message naming both files — modelled on `safix-installer-schema` (`modules/flake/checks/installer.nix:1225-1257`)
- [ ] 13.2 Give `reserved-words.nix` a severity-drill header in the shape `modules/flake/checks/keepassxc.nix:32-58` uses, naming the two drills: reordering the nix list, and dropping a word from the Rust array
- [ ] 13.3 Extend `modules/flake/checks/keepassxc.nix`'s fixtures: add `fields` to the local `mapping` helper (`keepassxc.nix:152-156`) as an optional fifth argument defaulting to `{ }`; add `tagsRefusedMessages`, `entrySourcedFieldMessages` and `soundFieldsMessages` to the `actual` set and their literals to `expected`; extend `bothFaultsMessages` with a mapping carrying an unknown user and a declared tag, so the two-faults claim covers the new rules
- [ ] 13.4 Extend `keepassxc.nix`'s severity-drill header with the two new drills: dropping `fieldUnsupported` from `violationsOf`'s list empties `tagsRefusedMessages`, and dropping `fieldSourceInArgv` empties `entrySourcedFieldMessages`
- [ ] 13.5 Add `capabilitiesAreTheSharedTable` to `keepassxc.nix`'s `actual`, asserting `keepassxc.capabilities` equals `(import ../safix/fields.nix { inherit lib; }).channels.keepassxc`, so the table this module refuses on cannot be forked from the shared one
- [ ] 13.6 Add check attributes to `modules/flake/checks/cli.nix` for the new integration claims, each through the `mode "<check-name>" "<test file>" "<test fn>"` form (`cli.nix:327-343`): `safix-sync-fields-push` (11.7), `safix-sync-fields-backup` (11.6), `safix-sync-fields-refused` (11.9), `safix-sync-fields-tag-refused` (11.8), `safix-audit-fields` (11.10), `safix-sync-fields-one-read` (11.11), each carrying the file's prose-comment convention: what the claim is, and which drill reddens it
- [ ] 13.7 Confirm `modules/flake/safix/checks.nix` needs no new `mk*Check`: the field refusals are messages of `keepassxc.violationsOf`, which `keepassxcMessages` (`checks.nix:144-152`) already returns whole — state the confirmation in 13.4's header so the next reader does not go looking
- [ ] 13.8 Severity drill: renaming any test function named in 13.6 without updating `cli.nix` turns that check red, because `integration.nix`'s `runOneWith` fails when zero tests ran — perform it on `safix-sync-fields-push`
- [ ] 13.9 Verify: `nix build .#checks.x86_64-linux.safix-reserved-words .#checks.x86_64-linux.safix-keepassxc .#checks.x86_64-linux.safix-keepassxc-drill .#checks.x86_64-linux.safix-sync-fields-push .#checks.x86_64-linux.safix-audit-fields` green, drills in 13.2, 13.4 and 13.8 observed

## 14. The changelog and the migration note

- [ ] 14.1 Add a `### Fields on the far side of a mapping, one judge, and one reserved-word list` entry first under `## [Unreleased]` in `CHANGELOG.md`, naming: the new `fields` declaration and its four fields; that `kdbx.username` is gone rather than aliased and how the old declaration now fails; the two new refusal codes by name; the two new sync report words and the new audit outcome; that `tags` is refused for keepassxc and why; that three mapping ids — `pass`, `bitwarden`, `1password` — became reserved before the targets that need them exist; and that no dependency was added and `deny.toml` is unedited
- [ ] 14.2 State the migration as a two-line before-and-after in that entry: `kdbx = { path = "alice/grafana"; username = "alice@example.com"; };` becomes `kdbx = { path = "alice/grafana"; fields.username = "alice@example.com"; };`
- [ ] 14.3 Do not rewrite any released changelog entry, and do not edit `README.md` or `examples/` — `rewrite-readme-and-examples` owns both, and the field prose is that change's
- [ ] 14.4 Record in the entry that `README.md:1211-1265`'s keepassxc section still shows the old `username` spelling until `rewrite-readme-and-examples` lands, so the staleness is a stated hand-off rather than an oversight
- [ ] 14.5 Verify: the `[Unreleased]` section re-read end to end, and the before-and-after lines evaluated against the real option tree by task 13.3's fixture rather than asserted in prose

## 15. Closing verification

- [ ] 15.1 Run `cargo test --locked --workspace` and record which failures are pre-existing environment-only ones rather than this change's
- [ ] 15.2 Run `nix flake check` and record every check that moved, confirming `safix-reserved-words` and the six `cli.nix` entries are among them and that no existing check attribute was renamed or removed
- [ ] 15.3 Confirm by diff that `Cargo.toml`, `Cargo.lock`, `deny.toml`, `openspec/specs/rust-supply-chain/spec.md`, `README.md` and everything under `examples/` are unedited
- [ ] 15.4 Confirm by diff that `modules/flake/safix/pass.nix`, `bitwarden.nix` and `onepassword.nix` do not exist in this change, and that `fields.nix` and `reserved.nix` carry every target's row so the three sibling changes consume rather than extend them
- [ ] 15.5 Run `openspec validate extend-bridge-fields --strict` and confirm the five delta specs validate
- [ ] 15.6 Severity drill roll-up: confirm every drill named in groups 1 through 14 was performed and its outcome recorded in the task line, and that any drill which did not reproduce carries the reason in place of the claim
