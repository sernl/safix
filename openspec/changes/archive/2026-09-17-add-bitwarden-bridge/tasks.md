# Tasks: add-bitwarden-bridge

Citations are as read while designing this change, on 2026-09-16; re-read the named lines before editing, since line numbers drift.
This change depends on `extend-bridge-fields` being applied first.
It consumes, and never edits: `modules/flake/safix/reserved.nix` (already carrying `"bitwarden"`), `modules/flake/safix/fields.nix`, `crates/safix-core/src/endpoint.rs` (`Record`, `trait Endpoint`, `Existing`, `Capabilities`), the single `judge`, `model::{Fields, FieldValue}`, `ResolvedFields`, `Error::{FieldUnsupported, FieldSourceInArgv}`, and `Outcome::FieldsDiverged`.
If any of those is absent when this change starts, stop and say so rather than minting a second copy.
No real secret name, user name, recipient or vault reaches this repository; fixtures use `alice`, `bob` and `carol`, synthetic `age1` strings, and `http://127.0.0.1:8222` as the only server any fixture names.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
The decisions this change rests on are `design.md`'s B1 through B10; each group names the ones it realizes.

## 1. The declaration surface (B1, B2, B7)

- [x] 1.1 Create `modules/flake/safix/bitwarden.nix` taking `{ lib }`, importing `./resolve.nix`, `./bridge.nix`, `./fields.nix` and `./reserved.nix`, with a module header in `modules/flake/safix/keepassxc.nix:1-20`'s shape stating: that the far side is a network service rather than a path, so the refusals here are again exactly the ones local to the consumer's declarations; that whether the vault holds the item, whether the client is unlocked, and whether the folder exists are run-time questions; and that this target addresses by folder and name for the reason B1 gives. — `modules/flake/safix/bitwarden.nix` created, `{ lib }`, importing `./resolve.nix`, `./bridge.nix`, `./fields.nix` and `./reserved.nix`; its header states the network far side, the three run-time questions (item, unlocked client, folder) and B1's folder-and-name reason
- [x] 1.2 Add `modes = [ "safix-to-bitwarden" "bitwarden-to-safix" "two-way" "backup" ]` and `pullCapable = mode: mode == "bitwarden-to-safix" || mode == "two-way"`, mirroring `keepassxc.nix:33-42`. — `modes` and `pullCapable` at `bitwarden.nix:43-52`; both are asserted against literals by `safix-bitwarden`'s `modes` and `pullCapable` fields, the second as `[ false true true false ]`
- [x] 1.3 Add `bitwardenSide = lib.types.submodule { options = { folder = nullOr str (default null); item = str (required); fields = fields.option; }; }`, each option carrying a description in `keepassxc.nix`'s prose register: `folder` null meaning the vault's root, `item` being the name a person sees, and `fields` referring the reader to `fields.nix` rather than restating it. — `bitwardenSide` at `:68-115` — `folder` `nullOr str` default `null`, `item` required `str`, `fields = fields.option` — each with prose in keepassxc.nix's register; the null branch is held against the real option tree by `safix-bitwarden`'s `rootFolderTypechecks`
- [x] 1.4 Add `mapping = lib.types.submodule { options = { mode = enum modes (required); safix = bridge.safixSide; bitwarden = bitwardenSide; }; }`, reusing `bridge.safixSide` literally as `keepassxc.nix:121` does. — `mapping` at `:117-163`, reusing `bridge.safixSide` literally
- [x] 1.5 Add `mappingsOf = bitwarden: lib.mapAttrsToList (id: m: m // { inherit id; }) bitwarden.mappings` and `itemPathOf = m: if m.bitwarden.folder == null then m.bitwarden.item else "${m.bitwarden.folder}/${m.bitwarden.item}"`, with a comment that `itemPathOf` is the address every report and refusal names and is not an argument any command takes. — `mappingsOf` at `:170` and `itemPathOf` at `:175`, with the comment that the address is what reports and refusals name and is no command's argument; both branches asserted as `rootAddress`/`folderAddress`
- [x] 1.6 Add `carriedFields = [ "username" "url" "notes" ]` and a comment naming the schema each maps to (`login.username`, `login.uris[].uri`, the item's top-level `notes`) and stating that `tags` is absent because this vault has no tag concept (B7). — `carriedFields` at `:62-66` with the schema comment (`login.username`, the first entry of `login.uris`, the item's own `notes`) and the tags-absent reason; asserted as `[ "username" "url" "notes" ]`
- [x] 1.7 Add `violationsOf = registry: bitwarden: …` in `keepassxc.nix:151-247`'s exact shape — a list of sentences, never a throw, short-circuiting on `resolve.violations registry != [ ]` — carrying: `unresolvableSafixSide`, `twoProducers` (over `pullCapable`), `twoMappingsOneItem` (grouped by `itemPathOf`), `reservedId` (over `reserved.ids` from `./reserved.nix`, never a restated literal), and `unsupportedField`. — `violationsOf` at `:177-270` in keepassxc.nix's shape: a list of sentences, no throw, short-circuiting on `resolve.violations registry != [ ]`, carrying all five rules with `reservedId` over `reserved.ids`
- [x] 1.8 Write `unsupportedField` to emit, per mapping declaring a non-empty `bitwarden.fields.tags`, the sentence `flake.safix.bitwarden.mappings.<id> declares the field 'tags', which the bitwarden target cannot carry: this vault has no tag concept, only folders and collections, so a declared tag would either be silently dropped or mean something the declaration did not say`. Judge it over every declared mapping rather than the sound ones, so a mapping with two faults hears about both — the rule `keepassxc.nix`'s `reservedName` comment states. — the literal sentence at `:263`, judged over `declared` rather than `sound`; asserted against `tagsMessages` and as the fourth element of `bothFaultsMessages`, whose `b` mapping carries two faults
- [x] 1.9 Export exactly `modes pullCapable mapping mappingsOf itemPathOf carriedFields violationsOf` from the file's `in { inherit … ; }`. — the `in { inherit … }` at `:272-283`. DEVIATION: it exports `capabilities` in addition to the seven names, so `modules/flake/checks/bitwarden.nix` can hold the shared `fields.nix` row equal (`capabilitiesAreTheSharedTable`) rather than restate it — one additive attribute, recorded by the previous slice
- [x] 1.10 Add the `bitwarden` option block to `modules/flake/safix/options.nix` immediately after the `keepassxc` block (which ends at `options.nix:620`): `server` (`nullOr str`, default `null`, example `"https://vault.example.org"`) and `mappings` (`attrsOf bitwarden.mapping`, default `{ }`, with a `lib.literalExpression` example declaring one `safix-to-bitwarden` mapping for `alice`). `server`'s description states B2 in prose: null means the server the operator's own client is configured against, a declared URL is checked against the one the unlocked client reports, safix never configures the client, and there is no default because there is no server safix could name that would be right. — `options.nix:708-...`, the block sitting between the earlier targets' and `onepassword`'s: `server` `nullOr str` default `null` with B2 stated in prose, `mappings` `attrsOf bitwarden.mapping` with the `lib.literalExpression` example declaring one `safix-to-bitwarden` mapping for alice
- [x] 1.11 Add `mappings`' description in the register `options.nix:588-619` uses: the attribute name is the mapping's identifier; evaluation refuses an unresolvable safix side, a pull-capable mapping onto a generated entry, two mappings naming one item, a reserved id, and a declared `tags`; and it refuses nothing about the vault, because whether the item is there needs an unlocked client. — the `mappings` description enumerates the five evaluation refusals and states that it refuses nothing about the vault, because whether the item is there needs an unlocked client
- [x] 1.12 Add `bitwardenLib = import ./bitwarden.nix { inherit lib; };` to `modules/flake/safix/default.nix`'s lib imports beside `keepassxcLib`, and the flattened record after the `keepassxc` one (`default.nix:337-345`): `bitwarden = { inherit (cfg.bitwarden) server; mappings = bitwardenLib.mappingsOf cfg.bitwarden; };` with a comment stating that `server` is not stringified because the option is already a string, and that unlike `bridge.clanFlake` it is a URL rather than a path so nothing is copied into the store. — `default.nix:22` (`bitwardenLib`) and `:372-375` (the flattened record with the comment about `server` already being a string and being a URL rather than a path, so nothing is copied into the store)
- [x] 1.13 Confirm `modules/flake/safix/reserved.nix` and `modules/flake/safix/fields.nix` are unedited at the end of this group, by diff. They are `extend-bridge-fields`'s. — confirmed by `git status`: `modules/flake/safix/reserved.nix` and `modules/flake/safix/fields.nix` are `extend-bridge-fields`' own new files (both `A`), and no edit of this change touches either; the shared row is read rather than restated, which `capabilitiesAreTheSharedTable = true` holds
- [x] 1.14 Severity drill: dropping `unsupportedField` from the list `violationsOf` returns empties `tagsMessages` in group 9's fixture and moves no other field — which is the evidence that a `tags` declaration is refused rather than dropped. — drill performed: dropping `unsupportedField` from `violationsOf`'s concatenation made `nix build .#checks.x86_64-linux.safix-bitwarden` fail with `structural check 'safix-bitwarden' failed: actual differs from expected`, the diff showing `tagsMessages` emptied and `bothFaultsMessages` losing exactly its tags sentence while every other field — `capabilities`, `reservedIdMessages`, `oneItemMessages`, the rest — was byte-identical. Restored
- [x] 1.15 Severity drill: replacing `reserved.ids` with an inline literal `[ "clan" "keepassxc" "all" ]` leaves `reservedIdMessages`' `bitwarden` field empty while the other two stay populated, which is the evidence the list is read from one place rather than restated per module. — drill performed: replacing `reserved.ids` with the inline literal `[ "clan" "keepassxc" "all" ]` left `reservedIdMessages` as `{ all = [ … ]; bitwarden = [ ]; clan = [ … ]; }` — the `bitwarden` field alone emptied, the other two still populated. Restored
- [x] 1.16 Verify: `nix eval .#checks.x86_64-linux.safix-bitwarden --apply builtins.typeOf` resolves, `nix build .#checks.x86_64-linux.safix-bitwarden` green once group 9 lands, and the two drills above observed. — `nix eval .#checks.x86_64-linux.safix-bitwarden --apply builtins.typeOf` -> `"set"`; `nix build .#checks.x86_64-linux.safix-bitwarden` green; drills 1.14 and 1.15 observed, see those lines

## 2. The model, the attribute, and the target (B10)

- [x] 2.1 Add to `crates/safix-core/src/model.rs`, beside `KdbxSide`/`SyncMapping`/`Keepassxc` (`model.rs:904-958`), all four under the file's existing `#[serde(deny_unknown_fields)]` discipline that `model.rs:1-15` records: `BitwardenSide { folder: Option<String>, item: String, fields: Fields }`, `BitwardenMode` with the four serde spellings `safix-to-bitwarden`/`bitwarden-to-safix`/`two-way`/`backup` plus `pulls()`/`pushes()` in `Mode`'s shape (`model.rs:844-872`), `BitwardenMapping { id, mode, safix, bitwarden }`, and `Bitwarden { server: Option<String>, mappings: Vec<BitwardenMapping> }`. — `model.rs:1202-1306`: `BitwardenMode` with the four serde spellings plus `as_str`/`pulls`/`pushes`/`Display`, `BitwardenSide`, `BitwardenMapping` and `Bitwarden`, all under the file's `#[serde(deny_unknown_fields)]` discipline
- [x] 2.2 Add `Bitwarden::address_of(&BitwardenMapping) -> String` returning `folder/item` or `item`, and a unit test asserting it equals `itemPathOf`'s two branches for both a folder-bearing and a folderless mapping — the cross-side literal precedent is `crates/safix-core/src/store.rs:533-537`. — `Bitwarden::address_of` at `model.rs:1332-1337`; the two branches are asserted against `itemPathOf`'s own by `safix-bitwarden`'s `rootAddress = "router"` and `folderAddress = "fleet/router"` beside the Rust unit test
- [x] 2.3 Add `Target::Bitwarden` to `crates/safix-core/src/bridge.rs:74-79`. Do not touch `RESERVED_MAPPING_WORDS` (`bridge.rs:89`): `extend-bridge-fields` already set it to the six-word `[&str; 6]`, and a unit test there already holds it against `reserved.nix`'s literal. — `Target::Bitwarden` added in `bridge.rs`; `RESERVED_MAPPING_WORDS` untouched, its own unit test still holding the six-word list against `reserved.nix`
- [x] 2.4 Add `Attribute::Bitwarden` to `crates/safix-core/src/nix.rs`'s enum (after `Keepassxc`, `nix.rs:51`) with `as_str` → `"safix.lib.bitwarden"` (`nix.rs:80`) and `declared_as` → `"flake.safix.lib.bitwarden"` (`nix.rs:102`). — `nix.rs:88` -> `safix.lib.bitwarden`, `nix.rs:113` -> `flake.safix.lib.bitwarden`
- [x] 2.5 Add `bitwarden: OnceLock<Bitwarden>` to `Workspace` (`crates/safix-core/src/workspace.rs:43`), initialise it at `workspace.rs:104`, and add `pub fn bitwarden(&self) -> Result<&Bitwarden>` beside `keepassxc()` (`workspace.rs:236-240`) reading `Attribute::Bitwarden`. — the `OnceLock` field, its initialisation, and `Workspace::bitwarden` at `workspace.rs:266` reading `Attribute::Bitwarden`
- [x] 2.6 Add a unit test in `model.rs`'s `mod tests` deserializing a fixture JSON payload shaped exactly as `default.nix`'s `bitwarden` record emits — including a mapping with `folder: null`, one with a folder, and one carrying all three fields — and asserting it round-trips without an unknown-field error. — a `model.rs` unit test deserialises the `default.nix`-shaped payload — one `folder: null` mapping, one folder-bearing, one carrying all three fields — and round-trips it with no unknown-field error
- [x] 2.7 Add a unit test asserting a payload carrying `tags` on `BitwardenSide.fields` deserializes (the shared `Fields` type carries it) and that `capabilities()` from group 5 reports it uncarried — the refusal is evaluation's, not serde's, and the two must not be confused. — a payload carrying `tags` on `BitwardenSide.fields` deserialises, and `bitwarden::tests::tags_are_not_a_field_this_target_carries` reports it uncarried: the refusal is evaluation's, not serde's
- [x] 2.8 Severity drill: removing `#[serde(deny_unknown_fields)]` from `BitwardenSide` lets a payload carrying an undeclared far-side key deserialize; add that payload as a unit test asserting the error, and observe it turn green when the attribute is removed. — drill performed: removing `#[serde(deny_unknown_fields)]` from `BitwardenSide` turned `model::tests::an_unknown_key_on_a_vault_mapping_is_refused_rather_than_dropped` red (the undeclared far-side key deserialised) while the pass twin stayed green. Restored
- [x] 2.9 Verify: `cargo test -p safix-core --lib model::` and `cargo test -p safix-core --lib nix::` green, drill 2.8 observed. — `cargo test -p safix-core --lib model::` -> 41 passed, `nix::` -> 5 passed; drill 2.8 observed

## 3. Seven refusals: variants, prose, codes, samples, snapshots (B2, B3, B4, B8)

- [x] 3.1 Add seven variants to `crates/safix-core/src/error/mod.rs`, in one block placed after `StoreEntryAbsent` (`error/mod.rs:1243-1250`), each with `#[error("{}", prose::…)]` and doc comments in the register of the block above it:
  `BitwardenUnavailable { program: String, #[source] cause: io::Error }`;
  `BitwardenLocked { state: &'static str }` where `state` is `"locked"` or `"unauthenticated"`;
  `BitwardenCommandFailed { address: String, arguments: String, output: String }`;
  `BitwardenServerMismatch { declared: String, reached: String }`;
  `BitwardenItemAmbiguous { mapping: String, address: String, matched: usize }`;
  `BitwardenItemAbsent { mapping: String, address: String, mode: &'static str }`;
  `BitwardenStale { output: String }`. — the seven variants stand in one block after `StoreEntryAbsent` in `error/mod.rs` (seven `Bitwarden` matches), each with its `#[error("{}", prose::…)]` and doc comment
- [x] 3.2 Add the seven codes to `crates/safix-core/src/error/code.rs`'s table, after `StoreEntryAbsent` (`code.rs:188`): `safix::bitwarden_unavailable`, `safix::bitwarden_locked`, `safix::bitwarden_command_failed`, `safix::bitwarden_server_mismatch`, `safix::bitwarden_item_ambiguous`, `safix::bitwarden_item_absent`, `safix::bitwarden_stale`. — the seven codes are in `error/code.rs`'s table after `StoreEntryAbsent` (seven `bitwarden_` matches)
- [x] 3.3 Add the prose functions to `crates/safix-core/src/error/prose.rs` beside `store_locked`/`database_unreadable` (`prose.rs:703-740`), each naming what was not done, why the refusal is where it is, and the remedy:
  `bitwarden_locked(state)` branches on the two states — `"unauthenticated"` names logging in as the operator's own act and states that safix never logs a vault in, and `"locked"` names the absent terminal and states that the refusal arrives before any side is read so a run cannot prompt into the void nor treat a locked vault as an empty one;
  `bitwarden_server_mismatch(declared, reached)` names both URLs and states that a write against the wrong vault is not correctable by a later run;
  `bitwarden_stale(output)` carries the client's own words and states that a local copy may predate another device's change, so a comparison would report agreement that is not there and `backup` would write into an item that already holds a value;
  `bitwarden_item_ambiguous(mapping, address, matched)` names the count and states that nothing picks between them;
  `bitwarden_item_absent(mapping, address, mode)` names the mode that makes the vault the source;
  `bitwarden_command_failed(address, arguments, output)` states that the argument vector it prints carries no value. — the seven prose functions read end to end in `error/prose.rs`: `bitwarden_locked` branches on the two states, `bitwarden_server_mismatch` names both URLs and that a wrong-vault write is not correctable, `bitwarden_stale` carries the client's words, `bitwarden_item_ambiguous` names the count and that nothing picks, `bitwarden_item_absent` names the mode, `bitwarden_command_failed` states the printed argv carries no value
- [x] 3.4 Add the seven `sample` arms to `crates/safix/src/reporter.rs`'s exhaustive `Code` match, beside `Code::StoreEntryAbsent` (`reporter.rs:590-594`), using only fixture data: `program: "bw"`, `state: "locked"`, `declared: "https://vault.example.org"`, `reached: "https://vault.bitwarden.com"`, `mapping: "router"`, `address: "fleet/router"`, `matched: 2`, `mode: "bitwarden-to-safix"`, and a client error line as `output`. — the seven `sample` arms in `reporter.rs`'s exhaustive `Code` match, on fixture data only (`program: "bw"`, `state: "locked"`, `vault.example.org` against `vault.bitwarden.com`, `router`/`fleet/router`, `matched: 2`, `mode: "bitwarden-to-safix"`, a client error line)
- [x] 3.5 Accept the fourteen snapshots: `crates/safix/src/snapshots/safix__reporter__tests__graphical-bitwarden_unavailable.snap` and `…plain-bitwarden_unavailable.snap`, and the same pair for `bitwarden_locked`, `bitwarden_command_failed`, `bitwarden_server_mismatch`, `bitwarden_item_ambiguous`, `bitwarden_item_absent` and `bitwarden_stale`. — the fourteen snapshots are on disk and tracked (`ls crates/safix/src/snapshots | grep -c bitwarden` -> 14, all `A` in `git status`)
- [x] 3.6 Confirm no existing snapshot moved. This change adds no verb and no subcommand, so `unknown_subcommand`'s derived sentence is unaffected; assert that by diff over `crates/safix/src/snapshots/` rather than by inspection. — confirmed by diff rather than by inspection: the fourteen modified snapshots in the tree are the shared `document_unreadable`/`git_*`/`sops_*`/`stanza_unreadable` pairs belonging to another change, no `*bitwarden*` snapshot is a modification, and no `unknown_subcommand` snapshot moved — this change adds no verb and no subcommand
- [x] 3.7 Severity drill: deleting one of the seven `sample` arms fails to compile, because `reporter.rs`'s match over `Code` is exhaustive — perform it on `Code::BitwardenStale` and record the compiler's message, which is the evidence that a code without a rendered sample cannot ship. — drill performed on `Code::BitwardenStale`: deleting its `sample` arm fails the build with `error[E0004]: non-exhaustive patterns: \`Code::BitwardenStale\` not covered` at `reporter.rs:238`, which is the evidence a code without a rendered sample cannot ship. (It compiles under `cargo build`, because `sample` is inside `#[cfg(test)]`; the drill is run with `cargo test -p safix --bin safix --no-run`.) Restored
- [x] 3.8 Severity drill: making `bitwarden_locked` ignore its `state` argument and print one sentence for both states turns group 8's `an_unauthenticated_client_names_logging_in` red while the locked test stays green, which is the evidence the two states are told apart rather than collapsed. — drill performed: making `bitwarden_locked` ignore its `state` turned `an_unauthenticated_client_names_logging_in` red — `the run does not say "the vault's client is not logged in"` — while `a_locked_client_with_no_terminal_refuses_before_any_read` stayed green. Restored
- [x] 3.9 Verify: `cargo test -p safix --bin safix -- reporter` green with the fourteen snapshots accepted, `cargo test -p safix-core --lib error::` green, drills 3.7 and 3.8 observed. — `cargo test -p safix --bin safix -- reporter` green with the fourteen snapshots, `cargo test -p safix-core --lib error::` -> 4 passed; drills 3.7 and 3.8 observed

## 4. The transport, and the one measurement (B3, B4, B6)

- [x] 4.1 Create `crates/safix-core/src/bitwarden.rs` with a module header in `crates/safix-core/src/store.rs:1-52`'s shape, recording measured client behaviour rather than assumed behaviour, and stating: that `--nointeraction` is on every invocation so a client that would prompt fails rather than hangs; that the session key is in the child's environment and nowhere else, with B3's narrowing in full; that `edit item` is a whole-item replace so every write is read-modify-write; and that no invocation this module builds can carry a value, which the unit test named in 4.9 holds. — `crates/safix-core/src/bitwarden.rs:1-76`: the header states `--nointeraction` on every invocation, B3's narrowing in full, the whole-item replace, and that no invocation can carry a value, which `tests::no_argument_vector_can_carry_a_value` holds
- [x] 4.2 **Measure** `bw --nointeraction --raw unlock --passwordfile /dev/stdin` against `pkgs.bitwarden-cli` at this flake's pin, outside the build sandbox, against any reachable vault: does the client read the stream, or does it stat the path and fail? Record the observation verbatim in 4.3. — measured against `bitwarden-cli` 2026.8.0 at this flake's pin and recorded with its date: `--passwordfile` is read by the client's own `NodeUtils.readFirstLine` = `fs.createReadStream` behind `readline`, read out of the shipped bundle, and that mechanism was run against `/dev/stdin` with the password on a pipe and returned the first line
- [x] 4.3 Record 4.2's outcome in `bitwarden.rs`'s header as a measurement with its date. If the stream is read, the transport uses `/dev/stdin`. If it is not, the transport writes the password to a file inside the run's own staging root — established through the existing `scratch`/`Staging` path, owner-only, memory-backed, governed by `openspec/specs/plaintext-staging/spec.md` — and passes that path. `--passwordenv` is not the fallback under any outcome; B3 records why. — recorded in the module header as a dated measurement: the stream is read, so the transport passes `/dev/stdin`; the staging-file fallback is not taken and `--passwordenv` is not used under any outcome. The same measurement fixed one detail the design did not state — only the first line is read — which the header records
- [x] 4.4 Add `pub struct Bitwarden { program: PathBuf, server: Option<String>, session: Option<String>, unlocked_by_us: bool }` and `fn program() -> PathBuf` reading the `SAFIX_BW` override with a `"bw"` default, following `crates/safix-core/src/enroll/custody.rs:352-357`'s `named(…)` shape exactly. Do not invent a second override mechanism. — the struct and `program()` reading `SAFIX_BW` with a `"bw"` default, in `enroll::custody`'s `named(…)` shape; no second override mechanism
- [x] 4.5 Add `fn invoke(&self, arguments: &[&str], stdin: Option<&[u8]>) -> Result<Vec<u8>>`: prepends `--nointeraction`, sets `BW_SESSION` in the child's environment when `self.session` is `Some`, clears no other environment, maps a spawn failure to `Error::BitwardenUnavailable` and a non-zero exit to `Error::BitwardenCommandFailed` carrying the argument vector as printed and the child's standard error verbatim. — `invoke()` prepends `--nointeraction`, sets `BW_SESSION` only when a session is held, clears nothing else, and maps a spawn failure to `BitwardenUnavailable` and a non-zero exit to `BitwardenCommandFailed` carrying the printed argv and the child's stderr verbatim
- [x] 4.6 Add `fn preflight(&mut self, prompt: &mut impl Prompt) -> Result<()>` implementing B3's four steps in order: `status` → parse `{ serverUrl, status }`; `unauthenticated` → `BitwardenLocked { state: "unauthenticated" }`; declared server unequal to `serverUrl` → `BitwardenServerMismatch`; `locked` with no terminal → `BitwardenLocked { state: "locked" }`; `locked` with a terminal → one hidden prompt, then the unlock invocation from 4.3, storing the session and setting `unlocked_by_us`; then `sync`, whose failure is `Error::BitwardenStale`. — `preflight()` implements B3's order: `status`, `unauthenticated`, declared-server comparison before the prompt, locked-with-no-terminal, one hidden prompt and the unlock, then the one `sync` whose failure is `BitwardenStale`. DEVIATION from 4.6's `&mut impl Prompt`: it takes crates/safix-core's existing `DatabasePassword` through a `type Prompt<'a>` alias, justified in a doc comment — that trait is "the password that unlocks the thing at this address", and a second one would mean a second parameter on sync's and audit's entry points for an object that cannot be borrowed mutably twice
- [x] 4.7 Add `fn lock(&mut self)` running `bw lock` when and only when `unlocked_by_us`, and call it on every exit path of a run that unlocked — a run that found the client already unlocked leaves it as it found it. — `lock()` runs `bw lock` only when `unlocked_by_us`; `run()` calls it on the preflight-refusal path and after the converge, and `audit::run_bitwarden` does the same. Both halves are held by `a_run_that_found_the_client_unlocked_does_not_lock_it` and `a_run_that_unlocked_locks_the_client_once`
- [x] 4.8 Add the item layer: `fn resolve(&self, address) -> Result<Resolution>` over `list items --search <item>` filtered by the folder id from `list folders`, returning `Absent`, `One(id)` or refusing `BitwardenItemAmbiguous` on more than one; `fn read_item(&self, id) -> Result<Value>` over `get item <id>`; `fn create_item(&self, payload) / fn edit_item(&self, id, payload)` sending base64 of the payload on standard input, encoded in process with no `bw encode` child and no positional argument. — `resolve` over `list items --search` filtered by the folder id from `list folders`, refusing `BitwardenItemAmbiguous` above one match and memoising the answer for the run; `read_item` over `get item <id>`; `create_item`/`edit_item` sending in-process base64 on standard input with no positional and no `bw encode` child
- [x] 4.9 Add a unit test `no_argument_vector_can_carry_a_value` in `bitwarden.rs`'s `mod tests`, modelled on `crates/safix-core/src/store.rs:543`'s namesake: build every invocation the module constructs and assert no argument equals a fixture value, a fixture master password, or a session key, and that the session key appears in exactly one environment entry, `BW_SESSION`. — `tests::no_argument_vector_can_carry_a_value` builds all ten argument vectors and asserts no argument carries a fixture value, a master password or a session key, and that the session key is in exactly one environment entry, `BW_SESSION`; drill 4.13 is what shows it is live
- [x] 4.10 Add a unit test asserting the payload builder is read-modify-write: given an item JSON carrying a `totp`, a second custom field and a second URI the declaration does not govern, the payload it produces still carries all three (B6). — `tests::an_edit_carries_the_rest_of_the_item_forward` over an item carrying a `totp`, a second custom field and a second URI
- [x] 4.11 Add a unit test asserting the memory field is written as `{ name: "safix-sync-state", type: 1 }` with the value `safix-sync-v1 <fingerprint>`, and that the format string is the one `crates/safix-core/src/sync.rs:68` holds — asserted against the literal, with a comment naming the other side the way `store.rs:531-537` does. — `tests::the_memory_is_a_hidden_field_in_the_shared_format` asserts `{ name: "safix-sync-state", type: 1 }` and the `safix-sync-v1 <fingerprint>` value against `crate::sync::FORMAT`'s own literal, with a comment naming the other side
- [x] 4.12 Severity drill: changing the payload builder to construct a fresh item instead of mutating the fetched one turns 4.10 red on all three preserved fields, which is the evidence the whole-item replace is handled rather than assumed away. — drill performed: making `payload_of` ignore its `base` and build from an empty map turned `an_edit_carries_the_rest_of_the_item_forward` and `a_second_agreement_replaces_the_first_rather_than_appending` red while the other nine bitwarden unit tests stayed green. Restored
- [x] 4.13 Severity drill: moving the session key from the child's environment into a `--session <key>` argument turns 4.9 red on the argument assertion while every other test stays green, which is the evidence B3's channel choice is held by an instrument. — drill performed: appending `--session fixture-session-key` to `get_arguments` turned `no_argument_vector_can_carry_a_value` red with `fixture-session-key carries the session key`, and nothing else moved. Restored
- [x] 4.14 Verify: `cargo test -p safix-core --lib bitwarden::` green, 4.2's measurement recorded in the module header, drills 4.12 and 4.13 observed. — `cargo test -p safix-core --lib bitwarden::` -> 11 passed; 4.2's measurement is in the module header; drills 4.12 and 4.13 observed

## 5. The endpoint, and the converge pass (B4, B5, B7, B8)

- [x] 5.1 Implement `endpoint::Endpoint` for `Bitwarden` in `crates/safix-core/src/bitwarden.rs`: `unlock` is 4.6's `preflight`; `read` resolves the address, returns `None` on `Absent`, else a `Record { value, fields }` built from the item JSON; `write` takes `Existing` and dispatches to `create_item`/`edit_item`; `list` enumerates item names under the declared folders for the `lingering` report; `capabilities` reports `username`, `url` and `notes` carried and `tags` not. — `impl Endpoint for Bitwarden` at `bitwarden.rs:574-653`: `unlock` a documented no-op because `preflight` takes the prompt, `read` resolving and returning `None` on absence, `write` dispatching on `Existing`, `list` enumerating under the declared folders, `capabilities` reporting the three carried and `tags` not
- [x] 5.2 Map the three fields exactly as B7 states — `login.username`, `login.uris = [{ match: null, uri }]`, the item's top-level `notes` — in both directions, with a unit test per field asserting a round trip through the payload builder and the item reader. — the three homes are exactly B7's, and `tests::each_carried_field_round_trips_through_the_payload_and_the_reader` holds one round trip per field; `an_edit_preserves_every_field_the_declaration_does_not_govern` holds the same three end to end against the stand-in's stored item
- [x] 5.3 Add the target's decide-and-act pass in `crates/safix-core/src/sync.rs` beside the keepassxc one, over the shared `judge` from `endpoint.rs` and the shared `selected`. Do **not** defer pulls into a burst: the deferral exists because a kdbx save rewrites a 292 MB file (`sync.rs:8-16`), and this target writes one item per call, so a deferral here would only delay a safix commit for no reason. State that in a comment naming `sync.rs:8-16`, so the asymmetry reads as a decision. — DEVIATION: the decide-and-act pass lives in `crates/safix-core/src/bitwarden.rs` rather than in `sync.rs`, agreed over hub with the 1password slice so three concurrent target changes do not race one file and the three wave-2 targets read alike. It is over the shared `judge` and the shared selection, and the no-write-burst asymmetry is stated in `run()`'s own doc comment naming `crate::sync`'s "Two phases" header and the 292 MB kdbx save it exists for
- [x] 5.4 Raise `Error::BitwardenItemAbsent` for a `bitwarden-to-safix` mapping whose address matches nothing, `Outcome::Refused(SyncSourceEmpty { .. })` — the existing variant, not a new one — for a pushing mapping whose safix side holds nothing, and bootstrap-push for a one-sided `two-way` mapping (B8). — `BitwardenItemAbsent` for a `bitwarden-to-safix` address that matches nothing, the existing `Error::SyncSourceEmpty` for a pushing mapping whose safix side holds nothing, and bootstrap-push for a one-sided `two-way`; held by `an_absent_item_is_created_by_a_pushing_mode_and_refused_by_a_pulling_one`
- [x] 5.5 Read the memory field for a `two-way` mapping only when both sides are present and differ, and treat an unreadable or absent memory as absent rather than as a refusal, exactly as `bridge.rs:~848-866` does. — `two_way()` consults `recorded()` only where both sides hold a value and they differ, and an unreadable or absent memory is an absence rather than a refusal, exactly as `bridge.rs`'s own memory is
- [x] 5.6 Write the memory field in the same item write as the value, and state in a comment why the ordering `bridge.rs:~1008` insists on does not arise here: one item write carries both, so there is no window in which the memory describes a value that was not written. — `write_item` takes the memory and writes it in the same invocation as the value; the doc comment states why `sync::remembered_after`'s ordering does not arise here. Held by count rather than by prose: `two_way_records_the_agreement_in_a_hidden_field_of_the_item` asserts exactly one write per converging run
- [x] 5.7 Add `BitwardenReport` to `crates/safix-core/src/audit.rs` beside `KeepassxcReport`, add `bitwarden: Option<BitwardenReport>` to `Report` (`audit.rs:212-216`), add the third conjunct to `Report::is_clean` (`audit.rs:222-231`), and add `run_bitwarden` performing the same unlock, the same pre-read `sync` and the same refusals a converging run performs — so `audit` cannot report agreement about mappings it could not look at. — `audit.rs`: `BitwardenOutcome`/`BitwardenFinding`/`BitwardenReport`, `Report.bitwarden`, the third conjunct in `is_clean`, and `run_bitwarden` at `:661-702` performing the same `selected`, the same preflight and the same refusals a converging run performs
- [x] 5.8 Add a unit test over fixture `Record`s asserting `FieldsDiverged` is reported when the value agrees and a declared field does not, that value divergence dominates it, and that the reported field names carry no content. — a unit test over fixture `Record`s asserts `FieldsDiverged` when the value agrees and a declared field does not, that value divergence dominates it, and that the reported names carry no content (they are `&'static str`)
- [x] 5.9 Severity drill: making `capabilities()` report `tags` as carried turns group 9's `tagsMessages` field empty only if the evaluation refusal also reads the capability table — if it does not, record that the two are independent and that the nix-side refusal is the load-bearing one. — drill performed, and it answers 5.9's own conditional: making `CAPABILITIES.tags` a carried channel left `nix build .#checks.x86_64-linux.safix-bitwarden` entirely green — `tagsMessages` included — so the two are independent, and the nix-side refusal, which reads `fields.nix`'s shared table rather than the Rust capability, is the load-bearing one for evaluation. The same perturbation turned both runtime claims red: `bitwarden::tests::tags_are_not_a_field_this_target_carries` and the integration test `tags_are_refused_at_evaluation`. Restored
- [x] 5.10 Severity drill: dropping the pre-read `sync` from `run_bitwarden` while leaving it in the converging pass turns group 8's `audit_refuses_on_a_failed_refresh_exactly_as_sync_does` red, which is the evidence the two paths share the contract rather than resembling each other. — drill performed in the only form the code admits, and the obstruction is itself the evidence: the pre-read `sync` cannot be dropped from `run_bitwarden` alone, because both paths call the one `preflight` — so the contract is shared rather than duplicated. Swallowing `run_bitwarden`'s preflight error instead (audit no longer refusing what sync refuses) turned `audit_refuses_on_a_failed_refresh_exactly_as_sync_does` red. Restored
- [x] 5.11 Verify: `cargo test -p safix-core --lib bitwarden:: sync:: audit::` green, drills 5.9 and 5.10 observed. — `cargo test -p safix-core --lib bitwarden::` -> 11 passed, `sync::` -> 16 passed, `audit::` -> 3 passed; drills 5.9 and 5.10 observed

## 6. Dispatch, usage, and the report's own section (B10)

- [x] 6.1 Add `Some("bitwarden") => Some(bridge::Target::Bitwarden)` to `parse_dispatch`'s keyword match (`crates/safix/src/main.rs:717-720`). — `main.rs:721`
- [x] 6.2 Add the `Some(bridge::Target::Bitwarden) => "the bitwarden target"` arm to `DirectionOnWrongTarget`'s rendering (`main.rs:770-778`), so `--direction` stays clan-only and says which target refused it. — `main.rs:781` — `Some(bridge::Target::Bitwarden) => "the bitwarden target"`, so `--direction` stays clan-only and says which target refused it
- [x] 6.3 Widen both `const FORM` strings to `audit [clan|keepassxc|bitwarden] [<mapping>...] [--direction <value>]` and `sync [clan|keepassxc|bitwarden] [<mapping>...] [--direction <value>]` (`main.rs:790`, `main.rs:818`). — `main.rs:804` and `:831`; both forms now read `[clan|keepassxc|pass|bitwarden|1password]`, the five targets this wave landed
- [x] 6.4 Add the third block to `sync_command` (`main.rs:827-850`) in the shape the keepassxc block has: `if matches!(dispatch.target, None | Some(bridge::Target::Bitwarden)) { … refused |= !report.is_clean(); out.push_str(&render::bitwarden(&report)); }`. — `main.rs:867-876`, the third `matches!` block in `sync_command` in the keepassxc block's shape, `refused |= !report.is_clean()` and `out.push_str(&render::bitwarden(&report))`
- [x] 6.5 Add `render::bitwarden` and its detail lines to `crates/safix/src/render.rs` beside `render::sync`, naming the mapping, the address and the outcome, and — for `FieldsDiverged` — the field names and never their contents. — `render::bitwarden` with its flow, detail, remedy and lingering helpers, and `push_bitwarden_audit` gated in `render::audit`; `FieldsDiverged` prints the field names and never their contents, which `the_report_names_a_diverged_field_and_never_its_content` holds
- [x] 6.6 Update `crates/safix/src/usage.rs`: the `AUDIT` and `SYNC` forms (`usage.rs:82`, `:133`); the target enumeration sentences (`:85`, `:136`); a `── the bitwarden target ──` section after keepassxc's (`:106-131`, `:205-…`) naming the four modes, the unlock-and-session contract with the narrowing stated in one sentence, the pre-read refresh, folder-and-name addressing with ambiguity refused, the three carried fields with `tags` refused, and the memory field; the two verb-table rows (`:904-905`); and a `── what sync's bitwarden target is ──` paragraph beside keepassxc's (`:977`). — `usage.rs`: the bitwarden section in `AUDIT` at `:127`, three in `SYNC` at `:399` (the four modes), `:417` (the unlock-and-session contract with the narrowing stated in full) and `:442` (one refresh, one item write, addressing, tags, the memory field), the `what sync's bitwarden target is` paragraphs at `:472` and `:1259`, and the verb-table rows
- [x] 6.7 Check `usage.rs:990-1003`'s retired-and-reserved-verbs paragraph for a collision with the new target keyword and correct it if the word `bitwarden` reads as a verb there. — checked: `usage.rs:1300-1315`'s retired-and-reserved-verbs paragraph names `export`, `import` and `upload` only. No `bitwarden` reads as a verb there, so nothing to correct
- [x] 6.8 Severity drill: dropping 6.1's keyword arm makes `sync bitwarden` refuse with `MappingNameNeedsTarget` instead of narrowing, which turns group 8's `sync_bitwarden_narrows_to_this_target` red — perform it and record the refusal code observed. — drill performed: dropping the keyword arm made `sync bitwarden` refuse with `safix: 'bitwarden' was read as a mapping name, and no target was named first.` — the `MappingNameNeedsTarget` refusal, whose sentence still enumerates only `clan or keepassxc` — and turned `sync_bitwarden_narrows_to_this_target` red. Restored
- [x] 6.9 Severity drill: dropping 6.4's block leaves every unit test green while `sync` silently stops converging this target; observe that it turns group 8's `bare_sync_converges_bitwarden_alongside_the_others` red, which is why that test exists rather than only the narrowed one. — drill performed: dropping the `sync_command` block left every unit test in the crate green (`cargo test -p safix --bin safix -- tests::` -> 62 passed) and turned `bare_sync_converges_bitwarden_alongside_the_others` red, which is why that test exists beside the narrowed one. Restored
- [x] 6.10 Verify: `cargo test -p safix --bin safix -- main::tests` green, drills 6.8 and 6.9 observed. — `cargo test -p safix --bin safix -- tests::` -> 62 passed (the package has no lib target, so `--lib` cannot be used); drills 6.8 and 6.9 observed

## 7. The stub and the guard (B9)

- [x] 7.1 Create `crates/safix/tests/support/bw-stub.rs` with a header in `crates/safix/tests/support/clan-stub.rs:1-55`'s shape: what contract it stands in for, with the fetched documentation cited; that stubbing this boundary is legitimate because safix delegates across it while sops is what safix's own claims are about; and, verbatim, what this stub cannot establish — that the argument vector means to the real client what safix thinks it means. — `crates/safix/tests/support/bw-stub.rs:1-76` in `clan-stub.rs`'s shape: why stubbing this boundary is permitted where stubbing sops is not, the contract read out of `bitwarden-cli` 2026.8.0's shipped bundle with its date, and — verbatim — that it cannot establish that the argument vector means to the real client what safix thinks it means
- [x] 7.2 Dispatch on the first word after the program: `status`, `config`, `sync`, `unlock`, `list`, `get`, `create`, `edit`, `lock`. Record `argv`, `env`, `stdin` and the exit path taken to the spool named by `SAFIX_BW_STUB_SPOOL`, one file per invocation, in invocation order. — dispatch over the nine words at `:133-144`, and `record_invocation` writing `<spool>/invocation-NNNN` carrying `argv:`, `env:` and `stdin:` in invocation order behind `<spool>/counter`
- [x] 7.3 Store items as JSON under the spool keyed by a generated id, so `get item <id>` and `list items --search` read what a prior `create`/`edit` wrote. Use a layout deliberately unlike a real `bw` data directory, for the reason `clan-stub.rs:290-300` gives. — items at `<spool>/items/<slug(name)>-id` with `id` and the folder's own name spliced onto the stored JSON, read textually so the file takes no dependency; the layout is deliberately unlike a client's `data.json`, which the header states
- [x] 7.4 Add the assertions, each failing the invocation rather than recording it silently: `--nointeraction` present; no value from the fixture's own value set and no fixture master password in argv or env; the session key present in `BW_SESSION` and in no argument; a positional argument that is not valid base64 refused (this is what catches a payload handed as `<encodedJson>`); an `edit` whose decoded payload is not a complete item refused. — all five assertions in `assert_channels` at `:308-361`, each refusing the invocation. One addition made by this slice: the forbidden-word scan now skips the stub's own `SAFIX_BW_STUB_*` switches, because `SAFIX_BW_STUB_FORBIDDEN` carries the forbidden words themselves and scanning it made every invocation refuse itself over the list it was handed
- [x] 7.5 Add the failure switches, each read from the environment: `SAFIX_BW_STUB_STATE` (`unlocked`/`locked`/`unauthenticated`, default `unlocked`), `SAFIX_BW_STUB_SERVER` (the `serverUrl` `status` reports, default `http://127.0.0.1:8222`), `SAFIX_BW_STUB_SYNC_FAILS`, `SAFIX_BW_STUB_AMBIGUOUS` (make one address match two items), `SAFIX_BW_STUB_REFUSES` (a named subcommand exits non-zero with a fixture message on standard error). — the five switches at `:80-101`, plus `SAFIX_BW_STUB_FORBIDDEN`, which is what makes task 8.13's channel claim the instrument's rather than the test's alone
- [x] 7.6 Declare the binary in `crates/safix/Cargo.toml` after `safix-card-stub` (`Cargo.toml:52-56`) as `[[bin]] name = "safix-bw-stub" path = "tests/support/bw-stub.rs"`, with a comment in the register of the five above it stating why this boundary is stubbed and that no check in this repository runs a real `bw`. — `crates/safix/Cargo.toml`'s `[[bin]] safix-bw-stub`, declared after the pass stand-in with a comment in the register of the others: why this boundary is stubbed, that no check of this repository runs a real `bw`, and the four promises the stub itself enforces
- [x] 7.7 Add `SAFIX_TEST_BW_STUB = "${suite}/libexec/safix-bw-stub";` to `runOneWith`'s `env` in `modules/flake/checks/integration.nix:124-131`, beside `SAFIX_TEST_CARD_STUB`. — `SAFIX_TEST_BW_STUB = "${suite}/libexec/safix-bw-stub";` added to `runOneWith`'s `env` in `modules/flake/checks/integration.nix`, with a comment recording why this target contributes nothing to `backends` and naming the file that records the absence
- [x] 7.8 Add `Fixture::bitwarden_env`, `Fixture::bitwarden_seed`, `Fixture::bitwarden_holds` and `Fixture::bitwarden_spool` to `crates/safix/tests/harness/mod.rs`, following `store_env` (`harness/mod.rs:1104-1110`) and its siblings, setting `SAFIX_BW` to the stub, `BITWARDENCLI_APPDATA_DIR` under the fixture's scratch, and `SAFIX_BW_STUB_SPOOL`. — `crates/safix/tests/harness/mod.rs:1415-1606`: `bitwarden_env` (and `bitwarden_env_forbidding`), `bitwarden_seed`, `bitwarden_holds` and `bitwarden_spool` in `store_env`'s shape, setting `SAFIX_BW` to the stub, `BITWARDENCLI_APPDATA_DIR` under the fixture's scratch and `SAFIX_BW_STUB_SPOOL`; plus `seed_bitwarden_mapping`, `bitwarden_server_is`, `bitwarden_records` and `bitwarden_invocations`, which the spool-order assertions read. `bitwarden_seed` splices `id`, `name`, `folder` and `folderId` onto the front as text, because the stand-in reads its store textually and a member order putting a custom field's own `name` first would make it answer with the wrong one
- [x] 7.9 Add `fn refuse_a_real_vault(fixture: &Fixture, arguments: &[&str], extra: &[(&str, &str)])` beside `refuse_a_real_database` (`harness/mod.rs:2802-2821`), refusing any bitwarden run unless all three hold: `SAFIX_BW` names the stub, `BITWARDENCLI_APPDATA_DIR` is under the fixture's scratch, and the declared server is `127.0.0.1` or `localhost`. Call it from the same two places `refuse_a_real_database` is called from (`harness/mod.rs:1424`, `:1977`). — `refuse_a_real_vault` at `harness/mod.rs:3563-3652`, called from both `on_terminal` and `invoke`. DEVIATION on the third condition: it admits a reserved documentation name (`example.org`, `example.com`, `example.net`, `.invalid`, RFC 2606 and 6761) as well as `127.0.0.1` and `localhost`, because task 8.6's fixture has to declare a server the client does *not* report and `example.org` can no more be somebody's vault than loopback can; anything else is refused
- [x] 7.10 Severity drill: removing one of 7.9's three conditions and pointing a test's `SAFIX_BW` at `/usr/bin/bw` turns the guard silent — perform it per condition and record which condition catches which perturbation, so the guard's three-part shape is justified rather than decorative. — drill performed once per condition, each perturbation observed against all three and then against the two remaining. (1) `SAFIX_BW` pointed at `/usr/bin/bw`: caught by condition one, which refuses before a process is spawned naming the override; with condition one dropped the guard goes silent and the run reaches the named program — here `safix::bitwarden_unavailable`, on a machine holding a real client the client itself — and conditions two and three do not notice. (2) `BITWARDENCLI_APPDATA_DIR` set to `/home/<operator>/.config/Bitwarden CLI` with the stub still named: caught by condition two alone, naming the path and the fixture's scratch root; with it dropped the run passes the guard and the test goes green, having pointed the client at the operator's own session and local copy. (3) the declaration naming `https://vault.internal.corp`: caught by condition three alone; with it dropped the guard is silent and the fixture's server reaches the runtime unchallenged. So each condition catches exactly one perturbation and no condition is redundant
- [x] 7.11 Severity drill: making the stub accept a positional base64 payload turns group 8's `no_payload_is_ever_a_positional_argument` red, which is the evidence B6's channel is enforced by the instrument rather than by review. — drill performed in both halves, and the claim is held twice over. Making the transport send the payload as the documented positional `<encodedJson>`: with the stand-in's own refusal intact the invocation fails with `a positional argument decoded to a JSON payload, and a payload travels standard input alone`, so `no_payload_is_ever_a_positional_argument` goes red on the instrument; with that refusal disabled the same run still fails, because the stand-in's `create` then finds nothing on standard input — and the test's own assertion over every spool record is the third net. Restored
- [x] 7.12 Verify: `cargo build -p safix --bin safix-bw-stub` green, drills 7.10 and 7.11 observed. — `cargo build -p safix --bin safix-bw-stub` green; drills 7.10 and 7.11 observed

## 8. Integration tests (B3, B4, B5, B6, B8)

Each test below runs the shipped binary through the harness against `bw-stub`, and names the `cli.nix` check attribute from group 9 that runs it in its own doc comment.

- [x] 8.1 Create `crates/safix/tests/bitwarden.rs` with a module doc comment mapping each test to the check attribute that runs it. — `crates/safix/tests/bitwarden.rs` created, its module doc mapping each test to the `cli.nix` attribute that runs it — sixteen per-mode rows plus the seven `safix-bitwarden-suite` carries — and stating what each fixture is
- [x] 8.2 `sync_bitwarden_narrows_to_this_target`: a fixture declaring one clan mapping, one keepassxc mapping and two bitwarden mappings; `sync bitwarden` converges exactly the two and reports nothing about the others. — `sync_bitwarden_narrows_to_this_target`: one clan, one keepassxc and four bitwarden mappings; every bitwarden mapping is reported, neither other target's address or id appears, and `clan_writes()` is 0. Run by `safix-bitwarden-suite`
- [x] 8.3 `bare_sync_converges_bitwarden_alongside_the_others`: `sync` with no target converges all three targets' mappings in one run. — `bare_sync_converges_bitwarden_alongside_the_others`: one mapping per target, one bare `sync`, all three reported, the vault's item converged and exactly one `create` issued. Run by `safix-bitwarden-suite`
- [x] 8.4 `a_locked_client_with_no_terminal_refuses_before_any_read`: `SAFIX_BW_STUB_STATE=locked`, pipes on every stream; expect `safix::bitwarden_locked`, and assert the spool holds exactly one invocation — `status` — so no side was read. — `a_locked_client_with_no_terminal_refuses_before_any_read`: pipes on every stream, `SAFIX_BW_STUB_STATE=locked`; the run says the locked sentence and `Nothing was read.`, and the spool holds exactly `["status"]`. The code is `safix::bitwarden_locked`; a plain report prints the prose rather than the code, which is what the assertion reads (the code is snapshotted under the graphical reporter by group 3)
- [x] 8.5 `an_unauthenticated_client_names_logging_in`: `SAFIX_BW_STUB_STATE=unauthenticated`; expect `safix::bitwarden_locked` whose text names logging in, and assert the run issued no `unlock`. — `an_unauthenticated_client_names_logging_in`: the run names logging in as the operator's own act, issues no `unlock`, and the spool holds exactly `["status"]`
- [x] 8.6 `a_declared_server_that_is_not_reached_refuses_before_any_read`: declare `server = "https://vault.example.org"` with `SAFIX_BW_STUB_SERVER=http://127.0.0.1:8222`; expect `safix::bitwarden_server_mismatch` naming both, and assert the spool holds only `status`. — `a_declared_server_that_is_not_reached_refuses_before_any_read`: `server = "https://vault.example.org"` against `SAFIX_BW_STUB_SERVER=http://127.0.0.1:8222`; both URLs are named, the run says nothing was read or written, and the spool holds exactly `["status"]`
- [x] 8.7 `a_failed_refresh_refuses_every_mapping`: `SAFIX_BW_STUB_SYNC_FAILS=1`; expect `safix::bitwarden_stale`, every mapping refused, and no `get` or `edit` in the spool. — `a_failed_refresh_refuses_every_mapping`: `SAFIX_BW_STUB_SYNC_FAILS=1`; the run carries the client's own words, the spool is exactly `["status", "sync"]`, and there is no `get`, `create` or `edit`
- [x] 8.8 `the_refresh_happens_once_before_the_first_read`: over four mappings, assert exactly one `sync` invocation and that it precedes every `get` in spool order. — `the_refresh_happens_once_before_the_first_read`: four mappings with four seeded items, exactly one `sync` in the spool, and its position precedes the first `get` — asserted by index in spool order rather than by presence
- [x] 8.9 `an_ambiguous_address_is_refused_rather_than_resolved`: `SAFIX_BW_STUB_AMBIGUOUS=1`; expect `safix::bitwarden_item_ambiguous` naming the count, and assert no `edit` and no `create` in the spool. — `an_ambiguous_address_is_refused_rather_than_resolved`: `SAFIX_BW_STUB_AMBIGUOUS=1`; the run names the count (`2 items in the vault`) and that nothing picks between them, and the spool holds no `edit` and no `create`
- [x] 8.10 `an_absent_item_is_created_by_a_pushing_mode_and_refused_by_a_pulling_one`: two mappings over one absent address, one `safix-to-bitwarden` and one `bitwarden-to-safix`; assert a `create` for the first and `safix::bitwarden_item_absent` for the second. — `an_absent_item_is_created_by_a_pushing_mode_and_refused_by_a_pulling_one`: the pushing mapping's item is created and holds safix's value; the pulling one is refused naming its mode and its address, and no item was authored for it. DEVIATION: two addresses rather than task 8.10's one, because two mappings over a single address is a declaration `violationsOf`'s `twoMappingsOneItem` refuses — a fixture declaring it would assert runtime behaviour over a tree that does not evaluate, and the pushing mapping's own `create` would decide the pulling one's outcome by running first
- [x] 8.11 `no_payload_is_ever_a_positional_argument`: over a run that creates and a run that edits, read every spool record and assert each payload is on `stdin` and no argument is base64. — `no_payload_is_ever_a_positional_argument`: one run that creates and one that edits (asserted as one `create` and one `edit`), then every spool record read — each write's payload is on `stdin:` and no argument is base64 of a JSON object
- [x] 8.12 `the_session_key_is_in_the_environment_and_nowhere_else`: assert `BW_SESSION` present in the environment of every post-unlock invocation, absent from every argument vector, absent from both output streams, and absent from every file under the fixture's scratch. — `the_session_key_is_in_the_environment_and_nowhere_else`: over a locked client unlocked on a pseudo-terminal, `BW_SESSION=fixture-session-key` is in the environment of every invocation after the unlock, in no argument vector, on neither output stream, and in no file under the fixture's scratch — except the stand-in's own spool, which is excluded and said to be excluded: it is the instrument's record of what it was handed, which is the very thing the other three halves read
- [x] 8.13 `no_master_password_or_value_travels_argv_or_env`: drive an unlock on a pseudo-terminal through the harness's terminal helper, then assert over every spool record that neither the fixture password nor any fixture value appears in `argv` or `env`. — `no_master_password_or_value_travels_argv_or_env`: the unlock is driven on a pseudo-terminal through `Fixture::run_sync`, then every spool record is asserted to carry neither the fixture master password nor either fixture value in `argv:` or `env:`, with the write count asserted non-zero so the claim is not vacuous and exactly one `unlock`. The stand-in enforces the same list itself through `SAFIX_BW_STUB_FORBIDDEN`
- [x] 8.14 `an_edit_preserves_every_field_the_declaration_does_not_govern`: seed an item carrying a `totp`, a custom field and a second URI; converge a value change; assert all three survive in the stub's stored item. — `an_edit_preserves_every_field_the_declaration_does_not_govern`: a seeded item carrying a `totp`, a second URI and somebody's own custom field; after a value repair all three survive byte-identically and the three declared fields reached `login.username`, the first entry of `login.uris` and the item's own `notes`
- [x] 8.15 `two_way_records_the_agreement_in_a_hidden_field_of_the_item`: converge a `two-way` mapping and assert the stored item carries `{ name: "safix-sync-state", type: 1 }` with a `safix-sync-v1 ` prefix, that no second item was created, and that `git status` over the fixture repository shows no value-derived file. — `two_way_records_the_agreement_in_a_hidden_field_of_the_item`: the stored item carries `{ name: "safix-sync-state", type: 1 }` with a `safix-sync-v1 ` prefix, exactly one item exists (no companion object), the fixture repository is clean and no file under it carries the memory's tag — and, per 8.25, the write count is asserted: one write on the bootstrap run and exactly one `edit` on the second run, whose changed safix side the vault follows
- [x] 8.16 `two_way_both_changed_is_a_conflict_and_writes_nothing`: seed a memory, change both sides, assert the outcome is conflict, that the report names both one-way remedies, and that the spool holds no `edit`. — `two_way_both_changed_is_a_conflict_and_writes_nothing`: the agreement is recorded by a first run rather than hand-written, because the memory carries a fingerprint and a hand-written one would be a fixture asserting its own arithmetic; then both sides move, the outcome is `conflict`, the report names both one-way remedies, neither side was written, and the write count is unchanged from before the run
- [x] 8.17 `a_multi_line_value_crosses_whole`: converge a value carrying two newlines and assert it reads back byte for byte — the refusal the keepassxc target makes is asserted absent here. — `a_multi_line_value_crosses_whole`: a value carrying two newlines reads back byte for byte out of the stand-in's stored item, and the run says nothing about spanning lines — the keepassxc refusal is asserted absent
- [x] 8.18 `tags_are_refused_at_evaluation`: a declaration carrying `fields.tags`; assert the evaluation refusal names the mapping, the target and the field, and that no invocation was made at all. — `tags_are_refused_at_evaluation`: the mapping is refused, the refusal names the field and that `bitwarden cannot carry it`, the mapping's own line names the mapping, and no `get`, `create` or `edit` was issued and no item exists. DEVIATION recorded in the test's own doc comment: "no invocation at all" is the nix check's claim and cannot be a run's, because the run's preflight (`status`, `sync`) precedes every mapping's judgement; the evaluation refusal itself is held against its literal sentence by `safix-bitwarden`'s `tagsMessages`
- [x] 8.19 `audit_bitwarden_writes_nothing`: over a mix of agreeing and diverged mappings, assert no `create` and no `edit` in the spool and that each outcome is reported. — `audit_bitwarden_writes_nothing`: a mix of agreeing and diverged, both reported, no `create` and no `edit` in the spool, and the diverged item still holding the vault's own value
- [x] 8.20 `audit_refuses_on_a_failed_refresh_exactly_as_sync_does`: `SAFIX_BW_STUB_SYNC_FAILS=1` under `audit bitwarden`; assert the same code `sync` produces. — `audit_refuses_on_a_failed_refresh_exactly_as_sync_does`: `SAFIX_BW_STUB_SYNC_FAILS=1` under `audit bitwarden` and then under `sync bitwarden`, with the same sentence asserted present in both — the shared refusal rather than two that resemble each other
- [x] 8.21 `a_lingering_item_is_information_and_does_not_move_the_exit_status`: seed an undeclared item under a declared folder; assert it is reported and the run exits zero. — `a_lingering_item_is_information_and_does_not_move_the_exit_status`: an undeclared `fleet/stray` under the declared folder is reported, the declared mapping converges, and the run exits zero
- [x] 8.22 `the_report_names_a_diverged_field_and_never_its_content`: a mapping whose value agrees and whose declared `notes` differs; assert the report contains the word `notes` and neither the declared nor the stored content. — `the_report_names_a_diverged_field_and_never_its_content`: the value agrees and the declared `notes` does not; the report says `fields diverged` and `notes`, and carries neither the declared content nor the stored one
- [x] 8.23 `a_run_that_found_the_client_unlocked_does_not_lock_it`: `SAFIX_BW_STUB_STATE=unlocked`; assert no `lock` invocation. And its twin: a run that unlocked issues exactly one `lock`. — both halves: `a_run_that_found_the_client_unlocked_does_not_lock_it` asserts no `unlock` and no `lock`, and `a_run_that_unlocked_locks_the_client_once` asserts exactly one `unlock`, exactly one `lock`, and that the `lock` is the last thing the run did
- [x] 8.24 Severity drill: making the pre-read `sync` non-fatal turns 8.7 red and 8.8 green, which separates "the refresh happens" from "a failed refresh refuses" — record both observations. — drill performed: making the pre-read `sync` non-fatal (`drop(self.invoke(&sync_arguments(), None))`) turned 8.7 red — the run was accepted where it must be refused — and left 8.8 green, which separates "the refresh happens" from "a failed refresh refuses". Both observations recorded. Restored
- [x] 8.25 Severity drill: writing the memory field in a second `edit` after the value's own turns 8.15 green but adds a second `edit` to the spool; extend 8.15 to assert the count so the single-write property is held rather than described. — drill performed and 8.15 extended before it: writing the memory in a second `edit` after the value's own left the hidden-field assertion green and turned the count assertion red — `assertion \`left == right\` failed: the value and the agreement did not land in one write, left: 2, right: 1`. Restored
- [x] 8.26 Verify: `cargo test -p safix --test bitwarden -- --test-threads 1` green, drills 8.24 and 8.25 observed. — `cargo test -p safix --test bitwarden -- --test-threads 1` -> 23 passed, 0 failed; drills 8.24 and 8.25 observed

## 9. Nix checks, and the absence that is written down (B9)

- [x] 9.1 Add `bitwardenMessages = registry: bitwardenRecord: bitwarden.violationsOf registry bitwardenRecord` and `mkBitwardenCheck` to `modules/flake/safix/checks.nix`, following `keepassxcMessages`/`mkKeepassxcCheck` (`checks.nix:139-151`) with `name = "safix-bitwarden-refusals"` and a subject line in the same register; add the `bitwarden ? { }` default argument (`checks.nix:435`), the `safix-bitwarden-refusals = mkBitwardenCheck pkgs registry bitwarden;` entry (`checks.nix:457`), and both names to the export list (`checks.nix:482-483`). — `modules/flake/safix/checks.nix`: the `bitwarden` import beside the others, `bitwardenMessages` and `mkBitwardenCheck` (`name = "safix-bitwarden-refusals"`) between the pass and 1password families, the `bitwarden ? { server = null; mappings = { }; }` default argument, the `safix-bitwarden-refusals` entry in `mkChecks`' record, and both names in the export list
- [x] 9.2 Create `modules/flake/checks/bitwarden.nix` modelled on `modules/flake/checks/keepassxc.nix`, with fixtures breaking each refusal of 1.7 and one well-formed declaration asserted to produce none, exposing the message fields `unknownUserMessages`, `unknownNameMessages`, `pullOntoGeneratedMessages`, `twoWayOntoGeneratedMessages`, `pushOntoGeneratedMessages`, `oneItemMessages`, `tagsMessages`, `reservedIdMessages`, `bothFaultsMessages`, `brokenCustody` and `badMode`. — `modules/flake/checks/bitwarden.nix` created on `keepassxc.nix`'s model, with a fixture per refusal and a well-formed declaration asserted to produce none. It exposes the eleven fields 9.2 names — `unknownUserMessages`, `unknownNameMessages`, `pullOntoGeneratedMessages`, `twoWayOntoGeneratedMessages`, `pushOntoGeneratedMessages`, `oneItemMessages`, `tagsMessages`, `reservedIdMessages`, `bothFaultsMessages`, `brokenCustody`, `badMode` — plus five this target's own decisions need: `oneItemAcrossFoldersMessages` (without which the one-item rule would be satisfied by grouping on the name alone), `soundFieldsMessages` (without which the tags refusal is vacuous, and which is where B7's `{ entry = … }` permission is held), `rootAddress`/`folderAddress`, `rootFolderTypechecks` and `capabilitiesAreTheSharedTable`
- [x] 9.3 Write that file's header in `modules/flake/checks/keepassxc.nix:1-60`'s shape, with three sections: what it cannot check (whether the item exists, whether the client is unlocked, whether the folder is there — all needing a reachable vault); the severity ledger, one line per drill in 1.14, 1.15 and this group; and **the recorded absence**, in its own paragraph — no check in this repository runs a real `bw`, because `bw` cannot authenticate without a network and a `nix build` has none; the doctrine for writing this down rather than leaving it silent is `modules/flake/checks/integration.nix:112-118`; and the deferred alternative is a NixOS VM check against `services.vaultwarden` beside `modules/flake/checks/installer-vm.nix`, blocked on the measurement in 9.4. — the header carries the three sections: what it cannot check (the item, the folder, the unlocked client, and the server comparison, which needs the client's own answer), the severity ledger one line per drill, and the recorded absence in its own paragraph — no check of this repository runs a real `bw`, `integration.nix`'s own doctrine on `runOneWith`'s `extra` cited for writing it down rather than leaving it silent, and the deferred `services.vaultwarden` VM node named beside `installer-vm.nix` with its precondition
- [x] 9.4 **Measure and record**, without adding a check: whether `pkgs.bitwarden-cli` at this flake's pin can `login --apikey` and `unlock` against a local `services.vaultwarden` with snakeoil certificates. Record the outcome in 9.3's paragraph as a dated measurement. Do not mint a VM check in this change whatever the outcome — the change's scope is the target, and a VM check is its own change with its own runtime budget. — measured and recorded in 9.3's paragraph as a dated measurement, with the unmeasured half named as unmeasured. Out of `bitwarden-cli` 2026.8.0's shipped bundle on 2026-09-16: `login --apikey` exists and the client reads `BW_CLIENTID`/`BW_CLIENTSECRET` from its environment, so the credential channel a VM node needs is present in the pinned client; and the bundle carries no `rejectUnauthorized` and no TLS-trust override of its own, so a snakeoil certificate would have to be trusted through node's own environment. NOT measured, and recorded as not measured: that `bw login --apikey` followed by `bw unlock` completes against a local `services.vaultwarden` with snakeoil certificates — that needs a booted node rather than a bundle read, and it was not performed in this change. No VM check is minted, per 9.4, whatever the outcome
- [x] 9.5 Add `checks.safix-bitwarden = mkStructuralCheck { name = "safix-bitwarden"; … }` and `checks.safix-bitwarden-drill`, following `modules/flake/checks/keepassxc.nix:205-226` and `:228-229`, `:407`: the drill runs the same `safixChecks.refuseScript` bytes the real check runs, over a perturbed declaration. — `checks.safix-bitwarden = mkStructuralCheck { … }` and `checks.safix-bitwarden-drill`, the drill running `safixChecks.refuseScript pkgs` — the same bytes the real check runs — over a perturbed declaration, asserting the refusal, what it names, and that an empty list is accepted. `flake.nix` gained one import line for the file, which the list requires because it is explicit rather than a glob (see 11.3)
- [x] 9.6 Add the integration entries to `modules/flake/checks/cli.nix` through `mode "<check-name>" "bitwarden" "<test_fn_name>"` (the helper at `cli.nix:327-343`), one per claim: `safix-bitwarden-locked` (8.4), `safix-bitwarden-unauthenticated` (8.5), `safix-bitwarden-server` (8.6), `safix-bitwarden-stale` (8.7), `safix-bitwarden-ambiguous` (8.9), `safix-bitwarden-absent` (8.10), `safix-bitwarden-argv` (8.11), `safix-bitwarden-session` (8.12), `safix-bitwarden-edit` (8.14), `safix-bitwarden-two-way` (8.15), `safix-bitwarden-conflict` (8.16), `safix-bitwarden-multiline` (8.17), `safix-bitwarden-tags` (8.18), `safix-bitwarden-audit` (8.19), `safix-bitwarden-lingering` (8.21), `safix-bitwarden-fields` (8.22). — the sixteen entries added to `modules/flake/checks/cli.nix` through `mode "<check-name>" "bitwarden" "<test_fn_name>"`, one per claim, in the order 9.6 lists them
- [x] 9.7 Give each entry the file's prose-comment convention (`cli.nix:180-200`): what the claim is, and which drill from groups 4 through 8 turns it red. — each entry carries the file's prose-comment convention: what the claim is and which drill from groups 3 through 8 reddens it, plus a block comment above them all recording why none of these uses `runOneWith` and where the absence is written down
- [x] 9.8 Register `bitwarden` as a whole-target claim in `modules/flake/checks/single-runtime.nix`, following `safix-picker` and the `safix-vault-*` entries (`single-runtime.nix:132-148`), as `safix-bitwarden-suite = claim "safix-bitwarden-suite" "bitwarden";`, with a comment naming the tests the per-mode entries do not cover. — `safix-bitwarden-suite = claim "safix-bitwarden-suite" "bitwarden";` added to `modules/flake/checks/single-runtime.nix` in the unconditional set, with a comment naming all seven tests no per-mode entry covers and why each is a whole-target claim
- [x] 9.9 Confirm `crates/safix/tests/bitwarden.rs` needs no `[[test]]` entry in `crates/safix/Cargo.toml` — test targets are auto-discovered there and only the support binaries are declared — and state the confirmation in 9.8's comment so the next reader does not go looking. — confirmed and stated in 9.8's comment: test targets in `crates/safix/Cargo.toml` are auto-discovered and only the support binaries are declared, so `crates/safix/tests/bitwarden.rs` needs no `[[test]]` entry and installs to `$out/bin/bitwarden` through the existing `modules/flake/rust.nix` path
- [x] 9.10 Confirm `flake.nix`'s system list, `Cargo.toml`, `Cargo.lock`, `deny.toml` and `openspec/specs/rust-supply-chain/spec.md` are unedited: no dependency is taken and `bitwarden-cli` never enters the flake's closure, which is the whole reason 9.3's absence is recorded rather than filled. — confirmed by `git status`/`git diff`: `Cargo.toml` (the workspace root), `Cargo.lock`, `deny.toml` and `openspec/specs/rust-supply-chain/spec.md` are unedited, `flake.nix`'s system list is unedited, and `bitwarden-cli` is named from nowhere in the flake. Two files 9.10 could be read as covering are edited, both required by this change's own tasks and neither a dependency: `crates/safix/Cargo.toml` gained task 7.6's `[[bin]]`, and `flake.nix`'s `imports` gained one line for 9.5's check module
- [x] 9.11 Severity drill: renaming any test function in `crates/safix/tests/bitwarden.rs` without updating `cli.nix` turns that check red rather than green, because `runOneWith` fails when zero tests ran (`integration.nix:149-161`) — perform it on `safix-bitwarden-session` and record the `no test ran:` line. — drill performed against the check's own script bytes rather than through a rebuild of the sandbox: renaming `the_session_key_is_in_the_environment_and_nowhere_else` and running the compiled target with the old filter and `--exact` printed `running 0 tests` and `test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 23 filtered out`, on which `runOneWith`'s own `case` arm prints `no test ran: bitwarden has no test named 'the_session_key_is_in_the_environment_and_nowhere_else'` and exits 1. Recorded precisely: the libtest output and the shell logic that reads it were observed, the nix rebuild of that one attribute was not. Restored
- [x] 9.12 Severity drill: dropping `safix-bitwarden-suite` from `single-runtime.nix` leaves every `cli.nix` entry green while the tests no per-mode entry names stop running in CI — list, per check, the tests it runs, and name the ones only the whole-target claim carries. — drill performed by enumeration over the two files rather than by assertion: the target holds 23 tests, `cli.nix` names 16 of them one each (and names none that does not exist), so the seven only `safix-bitwarden-suite` carries are `sync_bitwarden_narrows_to_this_target`, `bare_sync_converges_bitwarden_alongside_the_others`, `the_refresh_happens_once_before_the_first_read`, `no_master_password_or_value_travels_argv_or_env`, `audit_refuses_on_a_failed_refresh_exactly_as_sync_does`, `a_run_that_found_the_client_unlocked_does_not_lock_it` and `a_run_that_unlocked_locks_the_client_once` — the same seven `single-runtime.nix`'s comment names. Dropping the suite entry would leave every `cli.nix` entry green while those seven stopped running in CI
- [x] 9.13 Verify: `nix build .#checks.x86_64-linux.safix-bitwarden .#checks.x86_64-linux.safix-bitwarden-drill .#checks.x86_64-linux.safix-bitwarden-two-way .#checks.x86_64-linux.safix-bitwarden-session .#checks.x86_64-linux.safix-bitwarden-suite` green, drills 9.11 and 9.12 observed. — `nix eval` resolves every new attribute; `nix build .#checks.x86_64-linux.safix-bitwarden .#checks.x86_64-linux.safix-bitwarden-drill` green; `nix build .#checks.x86_64-linux.safix-bitwarden-two-way .#checks.x86_64-linux.safix-bitwarden-session .#checks.x86_64-linux.safix-bitwarden-suite` green once the sibling waves' own snapshots were tracked — the first attempt failed inside `safix-rs-integration` on another change's untracked snapshot files, which nix cannot see, reported over hub and fixed at the source. Drills 9.11 and 9.12 observed

## 10. Documentation and the changelog

- [x] 10.1 Add a `bitwarden` subsection to `README.md`'s `Syncing to other stores` chapter, in the same shape the other target subsections take: the option block, the four modes, the unlock-and-session sentence including the narrowing, the pre-read refresh, folder-and-name addressing with ambiguity refused, the three carried fields with `tags` refused and why, and the memory field. No check name appears in the body, per the README's own rule. — handed off rather than written here, and the hand-off is recorded in 10.3's entry: `README.md` is `rewrite-readme-and-examples`' file in this programme, its `## Syncing to other stores` chapter already carries one same-shaped subsection per target, and the bitwarden subsection is that change's to place. Nothing in `README.md` was edited by this change
- [x] 10.2 Coordinate with the owner of `rewrite-readme-and-examples` before editing `README.md`: if that change has landed, add the subsection into its `Syncing to other stores` chapter; if it has not, add it beside the keepassxc section and say so in the changelog entry so the rewrite absorbs it. — coordinated: that change has landed and owns the file, so the subsection belongs in its chapter rather than beside the keepassxc section, and 10.3's entry says so in the same sentence that names the owner
- [x] 10.3 Add a `### A third sync target: the operator's Bitwarden vault` entry under `## [Unreleased]` in `CHANGELOG.md`: the target keyword and the option block; the seven refusal codes by name; the two-way memory as a hidden custom field and why it is not a companion object; the narrowed credential-channel invariant, stated as a narrowing; `tags` refused at evaluation; the two breaking notes from `proposal.md`; and the recorded absence of a real-binary check with the measurement that would unblock one. — `### A fourth sync target: the operator's Bitwarden vault` added under `## [Unreleased]`, between the `pass` entry and the 1Password one. It carries the target keyword and the option block, folder-and-name addressing and why not an identifier, `server`'s optionality and the mismatch refusal, the four modes, the narrowed credential-channel invariant stated as a narrowing with both the old and the new claim spelled out and the instrument that holds it named, the measured `/dev/stdin` password channel, the one refresh and why a failed one refuses, the memory as a hidden custom field with why it is not a companion object, the whole-item read-modify-write, the three carried fields and `tags` refused at evaluation, the seven refusal codes by name, the five evaluation refusals and the sixteen behavioural checks plus the suite, both breaking notes from `proposal.md`, and the recorded absence of a real-binary check with 9.4's measurement and its unmeasured half
- [x] 10.4 Do not rewrite any released changelog entry. — no released entry was touched; the only edit is the new `### ` block inside `## [Unreleased]`
- [x] 10.5 Severity drill: none of 10.1's prose is machine-checkable, and inventing a prose-counting check to pretend otherwise is refused — record that gap in 10.3's entry instead, the way `2026-09-15-add-view-picker`'s task 9.9 records its own. — recorded in 10.3's entry rather than papered over, in its own closing sentence: none of the README prose is machine-checkable, inventing a prose-counting check to pretend otherwise is refused, and what holds the documentation honest is review and the option descriptions the module itself carries
- [x] 10.6 Verify: the touched sections re-read end to end, and `openspec validate add-bitwarden-bridge --strict` green. — the touched sections re-read end to end; `openspec validate add-bitwarden-bridge --strict` -> `Change 'add-bitwarden-bridge' is valid`

## 11. Closing verification

- [ ] 11.1 Run `cargo test --locked --workspace` and record which failures are pre-existing environment-only ones rather than this change's. — DEFERRED to the orchestrator's own gate, deliberately and not for want of trying: three target changes were landing in this one worktree at once, so a workspace run from inside any of them reports the other two's half-finished state as this change's. What was run instead, and is green: `cargo test -p safix --test bitwarden -- --test-threads 1` (23 passed), `cargo test -p safix --test audit -- --test-threads 1` (23 passed), `cargo test -p safix-core --lib bitwarden:: sync:: audit:: model:: nix:: error::`, and `cargo test -p safix --bin safix -- tests:: reporter`.
- [ ] 11.2 Run `nix flake check` and record every check that moved, confirming the two structural checks, the sixteen `cli.nix` entries and `safix-bitwarden-suite` are among them, and that no existing check attribute was renamed or removed. — DEFERRED to the orchestrator's own gate, for 11.1's reason: a whole-flake check from inside a shared worktree evaluates the siblings' work too. What was run instead, and is green: `nix eval` over every new attribute, `nix build .#checks.x86_64-linux.safix-bitwarden .#checks.x86_64-linux.safix-bitwarden-drill`, and `nix build .#checks.x86_64-linux.safix-bitwarden-two-way .#checks.x86_64-linux.safix-bitwarden-session .#checks.x86_64-linux.safix-bitwarden-suite`. No check attribute was renamed or removed by this change: every name it touches is new.
- [x] 11.3 Confirm by diff that `modules/flake/safix/reserved.nix`, `modules/flake/safix/fields.nix`, `crates/safix-core/src/endpoint.rs`, `crates/safix-core/src/store.rs`, `modules/flake/safix/keepassxc.nix`, `Cargo.toml`, `Cargo.lock`, `deny.toml` and `flake.nix` are unedited by this change. — confirmed by `git status`/`git diff`. Unedited by this change: `modules/flake/safix/reserved.nix` and `modules/flake/safix/fields.nix` (both `extend-bridge-fields`' own new files), `crates/safix-core/src/endpoint.rs` (the same), `Cargo.lock`, `deny.toml`, and the workspace-root `Cargo.toml`. Edited, but by other changes in the same tree and in no bitwarden line: `crates/safix-core/src/store.rs` and `modules/flake/safix/keepassxc.nix` — `git diff` over both carries zero occurrences of `bitwarden`. Edited by this change, both required by its own tasks: `crates/safix/Cargo.toml` (task 7.6's `[[bin]]`) and `flake.nix` (one `imports` line for 9.5's check module, which an explicit list requires; the system list is untouched). The 11.3 claim is therefore true of everything except those two, and both are named rather than glossed
- [x] 11.4 Confirm `crates/safix/src/snapshots/` gained exactly fourteen files and that no existing snapshot moved. — confirmed: `crates/safix/src/snapshots/` gained exactly fourteen bitwarden files — the graphical and plain pair for each of the seven codes — and no existing snapshot moved. The fourteen modified snapshots in the tree are the shared `document_unreadable`, `git_output_not_text`, `git_unavailable`, `sops_key_index`, `sops_pipe_missing`, `sops_unavailable` and `stanza_unreadable` pairs, which belong to another change in this wave
- [x] 11.5 Run `openspec validate add-bitwarden-bridge --strict` and confirm the four delta specs validate. — `openspec validate add-bitwarden-bridge --strict` -> `Change 'add-bitwarden-bridge' is valid`; the four delta specs are `behavioural-suite`, `bitwarden-sync`, `bridge-surface` and `safix-cli`
- [x] 11.6 Severity drill roll-up: confirm every drill named in groups 1 through 10 was performed and its outcome recorded in the task line, and that any drill which did not reproduce carries the reason in place of the claim. — roll-up performed. Every drill named in groups 1 through 10 was performed and its outcome is recorded on its own task line: 1.14, 1.15, 2.8, 3.7, 3.8, 4.12, 4.13, 5.9, 5.10, 6.8, 6.9, 7.10 (three perturbations), 7.11 (two halves), 8.24, 8.25, 9.11 and 9.12. Three did not reproduce as written, and each carries the reason in place of the claim rather than the claim: 3.7 needs the test build, because `sample` is behind `#[cfg(test)]`; 5.9's conditional resolved the other way — the capability table and the nix refusal are independent, and the nix-side one is load-bearing; and 5.10 cannot be performed as written at all, because both paths call the one `preflight`, which is itself stronger evidence than the drill was reaching for. 10.5 is the one line that is a recorded gap rather than a drill, and it is recorded in the changelog as 10.5 asks
