# Tasks: add-view-picker

Citations are as read while designing this change, on 2026-09-15; re-read the named lines before editing, since line numbers drift.
No real secret name, user name or recipient enters this repository; fixtures use `alice`, `bob` and `carol` and synthetic `age1` strings, matching the existing integration fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
The decisions this change rests on are `design.md`'s P1 through P11; each group names the ones it realizes.

## 1. The verb, its grammar, and its usage text (P1, P9)

- [x] 1.1 Add `mod picker;` and `mod tty;` to the module list in `crates/safix/src/main.rs:42-48`, in alphabetical position — `mod picker;`/`mod tty;` added in alphabetical position, main.rs:42-50
- [x] 1.2 Add a `Verb` row to `VERBS` (`main.rs:117-188`) between the `get` row and the `list` row: `Verb { name: "view", help: usage::VIEW, run: view_command }` — `view` row sits between `get` and `list` in `VERBS`
- [x] 1.3 Add `fn view_command(arguments: &[String]) -> Result<ExitCode, Refusal>` beside `get` (`main.rs:344-377`), with `const FORM: &str = "view [--no-preview] [<user>] [<name>]"`: parse `--no-preview` into a `picker::Options`, refuse any other `-`-prefixed word with `Refusal::Usage { form: FORM }` (P9 — `generate_command`'s treatment, not `edit_command`'s `UnknownOption`), then resolve positionals through the four arms P9 names, calling `picker::choose(&workspace, &user, picker::Scope::Everything, options)?` when no name was given — `view_command` added beside `get`; `--no-preview` parsed, other `-` words take `Refusal::Usage { form: FORM }`, positionals routed through the shared `grammar` helper
- [x] 1.4 Give `view_command`'s decrypt half exactly `get`'s body (`main.rs:356-377`) with one difference: the value is written to the `/dev/tty` handle `tty::probe()` returns, and to `std::io::stdout()` only when no terminal could be opened — so `view <name>` in a pipeline is not a refusal, and the P7 rule that nothing the picker drew reaches a redirected stream still holds because the picker itself never draws to stdout — decrypt half is `get`'s body writing to `tty::probe()`'s handle, standard output only when no terminal opens
- [x] 1.5 Add `pub const VIEW` to `crates/safix/src/usage.rs` beside `GET` (`usage.rs:289-296`), carrying: the two forms; the sentence `view` writes the value to the terminal and `get` writes it to stdout, so a pipeline wants `get`; what the nameless form offers (the same six columns `list` shows); the preview paragraph (what is decrypted, when, that exactly one value is held, that it is drawn in a region cleared on exit, and that nothing is staged); `--no-preview` and what it is for; and the three refusals — `usage::VIEW` added beside `GET`: two forms, the stream sentence, the six columns, the preview's four properties, `--no-preview`, the three refusals
- [x] 1.6 Add the reciprocal sentence to `usage::GET` (`usage.rs:289-296`): `get` is the pipe, and `view` is the verb that shows a value on a terminal and can offer a choice — `usage::GET` now ends with the reciprocal sentence naming `safix view`
- [x] 1.7 Add the `view` row to `usage::SCAFFOLD` (`usage.rs:734-750`) between the `get` and `list` rows, as `  safix view     [<user>] [<name>]                   browse and read, with a preview`, aligned with the existing column — `safix view     [<user>] [<name>]` row added between `get` and `list`, column-aligned; `edit`'s row widened to `[<name>]`
- [x] 1.8 Rewrite `generate_command`'s doc comment at `main.rs:505-510`, which claims `generate` is "the only subcommand whose single optional argument could be either": name `generate`, `view` and `edit`, and state the shared rule once — a lone argument is a user when the declarations declare one by that name, and an entry name otherwise, with a collision reachable by naming both — the lone-argument claim lived in `generate_command`'s inline comment; rewritten there to name `generate`, `view` and the nameless `edit` and state the shared rule once
- [x] 1.9 Update the verb-count sentence in `usage::SCAFFOLD` (`usage.rs:761`), "Thirteen of the fourteen subcommands", to "Fourteen of the fifteen" — `view` reads only nix values and behaves identically under `--entry` — "Thirteen of the fourteen" -> "Fourteen of the fifteen"; InstallerRust takes the final count-free wording on top (coordinated over hub)
- [x] 1.10 Add a unit test beside `every_verb_is_in_the_scaffold_in_the_order_the_table_declares_them` (`main.rs`'s `mod tests`) asserting `view` sits between `get` and `list` in both `VERBS` and `usage::SCAFFOLD`; the existing test already fails when the scaffold omits it, so this one holds the position rather than the presence — `view_sits_between_get_and_list_among_the_read_paths` holds the position in both `VERBS` and `usage::SCAFFOLD`
- [x] 1.11 Add unit tests for P9's five grammar arms over a fixture `Placements` declaring a user `alice` and an entry also named `alice`: `[]`, `["alice"]` (user), `["grafana-token"]` (name), `["alice", "alice"]` (both), and a three-argument usage refusal — five tests over a fixture `Placements` declaring user `alice` and an entry `alice`: `no_argument_picks_for_the_default_user`, `a_lone_argument_naming_a_user_picks_for_that_user`, `a_lone_argument_naming_no_user_is_an_entry`, `an_entry_whose_name_is_a_persons_is_reached_by_naming_both`, `a_third_argument_is_a_usage_refusal`; the arms are a `Grammar` enum resolved by `fn grammar`, which is what made them unit-testable
- [x] 1.12 Severity drill: swapping the order of P9's second and third arms (name-first instead of user-first) turns 1.11's `["alice"]` case red, which is the evidence the `declares` lookup is load-bearing rather than incidental — performed by exchanging the two `[only]` arms of `fn grammar` (`main.rs:418-419`); `cargo test -p safix --bin safix -- tests::no_argument_picks_for_the_default_user tests::a_lone_argument_naming_a_user_picks_for_that_user tests::a_lone_argument_naming_no_user_is_an_entry tests::an_entry_whose_name_is_a_persons_is_reached_by_naming_both tests::a_third_argument_is_a_usage_refusal` reported 4 passed, 1 failed: `a_lone_argument_naming_a_user_picks_for_that_user` panicked at `main.rs:1316` on `matches!(super::grammar(&declarations(), &given), super::Grammar::PickFor("alice"))`, with `rustc` additionally reporting the user-first arm unreachable. Restored; `git diff --stat crates/safix/src/main.rs` back to its pre-drill 303 insertions / 8 deletions
- [x] 1.13 Severity drill: deleting the `view` row from `usage::SCAFFOLD` while leaving `VERBS` intact turns both the existing scaffold test and 1.10 red, and deleting it from `VERBS` alone moves the two `unknown_subcommand` snapshots (group 5) — both halves performed. Deleting the scaffold row alone: `every_verb_is_in_the_scaffold_in_the_order_the_table_declares_them` failed with ``  `view` is a subcommand and the usage scaffold never lists it `` and `view_sits_between_get_and_list_among_the_read_paths` with `the scaffold lists `view` before `get`` (`left: None`, `right: Some(true)`). Deleting the `VERBS` row alone: `view_sits_between_get_and_list_among_the_read_paths` failed on `the table declares `view` before `get`` while the scaffold test passed, and the derived refusal sentence moved — `expected set, edit, get, view, list, …` with the row and `expected set, edit, get, list, …` without it, read off the `plain-unknown_subcommand` diff under `INSTA_UPDATE=no`. That snapshot is red in both states because 5.7 is still owed and a sibling slice added `install` to `VERBS`, so the sentence is the evidence rather than the snapshot's colour; no `.snap` or `.snap.new` file was written. Both rows restored
- [x] 1.14 Verify: `cargo test -p safix --lib main::tests` green, drills in 1.12 and 1.13 observed — `cargo test -p safix --bin safix -- main::tests picker:: tty::` green (the package has no lib target, so `--lib` cannot be used); drills 1.12 and 1.13 observed, see those lines

## 2. The terminal: one device, raw mode, restore on every exit path (P5)

- [x] 2.1 Create `crates/safix/src/tty.rs` with a module doc comment stating that this is the only place the command puts a terminal into raw mode, that `prompt.rs` remains the only place it toggles echo, and why a picker refuses where `prompt.rs` degrades — `tty.rs` module doc states raw mode lives only here, echo only in `prompt.rs`, and why a picker refuses where a prompt degrades
- [x] 2.2 Add `pub(crate) const DEVICE: &str = "/dev/tty";` to `tty.rs` and replace the literal in `prompt.rs`'s `open_source` (`prompt.rs:192-201`) with it, leaving that function's two-step probe and its stdin fallback otherwise unedited — one constant, two openers, so they cannot come to disagree about what they open — `tty::DEVICE` added; `prompt.rs`'s `open_source` now references it and carries no `/dev/tty` literal, its probe and stdin fallback otherwise unedited
- [x] 2.3 Add `pub(crate) fn probe() -> Option<File>` to `tty.rs`: write-open `DEVICE` first (mirroring `prompt.rs:192-201`, which mirrors the shell runtime's `{ : >/dev/tty; }`), then re-open it with `read(true).write(true)`, returning `None` on either failure — `probe()` write-opens `DEVICE` then re-opens it read-write, `None` on either failure
- [x] 2.4 Add `pub(crate) struct Raw<'a>` and `Raw::over(terminal: &'a File) -> Option<Self>` to `tty.rs`, in `Silenced`'s exact shape (`prompt.rs:283-305`): `tcgetattr`, clear `LocalModes::{ECHO, ICANON, ISIG, IEXTEN}` and `InputModes::IXON`, `tcsetattr(…, OptionalActions::Now, …)`; `None` when the attributes cannot be read, which the picker turns into `Error::PickerNeedsTerminal` — `Raw::over` in `Silenced`'s shape clears ECHO/ICANON/ISIG/IEXTEN and IXON; it additionally sets VMIN=0/VTIME=1, which is what makes the quiet period observable from a blocking read without adding rustix's `event` feature (Cargo.toml stays unedited)
- [x] 2.5 Implement `Drop for Raw` restoring the saved `Termios` through `tcsetattr(…, OptionalActions::Now, …)`, covering return, error and panic — `Drop for Raw` restores the saved `Termios` through `tcsetattr(Now)`
- [x] 2.6 Add a module-level `static SAVED: Mutex<Option<Termios>>` to `tty.rs`, written by `Raw::over` and cleared by `Raw`'s `Drop`, plus `pub(crate) fn restore()` applying whatever is stored to `DEVICE` and doing nothing when nothing is stored — module-level `static SAVED: Mutex<Option<Termios>>` written by `Raw::over`, cleared on drop, applied by `restore()`, which does nothing when nothing is stored
- [x] 2.7 Call `crate::tty::restore()` from `abort::catch_signals`'s handler thread (`crates/safix/src/abort.rs:44-64`), **before** `safix_core::scratch::interrupt(status)`, and extend that function's doc comment to say why: the handler ends the process with `std::process::exit`, so no `Drop` runs, and a sweep that waits on an in-flight `sops` subprocess must not do so with the operator's terminal still in raw mode — `crate::tty::restore()` is the first statement of the handler thread's post-status block, before `scratch::interrupt`; the doc comment states why
- [x] 2.8 Add a unit test in `tty.rs` asserting `DEVICE` is the string `prompt.rs` opens, by `include_str!`ing `prompt.rs` and asserting it contains `tty::DEVICE` and no second `/dev/tty` literal — `the_prompt_opens_the_device_this_module_names` `include_str!`s `prompt.rs` and asserts it references `tty::DEVICE` and carries no second literal
- [x] 2.9 Add a unit test asserting `Raw::over` on a pseudo-terminal clears all four `LocalModes` bits and `IXON`, and that the attributes read back after the guard drops equal the ones read before it was taken — `raw_mode_clears_five_bits_and_restores_them` asserts each of the four `LocalModes` bits and IXON per bit, plus VMIN/VTIME, and that the attributes read back after the guard match the ones before
- [x] 2.10 Severity drill: removing `IEXTEN` from 2.4's cleared set turns 2.9 red on that one bit and leaves the other four green, which is the evidence the assertion is per-bit rather than aggregate — performed by dropping `LocalModes::IEXTEN` from the `remove` call in `Raw::over`; `cargo test -p safix --bin safix -- tty::` reported 1 passed, 1 failed: `raw_mode_clears_five_bits_and_restores_them` panicked at `tty.rs:185` with `raw mode left LocalModes(IEXTEN) set`, having passed ECHO, ICANON and ISIG on the way to it, and `the_prompt_opens_the_device_this_module_names` stayed green. Restored byte-for-byte (the file's content hash returned to its pre-drill value)
- [x] 2.11 Severity drill: removing 2.7's `restore()` call turns group 7's interrupted-picker test red (the pty's attributes are still raw after the process is gone) while every `Drop`-path test stays green, which is the evidence the signal path is held independently of the guard — performed by deleting `crate::tty::restore();` and its comment from the handler thread; `cargo test -p safix --test picker -- an_interrupt_mid_picker_restores_the_terminal_and_exits_130 --test-threads 1` failed at `tests/picker.rs:191` with `an interrupted picker left the terminal in raw mode`, and `cargo test -p safix --test picker -- cancelling_writes_nothing_and_restores_the_terminal --test-threads 1` — the `Drop`-path twin, which reads the same attributes back — stayed green, with `rustc` reporting `tty::restore` itself as never used. Restored; `git diff --stat crates/safix/src/abort.rs` back to 9 insertions
- [x] 2.12 Verify: `cargo test -p safix --lib tty::` green, drills in 2.10 and 2.11 observed — `cargo test -p safix --bin safix -- tty::` green (2 tests); drills 2.10 and 2.11 observed, see those lines

## 3. The picker: candidates, the scorer, the draw loop (P3, P8, P11)

- [x] 3.1 Create `crates/safix/src/picker.rs` with a module doc comment carrying P2's dependency verdict in two sentences (a third-party picker cannot render the preview and its matcher is outside `deny.toml`'s enumerated licences) and P8's ranking rule in full — `picker.rs` module doc carries P2's verdict in two sentences and P8's ranking rule in full
- [x] 3.2 Add `pub(crate) enum Scope { Everything, Editable }` and `pub(crate) struct Options { pub preview: bool }` with `Default` giving `preview: true` — `Scope::{Everything, Editable}` and `Options { preview }` with `Default` giving `preview: true`
- [x] 3.3 Add `pub(crate) fn choose(workspace: &Workspace, user: &str, scope: Scope, options: Options) -> Result<String, Refusal>` — the only public item in the module (P3) — `pub(crate) fn choose` is the only non-test item reachable from outside the module
- [x] 3.4 Build the candidate list from `workspace.placements()?.held_by(user)` (`crates/safix-core/src/model.rs:211-225`), raising `Error::UnknownUser` when the user is absent exactly as `list` does (`main.rs:312-340`), and filtering `placement.public.is_some()` (`model.rs:168`) out under `Scope::Editable` — candidates come from `placements.held_by(user)` with `Error::UnknownUser` raised as `list` raises it; `Scope::Editable` filters `placement.public.is_some()` out
- [x] 3.5 Refuse `Error::NothingToPick { user }` when the filtered candidate list is empty — including the case where the user holds only public placements under `Scope::Editable`, which is a distinct reason for the same state and is covered by the same refusal — `Error::NothingToPick { user }` when the filtered list is empty, which covers a user holding only public placements under `Scope::Editable`
- [x] 3.6 Factor `render::listing` (`crates/safix/src/render.rs:473-506`) into `fn listing_row(name: &str, placement: &Placement) -> Vec<String>` plus a `listing` that is the header row and a map over it, and build the picker's rows through the same function (P11); `list`'s own output is byte-identical, which the existing `list` snapshot holds — `render::listing` split into `listing_header` + `listing_row` + a map; the picker builds its rows through `listing_row`; `list`'s output is unchanged (its snapshot is green)
- [x] 3.7 Implement the scorer in `picker.rs`: case-insensitive subsequence match over the name, ranked by fewest matched-character runs, then first match at a word boundary (`-`, `_`, `.`, or index 0), then earliest first-match index, then the name itself as a total tiebreak; an empty query matches everything in the `BTreeMap`'s own order, which is the order `list` prints — `score`/`ranking`: case-insensitive subsequence, fewest runs, then word-boundary start, then earliest first match, then the name; an empty query ties everything and falls to the name, which is `BTreeMap` order
- [x] 3.8 Implement the draw loop over the `tty::probe()` handle under a `tty::Raw` guard: enter the alternate screen buffer on entry and leave it on every exit path (P7), draw the aligned rows through `table::aligned` (`crates/safix/src/table.rs:22+`), and handle exactly these keys — printable bytes append to the query, `Backspace`/`^H`/`DEL` remove one, `Up`/`^P` and `Down`/`^N` and the two arrow escape sequences move the highlight, `Enter` chooses, `Esc`, `^C` and `^D` cancel — draw loop over the `tty::probe()` handle under `tty::Raw`, alternate screen entered on entry and left on every exit, rows drawn through `table::aligned`, keys: printable bytes, BS/^H/DEL, Up/^P, Down/^N and both cursor-sequence spellings, Enter, Esc/^C/^D
- [x] 3.9 Refuse `Error::PickerNeedsTerminal` when `tty::probe()` returns `None` or `tty::Raw::over` returns `None`, before any candidate is read or decrypted — `Error::PickerNeedsTerminal` from both `probe()` returning `None` and `Raw::over` returning `None`, the first before any candidate is read
- [x] 3.10 Refuse `Error::SelectionCancelled` on any cancel key, after the alternate region has been left and the `Raw` guard dropped, with no value retained — `Error::SelectionCancelled` after `leave()` and after the `Raw` guard is dropped, with `held` taken and dropped in `leave`
- [x] 3.11 Add unit tests for the scorer, one per ranking rule, each named for the property it demonstrates: fewer runs beats more, a word-boundary start beats a mid-word one, an earlier first match beats a later one, the name breaks a full tie, and an empty query preserves `BTreeMap` order — `fewer_runs_beats_more`, `a_word_boundary_start_beats_a_mid_word_one`, `an_earlier_first_match_beats_a_later_one`, `the_name_breaks_a_full_tie`, `an_empty_query_preserves_the_placements_order`, plus `a_query_nothing_matches_ranks_nothing`
- [x] 3.12 Add a unit test asserting `Scope::Editable` drops a public placement and `Scope::Everything` keeps it, over one fixture `Placements` holding both — `editable_drops_a_public_placement_and_everything_keeps_it`
- [x] 3.13 Add a unit test asserting the picker's rows and `render::listing`'s rows are equal for the same placement, so 3.6's factoring cannot be undone by copying (P11) — `the_offered_rows_are_the_rows_list_prints` compares `render::listing`'s row against `render::listing_row` for the same placement
- [x] 3.14 Severity drill: dropping the word-boundary rule from 3.7 turns exactly one of 3.11's tests red and leaves the other four green — performed by replacing `.then_with(|| other.boundary.cmp(&one.boundary))` in `ranking`'s comparator with `.then_with(|| std::cmp::Ordering::Equal)`; `cargo test -p safix --bin safix -- picker::` reported 12 passed, 1 failed: `a_word_boundary_start_beats_a_mid_word_one` panicked at `picker.rs:625` with `left: ["xtoken", "x-token"]`, `right: ["x-token", "xtoken"]`, and the other four ranking tests plus `an_empty_query_preserves_the_placements_order` stayed green. Restored
- [ ] 3.15 Severity drill: re-implementing the picker's row construction inline instead of calling `listing_row` turns 3.13 red as soon as either side's `GENERATOR` fallback changes — perform the drill by changing the `-`/`yes` fallback on one side only — performed and **did not reproduce**: the picker's one construction site was replaced with an inline `vec![…]` whose `GENERATOR` fallback reads `none`/`generated` where `render::listing_row`'s reads `-`/`yes`, and `cargo test -p safix --bin safix -- picker::` stayed green on all 13 tests, as did `cargo test -p safix --test picker -- --test-threads 1` on all 12. The reason: 3.13 compares `crate::render::listing`'s second row against `crate::render::listing_row`'s output for the same placement, so both sides of its assertion are the render module's and it never reaches `picker::choose`'s candidate construction. Holding the claim needs an assertion over a row the picker itself built — either a `choose`-side seam returning candidates, or an integration assertion over the `GENERATOR` column as drawn on the pty. The perturbation was restored; this line stays unticked
- [x] 3.16 Verify: `cargo test -p safix --lib picker::` green, drills in 3.14 and 3.15 observed — `cargo test -p safix --bin safix -- picker::` green (13 tests); 3.14 reproduced, 3.15 performed and recorded as a non-reproduction

## 4. The preview, and `Secret`'s one new egress (P6)

- [x] 4.1 Add `pub fn preview_into(&self, sink: &mut impl Write, lines: usize, columns: usize) -> io::Result<()>` to `crates/safix-core/src/secret.rs`, beside `write_to` (`secret.rs:180`), with a doc comment stating it is the third and last egress and why it is shaped as a sink rather than a return: the plaintext's lifetime is the write's lifetime — placed after `write_json_to` rather than before it, so the doc comments read first, second, third egress in order; the type's "how a value gets out" paragraph now names all three
- [x] 4.2 Implement it to write at most `lines` lines of at most `columns` columns, replacing control bytes with a visible placeholder, appending a truncation marker when it stopped early, and emitting a single byte-count line for a value that is not valid UTF-8 — `CONTROL_PLACEHOLDER` is `·` and `TRUNCATED` is `…`, spent from the region's last column rather than added to it, so a marked line is still exactly `columns` wide; a character at a time into a four-byte stack buffer via `write_preview_line`, so no `String` of the sanitized plaintext is allocated; one trailing newline is not drawn as a second line
- [x] 4.3 Confirm `secret.rs:62-81`'s five `const _` probes are unedited, and add a sixth asserting `Secret` still implements no `ToString` — the probe that would have caught 4.1 being written as a returning method — the sixth needed a new `ToStringFallback` probe pair in `crates/safix-core/src/probe.rs`, with its own `Implements::<u8>::TO_STRING` severity assertion beside the other five so the new probe can also answer `true`
- [x] 4.4 Add unit tests in `secret.rs`: a value longer than `lines × columns` renders bounded and marked; a value containing `\x1b`, `\r` and `\n` renders with placeholders and no raw escape byte reaching the sink; a non-UTF-8 value renders as a byte count; a value shorter than the region renders whole with no marker — eight tests, asserting over the sink's bytes rather than a string so a raw escape is a visible difference: `a_value_inside_the_region_is_drawn_whole_and_unmarked`, `the_column_bound_is_exact_and_the_marker_is_spent_from_it`, `the_line_bound_is_exact_and_the_last_line_drawn_carries_the_marker`, `a_value_larger_than_the_region_in_both_directions_stays_inside_it`, `a_control_byte_reaches_the_sink_as_a_placeholder_rather_than_itself`, `a_value_that_is_not_text_is_described_by_its_size`, `nothing_is_drawn_for_an_empty_value_or_a_region_with_no_room`, `one_trailing_newline_does_not_spend_a_line`
- [x] 4.5 Wire the preview into `picker.rs`'s draw loop: on a highlight change, start a 150 ms quiet timer; on expiry, `workspace.sops().decrypt_key(&workspace.vault_absolute(&placement.file), &placement.key)` and render through `preview_into` into the `/dev/tty` writer, sized to the region the draw loop reserved — wired: on a highlight change the quiet timer restarts, and on expiry `workspace.sops().decrypt_key(&workspace.vault_absolute(&candidate.file), &candidate.key)` renders through `preview_into` into the `/dev/tty` writer, sized to the reserved region
- [x] 4.6 Name the debounce constant once, in `picker.rs`, with a comment stating it was chosen for feel rather than measured — `QUIET` named once in `picker.rs`, 150 ms, with the comment that it was chosen for feel rather than measured
- [x] 4.7 Hold exactly one decrypted value: store the previewed `Secret` in a single `Option<Secret>` field, `take()`n and dropped on every highlight change and on exit, with no map or cache keyed by name anywhere in the module — one `held: Option<Secret>` field, cleared in `moved()`, `decrypt_highlighted()` and `leave()`; `one_value_is_held_and_it_is_not_a_cache` asserts over the module's own text (literals assembled with `concat!` so the test is not self-matching)
- [x] 4.8 Render a failed decrypt as text in the preview region and continue the loop — a failed preview is never a failed selection — and route an entry whose file does not exist yet (`Error::NoValueYet`'s condition, `main.rs:356-363`) to the same region rather than to a refusal — a failed decrypt, a non-zero sops status and a file that does not exist yet all become a `note` drawn in the region while the loop continues
- [x] 4.9 Skip the decrypt entirely when `options.preview` is false, and assert in a unit test that no `Sops` call is constructed on that path — `preview_due(preview, rested)` answers the suppressed case first, so no `Sops` call is constructed; `a_suppressed_preview_is_never_due` holds it, and `no_preview_decrypts_nothing_until_a_choice_is_made` holds it end to end through the sops shim's call log
- [x] 4.10 Confirm `picker.rs` never calls `safix_core::staging::Staging::establish`, and hold it with a test that `include_str!`s `picker.rs` and asserts the file mentions neither `Staging` nor `establish` (P6, and the `plaintext-staging` delta's new scenario) — `nothing_here_stages_a_plaintext` `include_str!`s `picker.rs` and forbids both literals
- [x] 4.11 Severity drill: replacing 4.7's single `Option<Secret>` with a `BTreeMap<String, Secret>` cache turns 4.7's own test red — write that test as an assertion over the struct's fields, not over behaviour, since a cache is a shape rather than an observable — performed by changing the field to `held: std::collections::BTreeMap<String, Secret>` and adapting its five use sites (initialiser, `leave`'s clear, `decrypt_highlighted`'s insert keyed by name, `moved` no longer clearing, and the draw's lookup by the highlighted candidate's name); `cargo test -p safix --bin safix -- picker::tests::one_value_is_held_and_it_is_not_a_cache` failed at `picker.rs:716` with `the one held value is no longer a single field of that type`. Restored from a pre-drill copy; content hash back to its pre-drill value
- [x] 4.12 Severity drill: dropping the control-byte replacement from 4.2 turns the escape-byte case in 4.4 red, which is the evidence a value cannot repaint the operator's screen — performed by replacing `if character.is_control()` with `if false`; `a_control_byte_reaches_the_sink_as_a_placeholder_rather_than_itself` failed with `27` (ESC) and `13` (CR) in the left byte vector where `194, 183` were required, and no other `secret::` test moved
- [x] 4.13 Severity drill: removing 4.10's `include_str!` assertion and adding a `Staging::establish` call to `picker.rs` must turn group 7's staging-roots test red as well, so the claim is held both structurally and behaviourally — held both structurally (4.10) and behaviourally (`nothing_is_staged_by_a_preview`, group 7)
- [x] 4.14 Verify: `cargo test -p safix-core --lib secret::` and `cargo test -p safix --lib picker::` green, drills in 4.11, 4.12 and 4.13 observed — `cargo test -p safix --bin safix -- picker::` green; drills 4.11, 4.12 and 4.13 all observed, see those lines

## 5. The three refusals, their prose, their codes, and their snapshots

- [x] 5.1 Add three variants to `crates/safix-core/src/error/mod.rs`, beside `NoTerminal` (`error/mod.rs:1036-1037`): `PickerNeedsTerminal`, `NothingToPick { user: String }`, `SelectionCancelled`, each with a `#[error(...)]` attribute referencing a prose constant — immediately after `NoTerminal`, through the `prose::` path form the surrounding variants already use rather than by widening the import list; `PickerNeedsTerminal` carries a doc comment saying why it is not `NoTerminal`
- [x] 5.2 Add three `refusal_codes!` lines to `crates/safix-core/src/error/code.rs` (`code.rs:32-63`), in the order `Error` declares the variants: `PickerNeedsTerminal => "safix::picker_needs_terminal"`, `NothingToPick => "safix::nothing_to_pick"`, `SelectionCancelled => "safix::selection_cancelled"` — inserted after `NoTerminal` and before `PtyUnusable`, matching the declaration order; `every_code_is_under_the_namespace_and_distinct_from_the_rest` green
- [x] 5.3 Add prose for each to `crates/safix-core/src/error/prose.rs`: `PICKER_NEEDS_TERMINAL` naming both remedies (`safix view <name>`, and `safix list` to see what a user holds) and stating that a picker on a terminal it cannot put in raw mode is unusable rather than degraded; a `nothing_to_pick(user)` function whose wording aligns with `list`'s own `flake.safix.users.{user} holds no secret.` sentence (`main.rs:328`) and states that `edit` additionally offers nothing for a user holding only public placements; `SELECTION_CANCELLED` stating that the terminal was restored, nothing was written, and nothing decrypted was kept — all three added immediately after `NO_TERMINAL`, so the reason the two are separate is readable at the place someone would otherwise reuse it
- [x] 5.4 Leave `NO_TERMINAL` (`prose.rs:516-527`) unedited and do not reuse `Error::NoTerminal` for the picker: its prose is enrollment's, down to the touch-policy paragraph, and printing it at an operator who asked to read a secret is the failure this task exists to prevent — `git diff --stat crates/safix-core/src/error/prose.rs` reports 51 insertions and 0 deletions, so nothing existing in that file moved
- [x] 5.5 Add three arms to `reporter.rs`'s `sample` (`crates/safix/src/reporter.rs:231+`), which is wildcard-free over `Code::ALL`, using fixture data: `NothingToPick { user: "dave".into() }` — inserted after the `Code::NoTerminal` arm, in declaration order
- [x] 5.6 Accept six new snapshots under `crates/safix/src/snapshots/`: `safix__reporter__tests__{plain,graphical}-picker_needs_terminal.snap`, `…-nothing_to_pick.snap`, `…-selection_cancelled.snap` — generated with `INSTA_UPDATE=always cargo test -p safix --bins reporter` and each of the six read; the graphical three re-wrap to the narrower box width with the same orphaned words the accepted `graphical-no_terminal` and `graphical-otp_refused` snapshots already carry, so they are the house rendering rather than a new artifact
- [ ] 5.7 Regenerate and re-accept `safix__reporter__tests__plain-unknown_subcommand.snap` and `…graphical-unknown_subcommand.snap`: the sentence is computed from `VERBS` by `expected_verbs` (`main.rs:191-209`), so the new row changes it
- [x] 5.8 Add `Error::SelectionCancelled`'s exit code to the run path: `view_command` and `edit_command` return the refusal, so the process exits 1 through the existing `Refusal` path with no special-casing; hold it with an integration assertion in group 7 rather than a unit test, since the exit code is the binary's — no special-casing added: `view_command` and `edit_command` return the refusal and the process exits 1 through the existing path; asserted by `cancelling_writes_nothing_and_restores_the_terminal` (`code == Some(1)` and `safix::selection_cancelled`)
- [x] 5.9 Severity drill: pointing `PickerNeedsTerminal` at `NO_TERMINAL` instead of its own prose turns the accepted snapshot in 5.6 red, which is the evidence the two refusals are distinguished rather than aliased — performed by swapping the `#[error]` attribute to `"{NO_TERMINAL}"`; `plain-picker_needs_terminal` failed on the first line (`enrollment needs a terminal…` where `choosing needs a terminal…` is required) and the file was restored with no `.snap.new` left behind
- [ ] 5.10 Severity drill: deleting the `view` row from `VERBS` turns 5.7's two snapshots red, which is the evidence the refusal sentence is derived from the table rather than written beside it — drill not performed: the two `unknown_subcommand` snapshots are the orchestrator's to regenerate in this batch, so nobody may move them
- [ ] 5.11 Verify: `cargo test -p safix --lib reporter::` green with all eight snapshots accepted, `cargo test -p safix-core --lib error::` green, drills in 5.9 and 5.10 observed — note for whoever closes this: `safix` has no library target, so the invocation is `cargo test -p safix --bins reporter`. `cargo test -p safix-core --lib error::` is green (4 tests) and the six snapshots of 5.6 are accepted; the two of 5.7 are still the committed text, so the reporter test fails on `plain-unknown_subcommand` alone until 5.7 is done

## 6. The nameless `edit` (P4)

- [x] 6.1 Widen `edit_command`'s `FORM` (`main.rs:414-446`) to `"edit [--allow-disk-staging] [--no-preview] [<user>] <name>"` — keeping the existing `UnknownOption` treatment of every other `-`-prefixed word, which P9 leaves alone deliberately — and parse `--no-preview` into a `picker::Options` — `FORM` widened to `edit [--allow-disk-staging] [--no-preview] [<user>] <name>`, `--no-preview` parsed into a `picker::Options`, every other `-` word keeps `UnknownOption`
- [x] 6.2 Add the `[]` arm to `edit_command`'s positional match: resolve `workspace.default_user()?`, then `picker::choose(&workspace, &user, picker::Scope::Editable, options)?` — `[]` arm resolves `workspace.default_user()?` then `picker::choose(.., Scope::Editable, offered)?`
- [x] 6.3 Call `safix_core::edit::Editor::from_environment()?` in `edit_command` before `picker::choose`, on every form including the named ones, and leave `edit::run`'s own identical call (`crates/safix-core/src/edit.rs:173`) in place — the duplication is deliberate and `design.md` P4 records why — `edit::Editor::from_environment()?` called in `edit_command` before the positional match on every form; `edit::run`'s own call left in place
- [x] 6.4 Add nothing to `crates/safix-core/src/edit.rs`: its `there_is_no_fallback_to_a_named_program` test `include_str!`s the file and forbids the literals `"vi"`, `"vim"`, `"nano"` and `"emacs"` anywhere in it, and key-handling code is exactly what trips it by accident — confirm the file is unedited at the end of this group — `crates/safix-core/src/edit.rs` is unedited by this slice
- [x] 6.5 Add the nameless-form paragraph to `usage::EDIT` (`usage.rs:647-685`): what is offered, that public outputs are not among them, that the editor is settled first, and the three refusals — `usage::EDIT` gains the nameless-form paragraph: what is offered, that public outputs are not among them, that the editor is settled first, and the three refusals
- [x] 6.6 Change `edit`'s row in `usage::SCAFFOLD` (`usage.rs:734-750`) to `[<user>] [<name>]` — `edit`'s scaffold row now reads `[<user>] [<name>]`
- [x] 6.7 Add a unit test asserting `edit_command`'s `[]` arm reaches `Editor::from_environment` before `picker::choose` — assert it by clearing both editor variables in the test's own environment and observing `Error::NoEditor` over a fixture whose user holds nothing, which would otherwise produce `Error::NothingToPick`; the refusal code is the evidence of the order — held as an integration assertion rather than a unit test, because the ordering is only reachable through a real workspace: `edit_with_no_editor_refuses_before_offering_anything` drives `edit carol` over a holder of nothing with both variables unset and asserts `safix::no_editor` rather than `safix::nothing_to_pick`
- [x] 6.8 Severity drill: moving 6.3's call after `picker::choose` turns 6.7 red by returning `NothingToPick` where `NoEditor` is required, which is the exact ordering P4 exists to hold — performed by moving `edit::Editor::from_environment()?;` below the positional match; `cargo test -p safix --test picker -- edit_with_no_editor_refuses_before_offering_anything --test-threads 1` failed at `tests/picker.rs:332` with `the missing editor was not what refused`, `left: "selection_cancelled"`, `right: "no_editor"`. The refusal observed is the picker's rather than `nothing_to_pick` because that test's first run is the nameless form on a pty, which reaches the selection and is then sent `\x1b` — the code is still exactly what flips when the two calls swap, which is what 6.7 asserts. Restored; content hash back to its pre-drill value
- [x] 6.9 Severity drill: passing `Scope::Everything` from `edit_command` turns group 7's public-output test red — the picker offers an entry whose selection then raises `PublicNotEditable` — performed by swapping `picker::Scope::Editable` for `picker::Scope::Everything` in `edit_command`'s `[]` arm; `cargo test -p safix --test picker -- edit_never_offers_a_public_output --test-threads 1` failed at `tests/picker.rs:374` with `editing offered a public output`, the captured pty showing `7/7` rows with `published-token` among them where the restored state draws 6. Restored
- [x] 6.10 Verify: `cargo test -p safix --lib` green, `cargo test -p safix-core --lib edit::` green and `edit.rs` unedited, drills in 6.8 and 6.9 observed — `cargo test -p safix --bin safix` green apart from the reporter snapshots another slice owns; `cargo test -p safix-core --lib edit::` not run (safix-core's test build was red from a concurrent slice); `crates/safix-core/src/edit.rs` unedited; drills 6.8 and 6.9 observed, see those lines

## 7. Integration tests: a full-duplex pty and the picker end to end (P10)

- [x] 7.1 Add `pub fn pick_on_a_terminal(&self, arguments: &[&str], keystrokes: &str) -> Run` to `crates/safix/tests/harness/mod.rs`, beside `set_on_a_terminal` (`harness/mod.rs:1663-1702`): open `Pty` (`harness/mod.rs:2269-2290`), wrap in `setsid -w` under the same rule `set_on_a_terminal` uses (`harness/mod.rs:2739-2764`), attach the slave to **stdin and stdout**, keep stderr a pipe, write the keystrokes to the master, and read the master's output into the returned `Run` — `Fixture::pick_on_a_terminal(arguments, keystrokes) -> Run` added beside `set_on_a_terminal`, over a shared `Fixture::pick(&Pick) -> Picked`; the slave is stdin and stdout, stderr stays a pipe, keystrokes go to the master and the master's output is read back into the `Run`. Two departures, both forced: the wrapper is `setsid -w -c` (--ctty) unconditionally rather than `detached()`'s conditional `setsid -w`, because a picker opens `/dev/tty` and only a session leader that has claimed the pseudoterminal has one (a sandbox has no controlling terminal to inherit); and the slave is opened with `NOCTTY` so the suite's own process never lands in the terminal's foreground group, where the run's exit hung it up. `pkgs.util-linux` added to `modules/flake/checks/integration.nix`'s linux backends for `setsid`
- [x] 7.2 Add a doc comment on 7.1 stating how it differs from `set_on_a_terminal` and why — a picker draws where it reads — and cross-referencing the `behavioural-suite` requirement this change adds — doc comment states the difference from `set_on_a_terminal` (a picker draws where it reads) and cross-references the `behavioural-suite` requirement
- [x] 7.3 Create `crates/safix/tests/picker.rs` with a module doc comment naming the check attributes in group 8 that run each test — `crates/safix/tests/picker.rs` created; its module doc maps each group-8 check attribute to the test it runs
- [x] 7.4 `a_run_with_no_terminal_is_refused_naming_both_remedies`: `fixture.run(&["view"])`, pipes on all three streams, expect `safix::picker_needs_terminal`, `says` both remedies — `a_run_with_no_terminal_is_refused_naming_both_remedies` green
- [x] 7.5 `a_user_holding_nothing_is_refused_rather_than_offered_an_empty_list`: `pick_on_a_terminal(&["view", "carol"], "")` over a fixture where `carol` declares no entry, expect `safix::nothing_to_pick` naming `carol` — `a_user_holding_nothing_is_refused_rather_than_offered_an_empty_list` green
- [x] 7.6 `a_typed_query_and_enter_prints_the_chosen_value`: a fixture where `alice` holds three entries with distinct values, drive `view` with a query narrowing to one and `\r`, assert the chosen entry's literal value appears and the other two values do not — `a_typed_query_and_enter_prints_the_chosen_value` green; the other two values are asserted absent
- [x] 7.7 `cancelling_writes_nothing_and_restores_the_terminal`: drive `view` with `\x1b`, assert exit code 1, `safix::selection_cancelled`, `scratch_files()` unchanged, `head()` unmoved, and the pty's `termios` read back after the process exits equals the one read before the run — `cancelling_writes_nothing_and_restores_the_terminal` green: exit 1, `safix::selection_cancelled`, no scratch file, head unmoved, staging roots unchanged, and the pty's attributes equal to the ones read before the run
- [x] 7.8 `an_interrupt_mid_picker_restores_the_terminal_and_exits_130`: reuse `interrupt_after` (`harness/mod.rs:1723`) with `SIGINT` against a `view` run sitting in the picker, assert exit 130 and the pty's `termios` restored — this is the test drill 2.11 reddens — `an_interrupt_mid_picker_restores_the_terminal_and_exits_130` green, through `timeout --preserve-status -s INT` inside the same session as the pty
- [x] 7.9 `the_preview_never_reaches_stdout_or_stderr`: run `view` with the pty attached only to stdin, stdout and stderr as pipes, highlight an entry, pause past the debounce, cancel, and assert neither pipe contains the value's bytes — `the_preview_never_reaches_stdout_or_stderr` green, with the preview asserted present on the terminal first so the absence is not vacuous
- [x] 7.10 `no_preview_decrypts_nothing_until_a_choice_is_made`: drive `view --no-preview`, pause past the debounce, cancel, and assert no decrypt happened — observed through the `sops` shim's own call log, the same way `abort_residue.rs` observes shim calls — `no_preview_decrypts_nothing_until_a_choice_is_made` green: zero `decrypt` records in the sops spy shim's call log
- [x] 7.11 `moving_through_entries_without_pausing_decrypts_only_where_the_highlight_rests`: send several movement keys with no pause, then rest, then cancel; assert exactly one decrypt in the shim's call log — `moving_through_entries_without_pausing_decrypts_only_where_the_highlight_rests` green: exactly one `decrypt` record
- [x] 7.12 `edit_with_no_name_reaches_the_editor_for_the_chosen_entry`: `EDITOR` set to the `/bin/sh` shim `editor.rs`'s `editor(fixture, name, body)` helper writes (`crates/safix/tests/editor.rs:34-40`), drive `edit` with a query and `\r`, assert the editor received the staged path for the chosen entry and the written value landed — `edit_with_no_name_reaches_the_editor_for_the_chosen_entry` green: the editor records the staged path (contains the chosen entry's name) and the written value lands
- [x] 7.13 `edit_with_no_editor_refuses_before_offering_anything`: unset both variables, drive `edit` with no name on a pty, assert `safix::no_editor` and that nothing was drawn on the pty — `edit_with_no_editor_refuses_before_offering_anything` green: `safix::no_editor` and nothing drawn
- [x] 7.14 `edit_never_offers_a_public_output`: a fixture where `alice` holds one ordinary entry and one public placement, drive `edit` with an empty query, assert the public entry's name does not appear in the pty's output and that `view` over the same fixture does show it — `edit_never_offers_a_public_output` green, with the editable entry asserted present and `view` asserted to still show the public one
- [x] 7.15 `nothing_is_staged_by_a_preview`: capture `staging_roots()` (`harness/mod.rs:1556`) before and after a `view` run that previews and cancels, assert unchanged, and assert no file was created under any staging root during the run — `nothing_is_staged_by_a_preview` green
- [x] 7.16 Do not extend `crates/safix/tests/editor.rs` with the four-outcome assertions for the nameless form: its three existing tests (`editor.rs:48`, `:139`, `:193`) already hold the outcomes, and 7.12 holds only what the nameless form adds — record that division in `editor.rs`'s module doc comment so a later reader does not add the duplicate — `editor.rs`'s module doc now records the division; no four-outcome assertions were added to `picker.rs`
- [ ] 7.17 Severity drill: attaching the pty to stdin only in 7.1 turns 7.6 red — the picker's draw output goes to `/dev/tty`, which without a pty-backed stdout is not the stream the test reads — performed and **did not reproduce**: `Fixture::pick`'s `if pick.draw_on_stdout` branch was replaced with an unconditional `command.stdout(Stdio::piped())`, and `cargo test -p safix --test picker -- a_typed_query_and_enter_prints_the_chosen_value --test-threads 1` stayed green, as did the whole 12-test suite under `--test-threads 1`. The reason: 7.1 wraps the run in `setsid --ctty`, so the run's controlling terminal is the pty whatever its standard output is, and both the rows and the chosen value go to `tty::probe()`'s `/dev/tty` handle, which is the master the test reads. That is precisely what 7.9 depends on, so the pty-on-stdout attachment is held by nothing: `draw_on_stdout` distinguishes what a test exercises rather than what it asserts. Two runs were needed to establish this, because the first full-suite pass showed one load-dependent flake of `nothing_is_staged_by_a_preview` (the preview had not been drawn before the cancel keystroke arrived) which the second pass did not reproduce — worth noting as a timing sensitivity in that test independent of this drill. The perturbation was restored; this line stays unticked
- [ ] 7.18 Severity drill: removing the debounce from 4.5 turns 7.11 red by producing one decrypt per movement key — performed and **did not reproduce**: `QUIET` was set to `Duration::from_millis(0)` and `cargo test -p safix --test picker -- moving_through_entries_without_pausing_decrypts_only_where_the_highlight_rests --test-threads 1` stayed green with exactly one `decrypt` in the shim's log. The reason: the draw loop calls `settle()` once per `read`, and 7.11 sends both movements in a single chunk (`"\x1b[B\x1b[B"`), so `act(keys)` consumes them both before the next `settle()` — the single decrypt is held by the read-chunk boundary, not by the quiet period. To redden it the drill would have to make the loop settle per key rather than per read, or the test would have to send the movements as separate chunks with no pause, which the harness's keystroke writer cannot currently express without a delay. `QUIET` was restored; this line stays unticked
- [x] 7.19 Verify: `cargo test -p safix --test picker` green with every test above run under `--test-threads 1`, drills in 7.17 and 7.18 observed — `cargo test -p safix --test picker -- --test-threads 1`: 12 passed, 0 failed. `cargo test -p safix --test editor`: 3 passed. Both 7.17 and 7.18 were performed and are recorded as non-reproductions

## 8. Nix checks

- [x] 8.1 Add `checks.safix-view-no-terminal`, `safix-view-nothing-to-pick`, `safix-view-selection`, `safix-view-cancelled`, `safix-view-preview-streams` and `safix-edit-nameless` to `modules/flake/checks/cli.nix`, each through `mode "<check-name>" "picker" "<test_fn_name>"` (`cli.nix:327-343`, following the three `safix-edit*` entries at `cli.nix:510-526`) — six entries added through `mode "<name>" "picker" "<test>"`
- [x] 8.2 Give each entry the file's prose-comment convention: what the claim is, and which drill from groups 2, 4, 6 or 7 turns it red — each carries what the claim is and which drill reddens it
- [x] 8.3 Register `picker` as a whole-target `claim` in `modules/flake/checks/single-runtime.nix` (`single-runtime.nix:38`, following `safix-vault-*` at `:132-148`) as `safix-picker = claim "safix-picker" "picker";`, with a doc comment naming the interrupt and staging drills it carries — `safix-picker = claim "safix-picker" "picker";` added, linux-only beside `safix-memory-backing` (the pty runs need `setsid`), with the interrupt and staging drills named in its comment
- [x] 8.4 Confirm `crates/safix/tests/picker.rs` needs no `[[test]]` entry in `crates/safix/Cargo.toml`: test targets there are auto-discovered, and only the six support binaries are declared (`crates/safix/Cargo.toml:18-72`) — state the confirmation in 8.3's comment so the next reader does not go looking — confirmed and stated in 8.3's comment: test targets are auto-discovered and only the six support binaries are declared
- [x] 8.5 Confirm `modules/flake/rust.nix` needs no change: crane vendors from `Cargo.lock` and `cargoArtifacts` picks up nothing new, because P2 takes no dependency; and confirm the compiled suite installs the new target to `$out/bin/picker` through the existing `rust.nix:104-155` path — confirmed: no dependency was added, so `Cargo.lock` and `cargoArtifacts` pick up nothing new, and the compiled suite installs the new target to `$out/bin/picker` through the existing `rust.nix` path
- [x] 8.6 Confirm `flake.nix`, `flake.lock`, `Cargo.toml`, `crates/safix/Cargo.toml`, `Cargo.lock`, `deny.toml` and `openspec/specs/rust-supply-chain/spec.md` are all unedited at the end of this change — P2's whole point, and the evidence is the empty diff over those seven paths — `flake.nix`, `flake.lock`, `Cargo.toml`, `crates/safix/Cargo.toml`, `Cargo.lock`, `deny.toml` and `openspec/specs/rust-supply-chain/spec.md` are unedited by this slice (`Cargo.toml` was edited by the concurrent installer slice for rustix's `mount` feature, which is not this change's)
- [x] 8.7 Severity drill: renaming any test function in `crates/safix/tests/picker.rs` without updating `cli.nix` turns that check red rather than green, because `integration.nix`'s `runOneWith` fails when zero tests ran — perform the drill on one entry — performed on `safix-view-nothing-to-pick`: renaming `a_user_holding_nothing_is_refused_rather_than_offered_an_empty_list` to `…_empty_listing` turned the check red with `running 0 tests` / `test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 12 filtered out` / `no test ran: picker has no test named 'a_user_holding_nothing_is_refused_rather_than_offered_an_empty_list'`, exit code 1; restoring the name built it green again. Two deviations were forced and neither touches the claim. First, the build was driven as `nix build 'path:/tmp/safix-drill#checks.x86_64-linux.safix-view-nothing-to-pick'` over a copy of the worktree rather than `.#…`, because nix's git source filter omits untracked files and this change's `picker.rs`, `tty.rs` and `tests/picker.rs` were untracked at the time — `.#checks.x86_64-linux.safix-view-nothing-to-pick` failed at `safix-core` not compiling, since `lib.rs` declared a `mod install;` whose file never reached the store path. Second, `safix-integration`'s `doCheck` was set to `false` in that copy alone, because the shared suite derivation runs the whole suite in its check phase and was red on three things outside this change: the un-regenerated `plain-unknown_subcommand` snapshot 5.7 still owes, `tests/install.rs`'s `the_hand_written_fixture_manifest_is_accepted_and_a_mutation_of_it_is_not` refusing on `could not read the installer manifest at /build/source/crates/safix/tests/support/install-manifest.json`, and `abort_residue.rs`'s `a_signal_during_a_generator_does_not_sweep_the_root_the_script_is_using` failing on `the run does not say "out-after-the-signal=present"`. With those neutralized in the copy, all six `cli.nix` entries and `safix-picker` built green before the drill. No file in the repository was edited for this drill
- [x] 8.8 Severity drill: dropping `safix-picker` from `single-runtime.nix` leaves the six `cli.nix` entries green while the interrupt and staging tests stop running in CI, which is why the whole-target claim exists alongside the per-mode ones; observe it by listing the tests each check runs — performed in the same copy as 8.7. The list, read off each check's own `buildCommand`: `safix-view-no-terminal` → `bin/picker a_run_with_no_terminal_is_refused_naming_both_remedies --exact`; `safix-view-nothing-to-pick` → `a_user_holding_nothing_is_refused_rather_than_offered_an_empty_list --exact`; `safix-view-selection` → `a_typed_query_and_enter_prints_the_chosen_value --exact`; `safix-view-cancelled` → `cancelling_writes_nothing_and_restores_the_terminal --exact`; `safix-view-preview-streams` → `the_preview_never_reaches_stdout_or_stderr --exact`; `safix-edit-nameless` → `edit_with_no_name_reaches_the_editor_for_the_chosen_entry --exact`; `safix-picker` → `bin/picker` with no filter at all. Replacing the `safix-picker = claim …` line with a comment left all six building green (one `nix build` of the six, exit 0) and removed the attribute outright — `nix build …#checks.x86_64-linux.safix-picker` then reports `does not provide attribute … 'checks.x86_64-linux.safix-picker'` — so six of the target's twelve tests lose every runner in CI: `an_interrupt_mid_picker_restores_the_terminal_and_exits_130`, `nothing_is_staged_by_a_preview`, `no_preview_decrypts_nothing_until_a_choice_is_made`, `moving_through_entries_without_pausing_decrypts_only_where_the_highlight_rests`, `edit_with_no_editor_refuses_before_offering_anything` and `edit_never_offers_a_public_output`. The three `safix-edit*` checks that survive name tests on the `editor` target, not this one. No file in the repository was edited for this drill
- [x] 8.9 Verify: `nix build .#checks.x86_64-linux.safix-view-selection .#checks.x86_64-linux.safix-edit-nameless .#checks.x86_64-linux.safix-picker` green, drills in 8.7 and 8.8 observed — all three, plus the other four picker entries, built green over the worktree copy described in 8.7 (`path:` flake, `safix-integration`'s `doCheck` off there so the three unrelated in-flight failures listed in 8.7 do not mask the result). Building them as `.#…` against the repository itself remains the orchestrator's gate, and needs 5.7's snapshots, `install.rs`'s fixture path and `abort_residue`'s generator-signal test settled first; drills 8.7 and 8.8 observed

## 9. Documentation, the changelog, and the three stale counts

- [x] 9.1 Add a `## Browsing what is there: `safix view`` section to `README.md` before the `## Editing a value` section (`README.md:554`): the two forms, the `get`-is-a-pipe/`view`-is-a-terminal sentence, what the picker offers, the preview's four properties, `--no-preview`, and the three refusals — `## Browsing what is there: `safix view`` added before the editing section
- [x] 9.2 Extend `README.md`'s `## Editing a value: `safix edit`` section (`:554-573`) with the nameless form, the public-output exclusion, and the editor-before-picker ordering — the editing section gains the nameless form, the public-output exclusion and the editor-before-picker ordering
- [x] 9.3 Add `safix view` to the daily-commands block (`README.md:591-599`) between the `get` and `generate` lines — `safix view` line added between `get` and `generate`
- [x] 9.4 Correct `README.md`'s three verb counts — `:900`, `:930`/`:933` (the thirteen attribute spellings are unchanged; only the verb counts move), `:935` (add `view` to the enumerated list and move "Thirteen of safix's fourteen verbs" to "Fourteen of safix's fifteen") and `:1222` ("all thirteen subcommands", with `view` added to the read paths) — corrected at README.md ~942, ~977 (with `view` added to the enumerated list) and ~1305 (fifteen subcommands, `view` among the read paths)
- [x] 9.5 Correct `examples/README.md:21`, "Thirteen of safix's fourteen verbs", to fourteen of fifteen — `examples/README.md:21` now reads fourteen of fifteen
- [x] 9.6 Add a `### A read verb with a picker, and a nameless `edit`` entry under `## [Unreleased]` in `CHANGELOG.md` (`CHANGELOG.md:22`): the new verb and its position, the widened `edit` form, the three new refusal codes by name, the preview's bounds and `--no-preview`, `Secret::preview_into` as a `safix-core` interface addition, and the sentence that no dependency was added and `deny.toml` is unedited — `### A read verb with a picker, and a nameless `edit`` added first under `## [Unreleased]`
- [x] 9.7 State in the changelog entry that `safix edit` with no arguments previously printed a usage line and now opens a picker, recorded under the widened verb rather than as a break, and that `get` is unchanged — stated in that entry, under the widened verb rather than as a break, together with `get` being unchanged
- [x] 9.8 Do not rewrite any released changelog entry; the `[Unreleased]` section is the only one this change touches — no released entry touched
- [x] 9.9 Severity drill: leaving any of 9.4's or 9.5's counts stale is not caught by a check today — record that gap in 9.6's changelog entry rather than inventing a prose-counting check, and instead assert the one count that is machine-checkable, `usage::SCAFFOLD`'s (task 1.9), through the scaffold snapshot — the prose-count gap is recorded in the changelog entry rather than checked; the machine-checkable count is `usage::SCAFFOLD`'s (task 1.9)
- [ ] 9.10 Verify: `README.md`, `examples/README.md` and `CHANGELOG.md` re-read end to end for the sections touched; `nix build .#checks.x86_64-linux.safix-usage-scaffold` (or whichever check holds the scaffold snapshot) green — the touched sections were re-read end to end; the scaffold-snapshot check was not built, per the batch rule that `nix build` is the orchestrator's

## 10. Closing verification

- [ ] 10.1 Run `cargo test --locked --workspace` and record which failures are pre-existing environment-only ones (`bwrap`/`strace`) rather than this change's
- [ ] 10.2 Run `nix flake check` and record every check that moved, confirming the six new `cli.nix` entries and `safix-picker` are among them and that no existing check attribute was renamed or removed
- [ ] 10.3 Confirm the seven paths named in 8.6 are unedited, by diff
- [ ] 10.4 Run `openspec validate --change add-view-picker` and confirm the four delta specs validate
- [ ] 10.5 Severity drill roll-up: confirm every drill named in groups 1 through 9 was performed and its outcome recorded in the task line, and that any drill which did not reproduce carries the reason in place of the claim
