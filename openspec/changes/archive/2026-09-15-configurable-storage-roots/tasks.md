# Tasks: configurable-storage-roots

Citations are as read while designing this change, on 2026-09-15; re-read the named lines before editing, since implementation may land after sibling changes and line numbers drift.
The symbol name, not the line number, is the anchor.
No real fleet identifier, hostname, or recipient enters this repository; fixtures use `alice`, `bob`, and `carol`, and synthetic `age1` strings, matching the existing fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
Throughout, `storage` means the resolved attrset `{ encrypted; plaintextOutputs; generatorRecords; }` threaded from `flake.safix.storage`, and every task that says "derive from `storage.<x>`" means string-interpolating that value where a literal stands today, with no trailing slash added (the option refuses one, so every join writes its own `/`).

## 1. The `storage` option and its two evaluation refusals

- [x] 1.1 Add `storage` to `modules/flake/safix/options.nix`, beside `vault` (`options.nix:242-333` is the shape to follow): `lib.mkOption { type = lib.types.submodule { options = { encrypted = ...; plaintextOutputs = ...; generatorRecords = ...; }; }; default = { }; }`, each sub-option `lib.types.str` with `default = "secrets/safix"` / `"public/safix"` / `"state/safix/definitions"` respectively — `options.nix:242-356`
- [x] 1.2 Write `storage.encrypted`'s description — `options.nix:246-276`
- [x] 1.3 Write `storage.plaintextOutputs`' description — `options.nix:278-304`
- [x] 1.4 Write `storage.generatorRecords`' description — `options.nix:306-334`
- [x] 1.5 Add to each of the three descriptions the one-parent worked example and the `rg`/`fd` `--hidden` caveat — present in all three
- [x] 1.6 Add `wellFormedStorageRoot` to `modules/flake/safix/resolve.nix`, following `wellFormedNamingKey`'s shape — `resolve.nix:225-231`
- [x] 1.7 Add `storageOverlap` beside it — `resolve.nix:239`
- [x] 1.8 Add `storageViolations = storage: ...` and add it to `resolve.nix`'s exports list beside `vaultViolations` — `resolve.nix:247-284`, exported at `:2531-2535` (with `defaultStorage` and `relativeTo`)
- [x] 1.9 Concatenate `resolve.storageViolations cfg.storage` into `violations` in `modules/flake/safix/default.nix` — `default.nix:193-197`
- [x] 1.10 Thread `storage` into `bound`/`registry` in `default.nix`, the way `namingKey` is threaded — `default.nix:47-51`; `registryOf` defaults it to `resolve.defaultStorage` (`resolve.nix:128-131`), so every `resolve.*Of` entry point and `selectFor` reach it with no second mechanism
- [x] 1.11 Add `modules/flake/checks/storage.nix` with a check named `safix-storage` and register it where the sibling check files are registered — new file; registered at `flake.nix:107`. `./storage-fixture/` carries one generated public value under each spelling, which is how `publicValueIn`'s following of a root is made observable
- [x] 1.12 In `safix-storage`, assert the default configuration byte-identically — `defaultLayout`, asserting `sopsFile`, `sharedSopsFile`, `sopsFileIsRooted`, `outputPath`, `publicValue`, `publicPaths`, `definitionRecords`, `pathRegexes` and `governedFiles` against the literal spellings. The two `sopsFile` rows carry the tail below the fixture root, with `sopsFileIsRooted` holding the "joined against `root`" half, so this check's derivation does not embed the flake's own source store path — which is what `mk-structural-check`'s cache scoping exists for
- [x] 1.13 In `safix-storage`, assert the four malformed-root refusals for each of the three options, and a well-formed root evaluating without refusal — `refusals` (12 rows) and `wellFormedAccepted`
- [x] 1.14 In `safix-storage`, assert the overlap refusals and the accept case — `overlapRefusals.{equalRoots,componentPrefix,nestedRecords}` (each also asserting the message names both options) and `nonComponentPrefixAccepted`
- [x] 1.15 Severity drill — executed rather than narrated: `overlapClausesAreLoadBearing` holds both weakened comparisons (`overlapAsRawPrefix`, `overlapWithoutEquality`) beside the real function, so `rawPrefixWouldRefuseIt = true` against `realAcceptsNonComponentPrefix = false`, and `withoutEqualityWouldAcceptIt = false` against `realRefusesEqualRoots = true`. Recorded in `storage.nix`'s drill commentary
- [x] 1.16 Severity drill: observed. Dropping `++ resolve.storageViolations cfg.storage` from `default.nix` turned all twelve malformed-root rows and all three overlap rows green at once (measured off the check's own `actualJson`); reverted. Recorded in `storage.nix`'s drill commentary
- [x] 1.17 Verify: `nix build .#checks.x86_64-linux.safix-storage` green; drills in 1.15 and 1.16 observed

## 2. The resolver derives every readable path from the roots

- [x] 2.1 `audienceFileOf` takes `storage` and emits `"${storage.encrypted}/users/${user}/secrets.yaml"` / `"${storage.encrypted}/shared/${members}/secrets.yaml"` — `resolve.nix:607-614`
- [x] 2.2 `publicFileOf` takes `storage` and emits `"${storage.plaintextOutputs}/users/…"` / `"…/shared/…"`, `<name>/value` leaf unchanged — `resolve.nix:687-692`
- [x] 2.3 The audience-separator comment now reads against the configured encrypted root — `resolve.nix:296-302`
- [x] 2.4 Rewrote the load-bearing rationale comment above `publicFileOf`: the argument is kept, its ground moved to the evaluation refusal on overlap plus the consumer's own naming, citing design S3 — `resolve.nix:674-686`
- [x] 2.5 The vault-ciphertext comment keeps its accurate "one flat file directly under `secrets/`" and gains the sentence that this bucket is deliberately not `storage.encrypted` and why — `resolve.nix:633-641`
- [x] 2.6 Confirmed and held: `outputPathIn`, `publicValueIn` and `publicPathsIn` needed no edit; `safix-storage`'s `renamedPlaintext` asserts all three follow a renamed root, with `publicValue` reading a different file's bytes (`clear/…` yields `renamed-root`, `public/safix/…` yields `default-root`)
- [x] 2.7 Confirmed and held: `policy.nix`'s `pathRegex` derives from `audiences.<file>.dir`; `safix-storage`'s `renamedPolicy` asserts `^cipher/…` over the renamed fixture, and `renamedCiphertext.dirs` asserts the `dir` it comes from
- [x] 2.8 Severity drill: observed. Reverting `audienceFileOf` to the literal while leaving `publicFileOf` derived turned `renamedCiphertext`, `renamedPolicy` and `renamedGoverned` red and left `renamedPlaintext` green; reverted. Recorded in `storage.nix`'s drill commentary
- [x] 2.9 Verify: `nix build .#checks.x86_64-linux.safix-storage` green including 2.6 and 2.7; drill in 2.8 observed

## 3. Nix emits `definitionRecord` on every placement; the runtime stops constructing it

- [x] 3.1 `placementsIn`'s `definitionRecord` is unconditional — `resolve.nix:808-814`: `"${storage.generatorRecords}/shared/${audienceDir}/${name}"` for a shared entry, `"${storage.generatorRecords}/${owner}/${name}"` otherwise, replaced by `"state/${opaqueOf r.namingKey "state" (relativeTo storage.generatorRecords logicalRecord)}"` in vault mode. The `"state"` hash input is byte-identical to the previous derivation (`relativeTo` strips exactly the root the local was prefixed with), so no vault-mode record name moves
- [x] 3.2 `logicalRecord` is emitted beside `logicalFile`/`logicalKey`/`logicalPublic`, non-null only in vault mode, carrying the readable record path — `resolve.nix:815`
- [x] 3.3 `Placement::definition_record` is `String` and `logical_record: Option<String>` sits beside `logical_public` — `model.rs:169-180`, `:199-207`; the `PLACEMENT` fixture at `model.rs:1037-1039` carries the readable record and a null `logicalRecord`
- [x] 3.4 `record_path(placement)` is `placement.definition_record.clone()`, the `name` parameter dropped because nothing derives any more — `definition.rs:129-150`; call sites `generate.rs:383` and `check.rs:545`
- [x] 3.5 `PREFIX`, `logical_record_path` and `audience_directory` deleted — `definition.rs` now holds `FORMAT`, `digest`, `line`, `recorded`, `record_path`, `canonical` and nothing that spells a path
- [x] 3.6 Module doc rewritten — `definition.rs:10-37`: the path is the resolver's, arrives on `Placement::definition_record`, the default spelling is shown as a default, and non-overlap is attributed to the evaluation refusal rather than to three literals
- [x] 3.7 `record_leaves` collapsed into `leaves_by` — `relocation.rs:86-104`. Dedup semantics are identical (both keyed the opaque half in a `BTreeMap`), so the collapse is exact; the doc keeps why a shared entry's record must not be queued twice and now names `logical_record` as the vault-mode gate, since `definition_record` is set in both modes and cannot be one. `use crate::definition;` dropped with the last caller
- [x] 3.8 `a_record_path_is_the_resolvers_own_answer` — `definition.rs:465-511`: owner-keyed, audience-keyed (both carriers), and a renamed `.safix/generator-records/...` root, all returned byte for byte. The renamed-root case is what would go red if any derivation came back
- [x] 3.9 Disjointness assertion deleted — `no_record_path_reaches_the_secret_or_the_public_tree` is gone with the constants it asserted about
- [x] 3.10 `a_vault_mode_record_path_is_the_nix_supplied_opaque_path` re-pointed at the emitted record — `definition.rs:513-544`; the fixture now carries `logicalRecord` beside the opaque one, and `owner`/`shared` are set so a reconstructed answer could not coincide
- [x] 3.11 Confirmed and recorded — `nix.rs:22-30` states why there is no variant for the generator-record root (or any `storage` value): the path arrives already computed on the placement through `Placements`, so a second `nix eval` would rebuild a string the runtime was handed and put the layout back in two languages
- [x] 3.12 In `safix-storage`, `recordsAreEmitted` asserts `noneNull` over every placement with no vault declared, the four `state/safix/definitions/alice/<name>` values under the defaults, and the four `bookkeeping/alice/<name>` values under the renamed root
- [x] 3.13 Severity drill: observed on the nix half. Making the emission conditional again turned `recordsAreEmitted.noneNull` `false` and all eight record values under both roots `null`; reverted. Recorded in `storage.nix`'s drill commentary. (The `crates/safix/tests/generators.rs` half is StorageRust's.)
- [x] 3.14 Verify: `cargo test -p safix-core --lib definition::` 10 passed, `relocation::` 5 passed; drill in 3.13 observed on the nix half by StorageNix, and the Rust-side equivalent is 11.9's (a `logicalRecord` pointed at an unplaced path turns the record-reading assertions red — observed, see 11.9)

## 4. `public::{PREFIX, LEAF, is_public_path}` and the property module are deleted

- [x] 4.1 `is_public_path` deleted — `public.rs` now ends at `holds_a_value`
- [x] 4.2 `PREFIX` and `LEAF` deleted with it
- [x] 4.3 Both unit tests deleted, with their local `CIPHERTEXT` constant
- [x] 4.4 The `properties` module deleted whole: the `NAME` alphabet, the `public_path` reimplementation and all three properties
- [x] 4.5 Module doc rewritten — `public.rs:10-46`: the default spelling is shown as a default, the root is named as `flake.safix.storage.plaintextOutputs`, design S3's argument replaces the literal-prefix one, and what stays mechanically enforced is named — the overlap refusal at evaluation, `safix-public-no-rule` and `safix-no-catch-all` — alongside the retained "one implementation of the layout" rule
- [x] 4.6 Confirmed unedited: `public::stage` (`public.rs:64-81`) and `public::holds_a_value` (`:90-92`) are the whole of the module's surface and neither reads a prefix
- [x] 4.7 Grep over the workspace for all six names returns zero code references. The one hit is `CHANGELOG.md:35`, which is prose naming the deletion. `modules/flake/checks/generators.nix` carries none
- [ ] 4.8 Severity drill: the claim these deletions rest on is that nix's two checks, not Rust, hold the separation. Plant `pathRegex = "^${storage.plaintextOutputs}/.*"` in a fixture with renamed roots and confirm both `safix-public-no-rule` and `safix-no-catch-all` fail. If either passes, the deletion is not yet safe and group 6 is incomplete — nix-side, left to StorageNix with group 6
- [x] 4.9 Verify: `cargo test -p safix-core --lib public::` runs 0 tests, which is the correct answer — 4.3 and 4.4 deleted every test the module had, and `stage`/`holds_a_value` are exercised through the write-path suites. `cargo build -p safix-core -p safix` clean, no dead-code warning

## 5. The committed `.sops.yaml` header names the configured roots

- [x] 5.1 The header paragraph "A shared audience gets `${storage.encrypted}/shared/<a>,<b>/`" interpolates the configured root — `policy.nix:63-67`, line wrapping preserved so the committed bytes are unchanged under the defaults
- [x] 5.2 The anchoring paragraph's worked example interpolates `storage.encrypted` in both spellings — `policy.nix:89-98`
- [x] 5.3 `header` is now `storage: ''…''` and `renderPlan` is `storage: p:`, with `render = registry: renderPlan (storageOf registry) (plan registry)` and `storageOf = registry: registry.storage or resolve.defaultStorage` — `policy.nix:34-41`, `:223-225`, `:270`. One path: a bare fixture registry gets the defaults
- [x] 5.4 Regenerated with `nix eval --raw .#safix.lib.policyText > modules/flake/checks/fixture-policy.yaml`; `git diff --stat` on the file is empty — byte-identical under the defaults
- [x] 5.5 Confirmed: `renderVaultRules` keeps its literal `^secrets/` (the escaped `\.yaml$` too), with the comment stating the vault's buckets are deliberately not `storage.encrypted` — `policy.nix:248-253`. Its hashed input did change, to the root-relative form, or a rendered rule would no longer match the file `secretsFileOf` names
- [x] 5.6 `safix-storage`'s `renamedPolicyHeader` asserts `sharedExample`, `anchoringExample` (both spellings) and `noDefaultSpelling` over the renamed fixture
- [x] 5.7 Severity drill: observed. Reverting the shared-audience paragraph alone turned `sharedExample` `false` and left `anchoringExample` `true`; reverted. Recorded in `storage.nix`'s drill commentary
- [x] 5.8 Verify: `safix-storage` and `safix-policy-drift` both green; `fixture-policy.yaml` unchanged under the defaults; drill in 5.7 observed

## 6. `catchAllProbes` is derived, or the check silently stops meaning anything

- [x] 6.1 The six tree-shaped probes are derived from the configured roots, `UNCLAIMED` component kept — `checks.nix:240-251` (`catchAllProbesOf`)
- [x] 6.2 The four non-tree probes stay literal — same list, with the comment stating why
- [x] 6.3 `catchAllProbesOf` takes `storage`, `catchAllMessagesOf` takes `storage: plan:`, and `catchAllMessages`/`mkNoCatchAllCheck` read it off the registry (`registry.storage or resolve.defaultStorage`); `mkChecks` accepts and reads `storage` — `checks.nix:253-272`, `:417-445`. The two external callers (`exported.nix:233`, `generators.nix:402`) pass `resolve.defaultStorage`
- [x] 6.4 Rewrote the comment block: the record tree's probes now guard a tree nix itself places, since the resolver emits `definitionRecord` — `checks.nix:215-239`
- [x] 6.5 The anchoring comment names `flake.safix.storage.encrypted` and the plaintext-output root instead of the literals — `checks.nix:281-284`
- [x] 6.6 Confirmed derived and held: `publicRuleMessagesOf`/`mkPublicRuleCheck` needed no edit; `safix-storage`'s `publicRuleFollowsRename` asserts the renamed fixture stays green and a rule planted at `^clear/.*` is reported
- [x] 6.7 Severity drill: executed inside the check rather than narrated. `catchAllProbesAreDerived` plants a rule reaching the renamed encrypted tree and asks both probe sets: `literalProbesStayGreen = true` (the silent loss) against `derivedProbesGoRed = true`, with `legitimateFixtureStaysGreen = true`. The transition is therefore re-run on every build rather than only once by hand
- [x] 6.8 Severity drill: executed inside the check. `unclaimedComponentIsLoadBearing` asserts every tree-shaped probe carries `UNCLAIMED`, and that substituting a really declarable name (`alice`) makes the legitimate fixture's own rules match a probe — which is the red a probe set without the component would produce
- [x] 6.9 Verify: `safix-no-catch-all`, `safix-public-no-rule` and `safix-storage` all green; drills in 6.7 and 6.8 observed

## 7. The vault's opaque hash input becomes root-relative

- [x] 7.1 `secretsFileOf` hashes `relativeTo storage.encrypted (audienceFileOf storage audience)` — `resolve.nix:730-734`. The one helper `relativeTo = root: path: lib.removePrefix "${root}/" path;` lives at `resolve.nix:631` and is used at every site in this group
- [x] 7.2 `publicFileOfVault` hashes relative to `storage.plaintextOutputs` — `resolve.nix:740-746`
- [x] 7.3 The vault branch of `definitionRecord` hashes relative to `storage.generatorRecords` — `resolve.nix:816-820`. Note the resulting input is byte-identical to the previous derivation, so no vault-mode record name moves; the document, key and public leaf names all do
- [x] 7.4 `opaqueKeyOf`'s `logicalPath` is the root-relative audience-file name at all three sites: `placementsIn` (`resolve.nix:811`), `selectFor.forMachine` (`:2288-2292`) and `selectFor.forUser` (`:2382`). `policy.nix`'s `renderVaultRules` is a fourth derivation of the same name and follows too (`policy.nix:263-267`); `safix-vault`'s `machineKeyAgreement` and `rules.regexesMatchFormula` are what hold the three in agreement
- [x] 7.5 Updated the `opaqueOf` comment: the tags are now the only separator, because two roots' relative names can coincide exactly — `resolve.nix:608-622`
- [x] 7.6 Confirmed unedited: `secrets/`, `public/` and `state/` stay literal at the vault root, in `secretsFileOf`, `publicFileOfVault`, the vault branch of `definitionRecord`, and `renderVaultRules`
- [x] 7.7 `expectedOpaqueFile`/`expectedOpaqueKey`/`expectedOpaquePublic`/`expectedDefinitionRecord` recompute against the root-relative input, literal bucket joins kept — `vault.nix:203-240`. `logicalRecordOf` now reads the placement's own emitted `definitionRecord` rather than reconstructing it
- [x] 7.8 `expectedRegexOf` follows 7.7 — `vault.nix:285-289`
- [x] 7.9 `prefixesStayDisjoint` kept, with the comment distinguishing the vault's literal buckets from the declaration-side disjointness `storageViolations` now refuses — `vault.nix:460-474`
- [x] 7.10 `safix-vault`'s `renamedRootsResolveIdentically` asserts `file`, `key`, `public` and `definitionRecord` equal between `withVault` and `withVaultRenamed` (roots `cipher`/`clear`/`bookkeeping`) for all four names, with `renamedRootsMoveTheReadableSide` asserting the readable side did move — so the row is a claim rather than a fixture that renamed nothing
- [x] 7.11 `safix-vault`'s `tagsAreTheOnlySeparator` feeds one root-relative identity through all four tags and requires four distinct names
- [x] 7.12 Severity drill: observed. Reverting `secretsFileOf`'s input to the root-prefixed path turned `renamedRootsResolveIdentically.<n>.file` `false` for every name while `.key`, `.public` and `.definitionRecord` stayed `true`; reverted. Recorded in `vault.nix`'s drill commentary
- [x] 7.13 Severity drill: observed. Collapsing `"public"` onto `"secrets"` inside `opaqueOf` turned `tagsAreTheOnlySeparator` `false` while `leakedFragments` stayed `[ ]`; reverted. Recorded in `vault.nix`'s drill commentary
- [x] 7.14 Verify: `nix build .#checks.x86_64-linux.safix-vault` and `.#checks.x86_64-linux.safix-storage` green; drills in 7.12 and 7.13 observed

## 8. The one-time opaque-name break relocates through the existing machinery

- [x] 8.1 Confirmed: beyond group 3's `logical_record` swap in `record_leaves`, nothing in `check.rs`'s `vault_relocation`, `relocation.rs` or `fix.rs`'s `relocate`/`document_move`/`leaf_move`/`named_move` changed. But the premise needs correcting, and 8.3's drill corrects it: this machinery moves between the declaration root and the vault root only — `named_move` resolves `(root, logical)` against `(vault_root, opaque)` — so an old-opaque-to-new-opaque move is *not* a case it serves. `named_move`'s resumability predicate is intact and is what makes the forward migration re-runnable; it is not what carries this break
- [x] 8.2 Confirmed: `Finding::VaultRelocationPending { file }` and its prose name a pending readable path, not a cause, so no new variant and no snapshot change. Reached only when a readable leaf is present at the declaration root, which is the correction 8.1 records
- [x] 8.3 `the_opaque_name_break_is_visible_but_does_not_relocate_itself` — `crates/safix/tests/vault_migration.rs:443-547`, on `populated_readable_fixture`'s shape. Written as the task asks and then corrected to what it observed, twice: (a) the break is **partial** — `opaqueOf`'s `state` input was already root-relative, so every definition record keeps its name while every document, in-document key and public leaf loses its; (b) design S5's migration plan is **wrong**: `check` reports no pending relocation over a vault holding pre-change names, because there is nothing at the declaration root to queue, and `fix --vault-rollback` recovers only the two records, whose names did not move. All three are asserted, so the test fails if any of them stops being true. The forward half (readable layout to post-change opaque names) is `a_populated_readable_fixture_migrates_into_a_vault`, whose destinations are now the root-relative names
- [x] 8.4 Recomputed, not hand-edited. `vault_opaque_names.rs:35-39`: its two constants *were* real hashes of the root-prefixed paths (`374e7f9…` is exactly `sha256("fc84b416…|secrets|secrets/safix/users/alice/secrets.yaml")`), so they moved; they are now `873b6adf…` (`shared/alice,bob/secrets.yaml`) and `49de4715…` (`users/alice/secrets.yaml`), with the key and both inputs named in the doc comment so the next reader can redo the arithmetic. `vault_migration.rs:26-75`: its constants were hex-shaped stand-ins (`1111…`, `2222…`, `3333…`); rather than leave a fixture that cannot detect a hash-input change, every one is now the resolver's real output over the same naming key, plus `OPAQUE_PUBLIC_RECORD`/`LOGICAL_PUBLIC_RECORD` for the public output's own record, plus the four `PREVIOUS_*` pre-change names 8.3's drill needs
- [x] 8.5 Confirmed: neither carries an opaque constant. `vault_scratch_rules.rs:102-105` is the readable `secrets/safix/users/bob/other.yaml` and `:241-260` is the `.gitignore` case with no path hash; `set.rs:417-420`'s `"secrets/opaque.yaml"` is a literal placeholder file name spelling the word, not a hash. Nothing to regenerate
- [x] 8.6 Confirmed: `vault-projection.nix:271-274`'s expected `audiences` list carries readable paths (`secrets/safix/shared/alice,deck/secrets.yaml`, `secrets/safix/users/alice/secrets.yaml`), no opaque name. Nothing to regenerate; relayed to StorageNix so the nix half did not look twice
- [x] 8.7 Drill observed, and its transition reproduces — through a different finding than the task predicted. Vault seeded at the pre-change names: `check` exits 1 and reports `api-token` and both carriers' `fleet-token` against the post-change opaque names they now point at, which are empty. Same fleet at the post-change names: silent about all three. So the names *are* being compared and the break is detectable; what it is not is `vault_relocation`, which is silent in both halves (asserted). Both halves are in 8.3's test
- [x] 8.8 Verify: `cargo test -p safix --test vault_migration --test vault_opaque_names --test vault_scratch_rules` — 4 + 1 + 6 passed, 0 failed; drill in 8.7 observed

## 9. Remaining nix surfaces that spell a root

- [x] 9.1 `extraGovernedFiles`' example keeps the default spelling and states so in prose beside it — `default.nix:81-83`. A `lib.literalExpression` was the alternative and is worse here: the option is a plain list of strings a consumer writes by hand, and an example that interpolated an option path would read as if interpolation were allowed in the value
- [x] 9.2 The `files.<n>.secret` description names `flake.safix.storage.plaintextOutputs` and describes the shape below it — `types.nix:103-107`, with the separability paragraph at `:119-125` rewritten onto the evaluation refusal
- [x] 9.3 Confirmed and held: `governedFiles` needed no edit; `safix-storage`'s `renamedGoverned` asserts `extra`, `required` and `managed` all under `cipher/…`
- [x] 9.4 Confirmed: `default.nix`'s vault-rules entry point takes one new argument (`cfg.storage`, `default.nix:396`) and is otherwise unchanged — the rendering itself needed the storage value to hash a root-relative name, which is 7.4's fourth site
- [x] 9.5 Confirmed unedited: `grep -rn 'secrets/safix\|public/safix\|state/safix' modules/consume/` finds nothing. The consumption modules deal in `/run/safix.d`, `symlinkPath` and resolver-supplied `sopsFile` values, so a root rename does not reach them at all
- [x] 9.6 Confirmed unedited: same grep over `modules/flake/checks/collision-fixture/` finds nothing
- [x] 9.7 Verify: `nix eval --json .#safix.lib.placements` shows `definitionRecord` non-null on all 14 placements of the fixture fleet; `safix-storage` green, with `renamedGoverned` resolving `governedFiles` under the new root

## 10. Nix fixture and check literals

Under the preserved defaults not one of these changes value; each task is to confirm it still passes and, where the fixture asserts a *literal prefix* rather than a resolved path, to re-point it at the configured root so it does not go vacuous.

- [x] 10.1 `rulesWellFormed` now compares against `"^${resolve.defaultStorage.encrypted}/"` — `checks/policy.nix:169-177`, with the comment stating that left literal it reads as an anchoring check while asserting one spelling
- [x] 10.2 Confirmed green: `nix build .#checks.x86_64-linux.safix-policy`. Literals left as fixture expectations
- [x] 10.3 Confirmed green: `nix build .#checks.x86_64-linux.safix-generators`. `catchAllMessagesOf`'s new `storage` argument is passed `resolve.defaultStorage` at `generators.nix:402-403`, which is the only edit this file needed
- [x] 10.4 Confirmed green: `nix build .#checks.x86_64-linux.safix-custody` and `.safix-custody-refusals`. `audienceFileOf`'s new `storage` argument is passed `resolve.defaultStorage` at `custody.nix:734-738`
- [x] 10.5 Confirmed green: `nix build .#checks.x86_64-linux.safix-consumption`
- [x] 10.6 Confirmed green: `nix build .#checks.x86_64-linux.safix-exported-drills`. `catchAllMessagesOf` call updated at `exported.nix:233-235`
- [x] 10.7 Confirmed green: `nix build .#checks.x86_64-linux.safix-materialization`
- [x] 10.8 Confirmed green: `nix build .#checks.x86_64-linux.safix-portability-home` and `.safix-portability-system` (the check names; there is no `safix-portability`)
- [x] 10.9 Confirmed green: `nix build .#checks.x86_64-linux.safix-subjects`. `audienceFileOf` call updated at `subjects.nix:1463-1467`
- [x] 10.10 The `state/safix/definitions/` prose now names `flake.safix.storage.generatorRecords` — `cli.nix:486-492`. The two behavioural items (`safix-governed-extras`, the evaluated attribute list) are cargo-test-backed checks over the runtime; they are unchanged by this group and are left to the batch's final gate rather than built mid-flight while sibling slices are editing `crates/`
- [x] 10.11 Confirmed green: `nix build .#checks.x86_64-linux.safix-examples`
- [x] 10.12 Verify: every "confirm green" item above was built rather than assumed. Also green in the same run: `safix-rule-shape`, `safix-audience-separator`, `safix-namespace`, `safix-module-entrypoints`, `safix-vault-projection`, `safix-no-catch-all`, `safix-public-no-rule`, `safix-policy-drift`, `safix-storage`, `safix-vault`. Whole-flake `nix flake check` is the batch's final gate, not this group's

## 11. Rust fixtures, CLI help, and snapshots

- [x] 11.1 `check`'s help now reads "answered from the generator-record tree — one plaintext file per generated value, under `flake.safix.storage.generatorRecords`, which defaults to state/safix/definitions/" — `usage.rs:327-333`, paragraph reflowed to the file's width
- [x] 11.2 Confirmed unaffected — `usage.rs:59-77`: `fix`'s vault paragraphs name `.sops-vault-rules.yaml` and the vault's own `.gitignore`, neither of which is storage-rooted
- [x] 11.3 Confirmed: `grep -n 'secrets/safix\|public/safix\|state/safix' crates/safix/src/render.rs` returns nothing. Every one of the named arms names an option or a path handed to it, so none changed
- [x] 11.4 Confirmed: every path literal in `reporter.rs`'s sample errors is a readable default-root path; none is an opaque name, so group 8's change moves none of them
- [x] 11.5 Confirmed no snapshot in the 22 named cases moved. Run under `INSTA_FORCE_PASS=1 INSTA_UPDATE=new` so every assertion was evaluated rather than stopping at the first mismatch: the only `.snap.new` written were `plain-unknown_subcommand` and `graphical-unknown_subcommand`, which this batch reserves for the orchestrator (new verbs), and both were deleted rather than accepted. No opaque name appears in any snapshot, which is why there was nothing for group 8 to regenerate
- [x] 11.6 The stub resolver emits `definitionRecord` on every placement — `harness/mod.rs:2467-2481`'s `placement` helper (which gained a `name` parameter, since the record is keyed by the declared name and `aliased-secret` is the one entry whose name and `key` differ), plus `seed_output`, `seed_public_output`, `seed_generator` and both `seed_two_way_mapping*` companions; `seed_shared` emits the audience-keyed `state/safix/definitions/shared/alice,bob/<name>` so both carriers carry the one record the resolver would give them. `logicalRecord` is emitted in vault mode by `vault_migration.rs`'s own placements, which is the only vault-mode seeder. `ALICE_FILE`, `SHARED_FILE`, the `dir` fields and the stub `.sops.yaml` rules are unchanged and green
- [x] 11.7 Green: `custody` 3, `read_path` 4, `subjects` 6, `sync_path` 11, `write_path` 6, `vault_reads` 4, `vault_workspace` 7, `vault_lock_bump` 4 — all 0 failed. Not confirmable on this machine, for reasons unrelated to this change, all of them environmental: `generators` (7 of 8) and `abort_residue`'s four cases fail with "bwrap is not available, so no generator ran" — this machine has no usable unprivileged user namespace, and the run refuses before it ever reaches the interrupt window, so the signal path is not exercised at all (diagnosed with ViewCli, who ran the cases directly; my first reading blamed the `crate::tty::restore()` call their slice added to `abort.rs` and was wrong — `tty::restore` returns before any syscall when no raw-mode guard has stored attributes, and `abort_residue`'s non-generator interrupt cases still exit 130). `vault_commit_ordering`/`vault_invariants`' two cases are PTY-and-PIN, the same class as `safix-core`'s two pre-existing `enroll::pty` failures. None of these touches a record path or a storage root
- [x] 11.8 Confirmed: `grep -n 'secrets/safix\|public/safix\|state/safix'` over all seventeen named suites returns nothing, so each reaches the layout only through `harness::ALICE_FILE`/`SHARED_FILE`
- [x] 11.9 Severity drill observed, re-aimed at the field this change introduces rather than at `ALICE_FILE`. `ALICE_FILE` no longer decides a record path — the record is a field, which is the claim — so pointing it elsewhere would not test the claim; and `generators.rs` cannot run on this machine (11.7). Instead: pointed one placement's emitted `logicalRecord` at `state/safix/definitions/alice/DRILL-UNPLACED`, a path the fixture does not place. `a_populated_readable_fixture_migrates_into_a_vault` went red at its record assertion and `the_opaque_name_break_is_visible_but_does_not_relocate_itself` went red at its rollback assertion — red rather than silently skipping. Reverted, both green again
- [ ] 11.10 Verify: `cargo test --locked --workspace` green; snapshot diffs in 11.5 reviewed line by line; drill in 11.9 observed — 11.5 reviewed and 11.9 observed; the whole-workspace run is the batch's final gate and is not run mid-flight while sibling slices are editing `crates/`

## 12. Documentation

- [x] 12.1 Public-store section rewritten against `flake.safix.storage.plaintextOutputs`; the "why separate" argument kept with its ground moved to the evaluation refusal and the consumer's own naming, layout block now `<plaintextOutputs>/…`, both checks still named — `README.md:467-477`
- [x] 12.2 Definition-record section rewritten against `flake.safix.storage.generatorRecords`; "third top-level tree" and "`state/` says what it is" replaced by "its own tree" and "the option's name says what this one holds" — `README.md:536-541`, `:549-551`
- [x] 12.3 The layout rows are now their own table with an option column, one row per tree naming the option, its default and its vault-mode spelling, plus the sentence that the vault's buckets are not configurable — `README.md:1210-1218`
- [x] 12.4 The anchoring paragraph names `flake.safix.storage.encrypted` — `README.md:1172`
- [x] 12.5 One sentence covers all five `sops <path>` examples at the first of them (`README.md:125`), and `credsCommand`'s own is marked at `:587`. One sentence rather than five: five copies of the same caveat is five places for it to rot
- [x] 12.6 Added, as "**What your own ignore, backup and exclusion rules should say.**" — `README.md:1160-1165`: no `.gitignore` at the declaration root, exactly one at the vault root covering only the scratch rules file, the one-parent worked example above it, and the `rg`/`fd` `--hidden` caveat
- [x] 12.7 Added, as "**Renaming a root.**" — `README.md:1167-1171`: the four steps, why no re-encryption in readable mode, what `safix check` reports between the option change and the `git mv`, and that a vault rename moves nothing inside the vault
- [x] 12.8 Two `[Unreleased]` entries added at the top of the section — `CHANGELOG.md:22-44`: the three options with their two refusals, and the **BREAKING** vault entry naming `safix check`/`safix fix` as the migration and stating the vault feature is itself unreleased
- [x] 12.9 Confirmed unedited: the three history passages are untouched; the new entries are inserted above `### One option for what arrived at system scope`, so nothing below `[Unreleased]`'s head moved except by line number
- [x] 12.10 Confirmed unedited: `grep -rn 'secrets/safix\|public/safix\|state/safix' examples/` finds nothing — the examples declare only `flake.safix.*` records
- [x] 12.11 Verify: read. `grep -n 'secrets/safix\|public/safix\|state/safix' README.md` returns 13 lines and every one is accounted for: five `sops` examples plus `credsCommand`'s, each covered by 12.5's marking sentences at `:125` and `:587`; four sentences of the form "defaults to `<spelling>`" (`:468`, `:542`, `:1154`, `:1158`); and the three "default" cells of the new layout table, which is where a default belongs. No path in the README's storage sections is stated as safix's own layout any more. There is no documentation check in `modules/flake/checks/` — confirmed by grep — so this one is read rather than built, and `cli.nix:486-492`'s prose is the closest mechanical cover, named in 10.10

## 13. Spec reconciliation and whole-change verification

- [ ] 13.1 Confirm the four delta specs in this change match what landed: `public-outputs` (the three options, the two refusals, the rewritten separability requirement, the derived probes), `secret-generators` (the record's single implementation and configured root), `secrets-vault` (the root-relative hash input), `recipient-policy` (the header's derived examples)
- [ ] 13.2 Confirm unedited, with the reason recorded: `openspec/specs/secret-custody/spec.md:39-53` (audience-named directory, and its vault-mode exception) and `openspec/specs/recipient-policy/spec.md:75-90` (the anchoring requirements) are already root-agnostic — neither names a prefix — so neither needs a delta
- [ ] 13.3 `openspec validate configurable-storage-roots --strict` green (the CLI takes the change name positionally; `--change` is not a flag it accepts)
- [ ] 13.4 Verify: `nix flake check` green in full
- [ ] 13.5 Severity drill roll-up: re-run the drills in 1.15, 1.16, 2.8, 3.13, 4.8, 5.7, 6.7, 6.8, 7.12, 7.13, 8.7 and 11.9 against the landed tree and record each one's observed red/green transition in the check file's drill commentary, the way `modules/flake/checks/vault.nix` records its own. A drill that does not reproduce is a finding, not a formality: record why, as `vault.nix:4.9` does
