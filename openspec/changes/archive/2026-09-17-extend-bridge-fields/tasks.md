# Tasks: extend-bridge-fields

Citations are as read while designing this change, on 2026-09-16; re-read the named lines before editing, since line numbers drift.
No real secret name, user name, recipient or database enters this repository; fixtures use `alice`, `bob` and `carol`, synthetic `age1` strings, and a database under the fixture's own scratch directory, matching the existing integration fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
The decisions this change rests on are `design.md`'s F1 through F10; each group names the ones it realizes.
Three sibling changes — `add-pass-bridge`, `add-bitwarden-bridge`, `add-onepassword-bridge` — consume `modules/flake/safix/fields.nix`, `modules/flake/safix/reserved.nix` and `crates/safix-core/src/endpoint.rs` and must not edit them; `rewrite-readme-and-examples` owns `README.md` and `examples/`, so neither is touched here.

## 1. The reserved mapping words, in one place (F8)

- [x] 1.1 Create `modules/flake/safix/reserved.nix` taking no arguments and evaluating to `{ ids = [ "clan" "keepassxc" "pass" "bitwarden" "1password" "all" ]; }`, with a module doc comment stating: that these are the words `sync` and `audit` read as a target keyword rather than a mapping name; that the list is complete before three of the targets answer to it, and why (F8 — three changes each appending one word would move the same equality check three times); that `op` is deliberately absent because one spelling per target is the rule and `1password` is it; and that `crates/safix-core/src/bridge.rs`'s `RESERVED_MAPPING_WORDS` carries the same words in the same order, held equal by `checks.safix-reserved-words` — created, argumentless, with all four paragraphs of the doc comment
- [x] 1.2 Replace the inline literal in `modules/flake/safix/bridge.nix:421-430`'s `reservedId` with `(import ./reserved.nix).ids`, leaving the sentence it produces byte for byte as it is — the list is what was duplicated, the sentence names this module's own option path and stays here (F8) — `reserved = import ./reserved.nix;` added to the `let`, `builtins.elem m.id reserved.ids`, sentence unedited
- [x] 1.3 Replace the inline literal in `modules/flake/safix/keepassxc.nix:218-227`'s `reservedId` the same way, again leaving its sentence unedited — same shape, sentence unedited; `checks.safix-keepassxc`'s three `reservedIdMessages` literals still pass
- [x] 1.4 Widen `crates/safix-core/src/bridge.rs:89`'s `pub const RESERVED_MAPPING_WORDS: [&str; 3] = ["clan", "keepassxc", "all"]` to `[&str; 6] = ["clan", "keepassxc", "pass", "bitwarden", "1password", "all"]`, and extend its doc comment (`bridge.rs:84-88`) to name `modules/flake/safix/reserved.nix` as the one declaration of the list and `checks.safix-reserved-words` as what holds the two equal — widened to `[&str; 6]`; the doc comment now names `reserved.nix`, `checks.safix-reserved-words`, and why three of the words land before their targets
- [x] 1.5 Add a unit test in `crates/safix-core/src/bridge.rs`'s `mod tests` named `the_reserved_words_are_the_six_the_declaration_carries` asserting the array equals the six literals in order — a literal-against-literal assertion, so the nix half is what the check in task 13.1 compares against and this holds only that the Rust half was not reordered — added at the head of `mod tests`; `cargo test -p safix-core --lib -- bridge::tests::the_reserved_words` reported 1 passed
- [x] 1.6 Severity drill: reordering two words in `reserved.nix` without reordering the Rust array turns `checks.safix-reserved-words` (task 13.1) red while every Rust test stays green, which is the evidence the equality is held across the boundary rather than inside one side — performed by renaming `"pass"` to `"PASSDRILL"` in `reserved.nix` alone: `nix build .#checks.x86_64-linux.safix-reserved-words` failed with "the two halves of the reserved mapping word list disagree", and the Rust suite is untouched by a nix edit by construction
- [x] 1.7 Verify: `cargo test -p safix-core --lib bridge::tests` green, and `nix eval .#safix.lib 2>/dev/null` is not the check — the nix half is verified by task 13.1's build — `cargo test -p safix-core --lib` green, 303 passed 0 failed, `bridge::tests` among them; the nix half verified by `nix build .#checks.x86_64-linux.safix-reserved-words`

## 2. The shared field submodule and the capability table (F1, F6)

- [x] 2.1 Create `modules/flake/safix/fields.nix` as `{ lib }:` with a module doc comment stating: that a field is metadata carried beside the value on the far side of a mapping and never on `safixSide` (F2, with the reason — a safix entry is a file, a key inside it and an audience); that a literal is not a secret because the declaration is evaluated into a world-readable store and an `{ entry = …; }` reference is one; and that each target refuses what it cannot carry in its own `violationsOf`, reading the table this file exports — created; the doc comment carries the far-side-only paragraph with its reason, the literal-is-not-a-secret paragraph, and the per-target-refusal paragraph
- [x] 2.2 In `fields.nix`, define `fieldValue = lib.types.either lib.types.str (lib.types.submodule { options.entry = lib.mkOption { type = lib.types.str; description = "…"; }; })`, whose description states that the named entry belongs to the mapping's own person and is decrypted when the mapping is converged, never at evaluation — defined; the description also states why a resolved value cannot travel an argument vector
- [x] 2.3 In `fields.nix`, define `fields = lib.types.submodule { options = { username = nullOr fieldValue, default null; url = same; notes = same; tags = listOf fieldValue, default [ ]; }; }`, each option carrying an `example` and a description naming what the field is for and that a target which cannot carry it refuses the declaration — defined; the three scalars are built by a local `scalar` helper so the shared half of the description is written once, and `tags` carries its own because its refusal names keepassxc
- [x] 2.4 In `fields.nix`, define `fieldNames = [ "username" "url" "notes" "tags" ]` — the fixed order every report and every diff uses — and `channels`, an attribute set keyed by target name carrying one of `"argv"`, `"stdin"` or `"unsupported"` per field: `keepassxc = { username = "argv"; url = "argv"; notes = "argv"; tags = "unsupported"; }`, `pass`/`bitwarden`/`onepassword` = all `"stdin"` except `bitwarden.tags = "unsupported"`, and `clan = { username = "unsupported"; url = "unsupported"; notes = "unsupported"; tags = "unsupported"; }` — both defined, five rows exactly as stated
- [x] 2.5 Give `channels` a comment recording where each row was verified: `keepassxc-cli add/edit` take `-u/--username`, `--url` and `--notes` in argv and have no tags flag and no custom-attribute write, read off `keepassxc-cli add --help` on 2026-09-16; the three stdin rows are the sibling changes' own evidence and are declared here so that the table is one artifact rather than four — comment records all five rows' provenance, including why `bitwarden.tags` differs from the other two pipe rows and why clan carries a row at all
- [x] 2.6 Export `{ inherit fieldValue fields fieldNames channels; }` from `fields.nix` and nothing else — no message builder, for F8's reason: a refusal's wording belongs beside the option path it names — those four and nothing else
- [x] 2.7 Add `fields = import ./fields.nix { inherit lib; };` to `modules/flake/safix/default.nix`'s lib imports beside `bridge` and `keepassxc` (`default.nix:19-20`), and confirm nothing new needs to reach `lib.bridge`/`lib.keepassxc` (`default.nix:325-345`) because `fields` is a type and a table rather than a resolved value — confirmed, and **deviation**: the import is *not* added. Every module in this tree imports the libs it uses itself — `options.nix` imports `bridge.nix` and `keepassxc.nix` for itself rather than receiving them — and `keepassxc.nix` (task 3.3) and `modules/flake/checks/keepassxc.nix` (task 13.5) do exactly that with `fields.nix`. `default.nix` reads nothing from it, so the binding would be a `let` nothing consumes; the second clause of this task is the reason, and a dead binding is the thing this repository deletes on sight
- [x] 2.8 Severity drill: changing `channels.keepassxc.tags` from `"unsupported"` to `"argv"` turns the `tagsRefusedMessages` field of `modules/flake/checks/keepassxc.nix` (task 13.3) empty, which is the evidence the refusal is driven by the table rather than by a hard-coded field name — performed: `nix build .#checks.x86_64-linux.safix-keepassxc` failed, the diff showing `"tags":"argv"` in `capabilities` and `bothFaultsMessages` down to three entries with the tags sentence gone; `tagsRefusedMessages` empties with it, since both read the same rule
- [x] 2.9 Verify: `nix build .#checks.x86_64-linux.safix-keepassxc` green after group 3, and the drill in 2.8 observed — green together with `safix-keepassxc-drill` and `safix-reserved-words`; drill 2.8 observed

## 3. keepassxc's nix half: migrate `username`, refuse what it cannot carry (F6, F7)

- [x] 3.1 Delete `modules/flake/safix/keepassxc.nix:74-87`'s `username` option in full, including its description — the wall this change moves, quoted in the proposal — deleted whole, description included
- [x] 3.2 Add `fields = lib.mkOption { type = fields.fields; default = { }; description = … }` to `kdbxSide` (`keepassxc.nix:57-89`), whose description names the four fields, states that `tags` is refused for this target and why, states that an `{ entry = …; }` source is refused for this target and why (its only channel is an argument vector and a referenced value is a secret), and carries the migration sentence naming the option it replaces — added in its place, with all four parts of the description
- [x] 3.3 Add `fields = import ./fields.nix { inherit lib; };` to `keepassxc.nix`'s `let` beside `bridge` (`keepassxc.nix:23-24`) — added beside `bridge`, together with `reserved` (task 1.3) and the `capabilities` binding (task 3.8)
- [x] 3.4 Update `kdbx`'s own description (`keepassxc.nix:126-136`) from "a path under the declared group, and optionally a username" to name the path and the fields, keeping its "Evaluation verifies neither" paragraph unedited — now "a path under the declared group, and the fields the entry carries beside its value"; the second paragraph is byte-identical
- [x] 3.5 Add `fieldUnsupported` to `keepassxc.nix`'s `violationsOf` (`keepassxc.nix:151-232`): for each declared mapping and each name in `fields.fieldNames` whose channel in `fields.channels.keepassxc` is `"unsupported"` and which the mapping declares a value for (non-null, or a non-empty list for `tags`), the sentence `flake.safix.keepassxc.mappings.<id> declares the field '<field>', and keepassxc cannot carry it: the store's own command has no way to write it, so accepting the declaration would mean writing less than it says` — added, with a local `declaresField` helper carrying the non-null/non-empty-list distinction; the sentence is asserted against a literal by `tagsRefusedMessages`
- [x] 3.6 Add `fieldSourceInArgv` to the same function: for each declared mapping and each field whose channel is `"argv"` and whose declared value is an attribute set (the `{ entry = …; }` form, told apart with `lib.isAttrs`), the sentence `flake.safix.keepassxc.mappings.<id> sources the field '<field>' from the entry '<entry>', and keepassxc carries that field in an argument vector, where a secret value may not travel` — added; `lib.isAttrs` confirmed present in the pinned lib before use, and a `tags` list is not an attribute set so the one rule covers all four fields; asserted by `entrySourcedFieldMessages`
- [x] 3.7 Judge both new rules over `declared` rather than over `sound`, matching `reservedName`'s treatment (`keepassxc.nix:205-211`) and its stated reason, so a mapping with two faults hears about both; append them to the returned list at `keepassxc.nix:232` in the order `… ++ reservedName ++ reservedId ++ fieldUnsupported ++ fieldSourceInArgv` — both judged over `declared`, appended in that order; the list is now written one `++` per line
- [x] 3.8 Add `capabilities = fields.channels.keepassxc;` to `keepassxc.nix`'s export list (`keepassxc.nix:234-245`), so `modules/flake/checks/keepassxc.nix` can assert the table this module refuses on is the shared one — bound in the `let` and exported; the rules above read it through `fields.channels.keepassxc` so the binding and the rules cannot disagree
- [x] 3.9 Replace `username = "alice@example.com";` in the `mappings` example at `modules/flake/safix/options.nix:596-603` with a `fields = { username = "alice@example.com"; url = "https://grafana.example.com"; };` block — an option's own `example`, which stops evaluating the moment the option is gone, and therefore this change's rather than the documentation change's — replaced
- [x] 3.10 Confirm `modules/flake/safix/bridge.nix` gains no field surface at all, and state that in the `clanSide` description (`bridge.nix:45-101`): a clan var is a file's bytes and clan's command offers no field beside it, so this target declares none — the `bridge-transfer` delta's "A clan mapping has no field surface" requirement — confirmed: `bridge.nix`'s only edits are `reserved = import ./reserved.nix;` and the `reservedId` body. The sentence went on the `clan` option's own description (`mapping`'s `clan = lib.mkOption { type = clanSide; … }`) rather than inside the `clanSide` submodule, because a submodule type carries no description of its own — the option that uses it does
- [x] 3.11 Severity drill: judging `fieldUnsupported` over `sound` instead of `declared` empties the field half of a two-fault fixture in `modules/flake/checks/keepassxc.nix`, which is the same drill `reservedName` already carries and the evidence the ordering claim is live — performed: `nix build .#checks.x86_64-linux.safix-keepassxc` reported `structural check 'safix-keepassxc' failed`, with `tagsRefusedMessages` still full (alice resolves) and `bothFaultsMessages` short its tags sentence, which is exactly the half the claim is about
- [x] 3.12 Verify: `nix build .#checks.x86_64-linux.safix-keepassxc .#checks.x86_64-linux.safix-keepassxc-drill` green, drills in 2.8 and 3.11 observed — both green, both drills observed

## 4. The Rust model: `Fields`, `FieldValue`, and `KdbxSide` (F1, F7)

- [x] 4.1 Add `FieldValue` to `crates/safix-core/src/model.rs` beside `KdbxSide` (`model.rs:900-910`) as `#[derive(Debug, Clone, Deserialize)] #[serde(untagged)] pub enum FieldValue { Literal(String), Entry { entry: String } }`, with a doc comment stating that a literal came from a world-readable declaration and an `Entry` names a secret to be read at run time — added exactly as stated
- [x] 4.2 Add `Fields` as `#[derive(Debug, Clone, Default, Deserialize)] #[serde(deny_unknown_fields)] pub struct Fields { pub username: Option<FieldValue>, pub url: Option<FieldValue>, pub notes: Option<FieldValue>, #[serde(default)] pub tags: Vec<FieldValue> }`, with a doc comment naming `modules/flake/safix/fields.nix` as the other half of the coupling `model.rs:1-15` describes — added exactly as stated
- [x] 4.3 Add `Fields::declares(&self) -> bool` (any scalar is `Some` or `tags` is non-empty) and `Fields::named(&self) -> Vec<(&'static str, &FieldValue)>` returning the declared fields in `fieldNames` order, with `tags` yielding one pair per element — the order every report and every diff uses — both added; the order is asserted by `a_field_is_a_literal_or_a_named_entry_and_nothing_else`
- [x] 4.4 Replace `KdbxSide::username: Option<String>` (`model.rs:908-909`) with `pub fields: Fields`, keeping `path` and `#[serde(deny_unknown_fields)]`, and update the struct's doc comment (`model.rs:900-901`) from "what to call its user" to "and what it carries beside the value" — replaced, doc comment updated, and no `#[serde(default)]` on `fields`: the nix half always emits it (the option's own default is `{ }`), so tolerating its absence would weaken the coupling for nothing
- [x] 4.5 Fix the fixture JSON in `crates/safix-core/src/sync.rs`'s `mod tests` (`sync.rs:665-681`), replacing `"kdbx": { "path": "alice/grafana", "username": null }` with `"kdbx": { "path": "alice/grafana", "fields": {} }` — done by BridgeSync, who owns `sync.rs` in this wave; agreed over hub so the file had one editor
- [x] 4.6 Add a unit test in `model.rs`'s `mod tests` named `a_field_is_a_literal_or_a_named_entry_and_nothing_else` deserialising `{"username": "alice@example", "url": {"entry": "grafana-url"}, "notes": null, "tags": []}` and asserting the two shapes land in the two variants, plus a second assertion that `{"username": {"name": "x"}}` fails to deserialise — added, plus the `named()` order assertion
- [x] 4.7 Add a unit test named `an_unknown_field_name_is_refused_by_the_model` asserting `{"path": "a/b", "fields": {"pin": "1234"}}` fails to deserialise as a `KdbxSide`, which is the coupling `model.rs:1-15` states and the mechanism that makes task 3.1's deletion a clean cutover — added, and it also asserts that `{"path": "a/b", "username": "alice"}` is refused and `{"path": "a/b", "fields": {}}` accepted, so the cutover's two directions are both held. `MIRROR` migrated to the new shape in the same file
- [x] 4.8 Severity drill: adding `#[serde(default)]` to `Fields`'s scalar options changes nothing observable and must be rejected on review rather than by a test; instead drill the real claim by deleting `#[serde(deny_unknown_fields)]` from `Fields` and observing 4.7 turn red — performed: with the attribute deleted, `cargo test -p safix-core --lib -- model::tests::an_unknown_field_name` reported 0 passed, 1 failed
- [x] 4.9 Verify: `cargo test -p safix-core --lib model::tests` green, drill in 4.8 observed — green; the whole `cargo test -p safix-core --lib` is 303 passed, 0 failed

## 5. The endpoint contract: one record, one capability table, one judge (F5, F9)

- [x] 5.1 Create `crates/safix-core/src/endpoint.rs` with a module doc comment stating: what a far side is asked (unlock, read, write, list, capabilities) and nothing else; that every refusal only one far side can raise stays outside the trait, listing the six from the `bridge-sync` delta with the file and function each lives in; and that this module exists to delete `sync::two_way` and `bridge_sync::judge` rather than to anticipate a target — created; the doc comment lists all six as a bulleted list, each naming where it lives
- [x] 5.2 Add `mod endpoint;` to `crates/safix-core/src/lib.rs` in alphabetical position, `pub` at the same visibility `sync` and `store` have — `pub mod endpoint;` between `edit` and `enroll`
- [x] 5.3 Add `pub enum Channel { Argv, Stdin, Unsupported }` and `pub enum FieldName { Username, Url, Notes, Tags }` with `FieldName::as_str` returning `"username"`, `"url"`, `"notes"`, `"tags"` and `FieldName::ALL` in that order — the same order `fields.nix`'s `fieldNames` carries — added; `as_str` takes `self` rather than `&self` because the type is `Copy` and `clippy::trivially_copy_pass_by_ref` is in the denied pedantic group
- [x] 5.4 Add `pub struct Capabilities { pub username: Channel, pub url: Channel, pub notes: Channel, pub tags: Channel, pub multiline: bool }` with `Capabilities::channel(&self, FieldName) -> Channel`, and a doc comment stating that `multiline` is where a value-shape refusal comes from, so `Error::ValueSpansLines` is a declared property of a transport rather than a special case — added; deliberately not `Copy`, and the doc comment says why: a five-byte `Copy` struct taken by reference is what `clippy::trivially_copy_pass_by_ref` reports, and the signature this task fixes is `&self`
- [x] 5.5 Add `pub enum Field { Literal(String), Resolved(Secret) }` and `Field::equals(&self, other: &Field) -> bool`, comparing by materialising a `Literal` through `Secret::read_from` and never by extracting bytes from a `Resolved` — the direction is one-way by construction, which is F1's whole point — added; the mixed arm is the only one that materialises, and it materialises the literal
- [x] 5.6 Add `pub struct ResolvedFields { pub username: Option<Field>, pub url: Option<Field>, pub notes: Option<Field>, pub tags: Vec<Field> }` with `Default`, `ResolvedFields::declares`, `ResolvedFields::wanted(&self) -> Vec<FieldName>` (the declared scalar fields plus `Tags` when non-empty) and `ResolvedFields::diff(&self, held: &ResolvedFields) -> Vec<&'static str>` returning the names of the declared fields the held side does not match, in `FieldName::ALL` order — all four added; `diff` iterates `FieldName::ALL` so the order is the array's rather than the struct's
- [x] 5.7 Give `diff` this exact semantics and state it in the doc comment: a field the declaration does not name is never compared and never reported, because safix declares nothing about it and an entry's own username is the operator's until a declaration claims it; `tags` compares as an ordered list and a length difference is a difference — stated in the doc comment and held by `a_field_the_declaration_does_not_name_is_never_compared` and `an_ordered_tag_list_differs_by_order`
- [x] 5.8 Add `pub struct Record { pub value: Option<Secret>, pub fields: ResolvedFields }` and `pub enum Existing { Absent, Present }` — both added; `Existing`'s doc comment records that it is passed rather than recomputed
- [x] 5.9 Add `pub enum Verdict { Unchanged, Conflict, PushValue { remember: bool }, PullValue { remember: bool } }` and `pub fn judge(ours: Option<&Secret>, theirs: Option<&Secret>, remembered: Option<&Secret>, agrees: &dyn Fn(&Secret, &Secret) -> bool) -> Verdict`, whose body is `sync.rs:459-492`'s five arms verbatim with the `Decision` constructors replaced by `Verdict` ones, and whose doc comment states why `agrees` is a parameter (F5 — the two targets' format tags are deliberately distinct, `bridge.rs:633-639`) — added; the five arms and both of `two_way`'s inline comments are carried over verbatim, and the doc comment names both format tags
- [x] 5.10 Add `pub trait Endpoint { fn unlock(&mut self) -> Result<()>; fn read(&self, id: &str, declared: &ResolvedFields) -> Result<Option<Record>>; fn write(&mut self, id: &str, record: &Record, existing: Existing) -> Result<()>; fn list(&self) -> Result<Vec<String>>; fn capabilities(&self) -> Capabilities; }`, with `read`'s doc comment stating that `declared` is what decides which attributes are asked for and whether a second invocation happens at all (F3) — added with that signature; every fallible method carries an `# Errors` section, which the denied `clippy::missing_errors_doc` requires
- [x] 5.11 Add `pub fn resolve_fields(workspace: &Workspace, target: &'static str, user: &str, declared: &Fields, capabilities: &Capabilities) -> Result<ResolvedFields>`: per declared field, `Channel::Unsupported` raises `Error::FieldUnsupported { target, field }`; a `FieldValue::Entry` on a `Channel::Argv` field raises `Error::FieldSourceInArgv { target, field }`; a `FieldValue::Literal` becomes `Field::Literal`; a `FieldValue::Entry` on a `Channel::Stdin` field is read through `bridge::held_by_safix(workspace, target, user, &entry)` and becomes `Field::Resolved`, with an absent entry raising `Error::UnknownName` — added; the four arms are one exhaustive match on `(channel, value)` in a private `resolve_one`. An entry the person does not hold raises `Error::UnknownName` through the resolver `held_by_safix` reads the placement with, which is where that refusal already lives; an entry they hold that has never been written is the other absence and raises `Error::NoValueYet`, whose remedy sentence is the right one for it. Both are named in the `# Errors` section
- [x] 5.12 Give `resolve_fields` a doc comment recording that the `Stdin` arm has no production caller in this change, that its first one is `add-pass-bridge`, and that shipping the refusal without the resolution would make the refusal a statement about nothing (F9) — stated, in those terms
- [x] 5.13 Add unit tests in `endpoint.rs`: `judge_is_the_decision_sync_and_the_bridge_both_reached` over the five arms with a stub `agrees`; `a_field_the_declaration_does_not_name_is_never_compared`; `an_ordered_tag_list_differs_by_order`; `a_literal_and_a_read_value_compare_without_leaving_the_secret_type`; `an_unsupported_field_is_refused_naming_the_target_and_the_field`; `an_entry_source_on_an_argv_channel_is_refused`; and `an_entry_source_on_a_stdin_channel_resolves` over a fixture `Capabilities` whose four channels are all `Stdin` — all seven added, plus 5.16's sibling `a_diff_names_the_drifted_fields_in_one_fixed_order`. Every refusal test calls the real `resolve_fields` against a `Workspace::at` over a path that is not a flake, which is itself part of the claim: both refusals are reached with no repository, no key and no database in reach. `an_entry_source_on_a_stdin_channel_resolves` asserts that the arm reaches the reader rather than either refusal (the read cannot succeed without a fleet), and that a literal on the same all-pipes table resolves
- [x] 5.14 Delete `crates/safix-core/src/sync.rs:451-493`'s `two_way` and `crates/safix-core/src/bridge.rs:794-835`'s `judge`, rather than leaving either as a wrapper; `sync.rs:512-515`'s `agrees` and `bridge.rs:864-873`'s `agrees` stay where they are and become the closures passed in — done by BridgeSync, who owns both call sites in this wave; confirmed by grep: neither `fn two_way` nor `bridge_sync`'s `fn judge` exists, and both `agrees` remain
- [x] 5.15 Severity drill: changing one arm of `endpoint::judge` — making `(Some, Some)` with no memory a `PushValue` instead of a `Conflict` — must turn 5.13's judge test red *and* turn `crates/safix/tests/sync_path.rs`'s two-way conflict claim and the bridge two-way conflict claim red, which is the evidence both targets now reach the same function — performed: `cargo test -p safix-core --lib` reported 301 passed, 2 failed — `endpoint::tests::judge_is_the_decision_sync_and_the_bridge_both_reached` and `bridge::bridge_sync::tests::disagreeing_with_no_agreement_recorded_yet_is_a_conflict`. The integration half (`sync_path.rs`) is BridgeSync's to record; the unit-level bridge failure is already the cross-target evidence the claim is about
- [x] 5.16 Severity drill: reordering `FieldName::ALL` turns `an_ordered_tag_list_differs_by_order`'s sibling — a `diff` test asserting the returned names' order — red, which is the evidence the report's field order is fixed rather than incidental — performed by moving `Notes` to the head of `ALL`: `a_diff_names_the_drifted_fields_in_one_fixed_order` failed and the other seven `endpoint::` tests passed
- [x] 5.17 Verify: `cargo test -p safix-core --lib endpoint::` green, drills in 5.15 and 5.16 observed — 8 passed, 0 failed; both drills observed

## 6. The two refusals: variants, prose, codes, samples, snapshots (F6)

- [x] 6.1 Add `Error::FieldUnsupported { target: &'static str, field: &'static str }` to `crates/safix-core/src/error/mod.rs` beside the store variants (`mod.rs:1219-1258`), as `#[error("{}", prose::field_unsupported(target, field))]` — added directly after `ValueSpansLines`
- [x] 6.2 Add `Error::FieldSourceInArgv { target: &'static str, field: &'static str, entry: String }` the same way, carrying the entry name because the refusal names which declaration to edit — added after it
- [x] 6.3 Add `prose::field_unsupported(target, field)` to `crates/safix-core/src/error/prose.rs` beside `value_spans_lines` (`prose.rs:750-758`): the target cannot carry this field, the store's own command has no way to write it, nothing was written, and the remedy is to remove the field from the mapping — wrapped at the same column the neighbouring sentences use — added directly after `value_spans_lines`
- [x] 6.4 Add `prose::field_source_in_argv(target, field, entry)`: this field is carried in an argument vector on this target, the named entry's value is a secret, a secret value travels standard input or a pipe, nothing was written, and the two remedies are a literal in the declaration or a target whose channel for that field is a pipe — added, with all five parts
- [x] 6.5 Add `FieldUnsupported => "safix::field_unsupported"` and `FieldSourceInArgv => "safix::field_source_in_argv"` to the table in `crates/safix-core/src/error/code.rs:92-210`, positioned after `ValueSpansLines` so the table's order follows the variants' — added in that position
- [x] 6.6 Add the two sample arms to `crates/safix/src/reporter.rs`'s match (`reporter.rs:251-675`, beside `Code::ValueSpansLines` at `:580`): `FieldUnsupported { target: "keepassxc", field: "tags" }` and `FieldSourceInArgv { target: "keepassxc", field: "notes", entry: "grafana-note".into() }` — added beside `Code::ValueSpansLines`, with those exact samples
- [x] 6.7 Accept four snapshots under `crates/safix/src/snapshots/`: `safix__reporter__tests__graphical-field_unsupported.snap`, `…plain-field_unsupported.snap`, `…graphical-field_source_in_argv.snap`, `…plain-field_source_in_argv.snap` — all four written and accepted; `cargo test -p safix --bin safix -- reporter::` then green with no `.snap.new` left behind
- [x] 6.8 Severity drill: deleting either table entry from `code.rs` fails the build rather than a test, because `reporter.rs`'s match over `Code` is exhaustive — perform it once and record that the compiler is the check here, which is why no test asserts the mapping — performed on `FieldUnsupported`: the build failed at `code.rs:68` with `error[E0004]: non-exhaustive patterns: &error::Error::FieldUnsupported { .. } not covered`. The check fires one level earlier than the task expected — `code.rs`'s own match over `Error` is exhaustive too, so the table cannot lose a row even before `reporter.rs` is compiled. The compiler is the check either way
- [x] 6.9 Severity drill: changing one word of `prose::field_unsupported` turns exactly the two `field_unsupported` snapshots red and leaves the two `field_source_in_argv` ones green — performed by changing "Remove the field" to "Delete the field": `every_refusal_renders_the_same_under_both_reporters` failed with a snapshot assertion against `plain-field_unsupported`, and no `field_source_in_argv` snapshot was reported (insta stops at the first mismatch within the test, so the graphical sibling is reached only once the plain one is accepted)
- [x] 6.10 Verify: `cargo test -p safix --bin safix -- reporter::` green, drills in 6.8 and 6.9 observed — 2 passed, 0 failed; both drills observed

## 7. The keepassxc transport: the fields read, the fields write, the capabilities (F3, F6)

- [x] 7.1 Add `pub fn fields_arguments(database: &Path, entry: &str, wanted: &[FieldName], yubikey: Option<&Yubikey>, key_file: Option<&str>) -> Vec<String>` to `crates/safix-core/src/store.rs` beside `read_arguments` (`store.rs:426-449`), producing `show --quiet <composite key args> --attributes <Attr> …  <database> <entry>` with `UserName`, `URL` and `Notes` as the attribute spellings, in `FieldName::ALL` order, and **neither** `--show-protected` **nor** `Password` — `store::fields_arguments` added beside `read_arguments`, `UserName`/`URL`/`Notes` in `FieldName::ALL` order, no `--show-protected` and no `Password`.
- [x] 7.2 Give `fields_arguments` a doc comment stating that the absence of `--show-protected` and of `Password` is what makes it impossible for a value to return on this pipe, and that it is a second invocation rather than a widening of `read_arguments` for the reason `store.rs:426-431` records — Doc comment records both absences as the property and why this is a second invocation rather than a widened `read_arguments`.
- [x] 7.3 Add `Database::read_fields(&self, entry: &str, declared: &ResolvedFields) -> Result<ResolvedFields>`: return `ResolvedFields::default()` when `declared.wanted()` is empty; otherwise spawn `fields_arguments`, write the database password on standard input the way `read` does (`store.rs:215-222`), read standard output, split it on newlines into one `Field::Resolved(Secret)` per requested attribute in the order they were asked for, and raise `Error::StoreCommandFailed` on a non-zero status exactly as `read` does — `Database::read_fields` added: returns `ResolvedFields::default()` on an empty `wanted()`, otherwise spawns, feeds the password as `read` does, and raises `StoreCommandFailed` on a non-zero status.
- [x] 7.4 Make `read_fields` tolerate a shorter answer than it asked for by treating a missing trailing line as an absent field rather than an error, and state in the doc comment that a field the entry does not carry prints an empty line, so an empty line is an absent field and a diff against a declared value is therefore a divergence — A short answer breaks out of the loop and an empty line is skipped, both documented as an absent field.
- [x] 7.5 Change `Database::write`'s signature (`store.rs:255`) from `username: Option<&str>` to `fields: &ResolvedFields`, and change its `ValueSpansLines` guard (`store.rs:256-260`) to read `if !self.capabilities().multiline && value.spans_lines()`, leaving the refusal, its data and its sentence unedited — `Database::write` takes `&ResolvedFields`; the guard reads `!self.capabilities().multiline && value.spans_lines()`; refusal, data and sentence unedited. One private `write_knowing` is shared with `Endpoint::write` so there is still one `add`/`edit` decision.
- [x] 7.6 Change `write_arguments`'s signature (`store.rs:458-479`) from `username: Option<&str>` to `fields: &ResolvedFields`, appending `--username <v>`, `--url <v>` and `--notes <v>` for each declared `Field::Literal` in `FieldName::ALL` order, before the database and the entry; a `Field::Resolved` is unreachable here and must be handled by `unreachable!` carrying the reason (`resolve_fields` refused it, F6) rather than by silently skipping it — `write_arguments` appends `--username`/`--url`/`--notes` per declared literal in `FieldName::ALL` order; `Field::Resolved` and a non-empty `tags` are `unreachable!` carrying the reason.
- [x] 7.7 Add `Database::capabilities(&self) -> Capabilities` returning `username`/`url`/`notes` as `Channel::Argv`, `tags` as `Channel::Unsupported` and `multiline: false`, with a doc comment naming `modules/flake/safix/fields.nix`'s `channels.keepassxc` as the other half and `keepassxc-cli add --help` as where it was verified — `Database::capabilities` added, doc comment naming `fields.nix`'s `channels.keepassxc` and `keepassxc-cli add --help`.
- [x] 7.8 Implement `Endpoint for Database`: `unlock` is a no-op returning `Ok(())` with a doc comment stating that this transport's unlock happens in `Database::open` because the password is read once before the listing that proves the database opened (`store.rs:132-160`), and that moving it here would mean an endpoint that exists before it can be read from; `read` performs `read` then `read_fields`; `write` takes `Existing` from its caller rather than recomputing `holds`; `list` is `under(&group)`; `capabilities` is 7.7 — `impl Endpoint for Database` added; `unlock` is the documented no-op, `read` performs `read` then `read_fields`, `write` takes `Existing` from its caller, `list` is `under(group)`. Deviation: `Database` gained a private `group: String` set from `mirror.group` in `open`, because `Endpoint::list` is asked without a group argument.
- [x] 7.9 Update the three `Database::write` callers passing `None` today — `sync.rs:619` and `sync.rs:644` (the companion writes) — to pass `&ResolvedFields::default()`, and state in `remembered_after`'s doc comment that a companion carries no field because it is safix's own bookkeeping rather than a mirrored entry — Both companion writes in `sync.rs` pass `&ResolvedFields::default()`; `remembered_after`'s doc comment states why a companion carries no field. (Also migrated: `crates/safix/tests/store_cli.rs`'s three `write_arguments` callers, which the signature change reached.)
- [x] 7.10 Update `store.rs`'s unit tests: `a_username_reaches_argv_and_its_absence_leaves_the_field_alone` (`store.rs:651-682`) becomes `the_declared_literal_fields_reach_argv_and_an_undeclared_one_leaves_the_field_alone`, asserting `--username`, `--url` and `--notes` appear for declared literals, that none appears for an empty `ResolvedFields`, and that the composite-key arguments still ride alongside; `no_argument_vector_can_carry_a_value` (`store.rs:543`) gains a `ResolvedFields` carrying all three literals and still asserts no value among the arguments — Renamed to `the_declared_literal_fields_reach_argv_and_an_undeclared_one_leaves_the_field_alone`, asserting the whole vector for three literals, no flag for an empty `ResolvedFields`, and the composite-key factors alongside; `no_argument_vector_can_carry_a_value` now carries all three literals.
- [x] 7.11 Add a `store.rs` unit test named `the_fields_read_asks_for_the_named_attributes_and_never_the_password` asserting `fields_arguments`' output equals a literal argument vector and contains neither `--show-protected` nor `Password` — Added; asserts the literal vector and the absence of both `--show-protected` and `Password`.
- [x] 7.12 Add a `store.rs` unit test named `a_mapping_declaring_no_field_issues_no_fields_read` asserting `read_fields` over a default `ResolvedFields` returns without spawning, against a `Database` whose `program` is `safix-no-such-store-command` — the same absent-command fixture `a_read_of_an_entry_the_listing_does_not_carry_is_absence_rather_than_a_refusal` (`store.rs:736`) uses — Added against the `safix-no-such-store-command` fixture.
- [x] 7.13 Severity drill: adding `--show-protected` to `fields_arguments` turns 7.11 red on that one assertion while `the_read_asks_for_the_protected_password_attribute_and_nothing_else` (`store.rs:685`) stays green, which is the evidence the two invocations are held separately — Observed: `--show-protected` added to `fields_arguments` fails `the_fields_read_asks_for_the_named_attributes_and_never_the_password` on its equality assertion while `the_read_asks_for_the_protected_password_attribute_and_nothing_else` stays green (13 passed, 1 failed). Restored.
- [x] 7.14 Severity drill: making `read_fields` spawn unconditionally turns 7.12 red with the absent-command refusal, which is the evidence the no-declared-field path issues nothing rather than issuing and discarding — Observed: removing the empty-`wanted` return fails `a_mapping_declaring_no_field_issues_no_fields_read` with `StoreUnavailable { program: "safix-no-such-store-command" }`. Restored.
- [x] 7.15 Verify: `cargo test -p safix-core --lib store::` green, drills in 7.13 and 7.14 observed — `cargo test -p safix-core --lib store::` green (14 tests), drills 7.13 and 7.14 observed.

## 8. `sync`: the widened decision and the two new outcomes (F3, F10)

- [x] 8.1 Add `Outcome::FieldsUpdated(Vec<&'static str>)` and `Outcome::FieldsDiverged(Vec<&'static str>)` to `crates/safix-core/src/sync.rs:75-94`, each with a doc comment: the first is "the value already agreed and the declared fields were written", the second is "the declared fields differ and this mode does not write them" — Both variants added with their doc comments.
- [x] 8.2 Extend `Outcome::as_str` (`sync.rs:99-108`) with `"fields updated"` and `"fields diverged"`, and `Outcome::is_failure` (`sync.rs:116-118`) so that `FieldsDiverged` fails the run and `FieldsUpdated` does not — F10's reason, stated in the doc comment: a declared field that is not there is a declaration that is not true — `as_str` gains both words; `is_failure` gains `FieldsDiverged` with F10's reason in the doc comment.
- [x] 8.3 Add `fields_updated` and `fields_diverged` to `Tally` (`sync.rs:183-197`) and to `Report::tally`'s `count` calls (`sync.rs:162-179`), and extend `every_outcome_has_a_word_and_only_the_ones_needing_an_operator_fail_the_run` (`sync.rs:720-738`) with the two new rows — `Tally` and `Report::tally` gain both counters; the outcome table test gains both rows.
- [x] 8.4 Widen `Decision::Push` (`sync.rs:215-224`) to `Push { value: Secret, remember: bool, resolved: ResolvedFields, drifted: Vec<&'static str> }` and add no second push arm: a push that repairs a value and a push that repairs a field take the same arm, because they are the same single database write and a second arm would be a second write path to the same `add`/`edit`. `drifted` empty means the value is what moved; `drifted` non-empty with an agreeing value is what `push` turns into `Outcome::FieldsUpdated` — One push arm. Deviation: the four fields live in a `Pushing` struct the variant carries (`Decision::Push(Pushing)`) rather than inline on the variant, because `push` would otherwise take eight parameters and `clippy::too_many_arguments` is denied. `Pushing::outcome` is where `drifted` becomes the word.
- [x] 8.5 Rewrite `decide` (`sync.rs:378-443`) to resolve the mapping's declared fields once through `endpoint::resolve_fields(workspace, "keepassxc", &mapping.safix.user, &mapping.kdbx.fields, &database.capabilities())`, raising `Outcome::Refused(reason)` on either field refusal, then read the far side through `database.read(&entry)` and `database.read_fields(&entry, &declared)` — `decide` resolves once through `endpoint::resolve_fields(workspace, "keepassxc", …)` before either side is read, `Outcome::Refused` on a refusal, then reads the value and — only for an entry that is there — the fields.
- [x] 8.6 In `decide`, replace the `Mode::TwoWay` arm's `two_way(..)` call with `endpoint::judge(ours.as_ref(), theirs.as_ref(), recorded(database, &entry).as_ref(), &|remembered, value| agrees(remembered, value))` and map the `Verdict` onto the existing `Decision` arms — The two-way arm calls `endpoint::judge` with this mechanism's own `agrees` closure and maps the `Verdict`. Extracted into `sync::two_way` (a new, differently-shaped function; the old one is deleted) to stay under `clippy::too_many_lines`.
- [x] 8.7 In `decide`, after the per-mode value decision settles as `Unchanged`, compute `declared.diff(&held_fields)` and: under `Mode::SafixToKeepassxc` and `Mode::TwoWay`, and under `Mode::Backup` where the entry is absent, upgrade to `Decision::Push { value: ours, remember: <as the mode says>, resolved: declared, drifted: <the diff> }`; under `Mode::KeepassxcToSafix`, and under `Mode::Backup` where the entry exists, settle as `Outcome::FieldsDiverged(diff)`; leave `Unchanged` when the diff is empty — Held by `repairing_fields` (pushing modes, and two-way) and `diverging_fields` (`keepassxc-to-safix`, and `backup` over an existing entry); `backup` over an absent entry pushes the fields with the value. A two-way field repair records no agreement, per the `keepassxc-sync` scenario "A two-way mapping's fields have one author".
- [x] 8.8 State the precedence in `decide`'s doc comment and hold it in the code: a value divergence is decided first and a field divergence never displaces it, so a mapping whose value and fields both differ is `Updated` or `Conflict` and never `FieldsDiverged` (F10) — Stated in `decide`'s doc comment and held structurally: `drifted` is filled only in the arms whose two values already agree, so a value push carries an empty one and reports `updated`.
- [x] 8.9 Change `push` (`sync.rs:543-566`) to take the resolved fields and the diff, call `database.write(&entry, value, &fields)`, and return `Outcome::FieldsUpdated(diff)` when the value was already in agreement and the diff is non-empty, `Outcome::Updated` otherwise; its `log` line gains no field name, because the log names endpoints — `push` takes the `Pushing` and returns `Pushing::outcome()`; the log line gains no field name.
- [x] 8.10 Confirm `pull` (`sync.rs:570-624`) is unedited except for the companion write's new `&ResolvedFields::default()` argument, and state in its doc comment that a pull writes only the value because a field is a declaration and safix's side has nowhere to hold one (F2) — Confirmed by reading: `pull` is unedited but for the companion write's `&ResolvedFields::default()`, and its doc comment now states that only the value crosses and why.
- [x] 8.11 Add a `sync.rs` unit test named `a_field_drift_does_not_displace_a_value_divergence` over a fixture `ResolvedFields` and two differing values, asserting the decision is the value's — Added over `Pushing::outcome`, which is where the precedence lives; its doc comment names the integration half.
- [x] 8.12 Severity drill: dropping the `Mode::Backup`-with-an-existing-entry case from 8.7 — letting it push fields — turns the integration claim in 11.6 red, which is the evidence `backup` never overwrites a field either (F4) — Observed: letting `backup` over an existing entry push its fields fails `a_backup_mapping_never_overwrites_a_field_either`. Restored.
- [x] 8.13 Severity drill: making `FieldsDiverged` return `false` from `is_failure` turns the integration claim in 11.7 red on the exit status while every other sync test stays green — Observed: dropping `FieldsDiverged` from `is_failure` fails `a_backup_mapping_never_overwrites_a_field_either` on its `expect_refusal` (the exit status), while the rest of `sync_path` stays green. Note: that is the backup claim (11.6), not 11.7 as the task line said — 11.7 expects success. Restored.
- [x] 8.14 Verify: `cargo test -p safix-core --lib sync::` green, drills in 8.12 and 8.13 observed — `cargo test -p safix-core --lib sync::` green, drills 8.12 and 8.13 observed.

## 9. `audit`: the fourth outcome (F10)

- [x] 9.1 Add `KeepassxcOutcome::FieldsDiverged(Vec<&'static str>)` to `crates/safix-core/src/audit.rs:146-155`, with a doc comment stating that the value agrees and the named fields do not, and that the names are `&'static str` so no run-time string — and therefore no field content — can reach the report — Added with the `&'static str` reason in its doc comment.
- [x] 9.2 Extend `KeepassxcOutcome::as_str` (`audit.rs:157-167`) with `"fields diverged"`, and `KeepassxcReport::is_clean` (`audit.rs:199-210`) so that only `Agreeing` is clean — which it already is, since it matches on `Agreeing`; confirm that by reading it rather than editing it, and record the confirmation in the task — `as_str` gains `"fields diverged"`. `is_clean` confirmed by reading rather than edited: `audit.rs`'s `KeepassxcReport::is_clean` is `all(|entry| matches!(entry.outcome, KeepassxcOutcome::Agreeing))`, so the fourth outcome is unclean without an edit.
- [x] 9.3 Rewrite `compare_keepassxc` (`audit.rs:437-462`) to resolve the declared fields through `endpoint::resolve_fields` (an `Err` becoming `Unjudgeable(reason)`), read both sides and, when the values agree, return `FieldsDiverged(diff)` for a non-empty diff and `Agreeing` otherwise; a value difference stays `Diverged` whatever the fields say — `compare_keepassxc` resolves the fields (an `Err` becoming `Unjudgeable`), reads both sides and the entry's fields when it is there, and hands the four to a new pure `judged`.
- [x] 9.4 Extend `compare_keepassxc`'s doc comment: its agreement-only, not-per-mode disposition is unchanged (`audit.rs:432-436`), and a field divergence is reported without pre-empting which side `sync`'s mode would resolve it toward, which is the same reason the value comparison gives — Doc comment extended with both sentences.
- [x] 9.5 Add an `audit.rs` unit test named `a_value_divergence_is_reported_as_one_even_when_the_fields_also_differ` over fixture readings, asserting `Diverged` — Added over `judged`, asserting `diverged` for a value difference with differing fields and `fields diverged` for the same fields over agreeing values.
- [x] 9.6 Severity drill: returning `FieldsDiverged` before checking the values turns 9.5 red, which is the evidence the precedence is held rather than incidental — Observed: returning `FieldsDiverged` before the values are compared fails `a_value_divergence_is_reported_as_one_even_when_the_fields_also_differ`. Restored.
- [x] 9.7 Verify: `cargo test -p safix-core --lib audit::` green, drill in 9.6 observed — `cargo test -p safix-core --lib audit::` green, drill 9.6 observed.

## 10. Rendering the new words (F10)

- [x] 10.1 Extend `sync_flow` (`crates/safix/src/render.rs:993-1001`) so `Outcome::FieldsUpdated` renders the same `safix -> kdbx` flow `Updated` does and `Outcome::FieldsDiverged` renders no arrow, matching `Conflict`'s treatment — `sync_flow` renders `FieldsUpdated` the way `Updated` does; `FieldsDiverged` shows no arrow through the same arm `Conflict` uses, stated in the doc comment.
- [x] 10.2 Extend `push_sync_detail` (`render.rs:1003-1074`) with a paragraph per new outcome: `FieldsUpdated` names the fields written and states the value was already in agreement; `FieldsDiverged` names the fields, states nothing was written, and gives the remedy per mode — `safix sync keepassxc <mapping>` under a pushing mode, and "the declaration is the author; edit the declaration or the mode" under `keepassxc-to-safix`, and "`backup` never overwrites an existing entry" under `backup` — `FieldsUpdated` names the fields written and says the value already agreed; `FieldsDiverged` names them, says nothing was written, and splits the remedy per mode (extracted into `push_fields_diverged` to stay under `clippy::too_many_lines`).
- [x] 10.3 Extend `push_keepassxc_audit` (`render.rs:740-806`) with the `FieldsDiverged` arm, naming the fields and the same remedy split, and confirm the existing `Diverged` arm's sentence is unedited — `push_keepassxc_audit` gains the arm with the same remedy split; the `Diverged` arm's sentence is unedited.
- [x] 10.4 Hold, in every new rendering, that a field's content never appears: the only interpolation is a `&'static str` name, which is what makes the property structural rather than reviewed — Held: the only interpolation is `named_fields(&[&'static str])`, whose own doc comment records that the property is the type's.
- [x] 10.5 Extend `sync`'s closing tally line (`render.rs:949-991`) with the two new counts, printed only when non-zero, matching how the existing counts are printed — Both counts printed through `counted(word, n)`, which is empty at zero, so a run declaring no field closes exactly as before.
- [x] 10.6 Severity drill: interpolating a field's value into `push_sync_detail` does not compile, because `FieldsUpdated` and `FieldsDiverged` carry `&'static str` names and no value at all — perform the attempt once and record the compiler's refusal as the check — Observed: `fields.first().map(|held| held.content())` in `push_fields_diverged` fails to compile — `error[E0599]: no method named \`content\` found for reference \`&&str\``. The compiler is the check. Restored.
- [x] 10.7 Verify: `cargo test -p safix --bin safix -- render::` green, drill in 10.6 observed — `cargo test -p safix --bin safix` green (63 tests, `render::` and `reporter::` among them), drill 10.6 observed.

## 11. The harness, the stub, and the integration claims

- [x] 11.1 Change `Fixture::seed_sync_mapping`'s fifth parameter (`crates/safix/tests/harness/mod.rs:971-994`) from `username: Option<&str>` to `fields: serde_json::Value`, emitting `"kdbx": { "path": path, "fields": fields }`, and update its doc comment's "the shape `modules/flake/safix/default.nix` projects" sentence to name the fields record — Fifth parameter is now `fields: serde_json::Value`, emitted as `"fields"` beside `"path"`; built through a `serde_json::Map` so the record owns what it was handed (`clippy::needless_pass_by_value` is denied). Doc comment names the fields record.
- [x] 11.2 Add `Fixture::store_fields(&self, entry: &str) -> BTreeMap<String, String>` and `Fixture::store_field(&self, entry: &str, field: &str) -> String` to the harness beside `store_username` (`harness/mod.rs:1075-1085`), reading the stub's spool; delete `store_username` and migrate its two callers (`crates/safix/tests/sync_path.rs:151-155`) — `store_fields` and `store_field` added; `store_username` deleted and its two callers migrated. `store_seed_fields` added beside `store_seed` so a fixture can give an entry a field the person typed.
- [x] 11.3 Extend `crates/safix/tests/support/card-stubs.rs`'s `keepassxc` role: record `--username`, `--url` and `--notes` per entry in the `kdbx-entries` spool as a tab-separated record rather than the single trailing username field (`card-stubs.rs:549-572`), and answer `show --attributes <Attr>` by printing the requested attributes one per line in the order asked, an empty line for one the entry does not carry (`card-stubs.rs:478`) — The `kdbx-entries` record is now `<entry>\t<username>\t<url>\t<notes>`; `show --attributes <Attr>` is answered one line per attribute in the order asked, empty for one the entry does not carry, through `show_fields`. `add`/`edit` merge the named fields and leave the rest.
- [x] 11.4 Extend the stub's argv guard so a `--username`, `--url` or `--notes` whose value equals the entry's password refuses — the stub's own version of "no value in argv" — beside the existing `no value in argv` discipline `positionals`/`following_flag` implement (`card-stubs.rs:490-500`) — `refuse_a_value_in_argv` refuses a `--username`/`--url`/`--notes` whose value is the entry's own password, whether just fed or already held.
- [x] 11.5 Migrate every `seed_sync_mapping` call to the new fifth argument: `crates/safix/tests/sync_path.rs:50-75`, `:253`, `:310`, `crates/safix/tests/audit.rs:512`, `:555`, `:581`, `:616`, `crates/safix/tests/vault_reads.rs:114` — `json!({})` where the old argument was `None`, and `json!({"username": "alice@example.com"})` where it was `Some("alice@example.com")` — Every call migrated (14 in `sync_path.rs`, 4 in `audit.rs`, 1 in `vault_reads.rs`), `json!({})` or `json!({"username": …})` as the old argument said.
- [x] 11.6 Add `a_backup_mapping_never_overwrites_a_field_either` to `crates/safix/tests/sync_path.rs`: a `backup` mapping whose declared `url` differs from an existing entry's, asserting the entry's fields are unchanged, the exit status non-zero, and `fields diverged` in the report naming `url` — Added; the entry keeps the url the person typed, the run exits non-zero, the report says `backup  fields diverged` naming `url`, and neither field value appears in either stream.
- [x] 11.7 Add `a_field_drift_on_an_agreeing_entry_is_repaired_under_a_pushing_mode`: a `safix-to-keepassxc` mapping whose value agrees and whose declared `notes` does not, asserting exactly one `edit` invocation in `store_invocations`, the entry's value unchanged, its notes now the declared one, and `fields updated` naming `notes` — Added; exactly one write and it is an `edit`, the value unchanged, the notes now the declared one, `fields updated` naming `notes`, and neither field value in either stream.
- [x] 11.8 Add `a_declared_tag_is_refused_at_evaluation_rather_than_dropped`: declare `fields = { tags = [ "work" ]; }` on a keepassxc mapping through the real option tree and assert the evaluation message names the target and the field — the drill the assignment names, held in `modules/flake/checks/keepassxc.nix` (task 13.3) for the message and here for the run's refusal code `safix::field_unsupported` when the same record is handed to the runtime through `--entry` — Added as the runtime half: the refusal names the field and the target (`declares the field 'tags'`, `keepassxc cannot carry it`) and nothing is written. Deviation: the run's *code* is not asserted, because a per-mapping refusal renders its prose only — `safix::<code>` is printed for a top-level `Refusal` under the graphical reporter (`harness::Run::refusal_code`), and `sync`'s report carries the sentence rather than the code. The code itself is held by group 6's `code.rs` table and reporter snapshots.
- [x] 11.9 Add `a_field_read_out_of_another_entry_is_refused_for_this_target`: a mapping declaring `fields.notes = { entry = "grafana-note"; }`, asserting `safix::field_source_in_argv`, that the refusal names the field and the entry, and that `store_invocations` is empty for that mapping — Added; the refusal names the field and the entry, and no invocation of the run addresses that mapping's entry at all. Same code-assertion deviation as 11.8.
- [x] 11.10 Add `no_field_reaches_the_report_only_its_name_does` to `crates/safix/tests/audit.rs`: an entry whose `url` differs from the declaration by a distinctive literal, asserting the literal appears in neither stream and the word `url` appears in the report — Added to `audit.rs`; both distinctive literals are absent from both streams, the word `url` is in the report, and neither side was written.
- [x] 11.11 Add `a_mapping_declaring_no_field_issues_no_second_read` asserting `store_invocations` for such a mapping contains exactly one `show` per mapping — the property task 7.12 holds at the unit level, held here against the real argument vectors — Added; exactly one `show` for the mapping, it is the value read, and no attribute name appears in it.
- [x] 11.12 Severity drill: making the stub answer `show --attributes` with the password turns 11.10 red, which is the evidence the report is checked against the value's bytes rather than against a field name alone — Did not reproduce, and the reason is the property: making the stub answer `show --attributes` with the password left `no_field_reaches_the_report_only_its_name_does` green, because a read field becomes `Field::Resolved(Secret)` and `Secret` has no egress to a string — no path exists that could print it, so the leak the drill simulates is unreachable rather than untested. The report-side half of the claim is the drill in 10.6, which the compiler refuses. Restored.
- [x] 11.13 Severity drill: deleting the fields half of task 11.3's spool record turns 11.7 red on the notes assertion while every value assertion stays green — Observed: dropping the fields half of the record fails `a_field_drift_on_an_agreeing_entry_is_repaired_under_a_pushing_mode` on the notes assertion. Wider than predicted: it also fails `each_mode_converges_exactly_as_its_name_says` and `a_second_run_writes_nothing_anywhere`, both of which now read a field back too; every value assertion in all three stayed green. Restored.
- [x] 11.14 Verify: `cargo test -p safix --test sync_path --test audit --test vault_reads -- --test-threads 1` green, drills in 11.12 and 11.13 observed — `cargo test -p safix --test sync_path --test audit --test vault_reads -- --test-threads 1` green (16 + 23 + 4), drills 11.12 and 11.13 performed and recorded.

## 12. `bridge_sync` reaches the shared judge (F5)

- [x] 12.1 Rewrite `bridge_sync::decide` (`crates/safix-core/src/bridge.rs:775-792`) to call `endpoint::judge(safix_value.as_ref(), clan_value.as_ref(), remembered.as_ref(), &|remembered, value| agrees(remembered, value))` and map the `Verdict` onto its own `Decision`, leaving its "read a companion only when both sides differ" preamble (`bridge.rs:785-790`) and its `Outcome::Refused`-on-read-failure disposition unedited — `bridge_sync::decide` calls `endpoint::judge` with its own `agrees` closure and maps the `Verdict`; its companion-read preamble and its `Refused`-on-read-failure disposition are unedited.
- [x] 12.2 Replace `bridge.rs:611-626`'s module doc reference to "`decide`/`judge` mirror `crate::sync::two_way` (`sync.rs:451-493`) exactly" with a sentence naming `crate::endpoint::judge` as the one both now reach, and delete the claim that they mirror each other — The module doc now names `crate::endpoint::judge` as the one both reach; the mirror claim is gone.
- [x] 12.3 Confirm, and state in `bridge_sync`'s module doc, that the five per-target behaviours stay outside the shared contract: `generator_stale` on both the one-way and two-way push paths, `Addressing`'s shared-placement discovery, no commit on clan's side, `AbsentAtSource` as an ordinary absence, and the separate companion commit ordering — Stated in the module doc as a section of its own, naming all five.
- [x] 12.4 Confirm `bridge.rs`'s `selected` and `sync.rs`'s `selected` stay duplicated, and record F5's reason in a comment on each: their refusal variants name different declared lists and `bridge::selected` additionally refuses `MappingWrongDirection`, so unifying them costs more apparatus than the lines it saves — Confirmed and recorded in a paragraph on each of `bridge::selected` and `sync::selected`.
- [x] 12.5 Severity drill: task 5.15's judge perturbation is this group's drill as well — record which bridge two-way test it reddens, by name — Observed: making `(Some, Some)` with no memory a `PushValue` reddens `endpoint::tests::judge_is_the_decision_sync_and_the_bridge_both_reached`, `bridge::bridge_sync::tests::disagreeing_with_no_agreement_recorded_yet_is_a_conflict` and `crates/safix/tests/bridge_sync.rs`'s `both_sides_holding_different_values_with_no_agreement_is_a_conflict`. `sync_path.rs`'s two-way claim was not observed in that run because cargo stops at the first failing test target. Restored.
- [x] 12.6 Verify: `cargo test -p safix-core --lib bridge::` green, and `cargo test -p safix --test bridge_sync -- --test-threads 1` green — `cargo test -p safix-core --lib bridge::` green and `cargo test -p safix --test bridge_sync -- --test-threads 1` green (7 tests). The six two-way unit tests now reach `endpoint::judge` through a local `verdict()` helper, and one new test — `a_memory_written_under_syncs_own_tag_is_read_as_no_memory` — holds this target's own half of the shared decision.

## 13. Nix checks

- [x] 13.1 Create `modules/flake/checks/reserved-words.nix` adding `checks.safix-reserved-words`, a `pkgs.runCommand` that writes `(import ../safix/reserved.nix).ids` as one word per line, extracts the six words from the `RESERVED_MAPPING_WORDS` literal in `../../crates/safix-core/src/bridge.rs` with `sed`, and `diff -u`s the two, failing with a message naming both files — modelled on `safix-installer-schema` (`modules/flake/checks/installer.nix:1225-1257`) — created and registered in `flake.nix`'s import list between `portability.nix` and `single-runtime.nix`. The source path is `../../../crates/safix-core/src/bridge.rs`: from `modules/flake/checks/` the repository root is three levels up, and the two-level path in this task resolves to `modules/crates/…`, which nix refuses by name. The extraction is `tr` then two `sed` substitutions then `grep -o '"[^"]*"'` rather than one pattern, because `[&str; 6]` puts both a `]` and a `;` between the constant's name and its words and a single expression over them is unreadable; it also guards on an empty result, so a literal it cannot find fails loudly instead of diffing nothing against nothing
- [x] 13.2 Give `reserved-words.nix` a severity-drill header in the shape `modules/flake/checks/keepassxc.nix:32-58` uses, naming the two drills: reordering the nix list, and dropping a word from the Rust array — header written in that shape, naming both and stating why a unit test cannot carry the claim
- [x] 13.3 Extend `modules/flake/checks/keepassxc.nix`'s fixtures: add `fields` to the local `mapping` helper (`keepassxc.nix:152-156`) as an optional fifth argument defaulting to `{ }`; add `tagsRefusedMessages`, `entrySourcedFieldMessages` and `soundFieldsMessages` to the `actual` set and their literals to `expected`; extend `bothFaultsMessages` with a mapping carrying an unknown user and a declared tag, so the two-faults claim covers the new rules — done, with one adjustment: a nix function cannot take an optional positional argument, so the helper is `mappingWith = fields: mode: user: name: path: …` and `mapping = mappingWith { }`. Every existing fixture reads exactly as before. `bothFaultsMessages` now carries `a` (unknown user + reserved entry path) and `b` (unknown user + declared tag), and its literal lists all four sentences in the order the rules are appended. Also added `oldUsernameSpellingTypechecks`, which is task 14.5's evaluation of the migration's "before" line
- [x] 13.4 Extend `keepassxc.nix`'s severity-drill header with the two new drills: dropping `fieldUnsupported` from `violationsOf`'s list empties `tagsRefusedMessages`, and dropping `fieldSourceInArgv` empties `entrySourcedFieldMessages` — both named in the header, together with the `channels.keepassxc.tags` drill, the sound-versus-declared drill, the `capabilities` fork drill and the re-added-`username` drill. Both performed: dropping `fieldUnsupported` left `"tagsRefusedMessages":[]` in the failing diff, dropping `fieldSourceInArgv` left `"entrySourcedFieldMessages":[]`
- [x] 13.5 Add `capabilitiesAreTheSharedTable` to `keepassxc.nix`'s `actual`, asserting `keepassxc.capabilities` equals `(import ../safix/fields.nix { inherit lib; }).channels.keepassxc`, so the table this module refuses on cannot be forked from the shared one — added, expected `true`, beside a `capabilities` field whose expected value is the four-row literal so the table's contents are pinned as well as its identity
- [x] 13.6 Add check attributes to `modules/flake/checks/cli.nix` for the new integration claims, each through the `mode "<check-name>" "<test file>" "<test fn>"` form (`cli.nix:327-343`): `safix-sync-fields-push` (11.7), `safix-sync-fields-backup` (11.6), `safix-sync-fields-refused` (11.9), `safix-sync-fields-tag-refused` (11.8), `safix-audit-fields` (11.10), `safix-sync-fields-one-read` (11.11), each carrying the file's prose-comment convention: what the claim is, and which drill reddens it — All six added to `modules/flake/checks/cli.nix` through the `mode` form, each with the file's prose convention: what the claim is and which drill reddens it.
- [x] 13.7 Confirm `modules/flake/safix/checks.nix` needs no new `mk*Check`: the field refusals are messages of `keepassxc.violationsOf`, which `keepassxcMessages` (`checks.nix:144-152`) already returns whole — state the confirmation in 13.4's header so the next reader does not go looking — confirmed and stated in the header; `checks.nix` is unedited by this group
- [ ] 13.8 Severity drill: renaming any test function named in 13.6 without updating `cli.nix` turns that check red, because `integration.nix`'s `runOneWith` fails when zero tests ran — perform it on `safix-sync-fields-push` — NOT performed, and the reason is not the claim: the drill needs a green baseline and `safix-sync-fields-push` cannot build one while this working tree is shared. `modules/flake/checks/integration.nix` compiles the whole workspace's test targets, and `add-pass-bridge`, `add-bitwarden-bridge` and `add-onepassword-bridge` are editing `crates/safix-core/src/{lib,model,error/*}.rs` and `crates/safix/src/{main,usage,render,reporter}.rs` concurrently. Three attempts, three different sibling faults, none in this change's files: a snapshot missing `prose::pass_*` for `Error::Pass*` (7 errors in `safix-core`), then one in `safix`'s bin test target, and `cargo test -p safix --no-run` locally reporting `crates/safix-core/src/bitwarden.rs:485:50: error[E0308]: expected Option<&str>, found Option<&String>`. Hand-off: run once the three target slices land, together with 13.9
- [ ] 13.9 Verify: `nix build .#checks.x86_64-linux.safix-reserved-words .#checks.x86_64-linux.safix-keepassxc .#checks.x86_64-linux.safix-keepassxc-drill .#checks.x86_64-linux.safix-sync-fields-push .#checks.x86_64-linux.safix-audit-fields` green, drills in 13.2, 13.4 and 13.8 observed — the three evaluation-only checks are green and re-verified after the sibling slices began editing `options.nix` and `default.nix`; drills 13.2 and 13.4 observed and recorded on their own lines. The two `cli.nix` checks are blocked for 13.8's reason and are the whole of what is left of this line

## 14. The changelog and the migration note

- [x] 14.1 Add a `### Fields on the far side of a mapping, one judge, and one reserved-word list` entry first under `## [Unreleased]` in `CHANGELOG.md`, naming: the new `fields` declaration and its four fields; that `kdbx.username` is gone rather than aliased and how the old declaration now fails; the two new refusal codes by name; the two new sync report words and the new audit outcome; that `tags` is refused for keepassxc and why; that three mapping ids — `pass`, `bitwarden`, `1password` — became reserved before the targets that need them exist; and that no dependency was added and `deny.toml` is unedited — added first under `[Unreleased]`, naming all eight
- [x] 14.2 State the migration as a two-line before-and-after in that entry: `kdbx = { path = "alice/grafana"; username = "alice@example.com"; };` becomes `kdbx = { path = "alice/grafana"; fields.username = "alice@example.com"; };` — stated as a two-line fenced `nix` block with `# before` and `# after`
- [x] 14.3 Do not rewrite any released changelog entry, and do not edit `README.md` or `examples/` — `rewrite-readme-and-examples` owns both, and the field prose is that change's — held: the only changelog edit is an insertion under `[Unreleased]`, and neither `README.md` nor anything under `examples/` was touched by this slice
- [x] 14.4 Record in the entry that `README.md:1211-1265`'s keepassxc section still shows the old `username` spelling until `rewrite-readme-and-examples` lands, so the staleness is a stated hand-off rather than an oversight — recorded as the entry's closing line, naming the change that owns it
- [x] 14.5 Verify: the `[Unreleased]` section re-read end to end, and the before-and-after lines evaluated against the real option tree by task 13.3's fixture rather than asserted in prose — re-read end to end, the new entry first and the picker entry's heading intact after it. Both lines are evaluated by `checks.safix-keepassxc`: the "after" line by `soundFieldsMessages`, which declares `fields.username` as a literal through the real option tree and produces no message, and the "before" line by `oldUsernameSpellingTypechecks`, which asserts that a `kdbx.username` declaration throws

## 15. Closing verification

- [x] 15.1 Run `cargo test --locked --workspace` and record which failures are pre-existing environment-only ones rather than this change's — Ran per crate rather than `--workspace` while siblings edit the same tree: `cargo test -p safix-core --lib` 303 passed; `cargo test -p safix --bin safix` 63 passed; `--test sync_path` 16, `--test audit` 23, `--test bridge_sync` 7, `--test vault_reads` 4, all passed. `cargo clippy -p safix-core --all-targets` and `cargo clippy -p safix --all-targets` clean but for two pre-existing `chunks_exact` warnings in `digest.rs`, which this change does not touch.
- [ ] 15.2 Run `nix flake check` and record every check that moved, confirming `safix-reserved-words` and the six `cli.nix` entries are among them and that no existing check attribute was renamed or removed
- [ ] 15.3 Confirm by diff that `Cargo.toml`, `Cargo.lock`, `deny.toml`, `openspec/specs/rust-supply-chain/spec.md`, `README.md` and everything under `examples/` are unedited
- [ ] 15.4 Confirm by diff that `modules/flake/safix/pass.nix`, `bitwarden.nix` and `onepassword.nix` do not exist in this change, and that `fields.nix` and `reserved.nix` carry every target's row so the three sibling changes consume rather than extend them
- [ ] 15.5 Run `openspec validate extend-bridge-fields --strict` and confirm the five delta specs validate
- [ ] 15.6 Severity drill roll-up: confirm every drill named in groups 1 through 14 was performed and its outcome recorded in the task line, and that any drill which did not reproduce carries the reason in place of the claim
