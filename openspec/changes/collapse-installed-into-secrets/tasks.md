# Tasks: collapse-installed-into-secrets

Citations are as read against the worktree on 2026-09-15; re-read the named lines before editing, since line numbers drift and the symbol name is the anchor.
No real fleet identifier, hostname, or recipient enters this repository; the fixtures already in place use `alice`, `bob`, `carol`, `server`, `deck`, and synthetic `age1` strings, and every fixture this change edits keeps them.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
Check names in this file are the ones the tree uses today: `safix-installer-store`, `safix-installer-manifest`, `safix-installer-sole`, `safix-installer-refusals`, `safix-consumption-system`, `safix-portability-system`, `safix-module-entrypoints`.

## 1. One option per scope, typed by a `secretsType` argument

- [ ] 1.1 Add `secretsType` to `common.sharedOptions`'s argument set in `modules/consume/common.nix:236-243`, beside `cfg`, `userDefault`, `userDefaultText`, `hostnameDefault`, `hostnameDefaultText`
- [ ] 1.2 Change `secrets` in `modules/consume/common.nix:379-392` from `type = types.attrsOf types.raw;` to `type = secretsType;`, keeping `readOnly = true` and keeping the absence of a `default`
- [ ] 1.3 Extend that option's description with the per-scope typing sentence: it is the shape the secret provisioner's own option tree takes; at system scope it is typed by the provisioner's own entry type and carries the path each entry will arrive at; at user scope it is the untyped projection and `sops.secrets` is where it lands
- [ ] 1.4 Delete `installedOptionFor` (`modules/consume/common.nix:207-218`) and its entry in the top-level `inherit` list (`:229`)
- [ ] 1.5 Move the surviving half of the comment at `modules/consume/common.nix:192-206` — that the provisioner's entry type carries the mode/owner/group/uid/gid coercions, the `sopsFile` default and the `sopsFileHash` default, but *not* the two refusals (`manifest-for.nix:11-28`'s missing-file and outside-the-store checks), which are copied into `modules/consume/installer.nix` because safix does not call that builder — to the `nixos.nix` call site added in 1.6; delete the rest with the option
- [ ] 1.6 In `modules/consume/nixos.nix:59-74`, pass `secretsType = options.sops.secrets.type;` into `common.sharedOptions` and delete the `// { installed = common.installedOptionFor options; }` suffix, so `options.safix` is one plain `sharedOptions` call
- [ ] 1.7 In `modules/consume/home.nix:154-166`, pass `secretsType = lib.types.attrsOf lib.types.raw;`; change nothing else in that file, and specifically leave `sops.secrets = cfg.secrets;` (`home.nix:232`) as the site where the user scope's typing still happens
- [ ] 1.8 Hold the system scope's typing: `safix-consumption-system`'s `systemCarriesOwnership` (`modules/flake/checks/consumption.nix:437-441`) and `safix-portability-system`'s `decided` (`portability.nix:262-268`) and `serviceOwnership.system` (`:535-543`) all read fields the provisioner's `secretType` supplies (`owner`, `group`, `mode`, `format`, `key`, `name`, `sopsFile`), so they are the standing evidence — confirm each is green against the collapsed option rather than adding a new check
- [ ] 1.9 Severity drill: change 1.6's `secretsType` to `lib.types.attrsOf lib.types.raw` and confirm `safix-consumption-system` reddens on `systemCarriesOwnership`, `safix-portability-system` reddens on `decided` and `serviceOwnership.system`, and `safix-installer-manifest` reddens on the key-set comparison against the provisioner's own builder; revert
- [ ] 1.10 Severity drill: add a second definition of `safix.secrets` to any nixos fixture and confirm the module system refuses it as read-only (`nixpkgs lib/modules.nix:1157-1172`), which is the evidence `readOnly = true` survived the collapse rather than being quietly dropped to make task 5 easier; revert
- [ ] 1.11 Verify: `nix build .#checks.x86_64-linux.safix-consumption-system .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-installer-manifest` green, and `options.safix ? installed` is false on a fixture system configuration; drills in 1.9 and 1.10 observed

## 2. The path mint moves into the unconditional definition

- [ ] 2.1 In `modules/consume/nixos.nix:84-107`, change the `else resolved;` branch to `else lib.mapAttrs (name: entry: entry // lib.optionalAttrs (!(entry ? path)) { path = "${cfg.installer.symlinkPath}/${name}"; }) resolved;` — over the local `resolved` binding, never over `cfg.secrets`, and leaving the definition unconditional and outside `lib.mkIf cfg.enable`
- [ ] 2.2 Move the comment at `modules/consume/nixos.nix:133-139` — the store-root-and-entry-default coupling with its `main.go:254-268` citation — up beside 2.1's mint, and add to it that the map is over `resolved` and the definition ungated because either alternative is an evaluation cycle (`cfg.secrets` self-reference; `mkIf cfg.enable` through `enable`'s `cfg.secrets != { }` default at `common.nix:245-248`)
- [ ] 2.3 Delete the `safix.installed = lib.mapAttrs …` definition at `modules/consume/nixos.nix:140-143` together with the comment half at `:128-132`, leaving the `lib.mkIf cfg.enable` block as the `sops.age.keyFile` / `sops.age.sshKeyPaths` half alone (`:145-155`)
- [ ] 2.4 Confirm the five `fires` probes still measure safix's own refusal rather than an evaluation cycle or a file hash: `modules/flake/checks/installer.nix:452` and `:777`, `modules/flake/checks/portability.nix:243` and `:417`, `modules/flake/checks/consumption.nix:243`
- [ ] 2.5 Add `sops.validateSopsFiles = false;` to `nixosFor` in `modules/flake/checks/portability.nix:276-300`, with a comment stating why: under the provisioner's entry type a deep force reaches each entry's `sopsFileHash` default, so a fixture left at `validateSopsFiles = true` can make `fires` true for a `hashFile` reason rather than for the resolver's refusal
- [ ] 2.6 Add a field to an existing nixos check (`safix-installer-sole`'s `soleFacts`, which already reads the enable-gated installer surfaces) asserting that with `safix.enable = false` over a non-empty resolution, `system.build ? safix-manifest`, `system.activationScripts ? safixInstallSecrets` and `systemd.services ? safix-install-secrets` are all false while `config.safix.secrets` is non-empty and its path-less entries carry minted paths — this is what makes 2.1's move outside the enable gate safe
- [ ] 2.7 Severity drill: rewrite 2.1's `lib.mapAttrs` to map over `cfg.secrets` instead of `resolved` and confirm evaluation of any nixos fixture fails with infinite recursion rather than succeeding; revert
- [ ] 2.8 Severity drill: wrap 2.1's definition in `lib.mkIf cfg.enable` and confirm the same infinite recursion, through `enable`'s own default; revert
- [ ] 2.9 Severity drill: drop 2.5's `validateSopsFiles = false` and confirm the `fires` probes in `portability.nix:417` still fire for the resolver's reason on a `broken` fleet, then point one such fixture at a non-broken fleet and confirm it fires for the `hashFile` reason — the evidence that the line is load-bearing rather than decorative; revert
- [ ] 2.10 Verify: `nix build .#checks.x86_64-linux.safix-installer-sole .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-consumption-system` green, 2.6's new field observed failing when the enable gate is removed from `modules/consume/installer.nix:298`; drills in 2.7, 2.8 and 2.9 observed

## 3. The installer manifest reads the collapsed option

- [ ] 3.1 In `modules/consume/installer.nix:144-149`, change `installedEntries = builtins.attrValues config.safix.installed;` to read `config.safix.secrets`, and rename the binding to `resolvedEntries`
- [ ] 3.2 Update the three other references to that binding: `modules/consume/installer.nix:104` (`preflightRemediation`'s count), `:178` (`failedAssertions`' fold), `:187` (`manifest.secrets`)
- [ ] 3.3 Rewrite the comment at `modules/consume/installer.nix:144-148`, which today distinguishes "the typed set" from "the raw resolution": after the collapse there is one set, and the sentence must say that the option a consumer reads and the set this manifest is built from are the same value, while keeping the citations for what the provisioner's type does carry (`modules/sops/default.nix:46`, `:51-53`)
- [ ] 3.4 Confirm the two refusals copied from `manifest-for.nix:11-28` into `installer.nix:151-178` are unedited: the collapse changes which option they read over, never what they refuse
- [ ] 3.5 Severity drill: point 3.1 at an empty attrset literal and confirm `safix-installer-sole`'s `established` (task 4.3) and `safix-installer-store`'s path diff (task 6) both redden, which is the evidence the manifest is built from the option rather than from anything else; revert
- [ ] 3.6 Verify: `nix build .#checks.x86_64-linux.safix-installer-manifest .#checks.x86_64-linux.safix-installer-store .#checks.x86_64-linux.safix-installer-sole .#checks.x86_64-linux.safix-installer-refusals` green; drill in 3.5 observed

## 4. Every check that reads the option follows it

- [ ] 4.1 `modules/flake/checks/consumption.nix:415-419`: change `systemView = viewOf (nixosFor "bob").config.safix.installed;` to read `.config.safix.secrets`, and rewrite the comment above it, which today explains the choice of `safix.installed` over `sops.secrets` — it must now say that the system scope delivers through safix's own single option while the provisioner's stays empty, so exactly one installer acts on the resolved set
- [ ] 4.2 `modules/flake/checks/installer.nix:399`: change `parityManifest`'s third argument from `manifestFixture.config.safix.installed` to `manifestFixture.config.safix.secrets`
- [ ] 4.3 `modules/flake/checks/installer.nix:861`: change `soleFacts.actual.established` to read `builtins.attrNames manifestFixture.config.safix.secrets`, keeping the expected list `[ bob-service ops-handover ops-tooling team-vault ]` and the comment recording that the fixture resolves the four entries `safix-consumption-system` also reads
- [ ] 4.4 `modules/flake/checks/portability.nix:350`, `:361`, `:377`, `:543`: change `person`, `machine`, `machineRaw` and `serviceOwnership.system` to read `.safix.secrets` on the nixos shape
- [ ] 4.5 Leave `portability.nix:262-268` (`decided`) and `:273` (`viewOf`) unedited: both already exclude `path` and `sopsFileHash`, which is what keeps the three shapes comparable now that the nixos shape carries a minted path
- [ ] 4.6 `modules/flake/checks/entrypoints.nix:21-23`: rewrite the sentence that names `nixos.nix`'s `installed` option to name `secrets` instead, keeping the mechanism claim verbatim in substance — the option's `type` field reads `options.sops.secrets.type` off a real provisioner module and stays an unforced thunk while `_module.check = false`, which is why building the `options` tree bare still returns
- [ ] 4.7 Confirm no other check file references either option: `modules/flake/checks/{vault,vault-projection,namespace,exported,materialization,custody,subjects,generators,policy,bridge*,cli,keepassxc,real-clan,examples,gate-guard,integration,single-runtime,envelope}.nix` and `collision-fixture/declaring-module.nix` need no edit; a repo-wide search for `safix\.installed` must return zero hits outside `openspec/changes/archive/`
- [ ] 4.8 Severity drill: revert 4.6's comment to name `installed` and confirm nothing reddens — then record beside it that this comment is prose a reader relies on rather than a checked claim, which is why 4.7's zero-hit search is the mechanism that catches it
- [ ] 4.9 Verify: `nix build .#checks.x86_64-linux.safix-module-entrypoints .#checks.x86_64-linux.safix-consumption-system .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-portability-home` green, and the repo-wide `safix\.installed` search returns hits only under `openspec/changes/archive/`

## 5. The refusal fixture enters through the resolver seam

- [ ] 5.1 Rebuild `refusalFixtureWith` (`modules/flake/checks/installer.nix:725-736`) so it no longer defines `safix.installed`: the fixture module sets `safix.lib = { violations = [ ]; materialize = _args: _cfg: entry; }`, `safix.user = "bob"`, `safix.hostname = "server"`, `safix.identity.sshKeyPaths = [ "/etc/ssh/safix-fixture-identity" ]`, and keeps `safix.enable = true`, `nixpkgs.hostPlatform = system`, `system.stateVersion = "24.05"`
- [ ] 5.2 Record beside it, in a comment, that the fixture now enters through `common.resolvedFor`'s own seam (`modules/consume/common.nix:449-468`, which on this path reads only `cfg.lib.violations` and `cfg.lib.materialize`) *because* `safix.secrets` is read-only — that is the collapse's point, not a workaround for it — and that `cfg.lib.subjects` is not needed because `tags`' default reads it only when `safix.machine` is set (`common.nix:361-364`)
- [ ] 5.3 Confirm the identity is present in the fixture so `common.noSystemIdentityMessage` (`nixos.nix:104-105`) cannot pre-empt the refusal under test, and that `sops.validateSopsFiles` is left at its default so the copied outside-the-store and missing-file refusals in `installer.nix:151-178` are reached
- [ ] 5.4 Leave `outsideStoreFixture` (`installer.nix:739-741`) and `missingFileFixture` (`:742-744`) calling `refusalFixtureWith` with the same entry literals (`outside.sopsFile = "/etc/hosts"`, `missing.sopsFile = "${builtins.storeDir}/safix-fixture-absent/nope.yaml"`), so what the refusals are measured against is unchanged
- [ ] 5.5 Severity drill: drop the `safix.identity.sshKeyPaths` line from 5.1 and confirm both refusal fields in `safix-installer-refusals` (`installer.nix:882`, `:899`) still report `refuses = true` but for safix's identity message rather than for the refusal under test — then restore it, which is the evidence the fixture measures the manifest builder's refusals and not the resolver's
- [ ] 5.6 Severity drill: drop the copied outside-the-store block from `modules/consume/installer.nix:151-178` and confirm `safix-installer-refusals`' `outsideStore` field reddens while `missingFile` stays green, and vice versa for the missing-file block — the evidence the two refusals are independently held through the rebuilt fixture; revert
- [ ] 5.7 Verify: `nix build .#checks.x86_64-linux.safix-installer-refusals` green; drills in 5.5 and 5.6 observed

## 6. `safix-installer-store`'s expected side becomes an independent oracle

- [ ] 6.1 Change `entryPathContract` (`modules/flake/checks/installer.nix:429-438`) to map over `config.flake.safix.lib.materialize { user = "bob"; machine = null; hostname = "server"; tags = [ ]; scope = "system"; } fixture.config` instead of over `fixture.config.safix.secrets`, keeping the per-entry formula `entry.path or "${fixture.config.safix.installer.symlinkPath}/${name}"` and keeping `symlinkPath` read off the fixture's own option so `movedFixture` still measures the coupling at the option rather than at a literal
- [ ] 6.2 Rewrite the comment at `installer.nix:429-433` to say what is now true: the expected side reads the declarations' own resolution through the same `materialize` call `common.resolvedFor` makes (`common.nix:460-468`; published at `modules/flake/safix/default.nix:64,175`), before any module minted anything, so the diff at `:1094-1103` is between two independent computations rather than one option against itself
- [ ] 6.3 Confirm both consumers are unedited in shape: `expectedPathsJson` and `movedPathsJson` (`installer.nix:1057-1058`) and the `jq '.secrets | map({(.name): .path}) | add'` comparison in `safix-installer-store` (`:1094-1103`)
- [ ] 6.4 Severity drill (the change's own severity risk, from `design.md` C4): remove the `lib.optionalAttrs (!(entry ? path)) { path = …; }` mint from task 2.1's definition and confirm `safix-installer-store` reddens on the entry-path contract for both `manifestFixture` and `movedFixture`; revert. If it stays green, the tautology is not repaired and this change is not done
- [ ] 6.5 Severity drill: leave the mint in place and move `movedFixture`'s `safix.installer.symlinkPath` without moving `secretsMountPoint`, confirming `safix-installer-store` still holds the root and the default as one claim rather than passing on the unmoved half; revert
- [ ] 6.6 Severity drill: point 6.1's oracle back at `fixture.config.safix.secrets` and re-run 6.4's drill, confirming the check goes green with the mint removed — the direct demonstration that the oracle, not the formula, is what makes `safix-installer-store` falsifiable; revert
- [ ] 6.7 Verify: `nix build .#checks.x86_64-linux.safix-installer-store` green; drills in 6.4, 6.5 and 6.6 observed, with 6.4's result recorded in the check's own comment

## 7. Documentation: the option table, the installer narrative, and the changelog

- [ ] 7.1 `README.md:783`: rewrite the option-table row to `| `safix.secrets` | read-only | what resolved; at system scope typed by `sops.secrets`' own entry type and carrying the path each entry arrives at |`, adding no row — the surface shrinks rather than grows, which is the visible win
- [ ] 7.2 `README.md:869-895`: add one sentence to the system-scope installer narrative, beside the `safix-installer-sole` sentence at `:871` that says `sops.secrets` stays empty, naming `config.safix.secrets` as both the single readable surface and the manifest's own source, so a consumer told the provisioner's option is empty is also told where to look
- [ ] 7.3 Confirm `README.md:851`'s "installed" is the English word in the home-activation guarantee and needs no edit, and that a search of `README.md` for `safix.installed` returns zero hits before and after
- [ ] 7.4 `CHANGELOG.md`: add a breaking nix-surface entry under `## [Unreleased]` (the section begins at `:22`), headed for the single option, stating that `config.safix.installed` is gone with no alias so reading it is an evaluation error naming the option; that `config.safix.secrets` is now the whole of what a system configuration reports — typed by the provisioner's own entry type, carrying the path each entry will arrive at, and the exact set the installer manifest is built from; why (the two options were one set read twice, and the one a consumer was told to read was the one the installer did not use); that the user scope is unchanged; and that an entry which declared no path now carries one at system scope, for a consumer who was relying on its absence
- [ ] 7.5 Leave `CHANGELOG.md:101-102` exactly as written: it is the released `own-secret-installer` entry, it was accurate when written, and 7.4's note supersedes it rather than rewriting history — per the versioning policy at `CHANGELOG.md:17-19`, which makes an option-surface change breaking whether or not any rust changed
- [ ] 7.6 Leave `openspec/changes/archive/2026-08-22-own-secret-installer/design.md:77-81,223-226` and `.../tasks.md:52-57,107` unedited for the same reason
- [ ] 7.7 Severity drill: none is available for prose, so instead confirm the `safix\.installed` repo-wide search from 4.7 covers `README.md`, `CHANGELOG.md`'s `[Unreleased]` section and every `.nix` file, and record in 7.4's entry that the archive is the one place the old name legitimately survives
- [ ] 7.8 Verify: the `safix\.installed` search returns hits only under `openspec/changes/archive/`; `README.md`'s option table and installer narrative read correctly end to end with no dangling reference to a second option

## 8. Whole-surface verification

- [ ] 8.1 Run `nix flake check` and confirm every check is green, with particular attention to the eight `safix-installer-*` checks, `safix-consumption-system`, `safix-consumption-refusals`, both `safix-portability-*` checks, and `safix-module-entrypoints`
- [ ] 8.2 Confirm `modules/flake/checks/gate-guard.nix`'s `linuxOnlyChecks` list (`:44-59`) is unchanged: this change adds and removes no check, so a diff there is a mistake
- [ ] 8.3 Confirm no rust target needs rebuilding for this change — nothing under `crates/**` reads either option — and that no refusal snapshot, `refusal_codes!` entry, or reporter arm is touched
- [ ] 8.4 Severity drill: re-run the three drills the report names as this change's own severity floor and record each result beside the mechanism it covers — 6.4 (mint removed → `safix-installer-store` red), 1.9 (`secretsType` weakened to `attrsOf raw` → `safix-consumption-system`, `safix-portability-system`, `safix-installer-manifest` red), 2.6/2.10 (`safix.enable = false` over a non-empty resolution → no installer surface appears)
- [ ] 8.5 Verify: `nix flake check` green; the three drills in 8.4 observed and recorded; `options.safix ? installed` false on both a nixos and a home fixture
