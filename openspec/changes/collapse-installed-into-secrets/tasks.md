# Tasks: collapse-installed-into-secrets

Citations are as read against the worktree on 2026-09-15; re-read the named lines before editing, since line numbers drift and the symbol name is the anchor.
No real fleet identifier, hostname, or recipient enters this repository; the fixtures already in place use `alice`, `bob`, `carol`, `server`, `deck`, and synthetic `age1` strings, and every fixture this change edits keeps them.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.
Check names in this file are the ones the tree uses today: `safix-installer-store`, `safix-installer-manifest`, `safix-installer-sole`, `safix-installer-refusals`, `safix-consumption-system`, `safix-portability-system`, `safix-module-entrypoints`.

## 1. One option per scope, typed by a `secretsType` argument

- [x] 1.1 Add `secretsType` to `common.sharedOptions`'s argument set in `modules/consume/common.nix:236-243`, beside `cfg`, `userDefault`, `userDefaultText`, `hostnameDefault`, `hostnameDefaultText` — added at `common.nix:214`; the argument-set comment now says five arguments and names the type alongside the defaults
- [x] 1.2 Change `secrets` in `modules/consume/common.nix:379-392` from `type = types.attrsOf types.raw;` to `type = secretsType;`, keeping `readOnly = true` and keeping the absence of a `default` — `common.nix:351-353`; `readOnly` kept, no `default` added
- [x] 1.3 Extend that option's description with the per-scope typing sentence: it is the shape the secret provisioner's own option tree takes; at system scope it is typed by the provisioner's own entry type and carries the path each entry will arrive at; at user scope it is the untyped projection and `sops.secrets` is where it lands — appended as its own paragraph at `common.nix:364-371`
- [x] 1.4 Delete `installedOptionFor` (`modules/consume/common.nix:207-218`) and its entry in the top-level `inherit` list (`:229`) — both gone; the `inherit` list ends `sopsFileOutsideStoreMessage wasSet`
- [x] 1.5 Move the surviving half of the comment at `modules/consume/common.nix:192-206` — that the provisioner's entry type carries the mode/owner/group/uid/gid coercions, the `sopsFile` default and the `sopsFileHash` default, but *not* the two refusals (`manifest-for.nix:11-28`'s missing-file and outside-the-store checks), which are copied into `modules/consume/installer.nix` because safix does not call that builder — to the `nixos.nix` call site added in 1.6; delete the rest with the option — moved to `nixos.nix:71-86`, beside the `secretsType` argument, naming `manifest-for.nix:11-18` and `:19-23` as the two refusals' home and `./installer.nix` as the copy
- [x] 1.6 In `modules/consume/nixos.nix:59-74`, pass `secretsType = options.sops.secrets.type;` into `common.sharedOptions` and delete the `// { installed = common.installedOptionFor options; }` suffix, so `options.safix` is one plain `sharedOptions` call — `nixos.nix:59-87` is now a single `common.sharedOptions { … }` with no suffix
- [x] 1.7 In `modules/consume/home.nix:154-166`, pass `secretsType = lib.types.attrsOf lib.types.raw;`; change nothing else in that file, and specifically leave `sops.secrets = cfg.secrets;` (`home.nix:232`) as the site where the user scope's typing still happens — passed at `home.nix:167-170` with a comment saying the typing happens downstream; `sops.secrets = cfg.secrets` untouched
- [x] 1.8 Hold the system scope's typing: `safix-consumption-system`'s `systemCarriesOwnership` (`modules/flake/checks/consumption.nix:437-441`) and `safix-portability-system`'s `decided` (`portability.nix:262-268`) and `serviceOwnership.system` (`:535-543`) all read fields the provisioner's `secretType` supplies (`owner`, `group`, `mode`, `format`, `key`, `name`, `sopsFile`), so they are the standing evidence — confirm each is green against the collapsed option rather than adding a new check — all three green against the collapsed option (built from a scratch copy of `HEAD` plus this change alone, so a sibling change in the same worktree could not colour the result); 1.9 records which of the three actually witnesses the typing and which are insensitive to it
- [x] 1.9 Severity drill: change 1.6's `secretsType` to `lib.types.attrsOf lib.types.raw` and confirm `safix-consumption-system` reddens on `systemCarriesOwnership`, `safix-portability-system` reddens on `decided` and `serviceOwnership.system`, and `safix-installer-manifest` reddens on the key-set comparison against the provisioner's own builder; revert — **partially reproduced, and the gap is recorded rather than papered over.** `safix-portability-system` reddens, at evaluation: `error: attribute 'format' missing` from `decided`'s `lib.getAttrs` while building `structure-safix-portability-system`'s `actualJson`. `safix-consumption-system` and `safix-installer-manifest` do **not** redden — each one's derivation is byte-identical to the typed build, so neither check can observe the weakening at all. The reasons are mechanical: consumption's `systemCarriesOwnership` reads `owner`, `group` and `mode`, which the resolver itself emits for a system-scope entry (the provisioner's type only coerces them), and `viewOf` there is a `filterAttrs` over whatever fields arrived; and `parityManifest` feeds the provisioner's own builder the same entries safix's manifest is built from, so the two field sets agree however those entries are typed. The standing evidence of the typing is therefore `decided`'s provisioner-only fields — `format`, and `sopsFileHash` by its deliberate exclusion — and not the ownership axis. Reverted.
- [x] 1.10 Severity drill: add a second definition of `safix.secrets` to any nixos fixture and confirm the module system refuses it as read-only (`nixpkgs lib/modules.nix:1157-1172`), which is the evidence `readOnly = true` survived the collapse rather than being quietly dropped to make task 5 easier; revert — a fixture adding `safix.secrets = { intruder.sopsFile = "/etc/hosts"; }` beside the module's own definition fails with `error: The option 'safix.secrets' is read-only, but it's set multiple times. Definition values:` followed by both definitions. Reverted (the drill lived in a scratch expression, never in the tree).
- [x] 1.11 Verify: `nix build .#checks.x86_64-linux.safix-consumption-system .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-installer-manifest` green, and `options.safix ? installed` is false on a fixture system configuration; drills in 1.9 and 1.10 observed — all three green; `options.safix ? installed` evaluates to `false` on a `bob`/`server` fixture, whose `safix.secrets.bob-service.owner` is `"bob"`, so the option is gone and the typing arrived

## 2. The path mint moves into the unconditional definition

- [x] 2.1 In `modules/consume/nixos.nix:84-107`, change the `else resolved;` branch to `else lib.mapAttrs (name: entry: entry // lib.optionalAttrs (!(entry ? path)) { path = "${cfg.installer.symlinkPath}/${name}"; }) resolved;` — over the local `resolved` binding, never over `cfg.secrets`, and leaving the definition unconditional and outside `lib.mkIf cfg.enable` — `nixos.nix:133-136`, in the `else` branch of the unconditional definition
- [x] 2.2 Move the comment at `modules/consume/nixos.nix:133-139` — the store-root-and-entry-default coupling with its `main.go:254-268` citation — up beside 2.1's mint, and add to it that the map is over `resolved` and the definition ungated because either alternative is an evaluation cycle (`cfg.secrets` self-reference; `mkIf cfg.enable` through `enable`'s `cfg.secrets != { }` default at `common.nix:245-248`) — `nixos.nix:120-132`; the `enable` citation is re-anchored to `common.nix:216-219`, where that default sits after 1.4's deletion
- [x] 2.3 Delete the `safix.installed = lib.mapAttrs …` definition at `modules/consume/nixos.nix:140-143` together with the comment half at `:128-132`, leaving the `lib.mkIf cfg.enable` block as the `sops.age.keyFile` / `sops.age.sshKeyPaths` half alone (`:145-155`) — the gated block at `nixos.nix:156-168` is now the `sops.age.*` half alone
- [x] 2.4 Confirm the five `fires` probes still measure safix's own refusal rather than an evaluation cycle or a file hash: `modules/flake/checks/installer.nix:452` and `:777`, `modules/flake/checks/portability.nix:243` and `:417`, `modules/flake/checks/consumption.nix:243` — confirmed: the two `installer.nix` sites read `identityFreeSystem`, whose own fixture already sets `validateSopsFiles = false`; the two `portability.nix` sites are covered by 2.5's line on `nixosFor`; `consumption.nix:243` is user scope, `attrsOf raw` before and after. All five still return a boolean rather than aborting, and `safix-installer-refusals`, `safix-portability-system` and `safix-consumption-system` are green over them
- [x] 2.5 Add `sops.validateSopsFiles = false;` to `nixosFor` in `modules/flake/checks/portability.nix:276-300`, with a comment stating why: under the provisioner's entry type a deep force reaches each entry's `sopsFileHash` default, so a fixture left at `validateSopsFiles = true` can make `fires` true for a `hashFile` reason rather than for the resolver's refusal — `portability.nix:286-293`, with that reason written out
- [x] 2.6 Add a field to an existing nixos check (`safix-installer-sole`'s `soleFacts`, which already reads the enable-gated installer surfaces) asserting that with `safix.enable = false` over a non-empty resolution, `system.build ? safix-manifest`, `system.activationScripts ? safixInstallSecrets` and `systemd.services ? safix-install-secrets` are all false while `config.safix.secrets` is non-empty and its path-less entries carry minted paths — this is what makes 2.1's move outside the enable gate safe — `soleFacts.disabledOverResolution` (`installer.nix:969-981`, expected at `:1014-1020`) over a new `disabledFixture` (`:877-896`), the same fleet and subject as `manifestFixture` with the gate off; its `paths` field is compared against `entryPathContract disabledFixture`, so the minted paths are held by task 6's independent oracle rather than by the option itself
- [x] 2.7 Severity drill: rewrite 2.1's `lib.mapAttrs` to map over `cfg.secrets` instead of `resolved` and confirm evaluation of any nixos fixture fails with infinite recursion rather than succeeding; revert — `error: infinite recursion encountered`, the trace ending `while evaluating the option 'safix.secrets'` … `while evaluating definitions from '…/modules/consume/nixos.nix'`. Reverted.
- [x] 2.8 Severity drill: wrap 2.1's definition in `lib.mkIf cfg.enable` and confirm the same infinite recursion, through `enable`'s own default; revert — same `error: infinite recursion encountered`, and the trace names the extra hop: `while evaluating the option 'safix.secrets'` … `while evaluating the option 'safix.enable'`. Reverted.
- [x] 2.9 Severity drill: drop 2.5's `validateSopsFiles = false` and confirm the `fires` probes in `portability.nix:417` still fire for the resolver's reason on a `broken` fleet, then point one such fixture at a non-broken fleet and confirm it fires for the `hashFile` reason — the evidence that the line is load-bearing rather than decorative; revert — first half: with the line dropped, `safix-portability-system` is green and its derivation byte-identical, so the probes still fire for the resolver's reason on the `broken` fleets. Second half, and worse than the task predicted: a fixture over a non-broken fleet with `validateSopsFiles` left at `true` does not report `fires = true` at all — forcing the resolution aborts inside `safix.secrets.bob-service.sopsFileHash` with `error: path '…/secrets/safix/users/bob/secrets.yaml' does not exist`, an error `builtins.tryEval` does not catch, so the probe stops being a probe. The line is load-bearing. Reverted.
- [x] 2.10 Verify: `nix build .#checks.x86_64-linux.safix-installer-sole .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-consumption-system` green, 2.6's new field observed failing when the enable gate is removed from `modules/consume/installer.nix:298` — now `:331` — ; drills in 2.7, 2.8 and 2.9 observed — all three green; removing the gate turns `disabledOverResolution.manifest` and `.activationStep` from `false` to `true` and `safix-installer-sole` reddens on exactly that field (`unit` stays `false`, since this fixture selects the activation form), while `resolutionNonEmpty` and the four minted `paths` are unchanged in both readings

## 3. The installer manifest reads the collapsed option

- [x] 3.1 In `modules/consume/installer.nix:144-149`, change `installedEntries = builtins.attrValues config.safix.installed;` to read `config.safix.secrets`, and rename the binding to `resolvedEntries` — `installer.nix:151`
- [x] 3.2 Update the three other references to that binding: `modules/consume/installer.nix:104` (`preflightRemediation`'s count), `:178` (`failedAssertions`' fold), `:187` (`manifest.secrets`) — all three now read `resolvedEntries` (`:104`, `:180`, `:189`)
- [x] 3.3 Rewrite the comment at `modules/consume/installer.nix:144-148`, which today distinguishes "the typed set" from "the raw resolution": after the collapse there is one set, and the sentence must say that the option a consumer reads and the set this manifest is built from are the same value, while keeping the citations for what the provisioner's type does carry (`modules/sops/default.nix:46`, `:51-53`) — `installer.nix:144-150`, both citations kept
- [x] 3.4 Confirm the two refusals copied from `manifest-for.nix:11-28` into `installer.nix:151-178` are unedited: the collapse changes which option they read over, never what they refuse — confirmed: `failedAssertions`' body (`:159-179`) and its provenance comment (`:153-158`) are unchanged; only the fold's final argument moved from `installedEntries` to `resolvedEntries`
- [x] 3.5 Severity drill: point 3.1 at an empty attrset literal and confirm `safix-installer-sole`'s `established` (task 4.3) and `safix-installer-store`'s path diff (task 6) both redden, which is the evidence the manifest is built from the option rather than from anything else; revert — both redden: `safix-installer-sole` fails while building its `actualJson` (`error: stack overflow; max-call-depth exceeded`, reached through the now-empty manifest) and `safix-installer-store` fails with the manifest's path map collapsing to `null` against the expected four paths. Reverted.
- [x] 3.6 Verify: `nix build .#checks.x86_64-linux.safix-installer-manifest .#checks.x86_64-linux.safix-installer-store .#checks.x86_64-linux.safix-installer-sole .#checks.x86_64-linux.safix-installer-refusals` green; drill in 3.5 observed — all four green

## 4. Every check that reads the option follows it

- [x] 4.1 `modules/flake/checks/consumption.nix:415-419`: change `systemView = viewOf (nixosFor "bob").config.safix.installed;` to read `.config.safix.secrets`, and rewrite the comment above it, which today explains the choice of `safix.installed` over `sops.secrets` — it must now say that the system scope delivers through safix's own single option while the provisioner's stays empty, so exactly one installer acts on the resolved set — `consumption.nix:415-419`, comment rewritten to the single option
- [x] 4.2 `modules/flake/checks/installer.nix:399`: change `parityManifest`'s third argument from `manifestFixture.config.safix.installed` to `manifestFixture.config.safix.secrets` — `installer.nix:399`
- [x] 4.3 `modules/flake/checks/installer.nix:861`: change `soleFacts.actual.established` to read `builtins.attrNames manifestFixture.config.safix.secrets`, keeping the expected list `[ bob-service ops-handover ops-tooling team-vault ]` and the comment recording that the fixture resolves the four entries `safix-consumption-system` also reads — `installer.nix:914`; expected list and comment unchanged
- [x] 4.4 `modules/flake/checks/portability.nix:350`, `:361`, `:377`, `:543`: change `person`, `machine`, `machineRaw` and `serviceOwnership.system` to read `.safix.secrets` on the nixos shape — all four moved (`portability.nix:359`, `:370`, `:386`, `:552`)
- [x] 4.5 Leave `portability.nix:262-268` (`decided`) and `:273` (`viewOf`) unedited: both already exclude `path` and `sopsFileHash`, which is what keeps the three shapes comparable now that the nixos shape carries a minted path — unedited; `safix-portability-system` and `safix-portability-home` are green with the nixos shape carrying minted paths, which is that exclusion doing the work
- [x] 4.6 `modules/flake/checks/entrypoints.nix:21-23`: rewrite the sentence that names `nixos.nix`'s `installed` option to name `secrets` instead, keeping the mechanism claim verbatim in substance — the option's `type` field reads `options.sops.secrets.type` off a real provisioner module and stays an unforced thunk while `_module.check = false`, which is why building the `options` tree bare still returns — `entrypoints.nix:22`; the mechanism sentence is otherwise byte-identical
- [x] 4.7 Confirm no other check file references either option: `modules/flake/checks/{vault,vault-projection,namespace,exported,materialization,custody,subjects,generators,policy,bridge*,cli,keepassxc,real-clan,examples,gate-guard,integration,single-runtime,envelope}.nix` and `collision-fixture/declaring-module.nix` need no edit; a repo-wide search for `safix\.installed` must return zero hits outside `openspec/changes/archive/` — zero hits across `modules/**`, `crates/**`, `examples/**`, `flake.nix` and `README.md`. The surviving hits are all prose: the archive; this change's own `proposal.md`/`design.md`/`tasks.md`; the queued `own-the-installer` change's artifacts, which state this collapse as their precondition; and two `CHANGELOG.md` entries — the released `own-secret-installer` one (7.5 leaves it) and 7.4's migration note, which has to name the option it removes
- [x] 4.8 Severity drill: revert 4.6's comment to name `installed` and confirm nothing reddens — then record beside it that this comment is prose a reader relies on rather than a checked claim, which is why 4.7's zero-hit search is the mechanism that catches it — nothing reddens, and the drill can say why exactly: `safix-module-entrypoints` has the same derivation path both ways (`/nix/store/bkl9kq9p8rpqi8n31snwjyh8wy4qdkhr-structure-safix-module-entrypoints.drv`), so no check anywhere can observe the sentence. It is prose a reader relies on, and 4.7's search is the only mechanism that catches a stale name in it. Reverted.
- [x] 4.9 Verify: `nix build .#checks.x86_64-linux.safix-module-entrypoints .#checks.x86_64-linux.safix-consumption-system .#checks.x86_64-linux.safix-portability-system .#checks.x86_64-linux.safix-portability-home` green, and the repo-wide `safix\.installed` search returns hits only under `openspec/changes/archive/` — all four green; the search's remaining hits are the prose enumerated in 4.7

## 5. The refusal fixture enters through the resolver seam

- [x] 5.1 Rebuild `refusalFixtureWith` (`modules/flake/checks/installer.nix:725-736`) so it no longer defines `safix.installed`: the fixture module sets `safix.lib = { violations = [ ]; materialize = _args: _cfg: entry; }`, `safix.user = "bob"`, `safix.hostname = "server"`, `safix.identity.sshKeyPaths = [ "/etc/ssh/safix-fixture-identity" ]`, and keeps `safix.enable = true`, `nixpkgs.hostPlatform = system`, `system.stateVersion = "24.05"` — `installer.nix:751-769`, exactly those definitions and no others
- [x] 5.2 Record beside it, in a comment, that the fixture now enters through `common.resolvedFor`'s own seam (`modules/consume/common.nix:449-468`, which on this path reads only `cfg.lib.violations` and `cfg.lib.materialize`) *because* `safix.secrets` is read-only — that is the collapse's point, not a workaround for it — and that `cfg.lib.subjects` is not needed because `tags`' default reads it only when `safix.machine` is set (`common.nix:361-364`) — `installer.nix:740-750`; the `tags` citation is re-anchored to `common.nix:333-336` after 1.4's deletion
- [x] 5.3 Confirm the identity is present in the fixture so `common.noSystemIdentityMessage` (`nixos.nix:104-105`) cannot pre-empt the refusal under test, and that `sops.validateSopsFiles` is left at its default so the copied outside-the-store and missing-file refusals in `installer.nix:151-178` are reached — present, and the pre-emption is real: an otherwise identical fixture without it makes `builtins.tryEval (builtins.deepSeq …safix.secrets)` report `success = false`, the no-system-identity `throw` firing before the manifest is built. `validateSopsFiles` is left at its default in the fixture, and the identity citation is re-anchored to `nixos.nix:117-118`
- [x] 5.4 Leave `outsideStoreFixture` (`installer.nix:739-741`) and `missingFileFixture` (`:742-744`) calling `refusalFixtureWith` with the same entry literals (`outside.sopsFile = "/etc/hosts"`, `missing.sopsFile = "${builtins.storeDir}/safix-fixture-absent/nope.yaml"`), so what the refusals are measured against is unchanged — both unedited (`installer.nix:771-777`)
- [x] 5.5 Severity drill: drop the `safix.identity.sshKeyPaths` line from 5.1 and confirm both refusal fields in `safix-installer-refusals` (`installer.nix:882`, `:899`) still report `refuses = true` but for safix's identity message rather than for the refusal under test — then restore it, which is the evidence the fixture measures the manifest builder's refusals and not the resolver's — confirmed, and sharper than the task's wording: with the line dropped `safix-installer-refusals` is green with a *byte-identical derivation*, so neither field moves at all. The reason the resolver has taken over is separately visible — forcing that fixture's own `safix.secrets` without the identity gives `tryEval` `success = false` (the identity `throw`), where the identity-carrying fixture resolves — but no field of the check can tell the two apart: `refuses` is a bare `fires` over the manifest, and `namesTheStore`/`namesTheFile` are computed from `common.sopsFileOutsideStoreMessage`/`sopsFileMissingMessage` directly rather than from the fixture's failure. Restored; the identity line stays precisely because the check cannot notice its absence.
- [x] 5.6 Severity drill: drop the copied outside-the-store block from `modules/consume/installer.nix:151-178` and confirm `safix-installer-refusals`' `outsideStore` field reddens while `missingFile` stays green, and vice versa for the missing-file block — the evidence the two refusals are independently held through the rebuilt fixture; revert — **did not reproduce, and the finding is recorded rather than worked around.** Dropping either block leaves `safix-installer-refusals` green with a byte-identical derivation. The reason is the one `refusalFixtureWith`'s own comment already states: in pure evaluation an out-of-store `sopsFile` is unreadable and an absent store path unhashable, so once safix's block is gone the forced `sopsFileHash` aborts instead, `fires` stays `true`, and the `namesThe*` fields never read the fixture's failure at all. No in-evaluation field can separate the reasons, because nix offers no way to read a `throw`'s message from `tryEval`; what the check does hold is the two messages' wording against the message functions, and what holds the blocks' presence is this task line plus `safix-installer-coexistence`'s real-binary runs. Not a regression of this change: the same structure held before the collapse. Reverted.
- [x] 5.7 Verify: `nix build .#checks.x86_64-linux.safix-installer-refusals` green; drills in 5.5 and 5.6 observed — green; both drills observed, both as byte-identical derivations, which is what 5.5's and 5.6's notes record

## 6. `safix-installer-store`'s expected side becomes an independent oracle

- [x] 6.1 Change `entryPathContract` (`modules/flake/checks/installer.nix:429-438`) to map over `config.flake.safix.lib.materialize { user = "bob"; machine = null; hostname = "server"; tags = [ ]; scope = "system"; } fixture.config` instead of over `fixture.config.safix.secrets`, keeping the per-entry formula `entry.path or "${fixture.config.safix.installer.symlinkPath}/${name}"` and keeping `symlinkPath` read off the fixture's own option so `movedFixture` still measures the coupling at the option rather than at a literal — `installer.nix:442-453`; formula and `symlinkPath` read unchanged
- [x] 6.2 Rewrite the comment at `installer.nix:429-433` to say what is now true: the expected side reads the declarations' own resolution through the same `materialize` call `common.resolvedFor` makes (`common.nix:460-468`; published at `modules/flake/safix/default.nix:64,175`), before any module minted anything, so the diff at `:1094-1103` is between two independent computations rather than one option against itself — `installer.nix:429-441`, both citations kept and 6.4's drill result recorded there
- [x] 6.3 Confirm both consumers are unedited in shape: `expectedPathsJson` and `movedPathsJson` (`installer.nix:1057-1058`) and the `jq '.secrets | map({(.name): .path}) | add'` comparison in `safix-installer-store` (`:1094-1103`) — both unedited; only `entryPathContract`'s own body moved
- [x] 6.4 Severity drill (the change's own severity risk, from `design.md` C4): remove the `lib.optionalAttrs (!(entry ? path)) { path = …; }` mint from task 2.1's definition and confirm `safix-installer-store` reddens on the entry-path contract for both `manifestFixture` and `movedFixture`; revert. If it stays green, the tautology is not repaired and this change is not done — reddens: `safix-installer-store: entry paths broke the root-and-default contract`, the diff showing `ops-handover`, `ops-tooling` and `team-vault` at `/run/secrets/<name>` — the provisioner's own default — where the oracle expects `/run/safix/<name>`, with the declared `bob-service` path unchanged on both sides. The check's script aborts at `manifestFixture`'s diff, and the mint it removes is the one definition both fixtures read; 6.5 is the separate evidence that `movedFixture`'s own paths are measured. Reverted.
- [x] 6.5 Severity drill: leave the mint in place and move `movedFixture`'s `safix.installer.symlinkPath` without moving `secretsMountPoint`, confirming `safix-installer-store` still holds the root and the default as one claim rather than passing on the unmoved half; revert — reddens with `safix-installer-store: secretsMountPoint did not follow its option`, so the unmoved half is caught rather than passed over. Reverted.
- [x] 6.6 Severity drill: point 6.1's oracle back at `fixture.config.safix.secrets` and re-run 6.4's drill, confirming the check goes green with the mint removed — the direct demonstration that the oracle, not the formula, is what makes `safix-installer-store` falsifiable; revert — green, with the mint removed and the oracle pointed back at the option: `entry.path or …` takes the `entry.path` branch on every entry and the manifest is diffed against itself. That is C4's tautology reproduced on demand, and 6.4 is the same tree with the oracle restored going red. Reverted.
- [x] 6.7 Verify: `nix build .#checks.x86_64-linux.safix-installer-store` green; drills in 6.4, 6.5 and 6.6 observed, with 6.4's result recorded in the check's own comment — green; all three drills observed, and 6.4's result is written into `entryPathContract`'s comment

## 7. Documentation: the option table, the installer narrative, and the changelog

- [x] 7.1 `README.md:783`: rewrite the option-table row to `| `safix.secrets` | read-only | what resolved; at system scope typed by `sops.secrets`' own entry type and carrying the path each entry arrives at |`, adding no row — the surface shrinks rather than grows, which is the visible win — `README.md:783`, no row added and none removed
- [x] 7.2 `README.md:869-895`: add one sentence to the system-scope installer narrative, beside the `safix-installer-sole` sentence at `:871` that says `sops.secrets` stays empty, naming `config.safix.secrets` as both the single readable surface and the manifest's own source, so a consumer told the provisioner's option is empty is also told where to look — one sentence added immediately after that sentence (`README.md:878`)
- [x] 7.3 Confirm `README.md:851`'s "installed" is the English word in the home-activation guarantee and needs no edit, and that a search of `README.md` for `safix.installed` returns zero hits before and after — confirmed: the only `installed` hits in `README.md` are "no user package installed" in the home-activation guarantee (now `:857`) and "what is installed" in 7.2's new sentence; `safix.installed` returns zero hits before and after
- [x] 7.4 `CHANGELOG.md`: add a breaking nix-surface entry under `## [Unreleased]` (the section begins at `:22`), headed for the single option, stating that `config.safix.installed` is gone with no alias so reading it is an evaluation error naming the option; that `config.safix.secrets` is now the whole of what a system configuration reports — typed by the provisioner's own entry type, carrying the path each entry will arrive at, and the exact set the installer manifest is built from; why (the two options were one set read twice, and the one a consumer was told to read was the one the installer did not use); that the user scope is unchanged; and that an entry which declared no path now carries one at system scope, for a consumer who was relying on its absence — added as "One option for what arrived at system scope" at the head of `[Unreleased]`, with all five statements and 7.7's archive sentence
- [x] 7.5 Leave `CHANGELOG.md:101-102` exactly as written: it is the released `own-secret-installer` entry, it was accurate when written, and 7.4's note supersedes it rather than rewriting history — per the versioning policy at `CHANGELOG.md:17-19`, which makes an option-surface change breaking whether or not any rust changed — unedited; it now reads at `:134-135`, having been pushed down by other `[Unreleased]` entries landing in the same wave
- [x] 7.6 Leave `openspec/changes/archive/2026-08-22-own-secret-installer/design.md:77-81,223-226` and `.../tasks.md:52-57,107` unedited for the same reason — unedited
- [x] 7.7 Severity drill: none is available for prose, so instead confirm the `safix\.installed` repo-wide search from 4.7 covers `README.md`, `CHANGELOG.md`'s `[Unreleased]` section and every `.nix` file, and record in 7.4's entry that the archive is the one place the old name legitimately survives — the search covers all three (zero `.nix` hits anywhere, zero in `README.md`, and in `[Unreleased]` only 7.4's own note), and 7.4's entry ends with the sentence naming the archive as the one place the old name legitimately survives
- [x] 7.8 Verify: the `safix\.installed` search returns hits only under `openspec/changes/archive/`; `README.md`'s option table and installer narrative read correctly end to end with no dangling reference to a second option — README reads end to end with one option named at both scopes; the search's non-archive hits are the planning prose and the two changelog entries enumerated in 4.7, and no source file names the removed option

## 8. Whole-surface verification

- [ ] 8.1 Run `nix flake check` and confirm every check is green, with particular attention to the eight `safix-installer-*` checks, `safix-consumption-system`, `safix-consumption-refusals`, both `safix-portability-*` checks, and `safix-module-entrypoints`
- [ ] 8.2 Confirm `modules/flake/checks/gate-guard.nix`'s `linuxOnlyChecks` list (`:44-59`) is unchanged: this change adds and removes no check, so a diff there is a mistake
- [ ] 8.3 Confirm no rust target needs rebuilding for this change — nothing under `crates/**` reads either option — and that no refusal snapshot, `refusal_codes!` entry, or reporter arm is touched
- [ ] 8.4 Severity drill: re-run the three drills the report names as this change's own severity floor and record each result beside the mechanism it covers — 6.4 (mint removed → `safix-installer-store` red), 1.9 (`secretsType` weakened to `attrsOf raw` → `safix-consumption-system`, `safix-portability-system`, `safix-installer-manifest` red), 2.6/2.10 (`safix.enable = false` over a non-empty resolution → no installer surface appears)
- [ ] 8.5 Verify: `nix flake check` green; the three drills in 8.4 observed and recorded; `options.safix ? installed` false on both a nixos and a home fixture
