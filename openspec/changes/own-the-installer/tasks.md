# Tasks: own-the-installer

Citations are as read while designing this change; re-read the named lines before editing, since this change lands after `collapse-installed-into-secrets` and line numbers drift.
This change assumes that one applied: one `safix.secrets` option per scope, read-only, typed through `common.sharedOptions`' `secretsType` argument, and no `safix.installed` anywhere.
No real fleet identifier, hostname, or recipient enters this repository; fixtures use `alice`, `bob` and `carol`, and synthetic `age1` strings, matching the existing consumption fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.

Group order is the landing order, and it is forced: the entry type must exist before the system module can stop reading `options.sops.secrets.type`, the manifest schema must exist before either module can emit it, the program must exist before either module can invoke it, and the `sops-nix` input can only be deleted once no check references it.
The input deletion is task 9.6 and nothing before it can be validated with the input already gone.

## 1. The entry type safix declares

- [x] 1.1 Add `secretEntryType` to `modules/consume/common.nix`, a `lib.types.submodule` taking the system-scope `cfg` it needs for the `path` default, declaring exactly: `name` (`str`, default `config._module.args.name`), `key` (`str`, default `name`), `path` (`str`, default `"${cfg.installer.symlinkPath}/${name}"`), `mode` (`str`, default `"0400"`), `owner` (`nullOr str`, default `null`), `group` (`nullOr str`, default `null`), `uid` (`int`, default `0`), `gid` (`int`, default `0`), `sopsFile` (`path`, no default), `format` (`enum [ "yaml" ]`, default `"yaml"`), `restartUnits` (`listOf str`, default `[ ]`), `reloadUnits` (`listOf str`, default `[ ]`) — `common.secretEntryType { cfg }` is the twelve-field submodule, and `cfg.installer.symlinkPath` is the only thing it reads off `cfg`, inside `path`'s default, so a check can call it with a stub
- [x] 1.2 Write each option's `description` so a newcomer needs no other document: what the field is for, who reads it, and for `path` that its default is what keeps the store root and the per-entry default inseparable — each of the twelve carries one; `path`'s states that a root moved without the default writes safix's links into whatever store owns the old root instead of colliding where a collision would be visible
- [x] 1.3 Change `modules/consume/nixos.nix:60`'s `common.sharedOptions` call to pass `secretsType = common.secretEntryType { inherit cfg; }` in place of `options.sops.secrets.type`; leave `modules/consume/home.nix:154`'s `lib.types.attrsOf lib.types.raw` unchanged — spelled `lib.types.attrsOf (common.secretEntryType { inherit cfg; })`, since `safix.secrets` is an attrset of entries and `secretEntryType` is the entry submodule task 1.1 names; agreed with the check side over hub, and `getSubOptions [ ]` reaches the entry's own options either way. Home scope unchanged.
- [x] 1.4 Delete the `<symlinkPath>/<name>` path mint from `modules/consume/nixos.nix:141-144` (the `lib.mapAttrs` over `cfg.secrets`), which the type's `path` default now performs; the definition site itself is retired, not merely emptied — the `else` branch is now bare `resolved`; observed in a real system manifest: `ops-handover` carries `path = "/run/safix/ops-handover"` with no mint in either module
- [x] 1.5 Rewrite `modules/consume/common.nix:192-206`'s comment block: it currently records what the provisioner's type carries and what it does not. It becomes a record of what safix's own type carries, why `sopsFileHash` is absent (task 2.4), and that the two store/existence refusals still live in `installer.nix` rather than in the type — the block above `secretEntryType` does all three; the corresponding block at `nixos.nix:71-85` was rewritten with it, since that is where the provisioner's type was described in this tree
- [x] 1.6 Rewrite `modules/consume/common.nix:184-190`'s `sopsFileMissingMessage` and `sopsFileOutsideStoreMessage` provenance comments: both messages stay byte-identical, and the comment stops calling them copies of `manifest-for.nix:11-28` and states them as safix's own — comments rewritten. Deviation, ruled by Main mid-flight: `sopsFileOutsideStoreMessage` is **not** byte-identical, because its remedy named `sops.validateSopsFiles`, an option safix no longer reads; one token changed, to `safix.installer.validate`, and the check side re-pinned the literal. `sopsFileMissingMessage` is byte-identical. Observed live: a fixture whose document sits outside the store refuses with the new sentence.
- [x] 1.7 Add to `modules/flake/checks/installer.nix` a `typeContract` assertion reading `common.secretEntryType`'s `getSubOptions [ ]` and comparing the option name set and each default against an accepted literal, so a field added, removed or re-defaulted is a failing check — landed as the new linux-only check `safix-installer-type` (`typeFacts`): a twelve-name `fields` list, the eight literal `defaults` (`mode` "0400", `owner` null, `group` null, `uid` 0, `gid` 0, `format` "yaml", `restartUnits` [ ], `reloadUnits` [ ]), `configuredDefaults` holding that `name`, `key` and `path` each carry a default at all — asking `getSubOptions` for their value would force `_module.args.name` outside the `attrsOf` that supplies it — and `documentIsRequired` holding that `sopsFile` carries none. `nix build .#checks.x86_64-linux.safix-installer-type` green.
- [x] 1.8 Add a fixture check that an entry declaring no `path` resolves to `<symlinkPath>/<name>` and one declaring a `path` keeps it, with the expectation derived from `config.flake.safix.lib.materialize …` as an independent oracle rather than from the type's own default — `typeFacts.mintedPaths` over a two-entry fixture (`declares`, carrying a path; `mints`, carrying none), read off `config.safix.secrets` so the type's own default is the thing measured, with the expectation written from the declarations rather than from the type. The fleet-wide half is `safix-installer-store`'s path map, whose oracle is `config.flake.safix.lib.materialize`.
- [x] 1.9 Severity drill: change the type's `path` default to a literal `/run/secrets/<name>` and confirm 1.8's assertion and `safix-installer-store`'s `entryPathContract` both redden, which is the evidence the store root and the entry default are held as a pair rather than separately — OBSERVED, both halves, once the peer's file was free: with `path`'s default changed from `"${cfg.installer.symlinkPath}/${config.name}"` to `"/run/secrets/${config.name}"`, `safix-installer-type` FAILED on `mintedPaths` (the path-less `mints` entry moved out of the store while the path-declaring `declares` entry stayed put) and `safix-installer-store` FAILED on "entry paths broke the root-and-default contract", its oracle still expecting the store's own root. Restored byte-for-byte and both checks are green again. 10.10 is this drill from the check's own side and is the same observation.
- [x] 1.10 Verify: `nix build .#checks.x86_64-linux.safix-installer-store` and the check carrying 1.7-1.8 green, drill in 1.9 observed — all three done

## 2. The manifest schema in `safix-core`

- [x] 2.1 Add `crates/safix-core/src/install.rs` with `pub struct Manifest` and `pub struct ManifestSecret`, both `#[serde(deny_unknown_fields)]` and `#[serde(rename_all = "camelCase")]` where the JSON spelling requires it, carrying exactly the fields design I2 lists, and register the module in `crates/safix-core/src/lib.rs` — `install.rs` carries `Manifest`, `ManifestSecret` and `ManifestLogging`, each `deny_unknown_fields` + `camelCase`, plus `Format`, a single-member enum so a `format` outside it is a deserialization refusal; `pub mod install;` is in `lib.rs`
- [x] 2.2 Add `pub const MANIFEST_VERSION: u32 = 1;` and a `Manifest::validate_version` that refuses a version it does not know, naming the version read and the version supported — `install.rs`'s `MANIFEST_VERSION` and `Manifest::validate_version`; the refusal reads "the installer manifest declares schema version 2, and this safix reads version 1"
- [x] 2.3 Add the refusal variants this group needs to `crates/safix-core/src/error/` with `refusal_codes!` entries in `error/code.rs`: `ManifestUnreadable { path, cause }`, `ManifestUnparsable { path, cause }`, `ManifestVersionUnknown { found, supported }`, `ManifestModeUnparsable { name, mode }`, `ManifestOwnerUnknown { name, owner }`, `ManifestGroupUnknown { name, group }`, `ManifestKeyMissing { name, document, key }` — all seven added, plus six the later groups need and this group is where they are spelled: `IdentityKeyFileUnreadable`, `InstallSshKeyUnconvertible`, `InstallDecryptFailed`, `InstallDocumentUnparsable`, `InstallMountFailed`, `InstallRuntimeDirUnknown { asked }` (the field is `asked` rather than `source`, which `thiserror` reads as an error source)
- [x] 2.4 Emit `manifestInputHash` from nix rather than from rust: in `modules/consume/installer.nix`, when `cfg.installer.validate`, set it to a `builtins.hashString "sha256"` over the concatenated `builtins.hashFile "sha256" sopsFile` of each distinct `sopsFile`, and `null` otherwise. `Manifest` deserializes it into an `Option<String>` the installer never reads — emitted at both scopes over `distinctSopsFiles = lib.unique (map (s: s.sopsFile) …)`. Observed: two store roots differing only in one document's bytes give `manifestInputHash` `1720d0d5…` and `92a21fda…`, and with `safix.installer.validate = false` the field is `null` in a real built manifest.
- [x] 2.5 Add a unit test per refusal in 2.3 over a hand-written JSON literal, plus one asserting the real manifest shape round-trips through `serde_json` unchanged — `install::tests`, 24 tests, all green: `the_real_manifest_shape_round_trips`, the version, unknown-entry-field, unknown-top-level-field, format, unreadable, unparsable, mode, owner and group refusals
- [x] 2.6 Add a reporter sample arm in `crates/safix/src/reporter.rs`'s `sample` for each new `Code`, and accept the two insta snapshots (plain and graphical) each produces — thirteen arms appended after `Code::VaultRelocationUnreadable`; 26 snapshots accepted under `crates/safix/src/snapshots/` (`plain-`/`graphical-` × `manifest_*`, `identity_key_file_unreadable`, `install_*`)
- [x] 2.7 Severity drill: remove `deny_unknown_fields` from `ManifestSecret` and confirm the unknown-field test in 2.5 turns green when it should refuse, which is the evidence the attribute is what refuses rather than a coincidence of field ordering — observed: with the attribute removed, `install::tests::an_unknown_entry_field_is_refused_rather_than_ignored` FAILED (the `neededForUsers` field parsed and was dropped, the whole `Manifest` printed in the panic), while the top-level test kept its own attribute and stayed green. Reverted.
- [x] 2.8 Severity drill: drop the `manifestInputHash` emission in 2.4 and confirm that editing a fixture ciphertext file no longer changes `system.build.safix-manifest`'s derivation path, which is the evidence the field is what makes the derivation a function of the ciphertext — **not observable as specified, and the reason is recorded rather than worked around**: under `safix.installer.validate` the store-membership refusal requires every `sopsFile` to be a nix store path, so editing a document's bytes moves that store path, which changes the manifest's own JSON text and therefore its derivation path whether or not the hash is emitted. The drill cannot isolate the field. What was observed instead, over two store roots differing only in one document's bytes: the field is non-null and differs (`1720d0d5…` against `92a21fda…`), so it is a function of the ciphertext rather than of the path; with the emission dropped (validation off) it is `null`. The field remains what makes the derivation ciphertext-dependent for a consumer whose document reaches the manifest by a stable path, which is the case sops-nix's own `sopsFileHash` existed for.
- [x] 2.9 Verify: `cargo test -p safix-core install::` green (30 passed), the new insta snapshots accepted, drill in 2.7 observed. 2.4 and 2.8 are the nix half of this group and belong to the wave that owns `modules/`.

## 3. `safix install`: the verb, the flags, the check modes

- [x] 3.1 Add `usage::INSTALL` to `crates/safix/src/usage.rs`, describing the manifest argument, the three check modes, `--ignore-passwd`, `--dry-run`, and stating that this is the verb an activation runs rather than one an operator types — added after `UPLOAD`; its second paragraph says "This is the verb an activation runs, not one an operator types"
- [x] 3.2 Add `Verb { name: "install", help: usage::INSTALL, run: install_command }` to `crates/safix/src/main.rs`'s `VERBS`, last, after `upload`, with a comment stating why the position is last rather than interleaved — appended last, with the comment naming it the only machine-facing verb in an operator-facing lifecycle order
- [x] 3.3 Add the `install` line to `usage::SCAFFOLD`'s verb list, in the same position, and rewrite the `upload` entry's closing sentence at `usage.rs:840-843` so that ongoing delivery is attributed to `safix install` during activation rather than to "sops-nix reading the committed file" — both done; the closing sentence now reads "activation already does, through `safix install` reading the manifest safix's own nix half built". Deviation, recorded: the scaffold's "--entry" paragraph counted the verbs ("Thirteen of the fourteen"), and two verbs land in this batch, so it is now count-free — "Every subcommand but generate behaves identically under --entry" — agreed with the slice adding `view`.
- [x] 3.4 Add `install_command` to `crates/safix/src/main.rs`: parse the manifest path and the three flags, refuse an unknown `--check-mode` value naming the three it accepts, read `NIXOS_ACTION` and treat `dry-activate` as implying `--dry-run`, and call into `install::run` — flags read in any order around the one positional; an unknown mode is the usage refusal, which is where the three spellings are written down once: `safix: usage: safix install <manifest> [--check-mode=off|manifest|document] [--ignore-passwd] [--dry-run]`
- [x] 3.5 Implement `install::run`'s two checking tiers: `CheckMode::Manifest` performs validation (version, mode octal parse, owner/group resolution with numeric fallback, `format`) and returns; `CheckMode::Document` additionally verifies each declared key is in its document; `CheckMode::Off` proceeds to the filesystem sequence — `format` is validated by the parse, since `Format` is a single-member enum. Deviation from design I3, decided in this batch: the document mode does **not** decrypt. It reads each distinct document's cleartext structure through the new `sops::document::holds_key`, because sops enciphers leaf values and leaves the mapping keys in the clear, and because this mode is what the manifest derivation's check phase runs inside a build sandbox holding no identity — a mode needing a key is a mode no build can run, which is also why the `-check-mode=sopsfile` it replaces read structure rather than plaintext. An absent or unreadable document is `FileUnreadable` naming it; a key that is not in it is `ManifestKeyMissing` naming the entry, the document and the key. Held by `the_document_mode_refuses_what_the_schema_mode_accepts` and by `the_document_mode_needs_no_identity_and_no_sops`, which runs the mode with `SAFIX_SOPS` pointed at a script that fails loudly if invoked and `SOPS_AGE_KEY_FILE` at a path that does not exist.
- [x] 3.6 Implement `--ignore-passwd`: skip every user, group and keys-group lookup and force owner and group to `0`, and apply the same forcing when `NIXOS_ACTION=dry-activate` — `validate`'s early return and `keys_group`'s; `install_command` sets both `dry_run` and `ignore_passwd` when `install::dry_activation_requested()`. Held by `install::tests::a_numeric_owner_needs_no_database_and_ignore_passwd_forces_zero`.
- [x] 3.7 Regenerate `crates/safix/tests/snapshots/upload__safix_help.snap` — regenerated after the `view` row landed; the diff carries both new verb rows, the count-free `--entry` sentence and the rewritten `upload` closing sentence. The two `unknown_subcommand` snapshots are deliberately left to the run that follows every verb landing.
- [x] 3.8 Add `crates/safix/tests/install.rs` driving the verb through the existing harness: a manifest accepted under each check mode, a manifest rejected under `document` but accepted under `manifest` (documents absent), and an unknown `--check-mode` value refused naming the three — nine tests, all green, against the real binary and the real sops: the schema mode over absent documents, the two tiers disagreeing, the unknown mode, an unknown version, an unknown field, a full user-mode install with generation rotation and pruning, the dry run, the dry activation, and the committed fixture manifest with a mutation of it
- [x] 3.9 Register `crates/safix/tests/install.rs` as a whole-target claim in `modules/flake/checks/single-runtime.nix` as `safix-install`, with a doc comment naming its drill, the way `safix-bridge-sync-converge` is registered — `safix-install = claim "safix-install" "install"`, last in the attribute set, with a comment enumerating the eleven windows the target holds, naming both of its drills (3.10's two-tier disagreement, 3.11's dry activation) and recording what the VM test holds instead of it: mounting, chowning and restart propagation, none of which a test process may do
- [x] 3.10 Severity drill: make `CheckMode::Manifest` also decrypt, and confirm the "accepted under `manifest`, rejected under `document`" test in 3.8 turns red — observed: with `if options.check_mode != CheckMode::Off` in place of `== CheckMode::Document`, `the_document_mode_refuses_what_the_schema_mode_accepts`, `a_manifest_is_accepted_under_the_schema_mode_without_reading_any_ciphertext` and `the_hand_written_fixture_manifest_is_accepted_and_a_mutation_of_it_is_not` all FAILED. Reverted.
- [x] 3.11 Severity drill: stop reading `NIXOS_ACTION` and confirm a test asserting a dry activation leaves the store's symlink unmoved turns red — observed: with `dry_activation_requested` returning `false`, only `the_host_s_own_dry_activation_signal_implies_the_switch` FAILED, on the symlink having moved from generation 1 to 2; the other eight stayed green. Reverted.
- [x] 3.12 Verify: `cargo test -p safix --test install` green (13 passed), drills in 3.10 and 3.11 observed. The `nix build` of `safix-install` waits on 3.9, which is the nix half. The verb was also run by hand against the committed fixture, which is the smoke run:

  ```
  $ SAFIX_ERROR_FORMAT=plain target/debug/safix install --check-mode=manifest crates/safix/tests/support/install-manifest.json
  $ echo $?
  0
  $ sed 's/"version": 1/"version": 2/' crates/safix/tests/support/install-manifest.json > /tmp/mutated-manifest.json
  $ SAFIX_ERROR_FORMAT=plain target/debug/safix install --check-mode=manifest /tmp/mutated-manifest.json
  safix: the installer manifest declares schema version 2, and this safix reads version 1
  $ echo $?
  1
  $ SAFIX_ERROR_FORMAT=plain target/debug/safix install --check-mode=sopsfile crates/safix/tests/support/install-manifest.json
  safix: usage: safix install <manifest> [--check-mode=off|manifest|document] [--ignore-passwd] [--dry-run]
  $ echo $?
  1
  ```

## 4. Identity assembly, `ssh-to-age`, and the two environment overrides

- [x] 4.1 Replace `Command::new("age-keygen")` at `crates/safix-core/src/keygen.rs:226` with a lookup of `SAFIX_AGE_KEYGEN` falling back to `age-keygen`, following `crates/safix-core/src/sops/mod.rs:68-75`'s shape exactly — `keygen::KEYGEN_BINARY_VARIABLE` and `keygen::keygen_binary()`, read at the one spawn site
- [x] 4.2 Add `install::ssh_to_age`, a subprocess driver looking up `SAFIX_SSH_TO_AGE` and falling back to `ssh-to-age`, reading one ssh private key path and returning its age form as a `Secret` — `install::ssh_to_age` over `install::SSH_TO_AGE_VARIABLE`; the conversion itself is `convert_ssh_key(program, key)`, so the identity assembly resolves the binary once and a test can name a script without setting a variable in its own process (`std::env::set_var` is `unsafe` under the 2024 edition)
- [x] 4.3 Add `ssh-to-age` to the package's runtime closure wherever `sops` already is, and to `modules/flake/devshell.nix` beside `pkgs.sops` — added to the devshell beside `pkgs.sops`. On the closure: the package itself pins neither, by policy (`modules/flake/rust.nix:56-60`, "the runtime drives sops, git and nix as subprocesses and links none of them"), so what pins them for an activation is the module: both `modules/consume/installer.nix`'s `installerScript` and `modules/consume/home.nix`'s export `SAFIX_SOPS=${pkgs.sops}/bin/sops` and `SAFIX_SSH_TO_AGE=${pkgs.ssh-to-age}/bin/ssh-to-age` by store path, which is task 5.4's mechanism applied to both tools. The check-side backend lists belong to the slice that owns `modules/flake/checks/`.
- [x] 4.4 Implement `install::assemble_identity`: create `<secretsMountPoint>/age-keys.txt` at mode `0600`, append the age form of each `ageSshKeyPaths` entry, append the contents of `ageKeyFile`, and export `SOPS_AGE_KEY_FILE` for the decrypt step — the export is on the sops child rather than on this process, through the new `Sops::decrypt_document_json(file, identity)`: `std::env::set_var` is `unsafe` under the 2024 edition and this workspace forbids unsafe code, and the effect on sops is the same one an exported variable would have had
- [x] 4.5 Preserve the asymmetry verbatim: an unreadable `ageKeyFile` is a refusal naming the path (new variant `IdentityKeyFileUnreadable { path, cause }`, with a code, a sample arm and two snapshots); an absent or unconvertible ssh key prints one line to stderr and is skipped — the variant, the code `safix::identity_key_file_unreadable`, the sample arm and both snapshots are in; the skip path prints the `InstallSshKeyUnconvertible` refusal as one line through `Progress`
- [x] 4.6 Update `modules/consume/common.nix:402-406` and `:422-426` — the `identity.keyFile` and `identity.sshKeyPaths` descriptions — so each cites safix's own installer for the behaviour it describes rather than `sops-install-secrets`, keeping the asymmetry itself stated — both rewritten: the keyFile description states that safix's own installer treats a set-but-unreadable key file as fatal naming the path while a missing ssh key path is written to stderr and skipped, and that safix defines no option outside its namespace from either; the sshKeyPaths description names safix's own `ssh-to-age` subprocess and adds that at system scope `deriveHostKeys` is the fallback and at user scope there is nothing to derive
- [x] 4.7 Add unit tests over a temporary directory: a key file that is present, one that is absent (refusal), three ssh keys of which one does not convert (two lines in the assembled file, one line on stderr), and the assembled file's mode — `an_absent_age_key_file_is_a_refusal_naming_the_path`, `a_present_key_file_is_appended_and_the_assembled_file_is_private` (which asserts mode `0600`), `three_ssh_keys_of_which_one_does_not_convert_leave_two_lines_and_one_complaint` (four paths: two convertible, one refusing script, one absent; two lines assembled, both skips reported)
- [x] 4.8 Add a hermetic check exercising both overrides by pointing `SAFIX_AGE_KEYGEN` and `SAFIX_SSH_TO_AGE` at scripts of the check's own, asserting each is invoked — `safix-installer-overrides` in `modules/flake/checks/installer.nix`, and `nix build .#checks.x86_64-linux.safix-installer-overrides` is green. Both stubs are `pkgs.writeShellScript`s that record the call in a marker file the check names in the environment and then behave as the real tool does. `SAFIX_SSH_TO_AGE` is driven through a real user-mode install over ciphertext the check encrypts, with two ssh keys of which one does not convert, so the asymmetry is exercised beside the invocation: the marker records the call, the installer's own skip line reaches stderr, and the entry decrypts with the identity assembled from the key that did convert. `SAFIX_AGE_KEYGEN` is driven through the user scope's own install script (`systemd.user.services.safix`'s `Service.ExecStart`), which is the only place in the tree `age-keygen` is reached outside the operator-facing `keygen` verb — and that verb needs a declaring repository a sandbox has none of.
- [x] 4.9 Severity drill: make an unreadable `ageKeyFile` a skip rather than a refusal and confirm 4.7's refusal test reddens; separately make an unconvertible ssh key fatal and confirm 4.7's three-key test reddens — both observed in one run, each mutation reddening only its own test: `an_absent_age_key_file_is_a_refusal_naming_the_path` FAILED ("a named key file that is not there is fatal: .../age-keys.txt") and `three_ssh_keys_of_which_one_does_not_convert_leave_two_lines_and_one_complaint` FAILED ("an unconvertible key is not fatal: InstallSshKeyUnconvertible { path: .../bad-one, reason: ... exited 1 }"); the other 22 stayed green. Reverted.
- [x] 4.10 Severity drill: ignore `SAFIX_AGE_KEYGEN` and confirm 4.8 reddens, which is the evidence the override is read rather than merely declared — OBSERVED: with `modules/consume/home.nix`'s `export SAFIX_AGE_KEYGEN="${SAFIX_AGE_KEYGEN:-${pkgs.age}/bin/age-keygen}"` reduced to the store path alone, so the activation's own value can no longer be overridden from the environment, `safix-installer-overrides` FAILED — the marker file records no `keygen-was-called` line, because the real `age-keygen` ran instead of the check's stub. Restored; the check is green again. The defect that blocked this drill is fixed: a check mode no longer expands `%r`, so the home-scope manifest's `checkPhase` builds in a sandbox and `nix build .#checks.x86_64-linux.safix-installer-overrides` is green.
- [x] 4.11 Verify: `cargo test -p safix-core install::` green (30 passed) and `keygen::` green, drill in 4.9 observed. 4.3, 4.6, 4.8 and 4.10 are the nix half of this group.

## 5. Decryption

- [x] 5.1 Add `install::decrypt_documents`: group the manifest's secrets by `sopsFile`, run `sops decrypt --output-type json <file>` once per distinct document through the existing `SAFIX_SOPS` lookup, and hold each result as a `Secret` — `decrypt_documents` over a `BTreeSet` of the distinct documents, through the new `Sops::decrypt_document_json`, which is `Sops::from_environment`'s own binary; held by `two_entries_of_one_document_cost_one_sops_invocation` in `crates/safix/tests/install.rs`. This is the installing path alone: the document check mode reaches no subprocess at all (see 3.5).
- [x] 5.2 Add `install::extract_key`: resolve a `/`-nested key path inside a decrypted JSON document, returning `ManifestKeyMissing` when any segment is absent, and add unit tests for a flat key, a nested key, a missing leaf, and a segment that is not an object — all four, plus a leaf that is not a scalar and the number/boolean renderings
- [x] 5.3 Hold the value discipline: no decrypted value reaches an argument vector or the environment, and no `String` or `Vec<u8>` of a decrypted value outlives the write. Add a unit test asserting the extraction returns the crate's own value type rather than a `String` — `the_extraction_returns_the_zeroizing_type_rather_than_a_string`; the type is what holds it, since `Secret` has no `Display`, no `ToString` and no byte accessor (compile-time probes in `secret.rs`), so the test could not be written any other way. The JSON parse inside `extract_key` runs over a `Zeroizing` buffer and nothing from it is returned.
- [x] 5.4 Reference the `sops` binary from the activation script by store path, never through `PATH`, in both `modules/consume/installer.nix`'s `installerScript` and the home-scope equivalent; keep `safix.installer.agePlugins` on `PATH`, which is the one thing that must be — both scripts export `SAFIX_SOPS=${pkgs.sops}/bin/sops` and `SAFIX_SSH_TO_AGE=${pkgs.ssh-to-age}/bin/ssh-to-age`; `activationEnvironment`'s `PATH` is `lib.makeBinPath cfg.installer.agePlugins` and nothing else, and the `agePlugins` description records why the plugins are the one thing that must be found by name — sops execs them itself, by the name the recipient string carries
- [x] 5.5 Add a check asserting the activation script names `sops` by store path and that `PATH` in `activationEnvironment` is composed of the age plugins alone — `safix-installer-refusals` gained four rows. `script.namesSopsByStorePath`: a store path ending `/bin/sops` appears in the installer script's text. `script.reachesSopsByNameAlone`: no line reaches a bare `sops decrypt`/`encrypt`, expected false, so the two answers are independent rather than one restated. `script.runsTheInstaller`: the script names safix's own binary by store path too, which is what makes the activation step's reference to the script a reference to safix's own build. And an `environment` row over the activation step's own text — `HOME='/var/empty'`, `PATH=''` where no plugin is named, and `PATH='<plugin>/bin'` over a second fixture naming one age plugin, so the claim is measured against a value rather than against emptiness. `nix build .#checks.x86_64-linux.safix-installer-refusals` green.
- [x] 5.6 Severity drill: replace the store-path reference with a bare `sops` and confirm 5.5 reddens, which is the evidence risk 6 of design I14 is closed by a check rather than by the prose — OBSERVED: with `modules/consume/installer.nix`'s `export SAFIX_SOPS=${pkgs.sops}/bin/sops` reduced to `export SAFIX_SOPS=sops`, `safix-installer-refusals` FAILED with `script.namesSopsByStorePath` answering `false` against a `true` expectation, and nothing else in the row moving — `reachesSopsByNameAlone` stayed `false`, which is the evidence the two regexes are independent rather than one restated. Restored; the check is green again.
- [x] 5.7 Severity drill: decrypt per entry rather than per document and confirm a test counting `sops` invocations over a two-entry, one-document fixture reddens — observed: with the distinct-document `BTreeSet` replaced by a `Vec`, `two_entries_of_one_document_cost_one_sops_invocation` FAILED with left 2, right 1. Reverted.
- [x] 5.8 Verify: `cargo test -p safix-core install::decrypt install::extract install::tests::the_document_check` green as part of the 30, the invocation-count claim green in `cargo test -p safix --test install`, drill in 5.7 observed. 5.4, 5.5 and 5.6 are the nix half of this group.

## 6. The filesystem sequence and the four preserved behaviours

- [x] 6.1 Implement `%r` expansion (`install::expand_runtime_dir`): `$XDG_RUNTIME_DIR` on linux, the output of `getconf DARWIN_USER_TEMP_DIR` on darwin, `%%` splitting to a literal `%`, applied to `secretsMountPoint`, `symlinkPath` and every secret `path`, and only when `userMode` — split in two: `runtime_directory()` is the platform lookup (both arms) and `expand_runtime_dir(template, runtime)` the substitution, so the substitution is stated against literals on either platform; `expand_user_paths` resolves the directory once and applies it to both roots and every path Expansion is step 1 of the installation and of nothing else: `run` reaches it only after the check-mode early return, because `%r` is a session's runtime directory and a check mode runs where there is no session — the manifest derivation's own check phase is a nix build. Held by `install::tests::a_user_mode_manifest_checks_without_a_runtime_directory_to_expand_against` and, deterministically, by the integration test `a_user_mode_manifest_checks_with_no_runtime_directory_in_the_environment`, which runs both modes with `XDG_RUNTIME_DIR` empty and then installs the same manifest with a real one.
- [x] 6.2 Implement the mount step: `mkdir -p` at `0751`; a no-op under `userMode`; otherwise `ramfs`, or `tmpfs` with `noswap` when `useTmpfs` with an `EINVAL` fallback dropping `noswap`, with `MS_NODEV|MS_NOSUID|MS_NOEXEC` and `mode=0751`, skipped when already mounted with that filesystem's magic, then chown `0:keys` — `mount_store` and `mount_memory_backed`; the magic read is `filesystem_magic` over `rustix::fs::statfs`, and a non-linux host reaching the mount is `InstallMountFailed` with `Unsupported` rather than a silent install onto a disk
- [x] 6.3 Add the mount and filesystem-magic syscalls through a crate exposing a safe interface (design I15), extending `crates/safix-core/Cargo.toml`'s existing `statfs` dependency rather than introducing an `unsafe` block, and confirm `#![forbid(unsafe_code)]` is untouched — the workspace `rustix` line gained the `mount` feature (a feature flag on a dependency already present; no new crate), and `rustix::mount::mount` is the safe wrapper. `#![forbid(unsafe_code)]` stands unweakened in both crate roots and no `allow` was added anywhere.
- [x] 6.4 Implement the generation directory: read the symlink at `symlinkPath`, parse its basename as an unsigned integer, increment, remove a same-named leftover, `mkdir` at `0751`, chown `0:keys` outside user mode — `next_generation`/`generation_of`; held by `the_generation_number_is_the_symlink_target_s_basename_incremented` (no store yet is 1, a target of `7` is 8, a target naming no number is 1 again)
- [x] 6.5 Implement the write step: parent directories at `0751` chowned `0:keys`, each file at its declared mode, chowned `owner:group` outside user mode — `make_directory`, `write_entry` and `own`; the declared modes are asserted on disk (`0400` and `0440`) by the user-mode install test
- [x] 6.6 Implement the restart/reload diff: byte-compare each old `<symlinkPath>/<name>` against the new one, collect `restartUnits`/`reloadUnits` for new and changed entries, skipped wholesale under `userMode` — `differs`, which compares through `Secret::equals` so the old value is held in the zeroizing type too
- [x] 6.7 Implement propagation: `systemctl --no-block restart/reload` when `SOPS_RESTART_UNITS_VIA_SYSTEMCTL` is set, otherwise write the unit names to `/run/nixos/activation-restart-list` and `-reload-list` — `propagate`, reading `install::RESTART_VIA_SYSTEMCTL` and nothing else to choose between them
- [x] 6.8 Implement the atomic symlink swap, skipped under `--dry-run`, followed by the per-entry symlinks for every entry whose `path` differs from `<symlinkPath>/<name>`, creating parent directories and replacing whatever is there until the link matches — `swap_symlink` (a staged link renamed over the old one, so no reader sees the store absent) and `link_entry`; both held by the user-mode install test, which asserts the declared out-of-store path is a link back into the generation
- [x] 6.9 Implement `keepGenerations` pruning, keeping the last N generation directories — `prune`; the install test's second run asserts generation 1 is gone and 2 is the symlink's target under `keepGenerations = 1`. It also never removes the generation the symlink currently names, whatever the count says: a booted host found the pair — a dry run wrote generation 2 without swapping onto it, and the arithmetic then deleted generation 1, which the live symlink still pointed at, so a run promising to change nothing emptied the store. Held by `install::tests::pruning_never_removes_the_generation_the_symlink_names`.
- [x] 6.10 Preserve, and test individually, the four behaviours design I9 names — three are held here and one is the VM test's: (a) a non-symlink at `symlinkPath` is removed — `something_that_is_not_a_symlink_at_the_store_root_is_removed`; (b) the diff returns early when `symlinkPath` does not exist — `units_of` carries it and `a_first_install_restarts_nothing_because_nothing_was_running` holds it, with drill 6.12 as its severity evidence; (d) dry-activate skips the swap and only the swap — `a_dry_run_performs_everything_but_the_swap` and `the_host_s_own_dry_activation_signal_implies_the_switch`, which now also assert the live generation survives. (c) the environment variable's selection between `systemctl` and the activation lists is a `!userMode` path and every run this suite can make is unprivileged, so it is group 12's VM test to hold.
- [x] 6.11 Severity drill: make the non-symlink case a refusal rather than a removal — observed against the rust analogue of `safix-installer-coexistence`, since the nix check is task 10.5 and lands later: with the removal replaced by an `AlreadyExists` refusal, `something_that_is_not_a_symlink_at_the_store_root_is_removed` FAILED and the other ten stayed green. Reverted.
- [x] 6.12 Severity drill: remove the early return when `symlinkPath` is absent and confirm a first-install test reddens on a missing previous generation, which is the evidence the stage-2-init case is held — now reachable, and observed. The early return moved out of `run` into `units_of(changed, previous_exists)`, which is what makes the case statable without privileges: with no previous generation every entry reads as changed, and propagating from that restarts every unit the manifest names on a machine still coming up. With the `if !previous_exists` return deleted, `install::tests::a_first_install_restarts_nothing_because_nothing_was_running` FAILED and the other 29 stayed green. Reverted.
- [x] 6.13 Severity drill: perform the swap under `--dry-run` and confirm the dry-activate test in 3.11 reddens — observed: with the guard removed, both `a_dry_run_performs_everything_but_the_swap` and `the_host_s_own_dry_activation_signal_implies_the_switch` FAILED on the symlink having moved to generation 2. Reverted. The guard is now an early return rather than a skipped step: a dry run ends after writing the new generation and performs no swap, no per-entry symlink and no prune, which is the shape the program this replaces has and the absence of which was the store-emptying defect recorded on 6.9. `a_dry_run_performs_everything_but_the_swap` now also asserts the live generation and its files survive.
- [x] 6.14 Verify: `cargo test -p safix-core install::` green (30 passed) and `cargo test -p safix --test install` green (13 passed), drills in 6.11, 6.12 and 6.13 observed.
- [x] 7.1 Declare, in `modules/consume/installer.nix`'s `options.safix.installer`: `package` (`package`, default the flake's own `packages.safix`), `validationPackage` (`package`, default the build-platform build), `validate` (`bool`, default `true`), `keepGenerations` (`int`, default `1`), `useTmpfs` (`bool`, default `false`), `log` (`listOf (enum [ "keyImport" "secretChanges" ])`, default `[ ]`), `environment` (`attrsOf str`, default `{ }`), `agePlugins` (`listOf package`, default `[ ]`), each with a description a newcomer can act on — all eight declared. The flake's own `packages.safix` reaches them through `flake.nix` rather than through the module, because a consumption module imports nothing and so holds no input to read a package out of: each published name (`nixosModules.{safix,default}`, `homeModules.{safix,default}`) is the consumption file plus one wrapper module, `installerPackageDefaults`, whose whole content is `safix.installer.package = lib.mkDefault inputs.self.packages.${pkgs.stdenv.hostPlatform.system}.safix` and `validationPackage = lib.mkDefault inputs.self.packages.${pkgs.stdenv.buildPlatform.system}.safix`. Verified against a real `nixosSystem` over the fixture fleet and a real home-manager profile, neither naming either option: `safix.installer.package`, `validationPackage`, `system.build.safix-manifest` and the home scope's `safix.installer.manifest` all evaluate, and the two packages resolve to `safix-rs-0.2.0.drv`. The modules' own defaults stay `pkgs.safix or (throw common.installerPackageMessage)`, which is the flakeless fallback for a tree importing `modules/consume/nixos.nix` as a plain path with no flake at all; the wrapper's `mkDefault` pre-empts it for every published name, and the wrapper's `imports` names one file inside this package's own consumption directory and nothing else.
- [x] 7.2 Replace `activationEnvironment` (`installer.nix:41-44`): `sopsCfg.environment` → `cfg.installer.environment`, `sopsCfg.age.plugins` → `cfg.installer.agePlugins`; `HOME=/var/empty` and the `lib.makeBinPath` construction unchanged — done, both unchanged
- [x] 7.3 Replace `requiredIdentities` (`:62-65`): `sopsCfg.age.keyFile` → `cfg.identity.keyFile`, and drop the `generateKey` conjunct, which has no system-scope counterpart — done; the conjunct survives only at home scope, where `safix.identity.generateKey` exists
- [x] 7.4 Delete `hasGnupgSource` (`:67`) and every use of it, and delete the gnupg conjunct from `sufficientIdentities`' guard (`:76`) — deleted in `installer.nix` and in `nixos.nix`; `grep -rn 'gnupg' modules/consume` is empty. Observed: a fixture with `services.openssh.enable = false` and no named identity now refuses with `noSystemIdentityMessage` rather than being suppressed.
- [x] 7.5 Replace `sufficientIdentities`' sources (`:76-81`): `sopsCfg.age.keyFile` → `cfg.identity.keyFile`, `sopsCfg.age.sshKeyPaths` → `cfg.identity.sshKeyPaths`; update each identity's `origin` string from `sops.age.*` to the safix option it now names, since those strings appear in the refusal text — done. Deviation with a reason: the list read is `identitySshKeyPaths`, one binding equal to `cfg.identity.sshKeyPaths` where a consumer named any and `cfg.identity.derivedHostKeys` otherwise, because the old read of `sops.age.sshKeyPaths` carried the derivation (`nixos.nix` defined that option from it) and reading `cfg.identity.sshKeyPaths` alone would have silently dropped every derived host key from the preflight, the manifest and the unit's mounts. The `origin` strings already distinguished the two cases and are unchanged.
- [x] 7.6 Replace the invocation (`:141`): `exec ${sopsCfg.package}/bin/sops-install-secrets ${manifest}` → `exec ${cfg.installer.package}/bin/safix install ${manifest}` — done, with the two store-path exports of task 5.4 ahead of it
- [x] 7.7 Replace the resolved-set read (`:149`): `config.safix.installed` → `config.safix.secrets` — already `config.safix.secrets` after `collapse-installed-into-secrets`; the comment above it now names `common.secretEntryType` as what types it
- [x] 7.8 Re-gate the assertion block (`:155-180`) on `cfg.installer.validate` in place of `sopsCfg.validateSopsFiles`, and re-gate `manifest`'s own guard (`:180-181`) the same way; both messages stay byte-identical — re-gated on `cfg.installer.validate` at both sites. Messages: `sopsFileMissingMessage` byte-identical; `sopsFileOutsideStoreMessage` one token changed per Main's ruling, recorded at 1.6. Observed live: a resolved entry whose document sits outside the store refuses with "Add it to the Nix store or set safix.installer.validate to false".
- [x] 7.9 Rewrite the manifest JSON (`:186-222`): add `version = 1`; keep `secrets`, `secretsMountPoint`, `symlinkPath`; `keepGenerations` ← `cfg.installer.keepGenerations`; `ageKeyFile` ← `cfg.identity.keyFile`; `ageSshKeyPaths` ← `cfg.identity.sshKeyPaths`; `useTmpfs` ← `cfg.installer.useTmpfs`; `userMode = false`; `logging.{keyImport,secretChanges}` ← `cfg.installer.log`; add `manifestInputHash` per 2.4; delete `templates`, `placeholderBySecretName`, `gnupgHome`, `sshKeyPaths` — done; `ageSshKeyPaths` reads `identitySshKeyPaths` for the reason at 7.5. Smoke run, against a real `nixosSystem` over the fixture fleet's `bob` on `server`: the built manifest carries exactly the twelve top-level keys and twelve per-entry keys the schema declares, `ageSshKeyPaths = ["/etc/ssh/ssh_host_ed25519_key"]` from the derivation, and `target/debug/safix install --check-mode=manifest --ignore-passwd <manifest>` exits 0 against it.
- [x] 7.10 Replace the `checkPhase` (`:226-229`): `${sopsCfg.validationPackage}/bin/sops-install-secrets -check-mode=${if sopsCfg.validateSopsFiles then "sopsfile" else "manifest"}` → `${cfg.installer.validationPackage}/bin/safix install --check-mode=${if cfg.installer.validate then "document" else "manifest"} --ignore-passwd` — done verbatim. The hazard I first recorded here (that document mode would need a decryptable identity inside the build sandbox) was resolved on the rust side rather than by diverging: `--check-mode=document` opens each named document and verifies every declared `/`-nested key is present in its cleartext mapping keys, which is what the `sopsfile` mode it replaces did, and decrypts nothing. So the sandbox needs no identity and no backend, and the stronger mode stays the default. The nix-side prose was corrected to say so in three places: the `validate` option's description, the `checkPhase` comment, and the README's installer section.
- [x] 7.11 Replace the unit's reads (`:372-398`): `environment = cfg.installer.environment // { SOPS_RESTART_UNITS_VIA_SYSTEMCTL = "1"; }`, `path = cfg.installer.agePlugins`, and `RequiresMountsFor` built from `cfg.identity.keyFile` and `cfg.identity.sshKeyPaths` alone, with the two gnupg contributions (`:390-391`) deleted; `wantedBy sysinit.target`, the `sysinit-reactivation.target` edges and `DefaultDependencies=no` unchanged — done; `RequiresMountsFor` is the key file plus `identitySshKeyPaths` (7.5's reason: a derived host key needs its mount too). The three unchanged facts are unchanged.
- [x] 7.12 Delete `modules/consume/nixos.nix`'s whole `sops = { … }` definition block (`:139-152`), and rewrite the comment at `:130-137` — which explains why `sops.secrets` is left empty — into a statement that safix defines no option outside its own namespace — deleted, and the now-single-element `lib.mkMerge` collapsed to a plain `config = { … }`. The comment above `config` states that every value safix defines is `safix.*` and that a consumer's other framework keeps every option they set. `grep -rn 'sops\.' modules/consume` is empty.
- [x] 7.13 Rewrite `derivedHostKeys`' comment (`nixos.nix:39-46`) so the rule is safix's own rather than a re-implementation of the provisioner's, keeping the catch-22 reason verbatim, since `secret-installation`'s "A host key another store deployed is usable" scenario rests on it; the expression itself is unchanged — the expression is unchanged but it moved: it is now the default of a read-only internal `safix.identity.derivedHostKeys` in `installer.nix`, because `installer.nix` needs the same list for the manifest and the unit's mounts and a second copy in `nixos.nix` would be two rules to keep in step. The catch-22 reason is kept, in that option's description and in `deriveHostKeys`'. Observed: the derived key reaches a built manifest's `ageSshKeyPaths`.
- [x] 7.14 Delete `modules/consume/nixos.nix:93`'s read of `config.sops.gnupg.*` and the identity-sufficiency branch it fed — deleted; `hasIdentity` is now `keyFile != null || sshKeyPaths != [ ] || derivedHostKeys != [ ]`, and the comment records that a gnupg configuration used to be tolerated here as evidence something could decrypt
- [x] 7.15 Rewrite `modules/consume/common.nix:113-131`'s prose block, which explains the provisioner's host-key default and its keyfile fatality, into a statement of safix's own defaults and its own fatality — rewritten inside `noIdentityMessage`; it now says a person is not a host, that the system scope derives and the user scope has nothing to derive, that safix's own installer is what treats an unreadable keyFile as fatal, and that safix refuses at evaluation because the installer's own check only reports on paths it was handed
- [ ] 7.16 Add a check asserting no file under `modules/consume/` contains the string `sops.` as an option read or definition, with the `sops` binary reference and the `sopsFile` field name excluded by exact spelling rather than by a loose pattern
- [ ] 7.17 Severity drill: reintroduce one `sopsCfg` read and confirm 7.16 reddens, which is the evidence the namespace claim is held mechanically rather than by review
- [x] 7.18 Severity drill: collapse `validationPackage` into `package` and confirm a cross-build evaluation of the manifest derivation fails, which is the evidence risk 5 of design I14 is closed — observed, in the shape the code actually has. Unmutated, a cross fixture (`nixpkgs.crossSystem = { config = "aarch64-unknown-linux-gnu"; }`) that names only `safix.installer.package` **refuses at evaluation** of `system.build.safix-manifest.drvPath`, with `installerPackageMessage`'s closing sentence — "Under a cross build set safix.installer.validationPackage as well: the manifest's own check runs on the build platform, where the activation runs on the host's." With the `checkPhase` collapsed onto `cfg.installer.package`, the same cross evaluation succeeds and the built derivation's `checkPhase` names the host-platform package (`/nix/store/dw688mz3…-safix/bin/safix install --check-mode=manifest --ignore-passwd "$out"`), i.e. a binary the build platform is not promised to be able to run. Natively the two options are the same derivation, so a native consumer still names one option. Reverted.
- [ ] 7.19 Verify: `nix build .#checks.x86_64-linux.safix-installer-store .#checks.x86_64-linux.safix-installer-ordering .#checks.x86_64-linux.safix-installer-sole .#checks.x86_64-linux.safix-installer-refusals .#checks.x86_64-linux.safix-installer-identity` green, the check in 7.16 green, drills in 7.17 and 7.18 observed

## 8. User scope installs

- [x] 8.1 Declare `options.safix.installer.{secretsMountPoint,symlinkPath}` at home scope, defaulting to `"%r/safix.d"` and `"%r/safix"`, with descriptions stating that `%r` is expanded by the installer against the platform's runtime directory and that the store therefore does not survive a reboot without a login — both declared with those defaults and those sentences; observed in a real home-manager profile's built manifest: `secretsMountPoint = "%r/safix.d"`, `symlinkPath = "%r/safix"`
- [x] 8.2 Declare `options.safix.identity.generateKey` at home scope (`bool`, default `false`), described as minting the key file at activation when it is absent, and naming `safix keygen` as the operator-run alternative — declared through `lib.recursiveUpdate` over `common.sharedOptions`' own `identity` attrset, so `keyFile` and `sshKeyPaths` keep their shared declarations; the description names `safix keygen` and says why the option exists anyway (a profile never activated has no other way to get a key file in place)
- [x] 8.3 Declare the home-scope subset of `safix.installer.*` the user-mode manifest needs — `package`, `validate`, `keepGenerations`, `log` — and state at each that `useTmpfs`, `environment` and `agePlugins` are system-scope only because user mode mounts nothing and runs no unit environment of its own — the four are declared, plus two the tasks' own later steps require: `validationPackage`, because 8.5 asks for the build-platform package, and `manifest`, a read-only handle on the built derivation so the check side can read a real profile's user-scope paths (agreed with the check slice over hub for task 11.12). The three system-scope-only options are named, with the reason, in one comment at the end of the `installer` block rather than repeated at each.
- [x] 8.4 Build the user-mode manifest in `modules/consume/home.nix`: the same `serde` shape as system scope, `userMode = true`, entries from `cfg.secrets` through `materializeFor`'s user-scope output, `ageKeyFile` ← `cfg.identity.keyFile`, `ageSshKeyPaths` ← `cfg.identity.sshKeyPaths`, `version = 1` — built. Each entry's twelve fields are filled here, since this scope's `secretsType` stays `attrsOf raw`: `key` falls back to the attribute name, `path` to `"${symlinkPath}/<name>"`, ownership to null and zero on both axes (the resolver refuses an ownership field at this scope outright). Smoke run against a real `home-manager.lib.homeManagerConfiguration` over the fixture fleet's `alice`: `userMode: true`, `version: 1`, an entry with no declared path at `%r/safix/web-token` and one with a declared path unchanged, and `target/debug/safix install --check-mode=manifest --ignore-passwd <manifest>` exits 0.
- [x] 8.5 Add the build-time check phase to the home-scope manifest derivation, mirroring 7.10, using the build-platform package — mirrored exactly, over `cfg.installer.validationPackage`. The hazard recorded at 7.10 applies here too. This scope also gained the two document refusals the system scope carries, under the same `validate` gate, because without them a document that is absent or outside the store was met by `builtins.hashFile`'s own trace instead of a message naming the entry — observed before the addition, and observed afterwards as `safix: '/tmp/…/secrets.yaml', the sops file of resolved entry 'api-token', is not in the Nix store…`.
- [x] 8.6 Add `systemd.user.services.safix`, linux only, invoking `safix install <manifest>`, gated on `cfg.enable` and a non-empty resolved set — added, gated on `pkgs.stdenv.hostPlatform.isLinux && cfg.secrets != { }` inside the `mkIf cfg.enable` block; `Type=oneshot`, `RemainAfterExit=true`, `ExecStart` the shared install script, `WantedBy=default.target`. Observed: `config.systemd.user.services ? safix` is `true` on a real x86_64-linux profile.
- [x] 8.7 Add `home.activation.safixInstall` on both platforms, registered with `lib.hm.dag.entryAfter [ "writeBoundary" ]`, with a comment stating why a bare string (which becomes `entryAnywhere`) is refused — added under exactly that name (the name my brief spelled `safixInstallSecrets`; `safixInstall` is what this task and design I6 say, and what the check slice is asserting against). The comment states that a bare string becomes `entryAnywhere`, which gives home-manager no ordering to reason about. Observed: `home.activation` carries `safixInstall` and `safixIdentityPreflight` on a real profile.
- [x] 8.8 Add the key-generation step to the activation entry, running `age-keygen -o <keyFile>` through `SAFIX_AGE_KEYGEN` when `cfg.identity.generateKey` and the file is absent, before the install step — in the shared install script, ahead of the install: `export SAFIX_AGE_KEYGEN="${SAFIX_AGE_KEYGEN:-${pkgs.age}/bin/age-keygen}"` then `"$SAFIX_AGE_KEYGEN" -o <keyFile>` under an existence test, with the parent directory created first. Defaulted to the pinned store path but honouring an inherited value, which is what lets the hermetic check of task 4.8 point it at a script of its own.
- [x] 8.9 Delete `modules/consume/home.nix`'s three `sops.*` definitions (`:232`, `:236`, `:240`) and every `sopsCfg` read (`:52-53`, `:58-61`, `:68-70`, `:106`); the refusal-text secret count at `:106` reads `cfg.secrets` instead — all gone; the binding `sopsCfg` no longer exists in the file and `grep -rn 'sops\.' modules/consume` is empty. The remediation's count reads `cfg.secrets`.
- [x] 8.10 Re-gate `home.activation.safixIdentityPreflight` (`:246-249`) from `sopsCfg.secrets != { }` to `cfg.secrets != { }`, keeping its `entryBefore [ "checkLinkTargets" ]` placement — done, placement unchanged
- [x] 8.11 Rewrite the header prose (`home.nix:4-32`), the ordering description (`:176-183`) and the key-source comment (`:190-198`), all three of which describe the provisioner's activation entry, its user unit, and its assertion placement; each becomes a statement about safix's own — the header now describes what this scope installs and the three things user mode does not do, why `%r` is expanded by the installer rather than by nix, and why the install entry sits after the write boundary while the preflight sits before `checkLinkTargets`; the `identityPreflight` option's description and the `safix.secrets` comment were rewritten with them
- [x] 8.12 Rewrite the remediation text (`:104-115`), which currently tells the reader that sops-nix has no per-person key default, into safix's own equivalent statement — it now says safix has no other way to decrypt, that this scope's identity is whatever the two `safix.identity.*` options name and nothing else counts, and that a key which is present and readable but not a recipient still fails later inside safix's own installer
- [ ] 8.13 Add a check evaluating a home-manager fixture on `x86_64-linux` and on `aarch64-darwin`, asserting the linux one carries the user unit and the darwin one does not, and that both carry the activation entry and a manifest with `userMode = true`
- [ ] 8.14 Add integration coverage in `crates/safix/tests/install.rs` for `%r` expansion: a user-mode manifest installed with `XDG_RUNTIME_DIR` set, asserting the store lands under it, and a test asserting `%%` yields a literal `%`
- [ ] 8.15 Add a test asserting a user-mode install mounts nothing, chowns nothing, and restarts nothing: observed by running it as an unprivileged user with a `restartUnits` entry declared and asserting no `systemctl` invocation and no ownership change
- [ ] 8.16 Severity drill: set `userMode = false` in the home-scope manifest and confirm 8.15 reddens on an attempted mount or chown, which is the evidence the three omissions are driven by the field rather than by the scope happening to lack privileges
- [ ] 8.17 Severity drill: register the activation entry as a bare string and confirm 8.13's ordering assertion reddens, which is the evidence the DAG placement is held
- [ ] 8.18 Verify: `nix build .#checks.x86_64-linux.safix-portability-home` and the check in 8.13 green, `cargo test -p safix --test install` green, drills in 8.16 and 8.17 observed

## 9. One module per scope, and the input deletion

- [x] 9.1 Change `flake.nix:63-72`'s `homeModules` binding so `default` and `safix` are the same value, `./modules/consume/home.nix`, and rewrite the comment above it: the dual naming is retained for compatibility, and it is no longer copied from another flake's convention — both names read one `let` binding, `homeModule`, so the equality is by construction. Deviation with a reason, ruled by Main: the binding is the file plus one wrapper module (`installerPackageDefaults`, task 7.1) rather than the bare path, because a consumption module holds no flake input and so cannot reach safix's own build for `safix.installer.package`; the wrapper's `imports` names one file inside this package's own consumption directory and the wrapper itself, and nothing outside. `nix eval .#homeModules --apply 'm: m.default == m.safix'` is `true`, and `modules/consume/home.nix` stays importable as a plain path with no flake.
- [x] 9.2 Change `flake.nix:138-143`'s `nixosModules` the same way — done, through a `nixosModule` binding of the same shape. Recorded finding, accepted by Main: `nix eval .#nixosModules --apply 'm: m.default == m.safix'` is **false** however it is bound, because flake-parts types `flake.nixosModules` as `lazyAttrsOf deferredModule` and stamps each attribute with its own `_file` breadcrumb (`…/flake.nix#nixosModules.default` against `…#nixosModules.safix`); `flake.homeModules` is untyped there, which is why its two names do compare equal. The right assertion is that both names resolve to the same value, and it holds: `let inner = v: (builtins.head (builtins.head v.imports).imports).imports; in inner m.default == inner m.safix` is `true`, and that value is `[ { imports = [ .../modules/consume/nixos.nix <installerPackageDefaults> ]; } ]` for both. Passed to the check slice for task 11.8.
- [x] 9.3 Delete `flake.nix:126-131`'s comment explaining the `.default`/`.safix` split, replacing it with one sentence stating that both names are one value and why both are retained — replaced; the new block states that each scope publishes one module under both names, that both are retained so every existing `imports` line keeps resolving, and records what the split existed for and why that reason has no content now. The `let` block above states what the one wrapper adds and why the bare files stay importable with no flake.
- [x] 9.4 Delete `sops-nix` from `flake.nix:61`'s `outputs = inputs@{ … }` destructuring — the destructuring is now `inputs@{ flake-parts, ... }`
- [x] 9.5 Delete `flake.nix:26-31`'s comment justifying the input — deleted with the input
- [x] 9.6 Delete `flake.nix:32-33` — `sops-nix.url` and `sops-nix.inputs.nixpkgs.follows` — and regenerate `flake.lock`, confirming the root `sops-nix` input edge (`flake.lock:26-27`) and the `sops-nix_2` node (`:263-282`) are gone while clan-core's own node (`:237-262`) remains — deleted; `nix flake lock` reported exactly "Removed input 'sops-nix'" and "Removed input 'sops-nix/nixpkgs'". Verified against `nix flake metadata --json`: the root's inputs are `advisory-db clan-core crane flake-parts home-manager nixpkgs treefmt-nix`, there is exactly one `sops-nix` node left in the lock, and the only node listing it as an input is `clan-core`. `grep -rn 'sops-nix' flake.nix modules/consume` returns nothing.
- [ ] 9.7 Add a check asserting that no attribute under `flake.nixosModules` or `flake.homeModules` has an import closure naming any flake input, over all four published names
- [ ] 9.8 Severity drill: add a fixture module importing a flake input and confirm 9.7 fires on it, which is the anti-vacuity probe the retired `namesSopsNix` predicate carried (`entrypoints.nix:58-71`) and which must survive the predicate's generalization
- [ ] 9.9 Severity drill: point `homeModules.default` back at a two-element `imports` list naming any input and confirm 9.7 reddens
- [ ] 9.10 Verify: `nix flake metadata --json | jq '.locks.nodes | keys'` shows sops-nix present only as clan-core's transitive node, the check in 9.7 green, drills in 9.8 and 9.9 observed

## 10. Check suite: `modules/flake/checks/installer.nix`

- [x] 10.1 Delete `manifestFor` (`:195-211`) and `parityManifest` (`:394-398`), the two bindings that call the provisioner's own manifest builder — both gone; `modules/flake/checks/installer.nix` was rewritten whole around safix's own builder, and no `callPackage` of another flake's `manifest-for.nix` remains
- [x] 10.2 Delete `providerFixture` (`:226-239`), including the `neededForUsers` entry (`:231-234`) and the read of `system.build.sops-nix-users-manifest` (`:239`), and delete the standalone evaluation of the provisioner's home-manager module (`:245`) — `providerFixture`, `usersManifest`, `homeManagerInstrument`, `optionPaths` and `namesAStoreRoot` are all gone with the check they served; the `neededForUsers` string survives in the tree only as one of `safix-installer-roundtrip`'s four refused mutations, which is the honest place for it
- [x] 10.3 Delete `safix-installer-mechanism` (`:953`) and every assertion feeding it; its subject is the provisioner's `extraJson` merge and its `secrets-for-users` relocation, and there is no second builder to measure — the check and its `structural` facts are gone. Deviation, deliberate: the `setupSecrets` merge fixture and the activation wrapper's `ERR`-trap-and-no-`set -e` reading were **not** deleted with it. Neither names sops-nix — both are hand-written NixOS definitions — and they are the mechanism that makes safix registering a name of its own load-bearing, which `secret-installation`'s "The entry has a name of its own" scenario requires recorded. They moved into `safix-installer-ordering` as its `merge` and `wrapper` rows, where the claim they support lives.
- [x] 10.4 Delete `safix-installer-manifest` (`:1008`) and the field-set parity diff (`:1037-1046`); replace it with two checks: `safix-installer-schema`, an accepted snapshot of the built manifest's full structure over the existing manifest fixture, and `safix-installer-roundtrip`, running `safix install --check-mode=document --ignore-passwd` over the built manifest and over four single-field mutations, asserting acceptance once and refusal four times, each naming its field — both landed. `safix-installer-schema` diffs the whole built manifest against the committed `modules/flake/checks/installer-manifest.json`, with exactly one normalization: each `sopsFile` is reduced to its path below the store, because a snapshot cannot carry a store hash. Nothing else is touched — `manifestInputHash` is snapshotted as null, which is what the fixture's `validate = false` emits, and the field's own emission is task 2.8's subject. `safix-installer-roundtrip` runs in two tiers. Deviation, agreed with Main's brief: the built manifest is checked under `--check-mode=manifest`, not `document`, because the fixture fleet's sops files are paths into this flake that no committed file backs, so a document-mode run over it would refuse for want of a file rather than for want of a key — and its four mutations are the unknown version, the non-octal mode, an unknown top-level field and an unknown entry field. The third mutation the task names, a `key` absent from its document, is only expressible under document mode, so the check has a second tier that encrypts a document in the sandbox and holds three answers over it: document mode accepts a key that resolves, schema mode accepts a key that does not, and document mode refuses it naming the key. That is the same evidence plus the evidence that the two modes are not interchangeable.
- [x] 10.5 Port `safix-installer-coexistence` (`:1166`): swap `inputs.sops-nix.packages.${system}.sops-install-secrets` (`:1173`) for the flake's own `packages.safix` invoked as `safix install`, leaving the sandbox, the fixture store and the assertions unchanged — swapped to `self'.packages.safix` and `safix install --ignore-passwd`; the manifest the check writes by hand moved to safix's own schema (`version`, no `sopsFileHash`/`templates`/`gnupgHome`/`sshKeyPaths`/`placeholderBySecretName`/`neededForUsers`, `owner`/`group` null rather than ""), and the sandbox, the two fixture stores, the tree walk and every assertion are unchanged. `nix build .#checks.x86_64-linux.safix-installer-coexistence` green — the destructive branch fires against safix's own binary, and the foreign store is byte-identical after safix's run.
- [x] 10.6 Confirm `safix-installer-store` (`:1051`), `-ordering` (`:1102`), `-sole` (`:1108`), `-refusals` (`:1114`) and `-identity` (`:1123`) evaluate unchanged now that `config.flake.nixosModules.default` needs no sops option tree, and update each fixture's expectation only where a default this change moved is asserted — all five build green. What moved, and nothing else: every fixture's `sops.validateSopsFiles = false` became `safix.installer.validate = false`, and every fixture that forces the manifest or the installer script names `safix.installer.package` (the default is `pkgs.safix`, which this flake's nixpkgs does not carry). `-sole`'s `provisionerInert` row lost its subject — there is no `sops` option tree to find empty — and was replaced by `noForeignNamespace`, `!(config ? sops)`, which is the mechanical form of the same requirement and a strictly stronger reading of it; its `invocations` scan now matches safix's own installer script as well as the old name. `-ordering`'s `runsTheInstaller` rows were re-pointed from a `/bin/safix install` infix to the store path of `system.build.safix-installer`, because the step and the unit both reach the installer through that script rather than naming the binary themselves.
- [x] 10.7 Confirm `safix-installer-store`'s `entryPathContract` still derives its expectation from `config.flake.safix.lib.materialize …` rather than from the type default now carrying the mint, so the check does not go vacuous when the mint moves (task 1.4) — confirmed and unchanged in mechanism; its comment was rewritten to say why it matters more now than before, since an expectation read off the option would be the mint compared with itself
- [x] 10.8 Severity drill: mutate one field of the built manifest and confirm `safix-installer-schema` reddens; mutate the `serde` struct instead and confirm the same check reddens, which is the evidence the snapshot holds both sides of the boundary — first half OBSERVED by the check slice: with `"mode": "0400"` changed to `"0440"` in the accepted snapshot, `safix-installer-schema` FAILED on the diff. Second half now OBSERVED too, by the slice owning `install.rs`: with `#[serde(rename = "keepGens")]` on `Manifest::keep_generations`, BOTH `safix-installer-schema` and `safix-installer-roundtrip` FAILED — and the way the first one fails is the finding. It does not fail on its own diff; it fails on its dependency, because the manifest derivation's own check phase runs `safix install --check-mode=…`, and that refused the built manifest naming the field: "unknown field `keepGenerations`, expected … `keepGens` …". So the snapshot check is a claim about both sides after all, through the derivation it reads. Reverted; both green again.
- [x] 10.9 Severity drill: make `--check-mode=document` accept a `key` absent from its document and confirm `safix-installer-roundtrip`'s third mutation turns green when it should refuse — OBSERVED: with `check_documents`' refusal replaced by a discard of `holds_key`'s answer, `safix-installer-roundtrip` FAILED with its own line, "safix-installer-roundtrip: a key absent from its document was accepted". Reverted; green again.
- [x] 10.10 Severity drill: remove the mint from the entry type and confirm `entryPathContract` reddens, repeating 1.9's drill from the check's own side, which is what holds 10.7's non-vacuity claim — OBSERVED in the same run as 1.9: `safix-installer-store` FAILED on "entry paths broke the root-and-default contract in …safix-manifest.json", the oracle expecting the store's own root where the moved mint put `/run/secrets/<name>`. That is the non-vacuity claim discharged: the expectation is `config.flake.safix.lib.materialize`'s, so it does not move when the mint does.
- [x] 10.11 Verify: every check in `modules/flake/checks/installer.nix` builds green, `nix eval .#checks.x86_64-linux --apply builtins.attrNames` shows `safix-installer-mechanism` and `safix-installer-manifest` gone and `safix-installer-schema` and `safix-installer-roundtrip` present, drills in 10.8-10.10 observed — the attribute list is confirmed: `safix-install`, `safix-installer-{coexistence,identity,ordering,overrides,refusals,roundtrip,schema,sole,store,type,vm}` are present and `safix-installer-mechanism`/`safix-installer-manifest` are gone. All ten sandbox checks are green: `-type`, `-schema`, `-store`, `-ordering`, `-sole`, `-refusals`, `-identity`, `-roundtrip`, `-overrides`, `-coexistence`. `safix-installer-vm` is group 12's own verification and is red on a real defect it found — see 12.5. Drills: 10.8's first half, 10.10 and 1.9 observed; 10.8's second half and 10.9 are rust-side mutations this slice did not make.

## 11. Check suite: consumption, entrypoints, materialization, portability

- [x] 11.1 Delete `sopsHomeCopy` (`consumption.nix:112`) and every use of it — gone, with the two `provisionerSamePathTwice`/`provisionerTwoPaths` rows it fed; `copyOf` stays and now makes two copies of safix's own `modules/consume` directory instead (task 11.5)
- [x] 11.2 Re-point `handForm` (`consumption.nix:187`): the hand-wired comparison arm stops importing the provisioner's module, and the `equivalence` claim (`:575`) becomes "the module form equals the resolver output read back through safix's own entry type"; update the check's own header prose to state the new premise — `handForm` is now a plain home-manager profile with no module of safix's in it at all, handed to the resolver directly. The profile is real rather than an attrset because `alice-alone` declares its path as a function of `cfg.home.homeDirectory`. Header prose rewritten, including a paragraph recording that the premise moved with the dependency. `nix build .#checks.x86_64-linux.safix-consumption` green.
- [x] 11.3 Re-point `viewOf` (`consumption.nix:204-207`) to read each entry field back through `common.secretEntryType` rather than through the provisioner's option types — `viewOf` types the whole set through `lib.types.attrsOf (systemCommon.secretEntryType { cfg = entryCfg; })` in one `evalModules` rather than each entry through the submodule alone, because `name`'s default is the attribute key and only `attrsOf` supplies it. Both sides of `equivalence` go through it, and both `moduleView` and `handView` now read `config.safix.secrets` rather than `config.sops.secrets`.
- [x] 11.4 Re-point the refusal fixture at `consumption.nix:316` to import safix's own module without home-manager's assertion wrapper, unchanged in intent — the sops-nix module is gone from `bareProfile`'s list; the instrument's comment now records that its reason survives the dependency, since safix's own wiring assertions sit outside the enable gate and would be collected together with the identity refusal
- [x] 11.5 Re-establish `safix-module-collision` (`consumption.nix:517`, fixtures `:547-548`, `:557-558`) over two distinct copies of `modules/consume/nixos.nix` reached by two paths that are not the same store path, so the fact `consumer-integration` needs held — importing one declaring module twice is an evaluation error rather than a merge — keeps a subject — `consumeCopyA`/`consumeCopyB` are two `builtins.path` copies of the whole `modules/consume` directory (the whole directory, because `nixos.nix` imports `./installer.nix` relatively), and the two new rows force `safix.lib`'s declaration: one path twice declares once, two paths collide. `nix build .#checks.x86_64-linux.safix-module-collision` green.
- [x] 11.6 Re-base the literal expectations at `consumption.nix:627-631` (`format = "yaml"` and its neighbours) onto safix's own declared defaults, and add a comment stating that the values are unchanged but their source is not, so a literal that silently agrees with a deleted dependency's default cannot go unnoticed — the `entry` literal is now the full twelve-field record safix's own type resolves, `gid`/`owner`/`group`/`reloadUnits`/`restartUnits`/`uid` included, with the comment the task asks for
- [x] 11.7 Generalize `namesSopsNix` (`entrypoints.nix:118`) into `namesAnyFlakeInput`, a predicate over every declared input — `declaredInputs = builtins.removeAttrs inputs [ "self" ]`, with the exclusion's reason recorded: `self` is this repository, so every consumption module is inside it by construction and counting it would answer true for every value under test
- [x] 11.8 Reduce `importAsymmetry` (`entrypoints.nix:159-164`) from four expectations to one symmetric assertion over all four published names, and rebuild the anti-vacuity probe (`:166`) on the new predicate, keeping the reasoning at `:58-71` up to date with what the probe now measures — the four rows became `publishedFormsNamingAnInput`, one row whose value is the *names* of any published form that reaches a flake input, expected `[ ]`, so a form that regains a dependency is named in the diff rather than reported as a bare `true`. The anti-vacuity block gained `namesAnyFlakeInputDetectsOne` over a fixture whose import list names a file inside nixpkgs, expected true, which is what makes the empty list evidence. A `collapsed` row holds each scope's two names reaching one module — measured over the consumption files each name reaches rather than by `==`, because flake-parts types `nixosModules` as `lazyAttrsOf deferredModule` and its merge wraps each attribute with a per-attribute `_file` breadcrumb, so two attributes bound to one value still compare unequal there while `homeModules`, untyped, compares equal. Header prose rewritten throughout. `nix build .#checks.x86_64-linux.safix-module-entrypoints` green. This is also task 9.7's check and 9.8's probe.
- [x] 11.9 Replace `materialization.nix:86-88`'s three `provisioner` evaluations with evaluations of safix's own modules, and re-point `axisOf` (`:90`) from `options.sops.secrets.type.getSubOptions [ ]` to `common.secretEntryType`'s own sub-options, leaving the ownership-asymmetry assertion (`:141-146`) claiming the same thing — the three became `typedThrough systemType`/`typedThrough userType` over one `evalModules` declaring exactly the option under test, where `systemType` is `attrsOf (secretEntryType { cfg = entryCfg; })` and `userType` is `attrsOf raw`, which are the two `secretsType` arguments the two modules pass. `axisOf` now takes a type rather than an evaluation, and the ownership rows read the two scopes' declared types: `user = [ ]`, `system = [ "group" "owner" ]`, unchanged in what they claim. `inputs` is no longer an argument of the file. `nix build .#checks.x86_64-linux.safix-materialization` green.
- [x] 11.10 Rewrite `materialization.nix:8-12`'s header prose: the premise changes from "hand safix's output to the real provisioner and read it back" to "hand safix's output to safix's own declared type and read it back" — done, together with the ownership-asymmetry paragraph, which now states the asymmetry as one between the two scopes' own declared types, and the two severity paragraphs that named the provisioner's defaults
- [x] 11.11 Re-base `portability.nix`'s `format = "yaml"` literals onto safix's own default, and rewrite `:257-264`'s justification for excluding `path`, `owner`, `group` and `sopsFileHash` from `decided`: `sopsFileHash` is dropped from the list entirely (it no longer exists) and the other three are justified as safix's own defaults — the justification is rewritten as asked. The literals themselves needed a mechanism, not a re-base: the home shapes' resolved set is typed `raw` and carries no `name` and no `format` at all, so `lib.getAttrs decided` over it failed outright. `viewOf` now reads every shape's set back through safix's own entry type before any field is compared, which is what makes `format` and `name` safix's own defaults on all three shapes rather than a dependency's on one. `safix.installer.validate = false` replaces `sops.validateSopsFiles = false`, with its comment rewritten: the reason is no longer a per-entry `sopsFileHash` default but the `manifestInputHash` emission and the two document refusals, which hash the same unbacked fixture paths.
- [x] 11.12 Re-base `portability.nix:819-820`'s expected user-scope path from `"/home/alice/.config/sops-nix/secrets/nginx/service-token"` to the `%r/safix/…` path the new default produces, and verify the new literal against a real home-manager profile rather than against the fixture alone — the two `servicePath` rows could not be re-based in place: at user scope the mint is applied while the manifest is built, not in `safix.secrets`, so there is no `path` on the entry to read. They moved into a new check, `safix-portability-user-manifest`, which jq's the path out of a **built** user-mode manifest derivation of a real `homeManagerConfiguration` and holds `%r/safix/nginx/service-token`, beside `userMode = true`, `version = 1`, `symlinkPath = "%r/safix"` and `secretsMountPoint = "%r/safix.d"`. `nix build .#checks.x86_64-linux.safix-portability-user-manifest` is green, and its home fixtures name `safix.installer.validate = false` for the reason the system shape does: this fleet's projection is rendered with an empty root, so every `sopsFile` is a repository-relative path no committed file backs. The `servicePath` rows' place in `safix-portability-home` was taken by `installSurface`, which is task 8.13.
- [x] 11.13 Re-point `portability.nix:500-506`'s `systemIdentity` at `derivedHostKeys`, which is now where the host-key default comes from where safix names no identity — `systemIdentity` now reads `safix.identity.derivedHostKeys` beside `safix.identity.sshKeyPaths`, as two rows rather than one: the derivation moved into its own read-only option, so the consumer's named list is empty on this fixture and the derived list is the host's ed25519 key. `safix-consumption-system`'s `inert.identity` row was re-pointed the same way, and both checks build green — the two were the only expectations in the tree still reading a host-key default off the option a consumer sets.
- [x] 11.14 Confirm by grep that `exported`, `namespace`, `examples`, `cli`, `single-runtime`, `policy`, `bridge`, `bridge-sync`, `custody`, `envelope`, `vault`, `vault-projection`, `subjects`, `generators`, `gate-guard`, `keepassxc`, `real-clan`, `fixture-fleet`, `integration` and `collision-fixture` name no `sops-nix` reference, and leave every one of them unedited — confirmed: `grep -rn 'sops-nix|sops-install-secrets|sopsHomeCopy|parityManifest' modules/flake/checks` returns nothing. Two of the twenty were edited for reasons this task does not cover and which are recorded here: `single-runtime.nix` gained the `safix-install` claim (task 3.9) and `gate-guard.nix`'s `linuxOnlyChecks` list was updated for the renamed and added check names (tasks 10.4, 12.1). Neither edit touches a sops-nix reference, because neither file had one.
- [x] 11.15 Severity drill: make `handForm` re-derive its expectation through the same `materializeFor` call the module form uses and confirm `safix-consumption`'s equivalence assertion goes vacuous — observable as the check staying green under a deliberate `materializeFor` mutation — which is the evidence the two sides stay independent after the re-point — OBSERVED as a standing fact rather than as a mutation, and recorded in the check's own header: `handForm` calls `safix.materialize` and so does the module form, so `equivalence` is vacuous under a `materializeFor` mutation **by construction** and always was — the file's severity prose has said so since before this change ("dropping `key` from `materializeFor`: both forms call that function, so they still agree, and what goes red is the `entry` literal beside them"). What the re-point had to preserve is that the `entry` literal is the independent half, and it does: the literal is now the twelve-field record written from safix's own declared defaults, so a mutation that drops a field reddens it while `equivalence` stays green. The drill as the task words it would therefore change nothing; the pair is the claim and both halves are in place.
- [x] 11.16 Severity drill: make the two collision fixtures in 11.5 the same store path and confirm `safix-module-collision` turns green when it should fail, which is the evidence the check measures two distinct copies rather than one imported twice by name — OBSERVED: with `consumeCopyB` pointed at `copyOf "safix-consume-a"`, `declaringModuleTwoPaths` answered `true` where the expectation is `false`, and `safix-module-collision` FAILED on that one row while `declaringModuleSamePathTwice`, `samePathTwice` and `twoPaths` stayed as they were. Reverted; the check is green again. Note on the task's wording: what a same-store-path fixture produces is a **red** check rather than a green one — the row it makes true is one the check expects false — which is the stronger outcome and the one that proves the two paths really are two.
- [x] 11.17 Verify: `nix build .#checks.x86_64-linux.{safix-consumption,safix-consumption-system,safix-consumption-refusals,safix-consumption-ordering,safix-module-collision,safix-module-entrypoints,safix-materialization,safix-portability-system,safix-portability-home}` green, drills in 11.15 and 11.16 observed — all nine green. `safix-portability-user-manifest`, the tenth and new, is green too. Drill 11.15 recorded as a standing fact, 11.16 observed red and reverted.

## 12. A NixOS VM test

- [x] 12.1 Add `modules/flake/checks/installer-vm.nix` with a `pkgs.testers.runNixOSTest`, registered under `checks.safix-installer-vm` on linux systems only, and add the file to `flake.nix`'s `imports` list — added, gated on `pkgs.stdenv.hostPlatform.isLinux` the way every check in `./installer.nix` is, and listed in `flake.nix`'s `imports` in sorted position (one line, ahead of `./modules/flake/checks/installer.nix`). Also added to `gate-guard.nix`'s `linuxOnlyChecks`, which is what stops the platform gate from shrinking the published set silently. `nix eval .#checks.x86_64-linux.safix-installer-vm.drvPath` evaluates.
- [x] 12.2 Build the test machine: import `config.flake.nixosModules.safix`, bind it to a fixture declaring one shared and one private entry, place an age key file on the machine through its own configuration, and declare one entry's `restartUnits` naming a trivial unit the test can observe — the machine imports `nixosModules.safix` and is bound to a projection written in the file, because the entries have to be encrypted to a key the test holds: a derivation of the file's own mints an age identity and encrypts two documents to it, and the machine carries the identity through `environment.etc."safix-identity.txt"` at mode `0400`. Two entries: `shared-token` at `0400` owned by nobody in particular, and `private-token` at `0440` owned by a declared `observer:observers`, which is what makes the ownership assertion about `owner` and `group` rather than about the default. `private-token` names `safix-observer.service` in `restartUnits`, a oneshot that appends a line to a file the test counts, so "was restarted" is read off the machine rather than off the installer's report.
- [x] 12.3 Assert, after boot: the symlink at `symlinkPath` names generation `1`; each entry's file exists at its declared mode and ownership; the mount at `secretsMountPoint` is the expected filesystem type — asserted: `readlink /run/safix` is `/run/safix.d/1`, `stat -f -c %T /run/safix.d` is `ramfs` (`useTmpfs` is off), both values decrypt to their fixture plaintext, the modes are `400` and `440`, and the owners are `root:root` and `observer:observers`. The shared entry's owner is `root:root` rather than the provisioner's `root:keys`, because safix's own type defaults `owner` and `group` to null and `uid`/`gid` to 0 — a default this change moved, recorded here rather than transcribed.
- [x] 12.4 Assert, after a second activation with one entry's value changed: generation `2` exists, the symlink moved to it, the retained generation count obeys `keepGenerations`, and the unit named in `restartUnits` was restarted — asserted against a specialisation rather than a second `nixosSystem`, so the second closure inherits the test machine's disk and boot configuration and differs in exactly one thing: `safix.lib` is `lib.mkForce`d to a projection naming the second document. A specialisation inherits its parent, so importing the fixture twice would define every option twice; forcing the projection alone is what keeps the rotation a consequence of a changed value rather than of a changed machine. The assertions are the symlink at generation `2`, the changed value read back, the unchanged value read back, exactly one retained generation directory (`keepGenerations = 1`), and a strictly larger start count for the unit.
- [x] 12.5 Assert a dry activation changes neither the symlink nor any file — asserted, and the assertion **found a defect on its first run**, which is the whole reason this check exists. On a booted machine with `keepGenerations = 1`, `switch-to-configuration dry-activate` leaves the symlink at generation `1` as design I9 requires — and then deletes generation `1`'s contents, because the prune is step 12 and runs on a store whose live symlink was deliberately not moved. Observed verbatim: `readlink /run/safix` passes, and the next line, `sha256sum /run/safix/private-token`, fails with "No such file or directory". So "dry-activate skips step 10 and only step 10" is destructive as written: the swap is what makes the new generation live, and pruning to the newest N without it prunes the generation still in use. Reported to Main. The fix is the program's — either the prune is skipped under a dry run too, or it counts from the generation the symlink names rather than from the newest — and this assertion is what holds it either way.
- [ ] 12.6 Severity drill: skip the chown step and confirm 12.3's ownership assertion reddens; skip the prune and confirm 12.4's generation-count assertion reddens. The pair is the evidence the VM test measures the sequence rather than only that the machine booted — NOT OBSERVED: both mutations are in `crates/safix-core/src/install.rs`, held open by a concurrent agent, and each drill costs two VM boots. The two assertions are red-capable by construction and were written for that: the ownership row reads a non-root owner on the private entry while the shared entry stays root, so a skipped chown reddens one and not the other, and the generation count is read off `find`'s own output against `keepGenerations = 1`, which a skipped prune leaves at two.
- [ ] 12.7 Severity drill: make the second activation reuse generation `1` and confirm 12.4 reddens, which is the evidence generation rotation is held on a real host rather than only in a unit test — NOT OBSERVED, for 12.6's reason. The assertion is `readlink /run/safix == /run/safix.d/2`, which a reused generation reddens directly, and the restart assertion reddens with it, since a reused generation has nothing to diff against.
- [ ] 12.8 Verify: `nix build .#checks.x86_64-linux.safix-installer-vm` green, drills in 12.6 and 12.7 observed — the VM builds, boots and runs: `nix build .#checks.x86_64-linux.safix-installer-vm` gets through the boot and every assertion of 12.3, and is **red** on 12.5, on the destructive dry activation recorded there. That is a finding rather than a check defect, and it is the finding the group was added to make reachable: every other installer check is a structural evaluation or a sandboxed invocation, and none of them can prune a live generation. Drills 12.6 and 12.7 are not observed, and deliberately not: each needs a mutation of `crates/safix-core/src/install.rs` plus two VM boots, and running them against a binary whose prune step is about to change would be evidence about a sequence the tree is fixing. They are the right first thing to do after that fix, since the fix moves exactly the step they drill.

## 13. Documentation, changelog, and the migration table

- [x] 13.1 Rewrite `README.md:6-7` and `:56-58`, which name sops-nix as a dependency and as what a consumer's profile reads — the opening line now says safix serves NixOS and home-manager through consumption modules of its own and installs what they resolve with its own `safix install`; the quickstart's fourth-line paragraph now attributes the system-scope host-key derivation to safix and states that a person is not a host, and the pointer to "which of the two module forms to import" is gone with the split
- [x] 13.2 Rewrite `README.md:804-822`, `:833`, `:838-846`, `:854-859` and `:868-886` — the consumption sections describing the `.default`/`.safix` split, the `sops.*` options a consumer tunes, and the normal-priority conflict hazard at `:839` — into the single-module surface, the `safix.installer.*` namespace, and a statement that safix now writes no `sops.*` option at all — done: the chapter opening states that safix reads and defines no option outside its namespace at either scope; the option table gained ten rows for `safix.installer.*` and `safix.identity.generateKey` and its `safix.secrets` row now names safix's own entry submodule; "Which of the two forms to import" became "The two published names", with the collision block restated over two copies of safix's own declaring module; the ownership paragraph, the resolver-refusal paragraph and the whole identity section no longer attribute anything to another framework, and the normal-priority hazard sentence is replaced by the statement that safix defines nothing outside its namespace
- [x] 13.3 Rewrite `README.md:761`, and add the migration rename table from design I13 verbatim to the README, including the user-scope path row and the three-absence row (templates, user-relocated secrets, gnupg) — the sentence that placed `safix.*` "beside sops-nix's `sops.*`" is rewritten, and a new `### Migrating from the sops-nix-backed surface` section carries I13's fifteen-row table verbatim, including the user-scope path row, together with the three absences stated as refusals in the installer section above it
- [x] 13.4 Verify the user-scope path row against a real home-manager profile before publishing it, per design I14 risk 1, and record what was observed beside the table — observed against a real `home-manager.lib.homeManagerConfiguration` for `alice` on `x86_64-linux`, reading `config.safix.installer.manifest`: every entry that declares no path of its own carries `%r/safix/<name>` (`%r/safix/web-token`, `%r/safix/api-token`, `%r/safix/team-vault`, …), the roots are `%r/safix.d` and `%r/safix`, and an entry that does declare a path keeps it (`/home/alice/.config/safix-fixture/alice-alone`). `%r` is what the installer expands per platform, which is what the table's right-hand column spells out as `$XDG_RUNTIME_DIR/safix/<name>` and `$(getconf DARWIN_USER_TEMP_DIR)/safix/<name>`. The old `~/.config/sops-nix/secrets/<name>` spelling was never a default — it was what a fixture's configuration produced — and the row says "was" rather than "defaulted to".
- [x] 13.5 Rewrite `docs/notes/research/zero-knowledge-vault.md:9`, `:21`, `:92`, `:98`, `:108` and `:132`, each of which reasons about what sops-nix can consume; the reasoning about the sops document format is unchanged, and only the attribution of who reads it moves — all six rewritten: the revision line keeps the sops-nix pin but says it is cited only where a finding rests on the installer safix replaced; the feasibility sentence names safix's own installer; the store-prefixed-`sopsFile` finding cites safix's own manifest builder and its `validate` gate; the key-nesting finding cites `secretEntryType` and `--check-mode=document`; and the two age-native-file sentences say that no sops-consuming installer can read them, keeping the upstream Go citation as the evidence
- [x] 13.6 Rewrite `crates/safix/tests/read_path.rs:176-186`'s doc comment, which references `sops-install-secrets` and `SOPS_AGE_KEY_FILE`, to reference safix's own installer and its own identity assembly — the quoted limit now reads "inside safix's own installer, when it decrypts", matching the message `modules/consume/home.nix` actually emits, and the boundary sentence attributes `SOPS_AGE_KEY_FILE` to the installer's own identity assembly, which writes and exports it
- [x] 13.7 Add a `[Unreleased]` CHANGELOG entry marked breaking for the nix surface: the entry type is safix's own, the manifest schema is safix's own and versioned, the `safix.installer.*` namespace replaces every `sops.*` read, the two published module names per scope are one value, the user-scope secret path moves to a runtime directory, templates and user-relocated secrets and gnupg identities are unsupported, and `safix install` is a new verb. The sops-nix claim is phrased exactly as "safix declares no sops-nix input", never as "sops-nix is gone" — added as "**BREAKING** (nix surface): safix owns its installer and declares no sops-nix input", first under `[Unreleased]`, covering all seven points plus the newly required `safix.installer.package` line and the one changed refusal sentence. Its first body line is exactly "safix declares no sops-nix input."
- [x] 13.8 Check the changelog's sops-nix phrasing against `nix flake metadata --json`'s actual node list, so the claim is verified rather than asserted (design I14 risk 8) — checked: the root flake's inputs are `advisory-db`, `clan-core`, `crane`, `flake-parts`, `home-manager`, `nixpkgs`, `treefmt-nix`; the lock holds exactly one `sops-nix` node, and the only node naming it as an input is `clan-core`. The changelog states both facts in those terms.
- [x] 13.9 Audit every guarantee the rewritten README sections state, sentence by sentence, against the check or test in this repository that holds it, and record the mapping in this task — mapping, one row per guarantee the rewritten sections make: one installer acts on the resolved set → `safix-installer-sole`; `config.safix.secrets` is the set the manifest is built from → `safix-installer-store` plus `installer.nix`'s single `resolvedEntries` read; the manifest is one schema and both scopes emit it → `safix-installer-schema` and `install::tests::the_real_manifest_shape_round_trips`, with the two smoke runs recorded at 7.9 and 8.4 as the live evidence; document mode refuses a mutated manifest naming the field → `safix-installer-roundtrip` and `crates/safix/tests/install.rs`; the two check tiers are distinguishable → `the_document_mode_refuses_what_the_schema_mode_accepts`; `manifestInputHash` makes the derivation ciphertext-dependent → the observation recorded at 2.4 (and 2.8's recorded limit); the store roots and the per-entry default move together → `safix-installer-store`'s `entryPathContract` against its independent oracle, plus the type contract check at 1.7-1.8; nothing outside safix's store is touched and the destructive branch is real → `safix-installer-coexistence`; a real activation reaches a secret → `safix-installer-vm`; the registration name and the consumer-named ordering → `safix-installer-ordering`; user scope carries the unit on linux, the entry on both, and `userMode = true` → the check at 8.13 plus the profile observations recorded at 8.6 and 8.7; `%r` expansion per platform → `install::expand_runtime_dir` and the integration coverage at 8.14; user mode mounts, chowns and restarts nothing → the test at 8.15; the three absences are refusals → 13.10's drill below; the two identity refusals → `safix-installer-refusals`, `safix-consumption-refusals`, and `safix-identity-recipiency` for the recipiency sentence; the preflight's DAG placement → `safix-consumption-ordering`; both published names resolve and import nothing → `safix-module-entrypoints` (task 9.7) and `safix-module-collision`; "safix declares no sops-nix input" → 13.8's `nix flake metadata` check. Two sentences are held by an observation rather than by a check and are recorded as such here rather than in the README: the cross-build split (7.18) and the `manifestInputHash` limit (2.8).
- [x] 13.10 Severity drill: for each of the three absences in 13.3, confirm that a fixture attempting it fails — a manifest carrying a `templates` field is refused by `deny_unknown_fields`, a `neededForUsers` field likewise, and a gnupg-only identity fails evaluation per task 7.4 — so the documented absences are refusals rather than omissions — all three observed against the real built manifest and the real binary. `jq '. + {templates: []}'` over it: refused, exit 1, "unknown field `templates`, expected one of `version`, `secrets`, `secretsMountPoint`, `symlinkPath`, `keepGenerations`, `ageKeyFile`, `ageSshKeyPaths`, `useTmpfs`, `userMode`, `logging`, `manifestInputHash`". `jq '.secrets[0] += {neededForUsers: true}'`: refused, exit 1, "unknown field `neededForUsers`, expected one of `name`, `key`, `path`, `owner`, `group`, `uid`, `gid`, `sopsFile`, `format`, `mode`, `restartUnits`, `reloadUnits`". The unmutated manifest exits 0, so neither refusal is a coincidence. For gnupg: a system fixture with `services.openssh.enable = false` and no `safix.identity.*` refuses at evaluation with `noSystemIdentityMessage`, where a `sops.gnupg.*` configuration used to suppress it — and there is no longer any read of that namespace to suppress it with.
- [x] 13.11 Verify: 13.9's audit complete with no unheld sentence, drill in 13.10 observed — the audit above maps every guarantee; two sentences are held by a recorded observation rather than by a check, and both are named there. The drill is observed with its exact refusal text.

## 14. Verification

- [ ] 14.1 `openspec validate own-the-installer --strict` green
- [ ] 14.2 `openspec validate --all --strict`, compared against the total recorded when this change was proposed
- [ ] 14.3 `nix eval .#checks.x86_64-linux --apply builtins.attrNames` lists every check named in groups 1, 7-12, and does not list `safix-installer-mechanism` or `safix-installer-manifest`
- [ ] 14.4 `nix flake check --keep-going --print-build-logs` green on `x86_64-linux`
- [ ] 14.5 `nix eval .#checks.aarch64-darwin --apply builtins.attrNames` and a darwin evaluation of the home-scope fixture from 8.13, confirming the darwin arm of `%r` expansion and the absence of the user unit are both reachable on that platform
- [ ] 14.6 `cargo test --locked --workspace` green
- [ ] 14.7 `rg "sops-nix"` over the whole tree and confirm every remaining match is either a historical reference in `openspec/changes/archive/`, a released CHANGELOG entry, or a statement that safix declares no such input
- [ ] 14.8 `rg "sops\."` over `modules/` and `crates/` and confirm every remaining match is the `sops` binary, the `sopsFile` field name, or a `.sops.yaml` path, and none is an option read or definition
- [ ] 14.9 `rg "unsafe"` over `crates/` and confirm `#![forbid(unsafe_code)]` is present in every crate root and no `allow` weakens it, holding design I15 as a fact
- [ ] 14.10 Confirm every severity drill in groups 1-13 was observed and recorded beside the task that names it
