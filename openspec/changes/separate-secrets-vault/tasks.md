# Tasks: separate-secrets-vault

Citations are as read while designing this change and its 2026-09-03 amendment; re-read the named lines before editing, since implementation may land after other changes in this program and line numbers drift.
No real fleet identifier, hostname, or recipient enters this repository; fixtures use `alice`, `bob`, and `carol`, and synthetic `age1` strings, matching the existing consumption fixtures.
Where a task says "hold", add a check that fails when the claim stops being true, not a sentence asserting it.

## 1. The vault option, its naming key, and the root flip

- [x] 1.1 Add `vault` to `modules/flake/safix/options.nix`: `lib.mkOption { type = lib.types.nullOr (lib.types.submodule { options = { root = lib.mkOption { type = lib.types.path; ...}; namingKey = lib.mkOption { type = lib.types.str; ...}; }; }); default = null; ... }`, documenting that a value moves every `sopsFile` to resolve rooted there with an opaque name, that `namingKey` must be at least 64 lowercase hexadecimal characters minted with `openssl rand -hex 32` (or `head -c 32 /dev/urandom | od -An -tx1 | tr -d ' \n'`), and that the key is visible to anyone who can evaluate the declaring flake
- [x] 1.2 Flip `bound` in `modules/flake/safix/default.nix:47` from `root = self;` to `root = if cfg.vault != null then cfg.vault.root else self;`, and thread `namingKey = if cfg.vault != null then cfg.vault.namingKey else null;` alongside it (implemented by threading `namingKey` through the `registry` binding `bound` already merges in, rather than as a second field of `bound`'s own `{ root = ...; }` attrset; same externally observable result)
- [x] 1.3 Add `vaultDeclared = cfg.vault != null;` under `flake.safix.lib` in `default.nix`, alongside the other `safix.lib.*` outputs
- [x] 1.4 Add `wellFormedNamingKey` and `vaultViolations` to `resolve.nix`, following the shape `noRecipientKey` establishes (`resolve.nix:1701-1704`): refuse a declared vault with no naming key, one shorter than 64 characters, or one containing a character outside `[0-9a-f]`
- [x] 1.5 Concatenate `resolve.vaultViolations cfg.vault` into `violations` at `default.nix:174`, alongside `resolve.violations registry` and `resolve.generatorViolations registry`
- [x] 1.6 Add a check asserting that a fixture with no `vault` set resolves every `sopsFile` and `publicValue` path exactly as it does on `main` today (byte-identical resolved paths, over a fixture carrying at least one shared and one private secret) — `modules/flake/checks/vault.nix`'s `rootFlip.noVault` and `readableMatchesFormula`
- [x] 1.7 Add a check asserting that a fixture with a well-formed `vault` resolves `flake.safix.lib.vaultDeclared` to `true` and an unset one to `false` — `vault.nix`'s `vaultDeclared`
- [x] 1.8 Add fixture checks for 1.4's three refusals (no key, short key, non-hex key) and one for a well-formed key evaluating without refusal — `vault.nix`'s `namingKeyRefusals`
- [x] 1.9 Severity drill: reverting 1.2's flip while leaving 1.3 in place turns 1.7's path assertion red but not `vaultDeclared`, which is the evidence the two are independently held (verified against a scratch copy; the check field this moves is `rootFlip`, since 1.7's own field is `vaultDeclared` alone — see `vault.nix`'s drill commentary)
- [x] 1.10 Severity drill: dropping any one of 1.4's three conditions from `vaultViolations` turns exactly the corresponding case in 1.8 green when it should refuse, and no other case (verified against a scratch copy)
- [x] 1.11 Verify: `nix build .#checks.x86_64-linux.safix-vault-root` (or the chosen check name) green, drills in 1.9 and 1.10 observed — chosen name `safix-vault` (one check covering groups 1, 4, 5.2-5.3, 10.1, 12); green, drills documented in `vault.nix`

## 2. `Workspace` carries two roots

- [x] 2.1 Add `vault_root: PathBuf` to `Workspace` (`crates/safix-core/src/workspace.rs:29-43`); add `vault_root` as a parameter to `Workspace::at`
- [x] 2.2 Add `Workspace::vault_root(&self) -> &Path` and `Workspace::vault_absolute(&self, relative: &str) -> PathBuf`, mirroring `root`/`absolute`
- [x] 2.3 Add `Workspace::read_vault_relative(&self, relative: &str) -> Result<Option<String>>`, identical in shape to `read_relative` but reading under `vault_root`
- [x] 2.4 Implement `Workspace::discover`'s vault resolution per design V1: read `SAFIX_VAULT_ROOT`, evaluate `flake.safix.lib.vaultDeclared` at the declaration root, and resolve `vault_root = root` when neither is set
- [x] 2.5 Add the two mismatch refusals from design V1 — `vaultDeclared` true with no `SAFIX_VAULT_ROOT`, and `SAFIX_VAULT_ROOT` set with `vaultDeclared` false — as new `Error` variants naming both the option and the environment variable (`Error::VaultDeclaredWithoutRoot`, `Error::VaultRootWithoutDeclaration`)
- [x] 2.6 Add unit tests constructing a `Workspace` via `at` with `vault_root != root` and asserting `vault_absolute`/`read_vault_relative` join against `vault_root` while `absolute`/`read_relative` still join against `root` — `workspace.rs`'s `the_two_roots_are_joined_independently`
- [x] 2.7 Add integration-level tests (following this crate's existing `SAFIX_REPO_ROOT`-driven fixture pattern) for `discover`'s four resolution branches in 2.4, and both refusals in 2.5 — `crates/safix/tests/vault_workspace.rs`, driven through `Fixture::declare_vault` and `run_env`'s `SAFIX_VAULT_ROOT`
- [x] 2.8 Severity drill: setting only one of the two signals in each of the four fixtures from 2.7 turns the corresponding refusal test red; setting neither leaves `vault_root == root` observed by the test in 2.6's style (the four branches and both refusals in `vault_workspace.rs` are separate tests asserting distinct `refusal_code()`s, so this drill is what those tests already hold apart rather than a separate manual check)
- [x] 2.9 Verify: `cargo test -p safix-core workspace::` green, both refusal tests and the drill in 2.8 observed
- [x] 2.10 Route every remaining ciphertext and public-output read through `vault_absolute`/`read_vault_relative` rather than `absolute`/`read_relative` — design V3's touch point 3, omitted from every group's task list until wave two found it: `bridge.rs`'s `held_by_safix` (the safix side of every clan and keepassxc mapping), `edit.rs`'s `read_existing`, `enroll/mod.rs`'s `wire`, `recipients_before` and `refuse_lost_recipients`, `enroll/proof.rs`'s `file_to_prove_with` and `decrypt_with`, `generate.rs`'s `holds_a_value` (both branches) and `mint`'s dependency read, `check.rs`'s `Documents::text` and `definitions`' record read, `fix.rs`'s `announce` and both re-wrap loops' existence checks, and `crates/safix/src/main.rs`'s `get`. Declaration-rooted reads (a scaffold, a group declaration, `.sops.yaml`, an onboarding or enroll hook) are unedited
- [x] 2.11 Add a fixture test asserting `edit`, `get`, `sync clan` and `sync keepassxc` each read a document seeded straight at the vault root, over a fixture that declares one — `crates/safix/tests/vault_reads.rs`; each seeds through the new `Fixture::encrypt_to_vault` rather than through a write path this wave has not yet moved, so the claim is about the read alone

## 3. The vault-is-a-git-repository refusal

- [x] 3.1 Add a check, folded into `Workspace` discovery, running `git -C <vault_root> rev-parse --show-toplevel` and comparing the canonicalized result against `vault_root` (both sides canonicalized, not only git's output, so a symlinked scratch root on a platform where one is not simply assumed away)
- [x] 3.2 Add a new `Error` variant naming the vault path and stating it must be a git repository's top level, raised on a failed git invocation there (`Error::VaultNotARepository`)
- [x] 3.3 Add a second variant for the top-level mismatch case, naming both the path git found and the path named, raised when the two disagree (`Error::VaultRootNotTopLevel`)
- [x] 3.4 Add fixture tests: a `vault_root` pointing at a plain directory (3.2's case), a `vault_root` pointing at a subdirectory of a real git repository (3.3's case), and a `vault_root` that is itself a repository's top level (passes) — `vault_workspace.rs`'s `a_root_naming_a_plain_directory_is_refused`, `a_root_naming_a_subdirectory_of_a_repository_is_refused`, `a_declared_vault_with_a_named_root_resolves_to_it`
- [x] 3.5 Severity drill: pointing `vault_root` at the subdirectory case while asserting only 3.2's variant fires turns the drill red, which is the evidence the two refusals are distinguished rather than collapsed into one message — encoded as a standing test, `vault_workspace.rs`'s `the_two_git_repository_refusals_are_distinguished_not_collapsed`, rather than a one-off manual check
- [x] 3.6 Verify: the git-repository refusal tests are integration-level rather than `safix-core` unit tests (discovery is only reachable through the compiled binary's environment, per 2.7's own pattern), so the command actually run is `cargo test -p safix --test vault_workspace`; green, drill in 3.5 observed as a passing standing test

## 4. Opaque physical layout

- [x] 4.1 Add `opaqueOf = namingKey: tag: logicalPath: builtins.hashString "sha256" "${namingKey}|${tag}|${logicalPath}";` beside `audienceFileOf` in `resolve.nix`
- [x] 4.2 Add `secretsFileOf = namingKey: audience: "secrets/${opaqueOf namingKey "secrets" (audienceFileOf audience)}.yaml";`; route the two `selectFor` `sopsFile` sites (`resolve.nix:2021`, `:2093`) through it when `namingKey != null`, keeping `audienceFileOf audience` unchanged otherwise
- [x] 4.3 Add the vault-mode counterpart of `publicFileOf`, `"public/${opaqueOf namingKey "public" (publicFileOf audience name)}"`, and route `placementsIn`'s `public` field through it when `namingKey != null`
- [x] 4.4 Add `opaqueKeyOf = namingKey: logicalPath: logicalKey: opaqueOf namingKey "key" "${logicalPath}#${logicalKey}";`; route `placementsIn`'s `key` field (`resolve.nix:595`) and `selectFor.forMachine`'s `sopsKey` field (`resolve.nix:2027`) through it when `namingKey != null`, both closing over the same `audienceFileOf audience` value (`selectFor.forUser` also routes its `sopsKey` through the same formula, unconditionally in vault mode — see 4.5's note)
- [x] 4.5 Drop the `lib.optionalAttrs (secret.sopsKey != null)` guard on `materializeFor`'s `key` emission (`resolve.nix:2249`) when `namingKey != null`, so the opaque key is always emitted rather than left to sops-nix's own attribute-name default (kept as a second, redundant guarantee: `forUser`, not one of this task's two named recompute sites, was also changed to set `sopsKey` unconditionally in vault mode, which is what a user-scope activation needs to find the opaque key `placementsIn` wrote into the document at all — see `vault.nix`'s drill commentary for 4.9)
- [x] 4.6 Add a check asserting that a fixture with `vault` set resolves every ciphertext, public output, and in-document key to its opaque form, and that no vault-rooted physical name or directory listing contains a readable audience member, secret name, or definition-record owner — `vault.nix`'s `opaque`, `publicOpaque`, `leakedFragments`
- [x] 4.7 Add a check asserting the same fixture's `placementsIn`-computed `key` and `selectFor.forMachine`-computed `sopsKey` agree bit for bit for every entry that resolves at both scopes — `vault.nix`'s `machineKeyAgreement`
- [x] 4.8 Add a check asserting a fixture with no `vault` set resolves every ciphertext, public output, and in-document key to exactly today's readable name (cross-referencing 1.6) — `vault.nix`'s `readableMatchesFormula`
- [x] 4.9 Severity drill: dropping 4.5's unconditional emission while keeping 4.4 turns 4.7 red on an entry with no declared `sopsKey`, which is the evidence the system-scope and CLI paths are held as a pair (does not reproduce against this implementation, and why is recorded in `vault.nix`'s drill commentary: `forUser`'s own unconditional `sopsKey` already makes `materializeFor`'s guard redundant)
- [x] 4.10 Severity drill: reusing one tag across two of `opaqueOf`'s four call sites turns 4.6 red on a coincidental collision fixture (a public output and a ciphertext file sharing one readable identity), which is the evidence the tags are load-bearing rather than decorative (verified directly against `builtins.hashString` with the tags collapsed; recorded in `vault.nix`)
- [x] 4.11 Verify: `nix build .#checks.x86_64-linux.safix-vault-opacity` (or the chosen check name) green, drills in 4.9 and 4.10 observed — chosen name `safix-vault`; green

## 5. The recipient policy stays at the declaration root; scratch creation rules

- [x] 5.1 Leave `.sops.yaml`'s write sites (`fix.rs:101-119`, `adduser.rs:168-180`, `group.rs:219-233`) and its read site (`check.rs:186-206`) at `workspace.root()`/`workspace.absolute()`, unedited; add a regression test asserting they never read `vault_root` — `crates/safix/tests/vault_scratch_rules.rs`'s `the_committed_policy_stays_at_the_declaration_root_with_a_vault_declared`
- [x] 5.2 Add `renderVaultRules` beside `renderPlan` in `policy.nix`, taking the same `plan registry` value plus `namingKey`, rendering `creation_rules:` alone — no header, no `Audience:` comments, no `keys:` anchors — with each rule's `key_groups` listing recipients' raw age public keys inline and `path_regex` matching the literal opaque ciphertext filename `secretsFileOf` computes
- [x] 5.3 Expose `flake.safix.lib.vaultCreationRulesText`, `null` when `cfg.vault == null`, otherwise `policy.renderVaultRules (policy.plan registry) namingKey` — verified in `vault.nix`'s `rules` assertions and `vaultRulesParse` (YAML parses to exactly `creation_rules`, via `yq-go`)
- [x] 5.4 Add `Attribute::VaultCreationRulesText` to `nix.rs` beside `Attribute::PolicyText` (`nix.rs:24-65`) — landed early by wave one's own `resolve_vault_root` work; confirmed present and correct
- [x] 5.5 In `Sops::create_empty_document` and `Sops::update_keys_command` callers (`set.rs`, `generate.rs`, `fix.rs`), when the target document is vault-rooted, evaluate `Attribute::VaultCreationRulesText`, write it to a scratch file named `.sops-vault-rules.yaml` inside `vault_root`, register it with `scratch::register_file` before creation, and pass `--config <scratch path>` to the sops invocation — `Workspace::stage_vault_rules` does the staging once per call site; since every ciphertext write these three modules make is vault-rooted by construction, the join for the write target itself moves from `workspace.absolute`/`workspace.root` to `workspace.vault_absolute`/`workspace.vault_root` in the same pass (a change the task text does not name but that the feature cannot be correct without — see the routing already generalized in task 2.10)
- [x] 5.6 Update the doc comment at `sops/mod.rs:292-294` to state that the working directory is the vault root when the target is vault-rooted, and that `--config` names the scratch rules there rather than relying on upward discovery
- [x] 5.7 Leave `Sops::set_key` (`sops/mod.rs:253-281`) unedited: no `--config`, no `current_dir` change — confirmed unedited
- [x] 5.8 Add a migration/setup step (task 11.1) that writes a `.gitignore` entry for `.sops-vault-rules.yaml` in the vault repository; add a `check` finding when a vault's `.gitignore` does not cover it — the `check` finding half was already done (`check.rs`'s `Finding::VaultGitignoreMissing`, rendered in `render.rs`, tested in `vault_scratch_rules.rs`); the migration write itself is `fix.rs`'s `ensure_vault_gitignore`, run unconditionally by the relocate phase on every `fix` with a vault declared, asserted in `vault_migration.rs`'s `a_populated_readable_fixture_migrates_into_a_vault`
- [x] 5.9 Add a fixture test asserting `encrypt`/`updatekeys` against a vault-rooted document succeed with the scratch config and no committed `.sops.yaml` in the vault fixture — `vault_scratch_rules.rs`'s `set_creates_a_vault_rooted_document_through_the_scratch_config`
- [x] 5.10 Add a fixture test asserting the scratch rules file does not exist after the run, on normal return, on a forced sops failure, and on a simulated `SIGINT` mid-call — `vault_scratch_rules.rs`'s `the_scratch_rules_file_never_survives_a_run`, the SIGINT case reusing `abort_residue.rs`'s shim pattern (`SAFIX_SHIM_HOLD=set`, since `--config`'s position ahead of the subcommand means the shim's own first-argument match cannot key off `encrypt` directly — the scratch file predates that later `sops set` call and is still present to observe)
- [x] 5.11 Add a fixture test asserting `decrypt` and `set` against an already-encrypted vault-rooted document succeed with no scratch rules file ever written — `vault_scratch_rules.rs`'s `setting_an_existing_vault_rooted_document_needs_no_scratch_rules`
- [x] 5.12 Severity drill: pointing the scratch config outside `vault_root` (e.g. at the declaration root) turns 5.9 red on a no-matching-creation-rules error, which is the evidence `config.go`'s directory-relative `path_regex` matching is exercised rather than assumed — `vault_scratch_rules.rs`'s `rules_staged_outside_the_vault_root_do_not_match`
- [x] 5.13 Severity drill: skipping 5.8's `.gitignore` entry turns the new `check` finding red on a fixture that leaves the scratch file present at commit time — `vault_scratch_rules.rs`'s `check_reports_a_vault_with_no_gitignore_entry_for_the_scratch_rules` toggles the `.gitignore` content and observes the finding appear and disappear
- [x] 5.14 Verify: `cargo test -p safix-core` green for `set`, `generate`, `fix`; drills in 5.12 and 5.13 observed — green (234 lib tests, 6 `vault_scratch_rules.rs` tests)

## 6. Commit ordering and the preflight

- [x] 6.1 Generalize `refuse_bad_repository_state` (`set.rs:224-250`) into a function taking a `Workspace` and the set of `(root, relative)` pairs an operation will touch, checking each in order and refusing on the first failure before any write
- [x] 6.2 Wire the generalized preflight into `adduser.rs`, `group.rs`, `enroll/mod.rs`, `set.rs`, `fix.rs`, and `generate.rs`, each naming the roots and paths it actually touches — vault-only for `set`/`fix`/`generate`; `enroll/mod.rs`'s `wire()` checks both, since it is the one operation that genuinely writes at both roots. `adduser.rs` and `group.rs` write nothing at the vault root at all (`.sops.yaml` is declaration-rooted in every case per V10 and `secrets-vault/spec.md`'s "What lands at the vault root and what stays at the declaration root", which is what settles the contradiction with design.md's own V3 table row 8 — see 11.1's docs commit), so their preflight names only the declaration root's paths. `enroll/mod.rs`'s declaration-side paths are checked for `MidOperation` alone, not `ConflictEntries`/`UncommittedChanges`: a re-run after `Error::VaultCommitHalfLanded` finds its own prior attempt's edit already staged there, and a path-dirtiness check would make the spec's own "re-running completes the operation" scenario unsatisfiable — see `enroll/mod.rs::wire`'s own comment
- [x] 6.3 Split the `written` vector at `adduser.rs:163`, `group.rs:187`, and `enroll/mod.rs:373` into `written_vault` and `written_declaration` — `written_vault` is always empty for `adduser.rs`/`group.rs` (see 6.2's note), so their split degrades through `git::commit_two_roots` to today's single declaration-root commit; `enroll/mod.rs`'s is populated from the managed governed files that exist at the vault root
- [x] 6.4 Change `set.rs:196-202` and `generate.rs:855-863`'s single `commit_written_files` call to pass `workspace.vault_root()` — landed early, in wave two, because group 5's own `set.rs` tests could not pass otherwise
- [x] 6.5 Change `adduser.rs`, `group.rs`, and `enroll/mod.rs` to call the new `git::commit_two_roots` — `vault_root` with `written_vault` first, then `root` with `written_declaration`, the second message carrying a `Safix-Vault: <short-id>` trailer built from `Git::head_short(vault_root)` read after the first commit lands; degrades to one commit with no trailer when `written_vault` is empty
- [x] 6.6 Add a new `Error` variant for the half-landed state, carrying the vault commit id and the pending declaration-root paths, raised when the vault-root commit in 6.5 succeeds and the declaration-root commit fails — `Error::VaultCommitHalfLanded`, with a `refusal_codes!` entry, a reporter sample arm and two accepted insta snapshots
- [x] 6.7 Add a fixture test driving `adduser` (or `group`) with the declaration-root commit forced to fail (a stubbed git driver refusing the second `commit_paths` call), asserting the vault-root commit is observed to have landed and the error names its id — corrected from the original text: `adduser`/`group` write nothing at the vault root (6.2's note), so a half-landed state is unreachable through either; driven through `enroll` instead, the one operation that is genuinely cross-root — `crates/safix/tests/vault_commit_ordering.rs`'s `a_forced_declaration_commit_failure_reports_the_half_landed_state_and_a_retry_completes_it`, using `tests/support/git-shim.rs` and `harness::git_shim()`. `git::commit_two_roots`'s own unit tests in `git.rs` hold the underlying half-landed mechanics directly, with a portable shim script rather than depending on this machine's own git identity configuration
- [x] 6.8 Add a fixture test re-running the same operation after 6.7's failure with the stub restored to succeeding, asserting no second vault-root commit is made (`git log` in the vault fixture shows one commit for the operation, not two) and the declaration-root commit lands carrying the trailer — the same `vault_commit_ordering.rs` test continues into the retry; `git.rs`'s `re_running_after_a_half_landed_failure_makes_no_second_vault_commit` holds it at the unit level too
- [x] 6.9 Add fixture tests for the preflight's two single-root-dirty cases and its both-clean case, asserting nothing is written at either root in the two dirty cases — at `refuse_bad_repository_state`'s own level, `set.rs`'s `preflight_tests` module: `both_roots_clean_admits_the_touches`, `the_vault_root_being_dirty_is_refused_naming_it`, `the_declaration_root_being_dirty_is_refused_naming_it`; the single-root-commits-once case is also held through `enroll`/`adduser` in `vault_commit_ordering.rs`'s `adduser_with_a_vault_declared_still_commits_once_with_no_trailer` and at `commit_two_roots`'s own level in `git.rs`'s `a_single_root_operation_commits_once_with_no_trailer`
- [x] 6.10 Severity drill: skipping the preflight's vault-root check while leaving the declaration-root check in place turns the vault-dirty case in 6.9 green when it should refuse, which is the evidence the two checks are independently load-bearing — observed manually against `set.rs`'s `the_vault_root_being_dirty_is_refused_naming_it`: narrowing its touch list to `[(declaration_root, "user.nix")]` alone returns `Ok(())`, recorded in that test's own doc comment
- [x] 6.11 Verify: `cargo test -p safix-core` green for `adduser`, `group`, `enroll`, `set`, `fix`, `generate`; every test and drill in this group observed — green (`cargo test --locked --workspace` clean save for pre-existing environment-only failures needing `bwrap`/`strace`, both present and green under `nix develop -c`)

## 7. The scratch sweep floor becomes two floors

- [x] 7.1 Change `scratch::Registry.floor: Option<PathBuf>` to `floors: Vec<PathBuf>`; change `set_floor` to append de-duplicated rather than replace
- [x] 7.2 Change `remove_empty_upwards`'s stop condition from equality against one floor to membership in `floors`
- [x] 7.3 Update `set.rs:111`, `generate.rs:147`, `bridge.rs:229`, `sync.rs:246` to floor at `workspace.vault_root()` (`bridge.rs` floors twice — `sync`'s `Run` path and `converge`'s two-way path both write only ciphertext, so both floor at `vault_root`)
- [x] 7.4 Update `adduser.rs:104` and the site in `enroll/mod.rs` to call `set_floor` twice, once per root — corrected from the original text naming `group.rs` too: `group.rs` registers no scratch path at all today (its writes, a group declaration and `.sops.yaml`, are both root-relative per V10), so there is no site there to double-floor; adding one preemptively would be dead code with nothing ever registered under it. `adduser.rs` and `enroll/mod.rs` are genuinely forward-compatible with group 6's future dual-root writes even though neither writes at `vault_root` today
- [x] 7.5 Add a unit test registering scratch files under two distinct floors and asserting `cleanup`/`remove_empty_upwards` respects both, removing an empty directory at either without disturbing the other — `scratch.rs`'s `two_floors_are_swept_independently`
- [x] 7.6 Severity drill: reverting 7.1-7.2 while keeping 7.4's double `set_floor` call turns 7.5 red, since the second call would silently overwrite the first floor under the old `Option`-replacing behaviour rather than accumulate (observed manually: reverted `set_floor` to clear-then-push, reran `two_floors_are_swept_independently`, it failed on "the sweep walked past the second floor", reverted the change)
- [x] 7.7 Verify: `cargo test -p safix-core scratch::` green, drill in 7.6 observed

## 8. What stays put, held rather than assumed

- [x] 8.1 Add a check or test asserting `Nix::shell` and the `nix eval` target in `nix.rs` are constructed from `declaration_root` alone, unaffected by whether `vault_root` differs from it, over a fixture pair identical except for `vault_root` — `crates/safix/tests/vault_invariants.rs`'s `every_evaluation_targets_the_declaration_root_regardless_of_the_vault_root`; the stubbed evaluator itself refuses an evaluation naming any root but `SAFIX_REPO_ROOT`, so the run's success is the proof
- [x] 8.2 Add a fixture test asserting `Git::author_identity` reads from `declaration_root`'s git configuration even when a commit's content is entirely vault-rooted (a `set` operation, whose commit lands only at `vault_root`, still authored per `declaration_root`'s identity) — implemented in two parts, both green today: `git.rs`'s unit test `author_identity_reads_the_named_roots_own_configuration` holds the primitive (two repositories with different identities read apart, by the root each is asked about), and `vault_invariants.rs`'s `onboarding_commits_at_the_declaration_root_with_a_vault_declared` holds it through a real command with a vault declared at a different root. The parenthetical's own premise — a `set` operation whose commit lands only at `vault_root` — is not yet true: group 6 has not landed, so `set` commits nothing at `vault_root` yet, only at `root`, unchanged from today. Wave two re-asserts this specific parenthetical once group 6's cross-root commit lands
- [x] 8.3 Add a fixture test asserting the onboarding hook (`adduser.rs`) and the enroll hook (`enroll/mod.rs`) both run with `current_dir` at `declaration_root`, unchanged from today, over a fixture where the two roots differ — `vault_invariants.rs`'s `the_onboarding_hook_runs_at_the_declaration_root_with_a_vault_declared` and `the_enroll_hook_runs_at_the_declaration_root_with_a_vault_declared`; both hooks are reachable and this claim is fully testable today, since neither hook's invocation site changes with group 6
- [x] 8.4 Severity drill: for each of 8.1-8.3, flipping the corresponding site to the wrong root turns that test red and no other test in this group red, which is the evidence each is independently held — observed manually for 8.3 (adduser.rs's hook `current_dir` flipped from `workspace.root()` to `workspace.vault_root()`, `the_onboarding_hook_runs_at_the_declaration_root_with_a_vault_declared` failed, no other test in this group failed, reverted). 8.1 has no site to flip: `nix.rs` carries no `vault_root` parameter anywhere, which is itself stronger evidence than a drill against one. 8.2's drill is exercised at the primitive by the two-repository unit test's own two distinct assertions (Alice's root vs Bob's), which is what "flipping the root" means at that level
- [x] 8.5 Verify: all checks/tests and the drill in 8.4 observed; this group intentionally adds no new behaviour, only coverage for behaviour this change must not disturb

## 9. The lock-bump disclosure

- [x] 9.1 Add a function computing the disclosure message: on success of a vault-root commit, run `nix flake metadata <declaration_root> --json`, search `.locks.nodes` for a node whose locked source matches `vault_root` (by path, falling back to a freshly computed NAR hash comparison), and format the specific or the general message per design V6 — `Nix::flake_metadata`, `Nix::nar_hash` and `lock_bump::disclosure`
- [x] 9.2 Corrected from the original text: wire the disclosure only into the vault-root commit success paths that actually land a vault-root commit, each guarded by `vault_root() != root()` — `set.rs`'s `run_committing`, `generate.rs`'s `write`, and `enroll/mod.rs`'s `wire()`, the last additionally guarded by `!written_vault.is_empty()` since its `commit_two_roots` call degrades to a declaration-root-only commit otherwise. Not wired into `fix.rs` (never commits) or into `adduser.rs`/`group.rs` (their `written_vault` is always empty per task 6.2's own note, so neither ever reaches a vault-root commit at all)
- [x] 9.3 Add a fixture test with a declaring flake whose lock names exactly one input matching the vault fixture, asserting the disclosure names that input and the exact `nix flake lock --update-input` command — `crates/safix/tests/vault_lock_bump.rs`'s `a_lock_naming_exactly_one_matching_input_names_it_in_the_disclosure`, driven through `Fixture::set_flake_lock_nodes`
- [x] 9.4 Add a fixture test with a lock naming zero or more than one matching input, asserting the disclosure falls back to the general phrasing and names no input — `vault_lock_bump.rs`'s `a_lock_naming_no_matching_input_falls_back_to_the_general_disclosure` and `a_lock_naming_more_than_one_matching_input_falls_back_to_the_general_disclosure`; a third test, `a_run_with_no_vault_declared_prints_no_lock_bump_disclosure`, holds the guard itself
- [x] 9.5 Severity drill: corrupting the fixture lock's matching entry in 9.3 turns that test red and 9.4 unaffected, which is the evidence the two paths are independently reached — observed manually: pointed 9.3's lock node at an unrelated path, reran `vault_lock_bump.rs`, 9.3's test failed on the missing `--update-input` line while both 9.4 tests and the no-vault test stayed green, reverted
- [x] 9.6 Verify: both fixture tests and the drill in 9.5 observed; measure `nix flake metadata`'s wall time against this repository's own flake and record it beside the open question in `design.md`, resolving it one way or the other before this task is checked off — measured 93-99 ms across three runs, recorded in `design.md`'s Open Questions section; kept

## 10. Runtime reversibility: opaque definition records

- [x] 10.1 Add `definitionRecord : nullOr str` to `placementsIn`'s per-entry attrset (`resolve.nix:585-613`), computed as `null` when `namingKey == null`, otherwise `"state/${opaqueOf namingKey "state" logical}"` where `logical` is `"shared/${lib.concatStringsSep audienceSeparator audience}/${name}"` for a shared entry or `"${owner}/${name}"` for an owned one — mirroring `definition::audience_directory`'s two branches (`definition.rs:145-164`) (`placementsIn` also gains `logicalFile`, `logicalKey`, `logicalPublic` per J3, the same four-field, null-outside-vault-mode shape)
- [x] 10.2 Add `pub definition_record: Option<String>` to `Placement` (`model.rs:147-169`, `#[serde(rename = "definitionRecord")]`); update every fixture literal that constructs a `Placement` in this crate's tests — implemented per the J3/J6 contract as all four fields (`definitionRecord`, `logicalFile`, `logicalKey`, `logicalPublic`), needed so the migration in group 11 can enumerate both a document's opaque and readable forms without the runtime ever hashing; every JSON fixture literal in `harness/mod.rs`, `definition.rs`'s and `model.rs`'s own test modules updated
- [x] 10.3 Change `definition::record_path` (`definition.rs:137-143`) to return `placement.definition_record.clone()` when `Some`, falling back to today's `audience_directory`-based derivation when `None`, unedited
- [x] 10.4 Add a fixture test asserting a vault-mode `record_path` matches the opaque path nix computed, and a no-vault fixture test asserting `record_path` is byte-identical to today's output (cross-referencing 4.8) — `definition.rs`'s `a_vault_mode_record_path_is_the_nix_supplied_opaque_path`; the no-vault half is `a_record_path_is_keyed_by_owner_or_by_audience`, unedited and still green
- [x] 10.5 Add a fixture test asserting `refOfElement` and `check.rs`'s shared/stray logic (`check.rs:291-375`) behave identically under a vault fixture and a no-vault fixture given the same logical audience, confirming design V14's claim that neither site is fed a physical name — `crates/safix/tests/vault_opaque_names.rs`'s `a_dropped_carrier_is_reported_as_a_revocation_over_opaque_paths_too`, the same revocation scenario `shared_entries.rs` drives over readable paths, driven instead over opaque-hex-shaped ones via `Fixture::encrypt_to` (so no creation rule, which would itself have to name a readable directory, is in the way of the claim under test); `refOfElement`'s own two call sites (`audienceKeysOf`, the silo-membership computation) consume the in-memory `audience` list rather than a physical path in either mode and are unedited, so no separate test exists for that half — it is not reachable from either
- [x] 10.6 Severity drill: reverting 10.3's `Some` branch while leaving 10.1 in place turns 10.4's vault-mode assertion red on a mismatched path, which is the evidence the nix-supplied field is actually consumed (observed manually: reverted the `Some` branch, `a_vault_mode_record_path_is_the_nix_supplied_opaque_path` failed with `state/safix/definitions/alice/api-token` in place of the opaque path, reverted the change)
- [x] 10.7 Verify: `cargo test -p safix-core definition::` and `check::` green, drills in 10.6 observed; the `check.rs` half of this task's own coverage is `crates/safix/tests/vault_opaque_names.rs` rather than `safix-core`'s `check::` unit tests, since `check`'s shared/stray logic is only reachable through the compiled binary's real `sops`/`git`, matching this crate's existing `shared_entries.rs` pattern

## 11. Migration: an opaque-aware move

- [x] 11.1 Implement the migration mechanism as a `check` drift finding plus a `fix` phase, per design V13's dated note, rather than a new `safix` subcommand or a one-shot script: `check` gains `Finding::VaultRelocationPending` for every readable-layout ciphertext document, public output, or definition record still present at the declaration root while a vault is declared; `fix` gains a relocate phase, run with no vault-only flag needed since it fires whenever `vault_root != root`, that for each pending path whose vault-rooted destination is absent decrypts every key under the operator's own identity and re-encrypts it into the opaque document and opaque key name, copies a public output or definition record's bytes directly, then removes the readable-layout source; `fix` also writes the vault's `.gitignore` entry for `.sops-vault-rules.yaml` on every run with a vault declared, completing task 5.8. `crate::relocation` holds the enumeration `check` and `fix` share; `fix` keeps its no-commit contract: it prints an informational note naming the commit order and the pending lock bump rather than committing or disclosing
- [x] 11.2 Implement the rollback as a `fix` flag, `safix fix --vault-rollback` (design V13's dated note): run while the vault is still declared, decrypting every leaf from the vault and re-encrypting it into the readable layout at the declaration root, then removing the vault-rooted source — leaving the vault declaration and `SAFIX_VAULT_ROOT` for the operator to remove afterwards, since the opaque-to-readable name mapping needs the naming key still in force to run
- [x] 11.3 Add a fixture test migrating a populated readable-layout fixture (shared and private secrets, a public output, a definition record) into a vault, asserting every leaf decrypts to the same plaintext before and after and every physical name matches the opaque form the placements declare — `crates/safix/tests/vault_migration.rs`'s `a_populated_readable_fixture_migrates_into_a_vault`, over fixed hex-shaped opaque literals standing in for `opaqueOf`'s real output, the way `vault_opaque_names.rs` already does
- [x] 11.4 Add a fixture test rolling the same migration back, asserting the readable layout is restored: a shared or private secret's decrypted value round-trips exactly (its ciphertext bytes do not, since re-encryption stamps a fresh nonce and `lastmodified`), and the public output and both definition records — genuine plaintext, moved by a copy rather than a re-encryption — are byte-identical — `vault_migration.rs`'s `a_vault_rollback_restores_the_readable_layout`
- [x] 11.5 Add a fixture test asserting an interrupted relocation leaves the vault-rooted destination absent and the readable-layout source untouched, and that a re-run completes it — `vault_migration.rs`'s `an_interrupted_relocation_leaves_the_destination_absent_and_a_re_run_completes_it`, the same `SAFIX_SHIM_HOLD` pattern `vault_scratch_rules.rs` uses. Corrected from the original text: the relocate phase reuses `set`'s/`generate`'s own candidate-beside-target pattern for ciphertext (a decrypted value travels as a `Secret` through `sops set`'s stdin, exactly as `set`/`generate` already do, never touching disk as plaintext) rather than `crate::staging`'s separate memory-backed directory, which is generator-script I/O's own mechanism; a public output or definition record is genuine committed plaintext before and after the move, so neither needs that discipline either
- [x] 11.6 Severity drill: `check` reports `Finding::VaultRelocationPending` for every readable-layout leaf and `Finding::VaultGitignoreMissing` before `fix` runs, and reports neither afterward — folded into `vault_migration.rs`'s `a_populated_readable_fixture_migrates_into_a_vault`, which asserts both the before and the after state; `ensure_vault_gitignore` runs unconditionally whenever a vault is declared, so there is no configuration to skip it with, and this before/after toggle is the drill in the sense `vault_scratch_rules.rs`'s own `check_reports_a_vault_with_no_gitignore_entry_for_the_scratch_rules` already established
- [x] 11.7 Verify: `cargo test -p safix-core relocation::` and `definition::` green for the enumeration the migration and the check finding share, `cargo test -p safix --test vault_migration` green for the migration itself, drills in 11.6 observed

## 12. Capabilities checked, and two that gain vault-mode scenarios

- [x] 12.1 Re-run `secret-installation` and `secret-consumption`'s existing check suites unmodified over a fixture that both declares a vault and consumes a resolved set at both scopes, asserting every existing assertion still passes with no new failure — no existing check file touched; `vault.nix`'s `consumptionResolves` proves `materializeFor`'s output at both scopes carries the opaque key `placements` computed, over a fixture with a vault declared
- [x] 12.2 Add a check for `secret-custody`'s new "A multi-member audience in vault mode" scenario: a shared audience resolves to a file whose name is opaque rather than a sorted member list, over a vault fixture — `vault.nix`'s `opaque.team-secret` (a three-member audience: two people and a machine)
- [x] 12.3 Add a check for `public-outputs`'s new "A vault-mode leaf is opaque, not keyed by audience or carrier" scenario, and re-run its existing prefix-separation and catch-all-policy assertions unmodified over the same vault fixture — `vault.nix`'s `publicLeafIsAFile`, `prefixesStayDisjoint`
- [x] 12.4 Add a check for `recipient-policy`'s new "A vault does not move the committed file" and "A vault-rooted command reads a disposable rendering, not the committed file" scenarios — `vault.nix`'s `committedPolicyUnmoved`, `noVaultHasNoRulesText`, and the `rules` group
- [x] 12.5 Add a check for `recipient-policy`'s new "A vault-mode rule matches one opaque file, not a directory wildcard" scenario, asserting the rendered `path_regex` for a vault fixture is a literal anchored filename — `vault.nix`'s `rules.regexesAreLiteral`/`regexesMatchFormula`
- [x] 12.6 Verify: 12.1-12.5 all green, with zero modification to `secret-installation`'s or `secret-consumption`'s existing check files, which is the evidence this change's "not touched" list in `design.md` holds for those two — confirmed: only `flake.nix`, `modules/flake/checks/vault.nix`, and `modules/flake/safix/{options,default,resolve,policy,checks}.nix` touched

## 13. Documentation

- [x] 13.1 Document `flake.safix.vault` at its option declaration: `root`, `namingKey` and its minting recipe, the default, the lock-bump cost accepting it incurs, and that the naming key is visible to anyone who can evaluate the declaring flake — `root`, `namingKey`, the minting recipe, the visibility statement and the default were already written; this wave added the lock-bump cost paragraph
- [x] 13.2 Document `SAFIX_VAULT_ROOT` beside `SAFIX_REPO_ROOT` wherever the latter is documented for operators — the top-level `safix -h` text (`usage.rs`'s `SCAFFOLD`, and the `upload__safix_help.snap` snapshot it renders through); `examples/README.md` never names `SAFIX_REPO_ROOT`, so it needed no edit
- [x] 13.3 Corrected from the original text, which named a "secrets-lifecycle section" this README does not have: document the two-commit sequence, the half-landed refusal, and its safe-to-re-run guarantee in a new README section, "A vault: ciphertext in a second repository"
- [x] 13.4 Document the opaque layout in the README: the four hashed name sites, that a vault is not browsable by hand, that `safix set`/`edit`/`get` are the operator's tools against a vault document rather than a bare `sops` invocation, and that renaming an entry re-encrypts its leaf — same section
- [x] 13.5 Document the residual visibility honestly: document counts, key counts, ciphertext lengths, recipient public keys, and one commit per write remain visible to a vault host, and that the naming key is an evaluation-time value rather than a secret held only by the vault's owner — same section
- [x] 13.6 Document the migration and rollback steps from `design.md`'s amended Migration Plan for an existing consumer adopting or abandoning a vault, and that rotating the naming key is a full re-run of the same migration — same section
- [x] 13.7 Add a CHANGELOG entry stating that `flake.safix.vault`'s shape changed from a bare path to a naming-keyed submodule, and that a vault's layout is now opaque by construction — `CHANGELOG.md`'s `[Unreleased]`, "A secrets vault, opaque by construction, in a second repository"
- [x] 13.8 Verify: every guarantee stated in the README names a check or test in this repository that holds it. Audited sentence by sentence against "A vault: ciphertext in a second repository": the option's own byte-for-byte-unaffected default and the readable-name formula — `vault.nix`'s `rootFlip.noVault`/`readableMatchesFormula` (1.6), `vault_invariants.rs`; opacity of the four name sites and the "path states who can open" reversal — `vault.nix`'s `opaque`/`publicOpaque`/`leakedFragments` (4.6), `vault_opaque_names.rs`; not browsable by hand and the disposable scratch config — `vault_scratch_rules.rs`'s `set_creates_a_vault_rooted_document_through_the_scratch_config` and `the_scratch_rules_file_never_survives_a_run`; renaming re-encrypts — `vault_migration.rs`'s `a_vault_rollback_restores_the_readable_layout`, whose own assertion is that a moved leaf's ciphertext bytes do *not* round-trip because re-encryption stamps a fresh nonce; the two-commit order and the half-landed refusal — `vault_commit_ordering.rs`'s `a_forced_declaration_commit_failure_reports_the_half_landed_state_and_a_retry_completes_it`, `git.rs`'s `re_running_after_a_half_landed_failure_makes_no_second_vault_commit`; the lock-bump disclosure — `vault_lock_bump.rs`'s four tests (task group 9); adoption, rollback and interrupted-run safety — `vault_migration.rs`'s three tests; naming-key determinism (losing the key never loses a secret) — `vault_migration.rs`'s fixed-hex-literal fixtures standing in for `opaqueOf`'s real output (11.3's own note). Two sentences trace to a design decision rather than a repository check, honestly: the residual-visibility paragraph's document/key/ciphertext/recipient counts and the naming key's evaluation-time visibility are both design V8/V11's stated accepted facts about nix's and sops's own formats, not properties any check here asserts one way or the other, because there is no code path that could hide either — nothing here pads a ciphertext, strips a recipient stanza, or stores a naming key outside evaluation

## 14. Verification

- [ ] 14.1 `openspec validate separate-secrets-vault --strict`
- [ ] 14.2 `openspec validate --all --strict`, compared against the baseline recorded when this change was proposed
- [ ] 14.3 `nix eval .#checks.x86_64-linux --apply builtins.attrNames` lists every check named in groups 1-12
- [ ] 14.4 `nix flake check` green
- [ ] 14.5 `cargo test` green
- [ ] 14.6 `rg` the whole tree for any real fleet identifier and confirm none
- [ ] 14.7 `rg "hashString|hash_string|sha256"` under `crates/safix-core` and confirm no match outside `digest.rs` and its existing callers, holding design V14's "nothing in the runtime may hash" as a fact rather than an inference
- [ ] 14.8 Register every `vault_*.rs` integration target as a whole-target `claim` in `modules/flake/checks/single-runtime.nix`, with a doc comment naming its drill, the way `safix-bridge-sync-converge` is registered there — deferred to this wave rather than done per-file, so every vault target (groups 2, 3, 5, 6, 8, 10, 11) is registered consistently in one pass; each target is already covered by `nix flake check` today through the `safix-integration` derivation's own `checkPhase` (`cargo test --locked`, not `--lib --bins`, which is `safix-rs-test`'s own narrower scope), so this task adds per-claim granularity rather than coverage
